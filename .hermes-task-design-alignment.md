# 设计稿对齐任务

## 目标
修复 XAML 代码与 .pen 设计稿之间的差距，确保 4 个页面与设计稿完全一致。

## 参考
- 差距分析报告：`docs/compose/reports/design-spec-gap-analysis.md`
- 设计稿：`designs/*.pen`
- XAML 代码：`src/Client/Desktop/`

## 页面 1：登录界面 (LoginView.xaml)

**文件**：`src/Client/Desktop/Modules/LYBT.Desktop.Auth/Views/LoginView.xaml`

**需修改**：
1. ✅ 自动登录 checkbox 已存在（line 243-244）
2. ✅ 版本信息已存在（line 273）
3. ⚠️ 主色 Token：当前 `#6D4C41`（深棕），设计稿要求 `#3E2723`（更深棕）— 保持现状，与设计稿一致
4. ⚠️ 设计稿有"忘记密码?"链接，代码无 — **需添加**

**修改内容**：
- 在"自动登录" checkbox 旁添加"忘记密码?"链接
- 位置：密码输入框下方，选项行右侧

## 页面 2：主界面 (MainWindow.xaml + AppShell.xaml + SideNavControl)

**文件**：
- `src/Client/Desktop/Shell/Views/MainWindow.xaml`
- `src/Client/Desktop/Shell/Views/AppShell.xaml`
- `src/Client/Desktop/Shell/Views/SideNavControl.xaml`

**需修改**：
1. ❌ 缺面包屑 — **需添加**
   - 位置：ContentRegion 上方，Footer 上方
   - 内容：当前页面路径（如"首页 > 患者管理"）
   - 样式：13px，灰色，左对齐

2. ❌ 侧边栏未分组 — **需添加分组折叠**
   - 当前：平铺 8 项（首页/患者/挂号/医案/药材/验方/报表/设置）
   - 设计稿：分 3 组（临床/目录/管理）
   - 分组方案：
     - 临床：首页、患者管理、挂号管理、医案工作台
     - 目录：药材管理、验方管理
     - 管理：用户管理、系统设置

## 页面 3：患者管理 (PatientManagementView.xaml)

**文件**：`src/Client/Desktop/Roles/LYBT.Desktop.Clinical/Views/PatientManagementView.xaml`

**需修改**：
1. ❌ 缺批量删除按钮 — **需添加**
   - 位置：顶部工具栏右侧，红色按钮
   - 样式：MaterialDesignFlatButton + 红色前景
   - 命令：`{Binding BatchDeleteCommand}`
   - 条件：仅选中行时显示

2. ⚠️ 状态文案不一致
   - 设计稿：`复诊中/新患者/已治愈`
   - 代码：`Enabled/Disabled`
   - **需统一**：在 StatusBadge 控件中映射显示文案

## 页面 4：报表首页 (ReportsHomeView.xaml)

**文件**：`src/Client/Desktop/Modules/LYBT.Desktop.MedicalCase/Reports/Views/ReportsHomeView.xaml`

**需修改**：
1. ⚠️ Tab 文案不一致
   - 设计稿：`收入报表/就诊报表/药材报表/趋势分析`
   - 代码：可能为 `收入/接诊/药材排行/绩效`
   - **需统一**：以设计稿为准

## 约束

1. **只改 4 个页面**，不触及其他 XAML
2. **保持现有功能**，不删除已有控件
3. **样式统一**：使用已有的 `Pen*Brush` 资源
4. **命令绑定**：新增按钮必须绑定到 ViewModel 命令（即使是空实现）
5. **构建验证**：每次修改后 `dotnet build` 确保 0 错误

## 验证

修改完成后：
1. `dotnet build src/Client/Desktop/Shell/LYBT.Desktop.Shell.csproj` → 0 错误 0 警告
2. 逐页截图对比 .pen 设计稿
3. 提交 commit：`fix(Desktop): align XAML with pen design specs`
