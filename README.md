# LYBTZYZS

This repository contains various modules for the LYBT healthcare management system. It is organized as a multi-project .NET solution.

## Database Migration

The current initial migration is `UpdateIdFieldsToGuid`. Run the following script to create or update the database schema:

```bash
./scripts/update_db.sh
```

