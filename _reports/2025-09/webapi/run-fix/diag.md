# WebAPI Environment Diagnostics

Generated: 2025-09-13 23:50:00

## System Information

### .NET Version
```
9.0.305
```

### Environment Variables
```
ASPNETCORE_ENVIRONMENT = (not set)
ASPNETCORE_URLS = (not set)
DOTNET_ENVIRONMENT = (not set)
ConnectionStrings__DefaultConnection = (not set)
JwtOptions__Secret = (not set)
LYBT_DB_CONNECTION = (not set)
```

### Port Availability Check
- Port 8080: Available
- Port 5080: Available (fallback)

**Selected Port: 8080**

### Development Certificates
```
A valid HTTPS certificate is already present.
```

### Active TCP Listeners
```
No listeners on 808x ports found
```

## Configuration Files

### appsettings.json
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "***MASKED***"
  },
  "JwtOptions": {
    "Secret": "***MASKED***",
    "Issuer": "LYBT.WebAPI",
    "Audience": "LYBT.Client",
    "ExpireMinutes": 480,
    "RememberMeExpireMinutes": 43200,
    "ClockSkewSeconds": 300
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
      }
    }
  },
  "AllowedHosts": "*",
  "DefaultPasswords": {
    "SystemAdmin": "***MASKED***",
    "NewUser": "***MASKED***"
  }
}
```

### appsettings.Development.json
```json
Not found - needs to be created
```

## Current Issues Identified

1. **Missing appsettings.Development.json** - Development-specific configuration not found
2. **No CORS configuration** - May cause issues for frontend integration
3. **Hard-coded secrets** - JWT secret and passwords are in appsettings.json (not ideal for production)
4. **No environment-specific logging** - Development logging may be too verbose

## Recommendations

- OK: Use port 8080 for WebAPI startup
- Set ASPNETCORE_URLS=http://localhost:8080 before running
- Create appsettings.Development.json with development-specific settings
- Add CORS configuration for frontend integration
- After startup, use health.ps1 to verify API availability

---
*Generated by manual diagnostics*