# 深度自查：DI 容器解析 SysadminHomeView 失败

## 问题
Shell 登录后导航到 SysadminHomeView 时报错：
`An unexpected error occurred while resolving 'System.Object', with the service name 'SysadminHomeView'`

## 自查维度

### 1. ViewModel 注册检查
- 检查所有 View-ViewModel 对应关系
- 检查每个 ViewModel 的构造函数依赖是否都已注册
- 检查 Options<T> 是否都已绑定
- 检查子 ViewModel（ConfigurationCenterViewModel、ServerConfigSectionViewModel、CardReaderDiagnosticsViewModel）的依赖

### 2. 服务注册检查
- 检查所有 IApiClient 接口是否已注册
- 检查所有 IXxxService 接口是否已注册
- 检查 IFeatureToggleService、IClientConfigurationStore、IServerConfigurationService、ICardReaderDiagnostics 等

### 3. Module 加载顺序检查
- 检查 Prism ModuleCatalog 是否正确加载了 SysadminModule
- 检查模块依赖顺序

### 4. 全局异常日志检查
- 读取 Shell 的日志文件，找到完整异常堆栈
- 异常路径：C:\Users\player\AppData\Local\LYBTZYZS\logs\

## 执行步骤
1. 读取日志找到完整异常堆栈
2. 列出所有 Module 的 RegisterTypes 方法
3. 逐一检查每个 ViewModel 的构造函数依赖
4. 交叉比对已注册的服务列表
5. 输出根因和修复方案
6. 修复后验证编译通过

## 约束
- 只修复 DI 注册问题，不改业务代码
- 修复后 dotnet build 必须 0 错误 0 警告
- 不要创建临时项目验证
