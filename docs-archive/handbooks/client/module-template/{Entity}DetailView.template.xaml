&lt;UserControl
    x:Class="LYBT.Desktop.{Module}.Views.{Entity}DetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="800"&gt;

    &lt;UserControl.Resources&gt;
        &lt;!-- 转换器资源引用 --&gt;
        &lt;BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" /&gt;
        &lt;!-- TODO: 添加其他需要的转换器 --&gt;
    &lt;/UserControl.Resources&gt;

    &lt;Grid&gt;
        &lt;Grid.RowDefinitions&gt;
            &lt;RowDefinition Height="Auto" /&gt; &lt;!-- 标题栏 --&gt;
            &lt;RowDefinition Height="*" /&gt;    &lt;!-- 内容区 --&gt;
        &lt;/Grid.RowDefinitions&gt;

        &lt;!-- 1. 标题栏 --&gt;
        &lt;Border Grid.Row="0" Style="{StaticResource TitleBarStyle}"&gt;
            &lt;Grid&gt;
                &lt;Grid.ColumnDefinitions&gt;
                    &lt;ColumnDefinition Width="Auto" /&gt;
                    &lt;ColumnDefinition Width="*" /&gt;
                    &lt;ColumnDefinition Width="Auto" /&gt;
                &lt;/Grid.ColumnDefinitions&gt;

                &lt;Button
                    Grid.Column="0"
                    Content="← 返回"
                    Command="{Binding BackCommand}"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Margin="0,0,20,0" /&gt;

                &lt;TextBlock
                    Grid.Column="1"
                    Text="{Binding PageTitle}"
                    Style="{StaticResource TitleTextStyle}" /&gt;

                &lt;StackPanel Grid.Column="2" Orientation="Horizontal"&gt;
                    &lt;!-- 编辑模式按钮 --&gt;
                    &lt;Button
                        Content="编辑"
                        Command="{Binding EditCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Visibility="{Binding IsReadOnly, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Margin="0,0,10,0" /&gt;

                    &lt;!-- 保存/取消按钮（编辑模式） --&gt;
                    &lt;Button
                        Content="保存"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Margin="0,0,10,0" /&gt;

                    &lt;Button
                        Content="取消"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}" /&gt;
                &lt;/StackPanel&gt;
            &lt;/Grid&gt;
        &lt;/Border&gt;

        &lt;!-- 2. 内容区 --&gt;
        &lt;ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"&gt;
            &lt;StackPanel Margin="20" DataContext="{Binding Current{Entity}}"&gt;
                &lt;!-- TODO: 根据实际 Dto 字段定义表单 --&gt;

                &lt;!-- 示例：名称字段 --&gt;
                &lt;StackPanel Margin="0,0,0,15"&gt;
                    &lt;TextBlock
                        Text="名称"
                        Style="{StaticResource FieldLabelStyle}"
                        Margin="0,0,0,5" /&gt;
                    &lt;TextBox
                        Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        IsReadOnly="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Style="{StaticResource StandardTextBoxStyle}" /&gt;
                &lt;/StackPanel&gt;

                &lt;!-- 示例：描述字段 --&gt;
                &lt;StackPanel Margin="0,0,0,15"&gt;
                    &lt;TextBlock
                        Text="描述"
                        Style="{StaticResource FieldLabelStyle}"
                        Margin="0,0,0,5" /&gt;
                    &lt;TextBox
                        Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        IsReadOnly="{Binding DataContext.IsReadOnly, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        TextWrapping="Wrap"
                        AcceptsReturn="True"
                        MinHeight="100"
                        Style="{StaticResource StandardTextBoxStyle}" /&gt;
                &lt;/StackPanel&gt;

                &lt;!-- 示例：创建时间（只读） --&gt;
                &lt;StackPanel Margin="0,0,0,15"&gt;
                    &lt;TextBlock
                        Text="创建时间"
                        Style="{StaticResource FieldLabelStyle}"
                        Margin="0,0,0,5" /&gt;
                    &lt;TextBox
                        Text="{Binding CreatedAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}, Mode=OneWay}"
                        IsReadOnly="True"
                        Style="{StaticResource ReadOnlyTextBoxStyle}" /&gt;
                &lt;/StackPanel&gt;

                &lt;!-- 示例：更新时间（只读） --&gt;
                &lt;StackPanel Margin="0,0,0,15"&gt;
                    &lt;TextBlock
                        Text="更新时间"
                        Style="{StaticResource FieldLabelStyle}"
                        Margin="0,0,0,5" /&gt;
                    &lt;TextBox
                        Text="{Binding UpdatedAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}, Mode=OneWay}"
                        IsReadOnly="True"
                        Style="{StaticResource ReadOnlyTextBoxStyle}" /&gt;
                &lt;/StackPanel&gt;
            &lt;/StackPanel&gt;
        &lt;/ScrollViewer&gt;

        &lt;!-- 加载遮罩 --&gt;
        &lt;Grid
            Grid.RowSpan="2"
            Background="#80000000"
            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"&gt;
            &lt;StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"&gt;
                &lt;ProgressBar
                    IsIndeterminate="True"
                    Width="200"
                    Height="20"
                    Margin="0,0,0,10" /&gt;
                &lt;TextBlock
                    Text="加载中..."
                    Foreground="White"
                    FontSize="16"
                    HorizontalAlignment="Center" /&gt;
            &lt;/StackPanel&gt;
        &lt;/Grid&gt;
    &lt;/Grid&gt;
&lt;/UserControl&gt;
