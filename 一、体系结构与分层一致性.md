------

# 一、体系结构与分层一致性

## 1) 异常处理分散、样板代码多

- **问题**：多数 Controller 手工 try/catch，返回风格不一。
- **处理方式**：
  - 增加**全局异常中间件**（统一捕获未处理异常，返回统一错误结构）。
  - 增加**统一结果过滤器**（把 Service 返回统一包成 ApiResponse）。
  - 清理各 Controller 的 try/catch，仅保留“正常路径”。
- **验收**：
  - 刻意抛出异常（例如服务内 `throw`）→ 所有端点返回相同格式：`{ code, message, data? }` 且 HTTP 码与场景匹配（400/401/403/404/500）。

## 2) 返回模型不统一

- **问题**：部分接口 `Ok(object)`、部分用 `ApiResponse<T>`。
- **处理方式**：
  - 统一所有控制器返回 `ApiResponse<T>`（或 `ApiResponse`）。
  - 在 `Program.cs` 注册统一包装过滤器，兜底规范。
- **验收**：
  - Swagger/OpenAPI 文档中所有 2xx 响应体均为同一结构；前端只需**一种**解析路径即可工作。

## 3) 接口风格不一致（如 QuickCreate vs Create）

- **问题**：“同一动作”的多端点，语义重复。
- **处理方式**：
  - 合并到标准 `POST /{resource}`，通过**请求参数**区分“简单/完整”模式。
  - 统一命名：资源名用复数、小写，动作用 HTTP 动词表达；状态切换用 `PATCH /{id}/status`。
- **验收**：
  - Swagger 端点数减少；端点命名/谓词统一；前端路由调用收敛。

------

# 二、事务与数据一致性

## 4) 多表更新的“部分提交”风险

- **问题**：Service 内多次 `SaveChanges`，跨方法组合缺显式事务。
- **处理方式**：
  - 把**强一致**流程（如 挂号→看诊→处方→记录）封装成**单个 Service 方法**。
  - 使用 `DbContext.Database.BeginTransactionAsync()` 包裹，**一次提交**。
  - 查询使用 `AsNoTracking`；写入聚合到流程末尾统一 `SaveChanges`。
- **验收**：
  - 在流程中途人为抛异常 → 所有表均**无脏写**；成功路径落地的数据完整一致。

------

# 三、数据库与索引

## 5) 常用查询缺索引

- **问题**：患者姓名/电话/拼音码、处方患者/日期等查询无索引。
- **处理方式**：
  - 迁移新增索引：
    - Patients：`Name`、`PhoneNumber`、`PinYinCode`
    - Herbs：`Name`、`PinYinCode`
    - Prescriptions：`PatientId`、`CreatedAt`
- **验收**：
  - 相关查询的 `Execution Plan` 使用索引；相同数据量下查询耗时明显下降（≥30%）。

## 6) 级联删除可能误删历史

- **问题**：Patients/Users 等被历史数据引用的外键使用 Cascade。
- **处理方式**：
  - 调整为 `OnDelete: Restrict`（在 Fluent API 或迁移中修改）。
- **验收**：
  - 删除患者/医生时数据库阻止操作并返回外键约束错误；历史处方/就诊记录保留。

------

# 四、权限与安全

## 7) 角色粗放、缺基础安全策略

- **问题**：默认 Admin；缺最小权限划分与登录安全。
- **处理方式**（**不加复杂RBAC**，最小闭环）：
  - 引入 4 角色：`Admin`、`Doctor`、`Receptionist`、`Cashier`；在 Controller 用 `[Authorize(Roles = "…")]` 控制敏感端点。
  - 密码强度：≥8 位、含字母数字；**登录失败锁定**：5 次失败锁 10 分钟（用 IMemoryCache 计数）。
  - **关键操作审计**：患者/处方/药材的增删改写入审计表（时间、操作者、对象、前后差异摘要）。
- **验收**：
  - 用不同角色的 JWT 调用端点：权限边界正确。
  - 连续 5 次错误密码 → 账户临时锁定；解锁后正常。
  - 审计表能追溯最近 N 次关键操作。

------

# 五、日志、监控与健康检查

## 8) 日志分散、格式不一

- **问题**：各处手写日志，字段不统一，难检索。
- **处理方式**：
  - 使用 **Serilog**（控制台 + 滚动文件，保留30天）。
  - 统一模板：`{TraceId} {UserId} {Action} {Resource} {Result} {ElapsedMs}`；Info 记录成功用例，Warning 记录业务失败，Error 记录未捕获异常（交由中间件）。
- **验收**：
  - 任意一次用例链路日志可串起来（通过 TraceId）；统计慢请求容易。

## 9) 缺健康检查与慢请求标记

- **问题**：运行自检薄弱，定位慢接口困难。
- **处理方式**：
  - 启用 **/healthz**（DB ping + 可选磁盘剩余）。
  - 新增中间件/Filter 记录 `ElapsedMs`，>500ms 记 Warning。
- **验收**：
  - `/healthz` 200；日志能看到慢请求清单并定位到端点与参数摘要。

------

# 六、缓存策略

## 10) 缓存使用零散、无统一过期

- **问题**：IMemoryCache 没有共同策略，易“缓存污染”。
- **处理方式**：
  - **只缓存稳定字典**：Herbs 列表、Formula 列表。
  - 统一策略：**滑动 5 分钟**；写操作时**精准失效**（按 Key 移除）。
  - **不缓存强一致**数据（就诊状态、收费等）。
- **验收**：
  - 字典类端点命中率显著（日志观测）；更新药材后列表立即变化（失效生效）。

------

# 七、代码一致性与可维护性

## 11) Service/Repository 重复模式较多

- **问题**：分页、CRUD 样板重复；新同事上手成本高。
- **处理方式**：
  - 提炼 `BaseService`/`BaseRepository` 轻量基类（**不做过度抽象**），沉淀分页/排序/筛选模板。
  - 规定 Service 方法命名对齐“用例语义”（读起来像业务，用于定位与排查）。
- **验收**：
  - 新增模块仅需覆写少量方法；同类查询的写法统一。

## 12) Swagger 文档与错误码松散

- **问题**：描述不全，错误码无约束。
- **处理方式**：
  - 在 DTO/端点上补充**注释与示例**；统一错误码表（如 `VALIDATION_ERROR`、`AUTH_FAILED`、`NOT_FOUND`、`CONFLICT`、`UNEXPECTED`）。
- **验收**：
  - Swagger 可直接作为前端对接文档；错误码在日志/响应中可检索、可统计。

------

# 八、测试与发布

## 13) 测试覆盖不足，缺主流程保障

- **问题**：单元/集成测试薄，回归风险高。
- **处理方式**：
  - **端到端集成测试**（优先）：三条链路
    1. 登记→看诊→开方
    2. 开方→修改/撤销→审计
    3. 处方查询→分页/筛选（含索引验证）
  - 使用 InMemory 或 Testcontainers(SQL Server)；断点注入异常验证**事务回滚**。
- **验收**：
  - 三条用例在 CI 里全绿；注入异常时库中无脏数据。

## 14) 冒烟与可观测性缺失

- **问题**：发布后只看“能不能访问”，没“关键业务可用”的检查。
- **处理方式**：
  - 写一个**冒烟脚本**（PowerShell/Curl）：`/healthz` + 登录 + 患者列表 + 药材列表 + 处方创建（模拟）→ 用后即删。
  - 纳入发布流水线，部署后自动执行并出报告。
- **验收**：
  - 每次发布均有冒烟报告；失败自动报警 & 回滚（手动或半自动）。

------

# 九、实施顺序（3 天量级）

- **Day 1：统一与兜底**
  - 全局异常中间件、统一响应过滤器、Serilog、慢请求记录、Swagger 注释模板。
- **Day 2：数据与安全**
  - 索引迁移、外键 `Restrict`、关键流程显式事务、四角色与登录锁定、审计钩子。
- **Day 3：缓存与测试**
  - 字典缓存与失效、/healthz、三条端到端集成测试、冒烟脚本入流水线。

------

# 十、验收清单（逐条打钩）

-  全接口统一 `ApiResponse`；异常不写 try/catch 也能规范返回
-  Swagger 文档统一、示例齐全、错误码表固定
-  关键流程显式事务，异常注入无脏写
-  患者/药材/处方常用索引生效（查询时延下降）
-  外键删除策略保护历史记录
-  角色权限就位；登录防爆破有效
-  审计日志可追溯关键操作
-  /healthz 健康检查 OK；慢请求有记录可定位
-  字典类缓存命中有效，更新实时失效
-  3 条端到端测试通过；冒烟脚本纳管发布

------

如果需要，我可以**按你当前仓库结构**直接给出：

- `Program.cs` 的中间件/过滤器增量片段
- 迁移脚本示例（索引 + 外键策略）
- Serilog 配置样例
- 登录失败锁定的 IMemoryCache 片段
- HealthCheck 与慢请求中间件代码
- 端到端测试骨架（xUnit）

告诉我你当前主分支名和 WebAPI 项目路径，我就按该结构给出可直接粘贴的差异代码。