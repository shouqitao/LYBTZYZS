# B-16 Swagger API 文档生成

## 目标
验证并补全 Swagger/OpenAPI API 文档，确保端到端可用。

## 现状
- Swashbuckle.AspNetCore 已引用（LYBT.WebAPI.csproj）
- SwaggerOptions.cs 已定义（Title/Description/Contact/License/XmlComments/RoutePrefix）
- ApiServiceCollectionExtensions.cs 已配置 AddSwaggerGen（OpenAPI info + JWT Bearer + XML comments + Schema ID）
- UnifiedMiddlewareConfiguration.cs 已配置 UseSwagger + UseSwaggerUI（非生产环境或 Swagger:Enabled=true 时启用）
- SecurityHeadersMiddleware.cs 已做 /swagger CSP 豁免
- DownloadService.cs 主页有 API 文档链接
- appsettings.Production.json 中 Swagger:Enabled=true

## 任务清单

### T1: 端到端验证
1. 构建项目 `dotnet build LYBTZYZS.sln --no-incremental` 确认 0 错误 0 警告
2. 启动 WebAPI 服务，访问 `/swagger/v1/swagger.json` 验证 OpenAPI 文档可获取
3. 访问 `/swagger` 验证 SwaggerUI 可渲染
4. 检查所有 Controller 端点是否都在文档中出现

### T2: XML 文档注释补全
1. 检查各 csproj 是否启用了 GenerateDocumentationFile
2. 检查 Controller 方法是否有完整的 XML 注释（summary/param/returns）
3. 补全缺失的 XML 注释（至少覆盖所有 Public API 端点）
4. 处理 CS1591 警告（缺失 XML 注释的编译警告）

### T3: OpenAPI 文档完善
1. 确保所有 Controller 的路由、HTTP 方法、请求/响应类型正确
2. 检查 DTO 的 DataAnnotation 是否正确反映到 OpenAPI schema
3. 验证 JWT Bearer 认证在 SwaggerUI 中可用（Authorize 按钮）
4. 检查错误响应（ApiResponse、ProblemDetails）是否在文档中正确显示

### T4: 配置与安全
1. 确认 Swagger 在生产环境默认关闭（Swagger:Enabled=false 或 EnableInProduction=false）
2. 确认非生产环境自动启用
3. 验证 CSP 豁免正确工作

## 约束
- 遵循项目架构规范：3-Layer、模块独立、DTO 来自 Shared.Models
- 修改后必须通过 `dotnet build LYBTZYZS.sln --no-incremental` 0 错误 0 警告
- 文档变更需同步受影响的架构文档
- 外科手术式修改：只改任务要求的代码

## 验收标准
- [ ] `dotnet build LYBTZYZS.sln --no-incremental` 0 错误 0 警告
- [ ] `/swagger/v1/swagger.json` 返回完整 OpenAPI 文档
- [ ] `/swagger` SwaggerUI 可正常渲染和交互
- [ ] 所有 Controller 端点在文档中可见
- [ ] JWT Authorize 按钮可用
- [ ] 生产环境 Swagger 默认关闭

## 连续执行至全部完成无需确认
