# Login 模式切换问题分析与修复

> 启动时"本地模式"但 URL 显示远程地址 + "网络连接失败"的问题

## 已修复
1. `ConnectionSettingsService.LoadInitialValues`：启动时根据 `_preferredMode` 同步 `_currentUrl`（Local→localhost，Remote→remoteUrl）
2. `ConnectionStatusViewModel.SwitchToRemote`：移除 `CanExecute` 限制，启动即可切换
3. `LoginView.xaml`：URL TextBlock 根据 ApiStatus 变色（Healthy→绿，Unhealthy→红）

## 待分析
1. 启动时"网络连接失败"的触发链路：`DetectModeAsync` → `CheckRemoteAvailableAsync` → 失败 → 显示错误
2. `SyncModeDisplay` 的远程探测逻辑是否需要在启动时执行
3. `ConnectionStatusViewModel` 初始化顺序：`UpdateServerUrl()` vs `DetectModeAsync` 的时序

## 要求
- 每次只修改一个文件，修改后立即验证构建
- 修改前备份当前文件状态（git stash）
- 完成后提交并报告修改内容
