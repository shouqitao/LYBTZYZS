# Epic #1886 边界测试清单

**创建时间**: 2025-11-07
**测试范围**: 用户个人信息与密码修改功能的边界条件
**Issue**: #1904 - 边界测试

---

## 📋 测试前准备

### 环境要求

- [ ] SQL Server数据库已启动
- [ ] Server端（LYBT.WebAPI）已启动
- [ ] Client端（LYBT.Desktop）已启动

### 测试账户

**Doctor测试账户**:
- 用户名: `boundary_test_doctor`
- 真实姓名: `边界测试医生`
- 角色: `Doctor`
- 电话: `13800138001`
- 邮箱: `boundary@test.com`
- 密码: `Bound@123`

---

## 🔍 个人信息修改边界测试

### 测试1: 电话号码格式验证

#### 1.1 电话号码为空
**步骤**:
1. 打开UserProfileDialog
2. 清空电话号码字段
3. 尝试保存

**预期结果**:
- [ ] ❌ 保存按钮禁用或显示验证错误
- [ ] 提示: "电话号码不能为空"

---

#### 1.2 电话号码长度不足
**测试数据**:
- `138001380` (9位)
- `1380013800` (10位)

**预期结果**:
- [ ] ❌ 验证失败
- [ ] 提示: "电话号码必须是11位数字"

---

#### 1.3 电话号码长度超长
**测试数据**:
- `138001380012` (12位)
- `13800138001234` (14位)

**预期结果**:
- [ ] ❌ 验证失败
- [ ] 提示: "电话号码必须是11位数字"

---

#### 1.4 电话号码包含非数字字符
**测试数据**:
- `1380013800a`
- `138-0013-8001`
- `138 0013 8001`
- `+8613800138001`

**预期结果**:
- [ ] ❌ 验证失败
- [ ] 提示: "电话号码只能包含数字"

---

#### 1.5 电话号码重复（唯一性）
**步骤**:
1. 创建两个Doctor账户: `doctor_a`, `doctor_b`
2. `doctor_a`的电话: `13800138001`
3. `doctor_b`尝试修改电话为: `13800138001`

**预期结果**:
- [ ] ❌ 保存失败
- [ ] 提示: "该电话号码已被其他用户使用"

---

### 测试2: 邮箱格式验证

#### 2.1 邮箱为空
**步骤**:
1. 打开UserProfileDialog
2. 清空邮箱字段
3. 尝试保存

**预期结果**:
- [ ] ⚠️ 取决于设计：如果邮箱是必填项，应该失败；如果可选，应该成功

---

#### 2.2 邮箱格式不正确
**测试数据**:
- `invalid-email` (缺少@和域名)
- `user@` (缺少域名)
- `@example.com` (缺少用户名)
- `user@example` (域名不完整)
- `user example@test.com` (包含空格)

**预期结果**:
- [ ] ❌ 验证失败
- [ ] 提示: "邮箱格式不正确"

---

#### 2.3 邮箱长度限制
**测试数据**:
- 超长邮箱: `veryverylongusername@veryverylongdomainname.com` (如果超过数据库字段长度)

**预期结果**:
- [ ] ❌ 验证失败或截断
- [ ] 提示: "邮箱长度不能超过XX字符"

---

#### 2.4 邮箱重复（唯一性）
**步骤**:
1. `doctor_a`的邮箱: `test@example.com`
2. `doctor_b`尝试修改邮箱为: `test@example.com`

**预期结果**:
- [ ] ❌ 保存失败
- [ ] 提示: "该邮箱已被其他用户使用"

---

### 测试3: 真实姓名验证

#### 3.1 真实姓名为空
**步骤**:
1. 打开UserProfileDialog
2. 清空真实姓名字段
3. 尝试保存

**预期结果**:
- [ ] ❌ 保存按钮禁用或验证失败
- [ ] 提示: "真实姓名不能为空"

---

#### 3.2 真实姓名长度限制
**测试数据**:
- 最小长度: `张` (1字符)
- 超长姓名: `张三李四王五赵六孙七周八吴九郑十冯十一陈十二` (25字符)

**预期结果**:
- [ ] ⚠️ 根据设计：
  - 如果有最小长度限制（如≥2字符），1字符应该失败
  - 如果有最大长度限制（如≤20字符），超长应该失败

---

#### 3.3 真实姓名包含特殊字符
**测试数据**:
- `张三@123`
- `<script>alert('xss')</script>`
- `张三&李四`

**预期结果**:
- [ ] ⚠️ 根据设计：
  - 如果允许所有字符，应该成功（需防XSS）
  - 如果仅允许中文、字母、数字，应该失败

---

### 测试4: PinYinCode自动生成

#### 4.1 中文姓名自动生成PinYinCode
**测试数据**:
- 姓名: `张三`
- 预期PinYinCode: `ZhangSan` 或 `zhangsan`

**步骤**:
1. 修改真实姓名为: `张三`
2. 保存
3. 查询数据库Users表

**预期结果**:
- [ ] ✅ PinYinCode自动生成
- [ ] PinYinCode格式正确（与业务规则一致）

---

#### 4.2 英文姓名处理
**测试数据**:
- 姓名: `John Smith`
- 预期PinYinCode: `JohnSmith` 或 `john smith`

**预期结果**:
- [ ] ✅ PinYinCode正确处理英文姓名

---

#### 4.3 混合姓名处理
**测试数据**:
- 姓名: `张三John`

**预期结果**:
- [ ] ✅ PinYinCode正确处理混合姓名

---

### 测试5: 并发修改

#### 5.1 同一用户在多个客户端修改
**步骤**:
1. 同一账户在两个Client端同时登录
2. 在Client A修改电话为: `13900139000`
3. 在Client B修改电话为: `13900139001`
4. 先保存Client A，再保存Client B

**预期结果**:
- [ ] ⚠️ 根据设计：
  - 如果有版本控制或时间戳检查，Client B保存应该失败（乐观锁）
  - 如果无版本控制，Client B会覆盖Client A的修改（最后写入胜出）

---

## 🔐 密码修改边界测试

### 测试6: 旧密码验证

#### 6.1 旧密码错误
**步骤**:
1. 打开ChangePasswordDialog
2. 输入：
   - 旧密码: `WrongPassword@123`
   - 新密码: `NewPass@123`
   - 确认密码: `NewPass@123`
3. 点击"确定"

**预期结果**:
- [ ] ❌ 保存失败
- [ ] 提示: "原密码错误"

---

#### 6.2 旧密码为空
**步骤**:
1. 打开ChangePasswordDialog
2. 旧密码留空，输入新密码
3. 尝试保存

**预期结果**:
- [ ] ❌ "确定"按钮禁用或验证失败
- [ ] 提示: "旧密码不能为空"

---

### 测试7: 新密码长度验证

#### 7.1 新密码长度不足
**测试数据**:
- `` (0字符)
- `Ab@1` (4字符)
- `Ab@12` (5字符)

**预期结果**:
- [ ] ❌ 验证失败
- [ ] 提示: "密码长度至少6位"（根据实际规则）

---

#### 7.2 新密码长度超长
**测试数据**:
- 128字符密码
- 256字符密码

**预期结果**:
- [ ] ⚠️ 根据设计：
  - 如果有最大长度限制（如≤50字符），应该失败
  - 如果无限制，应该成功（但数据库字段要能容纳）

---

### 测试8: 新密码复杂度验证

#### 8.1 新密码无大写字母
**测试数据**:
- `test@123456`
- `testtest@123`

**预期结果**:
- [ ] ⚠️ 根据密码策略：
  - 如果要求大小写混合，应该失败
  - 如果无此要求，应该成功

---

#### 8.2 新密码无小写字母
**测试数据**:
- `TEST@123456`
- `TESTTEST@123`

**预期结果**:
- [ ] ⚠️ 根据密码策略：
  - 如果要求大小写混合，应该失败
  - 如果无此要求，应该成功

---

#### 8.3 新密码无数字
**测试数据**:
- `Test@Password`
- `TestPass@word`

**预期结果**:
- [ ] ⚠️ 根据密码策略：
  - 如果要求包含数字，应该失败
  - 如果无此要求，应该成功

---

#### 8.4 新密码无特殊字符
**测试数据**:
- `Test123456`
- `TestPassword123`

**预期结果**:
- [ ] ⚠️ 根据密码策略：
  - 如果要求包含特殊字符，应该失败
  - 如果无此要求，应该成功

---

### 测试9: 新密码与确认密码一致性

#### 9.1 新密码与确认密码不一致
**步骤**:
1. 打开ChangePasswordDialog
2. 输入：
   - 旧密码: （正确密码）
   - 新密码: `NewPass@123`
   - 确认密码: `NewPass@456`
3. 尝试保存

**预期结果**:
- [ ] ❌ 验证失败
- [ ] 提示: "两次输入的密码不一致"

---

#### 9.2 确认密码为空
**步骤**:
1. 输入旧密码和新密码
2. 确认密码留空
3. 尝试保存

**预期结果**:
- [ ] ❌ "确定"按钮禁用或验证失败
- [ ] 提示: "请确认新密码"

---

### 测试10: 新密码与旧密码相同

**步骤**:
1. 打开ChangePasswordDialog
2. 输入：
   - 旧密码: `Bound@123`
   - 新密码: `Bound@123`
   - 确认密码: `Bound@123`
3. 尝试保存

**预期结果**:
- [ ] ❌ 验证失败
- [ ] 提示: "新密码不能与旧密码相同"

---

### 测试11: sysadmin密码修改特殊逻辑

#### 11.1 sysadmin修改密码成功
**步骤**:
1. 以sysadmin登录
2. 修改密码
3. 验证AdminSecrets表更新

**预期结果**:
- [ ] ✅ AdminSecrets表PasswordHash更新
- [ ] ✅ JWT Token重新生成
- [ ] ❌ Users表中不应该有sysadmin记录

---

#### 11.2 防止sysadmin数据污染
**步骤**:
1. 修改密码后立即查询数据库

```sql
SELECT * FROM Users WHERE UserName = 'sysadmin' OR RealName = '系统管理员';
```

**预期结果**:
- [ ] ❌ **应该返回0条记录**
- [ ] 如果有记录，说明数据污染问题仍存在（参考`.verification/sysadmin-数据污染-分析报告.md`）

---

### 测试12: API端点边界测试

#### 12.1 无权限修改他人信息
**步骤**:
1. Doctor A登录，获取Token
2. 使用Doctor A的Token调用API修改Doctor B的个人信息

```http
PUT /api/v1/users/{doctor_b_id}/profile
Authorization: Bearer {doctor_a_token}
{
  "realName": "恶意修改",
  "phoneNumber": "19900000000",
  "email": "hacker@evil.com"
}
```

**预期结果**:
- [ ] ❌ 返回403 Forbidden
- [ ] 提示: "无权修改他人信息"

---

#### 12.2 无Token访问
**步骤**:
1. 不提供Authorization Header
2. 调用修改个人信息API

**预期结果**:
- [ ] ❌ 返回401 Unauthorized

---

#### 12.3 过期Token访问
**步骤**:
1. 使用过期的Token调用API

**预期结果**:
- [ ] ❌ 返回401 Unauthorized
- [ ] 提示: "Token已过期"

---

### 测试13: 数据库约束测试

#### 13.1 电话号码唯一性约束
**步骤**:
1. 直接在数据库中插入重复电话号码

```sql
INSERT INTO Users (UserName, RealName, PhoneNumber, Email, Role, Status)
VALUES ('test_duplicate', '测试重复', '13800138001', 'dup@test.com', 1, 1);
```

**预期结果**:
- [ ] ❌ 数据库抛出唯一性约束错误
- [ ] 如果没有约束，应该创建Issue记录缺失的约束

---

#### 13.2 邮箱唯一性约束
**步骤**:
1. 直接在数据库中插入重复邮箱

```sql
INSERT INTO Users (UserName, RealName, PhoneNumber, Email, Role, Status)
VALUES ('test_duplicate2', '测试重复2', '13800138002', 'boundary@test.com', 1, 1);
```

**预期结果**:
- [ ] ❌ 数据库抛出唯一性约束错误
- [ ] 如果没有约束，应该创建Issue记录缺失的约束

---

## 🛡️ 安全测试

### 测试14: SQL注入防护

#### 14.1 个人信息字段SQL注入
**测试数据**:
- 真实姓名: `'; DROP TABLE Users; --`
- 电话: `' OR '1'='1`
- 邮箱: `test' OR 1=1; --@test.com`

**预期结果**:
- [ ] ✅ EF Core参数化查询自动防护
- [ ] 数据正常保存（作为普通字符串）或验证失败
- [ ] ❌ **绝不应该执行SQL注入**

---

### 测试15: XSS防护

#### 15.1 个人信息字段XSS
**测试数据**:
- 真实姓名: `<script>alert('XSS')</script>`
- 真实姓名: `<img src=x onerror=alert('XSS')>`

**预期结果**:
- [ ] ✅ WPF控件自动转义HTML
- [ ] 在UI中显示时不会执行脚本
- [ ] 数据库中保存原始字符串

---

### 测试16: 密码安全

#### 16.1 密码哈希存储
**步骤**:
1. 修改密码后查询数据库

```sql
SELECT UserName, PasswordHash FROM Users WHERE UserName = 'boundary_test_doctor';
```

**预期结果**:
- [ ] ✅ PasswordHash字段是哈希值（BCrypt格式：`$2a$` 或 `$2b$`开头）
- [ ] ❌ **绝不应该存储明文密码**

---

#### 16.2 旧密码验证安全
**步骤**:
1. 尝试10次错误的旧密码

**预期结果**:
- [ ] ⚠️ 根据设计：
  - 理想状态：有账户锁定机制（5次错误后锁定）
  - 最低要求：每次验证返回"原密码错误"（不泄露其他信息）

---

## 📊 边界测试总结模板

### 测试结果分类

**个人信息验证（测试1-5）**:
```
✅ 电话号码格式验证 - 通过
✅ 电话号码唯一性 - 通过
✅ 邮箱格式验证 - 通过
✅ 邮箱唯一性 - 通过
✅ 真实姓名验证 - 通过
✅ PinYinCode自动生成 - 通过
⚠️ 并发修改 - （根据设计评估）
```

**密码验证（测试6-11）**:
```
✅ 旧密码验证 - 通过
✅ 新密码长度验证 - 通过
✅ 新密码复杂度验证 - 通过
✅ 密码一致性验证 - 通过
✅ 新旧密码不同验证 - 通过
✅ sysadmin密码修改 - 通过
✅ sysadmin数据污染防护 - 通过
```

**API安全（测试12-13）**:
```
✅ 无权限修改保护 - 通过
✅ Token验证 - 通过
✅ 数据库约束 - 通过
```

**安全测试（测试14-16）**:
```
✅ SQL注入防护 - 通过
✅ XSS防护 - 通过
✅ 密码哈希存储 - 通过
⚠️ 账户锁定机制 - （可选）
```

### Issue #1904验证结论

**总体评估**: 
```
✅ **通过** / ⚠️ **部分通过** / ❌ **失败**

**发现的问题**:
1. （列出失败的测试）
2. （列出需要改进的地方）

**建议后续行动**:
1. （创建Issue跟踪发现的Bug）
2. （创建Issue跟踪需要的改进）
```

---

**清单创建时间**: 2025-11-07
**预计测试时间**: 1-2小时
**下一步**: 执行测试，根据结果关闭Issue #1904或创建Bug Issue
