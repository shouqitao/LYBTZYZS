# 代码审查任务：Desktop VM + View 全量审查

## 目标
对 `src/Client/Desktop/` 下所有 ViewModel 和 View 进行代码质量审查，产出审查报告。

## 审查范围

### ViewModel 层（53 个）
- `src/Client/Desktop/Shell/ViewModels/` — Shell VM
- `src/Client/Desktop/Modules/*/ViewModels/` — 业务模块 VM
- `src/Client/Desktop/Roles/*/ViewModels/` — 角色 VM

### View 层（25 个 XAML + 17 个 Control）
- `src/Client/Desktop/Shell/Views/` — Shell View
- `src/Client/Desktop/Modules/*/Views/` — 业务模块 View
- `src/Client/Desktop/Roles/*/Views/` — 角色 View
- `src/Client/Desktop/*/Controls/` — 自定义控件

## 审查维度

### 1. ViewModel 质量
- [ ] 是否继承正确的基类（NavigableViewModelBase / DialogViewModelBase）
- [ ] 是否使用 `[ObservableProperty]` 或 `SetProperty` 正确实现 INPC
- [ ] `[RelayCommand]` 方法命名是否符合 async 后缀规范
- [ ] 是否有未使用的 private 字段
- [ ] 构造函数注入是否正确（null 检查）
- [ ] 是否正确实现 IDisposable（订阅 EventAggregator / Timer 等）
- [ ] 异步方法是否正确处理 CancellationToken
- [ ] 错误处理是否统一（Logger + ErrorMessage）

### 2. View-XAML 质量
- [ ] 是否使用 `prism:ViewModelLocator.AutoWireViewModel="True"`
- [ ] 绑定是否都有对应 VM 属性（无孤立绑定）
- [ ] 是否正确使用 DynamicResource vs StaticResource
- [ ] 是否有硬编码颜色（应该用 TcmBrands.xaml 主题资源）
- [ ] 控件命名是否规范（x:Name 仅在需要时使用）
- [ ] 是否有未使用的 xmlns 引用

### 3. 跨层一致性
- [ ] View 绑定的命令名是否与 VM 的 `[RelayCommand]` 方法匹配
- [ ] View 绑定的属性名是否与 VM 的 `[ObservableProperty]` 或 computed property 匹配
- [ ] Region 导航参数是否与目标 VM 的 `OnNavigatedTo` 接收参数一致
- [ ] 事件订阅是否在 VM Dispose 中取消

### 4. 架构合规
- [ ] 是否遵循 MVVM — View 无 code-behind 逻辑（除了纯 UI 操作）
- [ ] 是否遵循单一职责 — VM 不直接操作 View 元素
- [ ] 是否遵循依赖倒置 — 通过接口注入服务

## 产出格式

```
## 审查报告

### P0 — 必须修复（编译/运行时错误）
| # | 文件 | 行 | 问题 | 修复建议 |
|---|------|-----|------|----------|

### P1 — 建议修复（质量/规范问题）
| # | 文件 | 行 | 问题 | 修复建议 |
|---|------|-----|------|----------|

### P2 — 可选优化
| # | 文件 | 行 | 问题 | 优化建议 |
|---|------|-----|------|----------|

### 统计
- 审查 VM 数：XX
- 审查 View 数：XX
- P0 问题：X 个
- P1 问题：X 个
- P2 问题：X 个
```

## 约束
- 只产出报告，不修改代码
- 报告写入 `docs/compose/reports/vm-view-code-review.md`
- 逐个文件审查，不要跳过
- 每个问题必须标注具体文件和行号

不要创建临时项目验证。连续执行至全部完成无需确认。
