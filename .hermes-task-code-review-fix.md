# 代码审查问题修复任务

## 目标
修复 `docs/compose/reports/vm-view-code-review.md` 中的 P0（5个）和 P1（12个）问题，共17个。

## 约束
- 只改任务要求的代码，不顺手改相邻代码
- 每个修复后必须编译通过（0 错误 0 警告）
- 修复完毕后运行 `dotnet build src/Client/Desktop/Shell/LYBT.Desktop.Shell.csproj --no-restore` 验证
- 不要创建临时项目验证
- 连续执行至全部完成无需确认

---

## P0 修复（必须修复，5个）

### P0-1: BaseDetailContainer 无限递归
**文件**: `src/Client/Desktop/Core/LYBT.Desktop.Controls/Controls/BaseDetailContainer.xaml.cs`
**问题**: `OnGoBackCommandChanged` 回调内 `UpdateGoBackCommandWithDirtyCheck` 执行 `SetValue(GoBackCommandProperty, wrappedCommand)` — SetValue 再次触发同一 DP 回调 → 无限递归 / StackOverflow
**修复**: 在回调内判断 `e.NewValue` 是否已是包装命令（检查类型是否为包装器），避免重复包装。

### P0-2: MedicalCaseMasterDetailControl 绑定断链
**文件**: `src/Client/Desktop/Modules/LYBT.Desktop.MedicalCase/Controls/MedicalCaseMasterDetailControl.xaml`
**问题**: line 191/198 绑定 `Consultation` / `Prescription`，但 VM 暴露的是 `ConsultationEditor` / `PrescriptionEditor`
**修复**: 改为 `{Binding ConsultationEditor.Consultation}` / `{Binding PrescriptionEditor.Prescription}`

### P0-3: MedicalCaseEditControl Completeness 绑定失效
**文件**: `src/Client/Desktop/Modules/LYBT.Desktop.MedicalCase/Controls/MedicalCaseEditControl.xaml`
**问题**: 10 个 `Completeness.*` 绑定（line 572-640）DataContext=控件自身，但控件无 Completeness 依赖属性
**修复**: 在控件添加 `Completeness` 依赖属性（类型 object 或具体 DTO），由宿主通过 Binding 传入。或在 XAML 中移除失效绑定。

### P0-4: FormulaImportDialog Indications 断链
**文件**: `src/Client/Desktop/Modules/LYBT.Desktop.MedicalCase/Dialogs/FormulaImportDialog.xaml`
**问题**: line 172 绑定 `Indications`（复数），但 `FormulaListDto` 属性名是 `Indication`（单数）
**修复**: 改为 `{Binding Indication, TargetNullValue=暂无适应症}`

### P0-5: ClinicalWorkspaceView 资源键拼写错误
**文件**: `src/Client/Desktop/Roles/LYBT.Desktop.Clinical/Views/ClinicalWorkspaceView.xaml`
**问题**: line 62 `DynamicResource "MaterialDesignBrush Primary"` 缺少 `.` → 资源不可解析
**修复**: 改为 `{DynamicResource MaterialDesign.Brush.Primary}`

---

## P1 修复（建议修复，12个）

### P1-6: 侧边栏状态双份不同步
**文件**: `src/Client/Desktop/Shell/Views/AppShell.xaml`
**问题**: AppShell 列宽绑 `MainWindowViewModel.SidebarWidth`，汉堡按钮绑 `SideNavViewModel.IsSidebarExpanded` — 两个独立 VM 各持一份状态
**修复**: AppShell 列宽改绑 `SideNavViewModel.SidebarWidth`（通过 `SideNavControl.DataContext` 或 `ElementName` 绑定）。

### P1-7: DialogViewModelBase 重复声明 _isLoading
**文件**: `src/Client/Desktop/Core/LYBT.Desktop.Infrastructure/ViewModels/Base/DialogViewModelBase.cs`
**问题**: line 32-33 重新声明 `[ObservableProperty] _isLoading` 遮蔽基类
**修复**: 删除子类重复声明，复用基类 `IsLoading`。

### P1-8: EditorViewModelBase 订阅未退订
**文件**: `src/Client/Desktop/Core/LYBT.Desktop.Infrastructure/ViewModels/Base/EditorViewModelBase.cs`
**问题**: `SubscribeContext()` 订阅 `Context.PropertyChanged`，但未实现 IDisposable，仅 Reset() 退订
**修复**: 实现 IDisposable，在 Dispose 中对称退订。

### P1-9: ReceptionistHomeView 绑定不存在的命令
**文件**: `src/Client/Desktop/Roles/LYBT.Desktop.Clinical/Receptionist/Views/ReceptionistHomeView.xaml`
**问题**: line 270 绑 `RefreshDataCommand`，VM 无此命令
**修复**: 绑定 VM 已有的刷新命令，或删除该按钮。

### P1-10: PasswordBox.Password 绑定不更新
**文件**: `src/Client/Desktop/Roles/LYBT.Desktop.Admin/Sysadmin/Views/SysadminHomeView.xaml`
**问题**: line 166-167 `CommandParameter="{Binding Password, ElementName=NewUserPasswordBox}"` — PasswordBox.Password 非 INPC
**修复**: 在 code-behind 用 PasswordChanged 事件刷新参数，或改用 `PasswordBoxHelper.BoundPassword` 附加属性。

### P1-11: PatientSelectionView 状态绑定错误
**文件**: `src/Client/Desktop/Roles/LYBT.Desktop.Clinical/Views/PatientSelectionView.xaml`
**问题**: line 170 绑基类 `StatusMessage`，VM 写的是 `PageStatusMessage`
**修复**: 改绑 `PageStatusMessage`。

### P1-12: SysadminHomeViewModel 事件未退订
**文件**: `src/Client/Desktop/Roles/LYBT.Desktop.Admin/Sysadmin/ViewModels/SysadminHomeViewModel.cs`
**问题**: `_connectionMode.ModeChanged += OnModeChanged` 从不退订
**修复**: 重写 Dispose 退订 ModeChanged + 取消 `_pollCts`。

### P1-13: WorkflowStepIndicator 样式未应用
**文件**: `src/Client/Desktop/Modules/LYBT.Desktop.MedicalCase/Controls/WorkflowStepIndicator.xaml`
**问题**: `ActiveStepStyle` / `CompletedStepStyle` 已定义但从未应用
**修复**: 在步骤模板加 `IsActive` 触发器引用对应样式，或删除未使用的样式定义。

### P1-14: MessageDialog/InputDialog 引用未定义样式
**文件**: `src/Client/Desktop/Shell/Dialogs/Views/MessageDialog.xaml:86` + `InputDialog.xaml:41,57`
**问题**: 引用未定义主题样式 `ButtonPrimary` / `TextBoxExtend`
**修复**: 改用已定义的 `MaterialDesign*` 样式，或在 Themes 中补定义。

### P1-15: AccountSettingsViewModel 缺 null 检查
**文件**: `src/Client/Desktop/Shell/ViewModels/AccountSettingsViewModel.cs`
**问题**: 构造函数对注入依赖无 null 检查
**修复**: 补 `?? throw new ArgumentNullException`。

### P1-16: AuditLogViewModel 缺 null 检查
**文件**: `src/Client/Desktop/Modules/LYBT.Desktop.MedicalCase/ViewModels/AuditLogViewModel.cs`
**问题**: 构造函数对注入依赖无 null 检查
**修复**: 补 `?? throw new ArgumentNullException`。

### P1-17: BreadcrumbBar 死控件
**文件**: `src/Client/Desktop/Core/LYBT.Desktop.Controls/Controls/BreadcrumbBar.xaml(.cs)`
**问题**: `NavigationPath` 从未被任何视图赋值 → 面包屑空渲染
**修复**: 由宿主视图传入 NavigationPath，或暂时移除死控件引用。
