<Window x:Class="LYBT.WPF.Client.Modules.Users.Views.ChangePasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="修改密码" 
        Height="380" Width="450"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <!-- 密码强度颜色 -->
        <Style x:Key="PasswordStrengthStyle" TargetType="Border">
            <Setter Property="Height" Value="4"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
            <Setter Property="CornerRadius" Value="2"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding PasswordStrength}" Value="0">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding PasswordStrength}" Value="1">
                    <Setter Property="Background" Value="#F44336"/>
                    <Setter Property="Width" Value="100"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding PasswordStrength}" Value="2">
                    <Setter Property="Background" Value="#FF9800"/>
                    <Setter Property="Width" Value="200"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding PasswordStrength}" Value="3">
                    <Setter Property="Background" Value="#4CAF50"/>
                    <Setter Property="Width" Value="300"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="修改密码" 
                       FontSize="20" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="为了您的账户安全，请定期修改密码"
                       FontSize="12"
                       Foreground="#666"
                       HorizontalAlignment="Center"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- 提示信息 -->
        <Border Grid.Row="1" 
                Background="#E3F2FD"
                CornerRadius="4"
                Padding="10"
                Margin="0,0,0,20">
            <TextBlock TextWrapping="Wrap"
                       Foreground="#1976D2"
                       FontSize="12">
                <Run Text="密码要求："/>
                <LineBreak/>
                <Run Text="• 长度至少8个字符"/>
                <LineBreak/>
                <Run Text="• 包含大小写字母、数字和特殊字符"/>
                <LineBreak/>
                <Run Text="• 不能与最近使用的密码相同"/>
            </TextBlock>
        </Border>

        <!-- 表单内容 -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 当前密码 -->
            <TextBlock Grid.Row="0" Text="当前密码" 
                       FontWeight="SemiBold"
                       Margin="0,0,0,5"/>
            <PasswordBox Grid.Row="1" 
                         x:Name="CurrentPasswordBox"
                         Height="35"
                         Padding="10,0"
                         PasswordChanged="OnPasswordChanged"
                         Margin="0,0,0,15">
                <PasswordBox.Style>
                    <Style TargetType="PasswordBox">
                        <Setter Property="BorderBrush" Value="#DDD"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Style.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </PasswordBox.Style>
            </PasswordBox>

            <!-- 新密码 -->
            <TextBlock Grid.Row="2" Text="新密码" 
                       FontWeight="SemiBold"
                       Margin="0,0,0,5"/>
            <PasswordBox Grid.Row="3" 
                         x:Name="NewPasswordBox"
                         Height="35"
                         Padding="10,0"
                         PasswordChanged="OnPasswordChanged">
                <PasswordBox.Style>
                    <Style TargetType="PasswordBox">
                        <Setter Property="BorderBrush" Value="#DDD"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Style.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </PasswordBox.Style>
            </PasswordBox>

            <!-- 密码强度指示器 -->
            <Grid Grid.Row="4" HorizontalAlignment="Left">
                <Border Style="{StaticResource PasswordStrengthStyle}"/>
                <TextBlock Text="{Binding PasswordStrengthText}"
                           FontSize="11"
                           Foreground="#666"
                           Margin="0,10,0,15"/>
            </Grid>

            <!-- 确认密码 -->
            <TextBlock Grid.Row="5" Text="确认新密码" 
                       FontWeight="SemiBold"
                       Margin="0,0,0,5"/>
            <PasswordBox Grid.Row="6" 
                         x:Name="ConfirmPasswordBox"
                         Height="35"
                         Padding="10,0"
                         PasswordChanged="OnPasswordChanged"
                         Margin="0,0,0,10">
                <PasswordBox.Style>
                    <Style TargetType="PasswordBox">
                        <Setter Property="BorderBrush" Value="#DDD"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Style.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </PasswordBox.Style>
            </PasswordBox>

            <!-- 错误提示 -->
            <TextBlock Grid.Row="7" 
                       Text="{Binding ErrorMessage}"
                       Foreground="#F44336"
                       FontSize="12"
                       TextWrapping="Wrap"
                       Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>

        <!-- 按钮 -->
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0" 
                    Content="确认修改" 
                    Height="35"
                    Margin="0,0,10,0"
                    Command="{Binding ConfirmCommand}"
                    IsDefault="True">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#2196F3"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            
            <Button Grid.Column="1" 
                    Content="取消" 
                    Height="35"
                    Margin="10,0,0,0"
                    Command="{Binding CancelCommand}"
                    IsCancel="True">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#F5F5F5"/>
                        <Setter Property="Foreground" Value="#666"/>
                        <Setter Property="BorderBrush" Value="#DDD"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</Window>