<UserControl x:Class="LYBT.WPF.Client.Shell.Views.WorkingHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:LYBT.WPF.Client.Shell.ViewModels"
             Background="LightBlue">
    
    <UserControl.DataContext>
        <vm:SimpleHomeViewModel />
    </UserControl.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#2E86AB" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="凌隐宝堂中医诊所" 
                              FontSize="24" 
                              FontWeight="Bold" 
                              Foreground="White"/>
                    <TextBlock Text="{Binding SubTitle}" 
                              FontSize="16" 
                              Foreground="#B3D9F2" 
                              VerticalAlignment="Center" 
                              Margin="20,0,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="{Binding WelcomeMessage}" 
                              Foreground="White" 
                              VerticalAlignment="Center" 
                              Margin="0,0,20,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主内容区 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="40">
                <Grid Visibility="{Binding IsAdminRole, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Border Background="White" 
                           BorderBrush="#E0E0E0" 
                           BorderThickness="1" 
                           CornerRadius="5"
                           Margin="0,0,0,20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" 
                                      Text="系统管理" 
                                      FontSize="18" 
                                      FontWeight="Bold"
                                      Margin="20,15,20,10"/>
                            
                            <TextBlock Grid.Row="1" 
                                      Text="✅ HomeView基本显示功能正常！问题在于原始HomeViewModel的依赖注入。" 
                                      FontSize="14" 
                                      Foreground="Green"
                                      Margin="20,10,20,20"/>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </ScrollViewer>

        <!-- 状态栏 -->
        <Border Grid.Row="2" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid Margin="10,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}"/>
                <TextBlock Grid.Column="2" Text="{Binding CurrentDateTime}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>