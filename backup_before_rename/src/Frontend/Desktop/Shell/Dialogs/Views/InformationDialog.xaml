<Window x:Class="LYBT.WPF.Client.Shell.Dialogs.Views.InformationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        mc:Ignorable="d"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}"
        Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 消息内容 -->
        <Grid Grid.Row="0" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 信息图标 -->
            <Path Grid.Column="0" Width="48" Height="48" 
                  VerticalAlignment="Center"
                  Margin="0,0,15,0"
                  Fill="#FF2196F3"
                  Data="M12,2A10,10 0 0,1 22,12A10,10 0 0,1 12,22A10,10 0 0,1 2,12A10,10 0 0,1 12,2M12,17L17,12L15.59,10.59L13,13.17V7H11V13.17L8.41,10.58L7,12L12,17Z"/>

            <!-- 消息文本 -->
            <TextBlock Grid.Column="1" 
                       Text="{Binding Message}"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"
                       FontSize="14"/>
        </Grid>

        <!-- 按钮区域 -->
        <Border Grid.Row="1" 
                Background="#F5F5F5"
                BorderBrush="#E0E0E0"
                BorderThickness="0,1,0,0">
            <Button Content="确定" 
                    Command="{Binding OkCommand}"
                    Width="80" Height="30"
                    HorizontalAlignment="Right"
                    Margin="20,10"
                    IsDefault="True"/>
        </Border>
    </Grid>
</Window>