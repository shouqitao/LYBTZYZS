## LYBT.Entities
- 路径：src/Server/Core/LYBT.Entities · 类型：Library · TFM：net8.0
- 职责事实：服务端核心实体定义层，包含16个实体模型覆盖8个业务模块完整数据结构：Users(User/AdminSecret)、Patients(Patient)、MedicalCase(MedicalCase)、Consultation(Consultation)、Prescriptions(Prescription/PrescriptionItem)、Herbs(Herb)、Formula(Formula/FormulaHerbItem)、Auth(AuthSession)、Common(TransactionLog/TransactionStepLog/IHerbItem)、Compatibility(HerbCompatibilityNote)。实体设计遵循EF Core最佳实践，包含完整的数据注解、显示名称、字段长度限制，支持UltraThink架构的统一数据访问。
- 关键类型计数：Controller/Service/Repository/Entity/DTO/ViewModel/Enum：0/0/0/16/0/0/0，实体模型：16个，接口：1个(IHerbItem)，属性类：1个(SensitiveDataAttribute)
- 发现到的外露面：API端点数：0 / Views&VM数：0 / DbContext&DbSet数：0
- 疑似问题标注：
  - SuspectApiOutsideWebApi: false（纯实体定义，无Web功能）
  - SuspectDtoDup: false（实体模型，不是DTO）
  - SuspectEnumDup: false（不包含枚举，引用共享枚举）
  - SuspectRoleMismatch: false（符合实体层职责：数据模型定义）
  - SuspectMissing: false（8个业务模块实体完整，包含审计、事务日志等基础设施实体）
  - OtherInconsistencies: ["TransactionLog/TransactionStepLog等事务相关实体可考虑移动到基础设施项目", "SensitiveDataAttribute可能更适合放在共享项目"]
- 供产品负责人勾选：
  - [ ] 保留
  - [ ] 下线
  - [ ] 迁出（仅文件/类型维度，稍后进入迁移计划）
  - [ ] 缺失（后续是否补齐由你确认）