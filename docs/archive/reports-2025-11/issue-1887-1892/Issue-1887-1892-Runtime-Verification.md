# Issue #1887-1892 运行时验证清单

## 验证目标
验证用户资料对话框的双模式功能（sysadmin vs 普通用户）

## 前置条件
- ✅ Server端编译成功（Release配置）
- ✅ Client端编译成功（Release配置）
- ✅ 单元测试通过（13/13 UserProfileDialogViewModelTests）

---

## Task 5.3: sysadmin场景验证（15分钟）

### 1. 启动应用
```powershell
# 1. 启动Server端
cd D:\source\repos\LYBTZYZS\BIN\Server\Release\net8.0
dotnet LYBT.WebAPI.dll

# 2. 启动Desktop端（新窗口）
cd D:\source\repos\LYBTZYZS\BIN\Desktop\Release\net8.0-windows
.\LYBT.Desktop.Shell.exe
```

### 2. 登录验证

#### 2.1 使用sysadmin账户登录
- 用户名：`sysadmin`
- 密码：（系统默认密码）

**预期结果**：
- ✅ 登录成功
- ✅ 进入主界面

### 3. 打开用户资料对话框

#### 3.1 导航路径
1. 点击右上角用户头像/用户名
2. 选择"个人资料"菜单项

**预期结果**：
- ✅ UserProfileDialog打开
- ✅ 对话框标题为 **"修改密码"**（sysadmin模式的关键标识）
- ✅ 界面只显示密码修改相关字段
- ✅ **不显示**个人资料字段（RealName、Email、PhoneNumber等）

#### 3.2 UI元素验证

**应该显示的元素**：
- [ ] 当前密码输入框（OldPassword）
- [ ] 新密码输入框（NewPassword）
- [ ] 确认密码输入框（ConfirmPassword）
- [ ] 密码强度指示器
- [ ] "保存"按钮
- [ ] "取消"按钮

**不应该显示的元素**：
- [ ] 真实姓名输入框
- [ ] 邮箱输入框
- [ ] 手机号码输入框
- [ ] 头像选择/上传功能

### 4. 密码修改功能测试

#### 4.1 正常修改密码流程

**步骤**：
1. 输入当前密码：`（当前密码）`
2. 输入新密码：`NewPass123!@#`
3. 确认新密码：`NewPass123!@#`
4. 点击"保存"按钮

**预期结果**：
- ✅ 显示"正在保存..."加载提示
- ✅ 调用API: `POST /api/auth/sysadmin/change-password`
- ✅ 请求体包含：
  ```json
  {
    "OldPassword": "（当前密码）",
    "NewPassword": "NewPass123!@#"
  }
  ```
- ✅ 成功后显示提示：密码修改成功
- ✅ 对话框关闭
- ✅ （可选）自动跳转到登录页面

**验证方法**：
- 打开浏览器开发者工具（如果是混合应用）
- 或检查Server端日志：`D:\source\repos\LYBTZYZS\logs\webapi-*.log`
- 查找包含 "ChangeSysAdminPassword" 的日志条目

#### 4.2 密码验证规则测试

**测试用例1：密码长度不足**
- 新密码：`Abc1!`（5字符）
- 预期：显示错误"密码长度至少8位"

**测试用例2：密码缺少大写字母**
- 新密码：`abc12345!`
- 预期：显示错误"密码必须包含大小写字母、数字和特殊字符"

**测试用例3：新旧密码相同**
- 新密码：`（当前密码）`
- 预期：显示错误"新密码不能与旧密码相同"

**测试用例4：两次密码不匹配**
- 新密码：`NewPass123!@#`
- 确认密码：`NewPass456!@#`
- 预期：显示错误"两次密码输入不一致"

#### 4.3 错误处理测试

**测试用例1：旧密码错误**
- 输入错误的当前密码
- 预期：服务器返回401错误，显示"当前密码不正确"

**测试用例2：服务器不可用**
- 关闭Server端应用
- 尝试保存
- 预期：显示"服务器连接失败，请稍后重试"

### 5. 验证点总结

#### 核心验证点
- [ ] 对话框标题正确（"修改密码"）
- [ ] 只显示密码修改字段
- [ ] 不显示个人资料字段
- [ ] API调用正确（`POST /api/auth/sysadmin/change-password`）
- [ ] 密码验证规则生效
- [ ] 错误处理正确

#### 数据库验证
```sql
-- 验证密码修改是否生效（不要直接查看PasswordHash）
-- 通过重新登录来验证
SELECT UserName, UpdatedAt
FROM Users
WHERE UserName = 'sysadmin'
ORDER BY UpdatedAt DESC;
```

---

## Task 5.4: Doctor场景验证（15分钟）

### 1. 切换账户

#### 1.1 登出sysadmin
- 点击右上角头像
- 选择"退出登录"

#### 1.2 使用Doctor账户登录
- 用户名：`doctor1`（或其他医生账户）
- 密码：（对应密码）

### 2. 打开用户资料对话框

#### 2.1 导航路径
1. 点击右上角用户头像/用户名
2. 选择"个人资料"菜单项

**预期结果**：
- ✅ UserProfileDialog打开
- ✅ 对话框标题为 **"个人资料"**（普通用户模式的关键标识）
- ✅ 界面显示个人资料编辑字段
- ✅ **不显示**密码修改字段

#### 2.2 UI元素验证

**应该显示的元素**：
- [ ] 用户名（只读，灰色显示）
- [ ] 真实姓名输入框（可编辑）
- [ ] 邮箱输入框（可编辑）
- [ ] 手机号码输入框（可编辑）
- [ ] 头像显示区域
- [ ] "选择头像"按钮（可选）
- [ ] "移除头像"按钮（可选）
- [ ] "保存"按钮
- [ ] "取消"按钮

**不应该显示的元素**：
- [ ] 当前密码输入框
- [ ] 新密码输入框
- [ ] 确认密码输入框
- [ ] 密码强度指示器

### 3. 个人资料修改功能测试

#### 3.1 正常修改个人资料流程

**步骤**：
1. 修改真实姓名：`张医生`
2. 修改邮箱：`zhangdoctor@example.com`
3. 修改手机号：`13800138000`
4. 点击"保存"按钮

**预期结果**：
- ✅ 显示"正在保存..."加载提示
- ✅ 调用API: `PUT /api/v1/users/{userId}/profile`
- ✅ 请求体包含：
  ```json
  {
    "RealName": "张医生",
    "Email": "zhangdoctor@example.com",
    "PhoneNumber": "13800138000"
  }
  ```
- ✅ 成功后显示提示：个人资料修改成功
- ✅ 对话框关闭
- ✅ 界面刷新，显示更新后的用户信息

**验证方法**：
- 检查Server端日志：查找 "ChangeProfile" 相关日志
- 重新打开个人资料对话框，确认数据已更新
- 检查顶部导航栏用户名是否更新

#### 3.2 字段验证规则测试

**测试用例1：真实姓名为空**
- 真实姓名：`（清空）`
- 预期：显示错误"真实姓名不能为空"，保存按钮禁用

**测试用例2：手机号格式错误**
- 手机号：`22345678901`（不以1开头）
- 预期：显示错误"手机号码格式不正确"

**测试用例3：手机号长度错误**
- 手机号：`138001380`（9位）
- 预期：显示错误"手机号码必须为11位"

**测试用例4：邮箱格式错误**
- 邮箱：`invalid-email`
- 预期：显示错误"邮箱格式不正确"

#### 3.3 头像功能测试（可选）

**测试用例1：选择头像**
- 点击"选择头像"
- 选择图片文件
- 预期：显示选中的头像预览

**测试用例2：移除头像**
- 点击"移除头像"
- 预期：头像被移除，显示姓名首字母

### 4. 验证点总结

#### 核心验证点
- [ ] 对话框标题正确（"个人资料"）
- [ ] 只显示个人资料字段
- [ ] 不显示密码修改字段
- [ ] API调用正确（`PUT /api/v1/users/{userId}/profile`）
- [ ] 字段验证规则生效
- [ ] 数据保存成功

#### 数据库验证
```sql
-- 验证个人资料修改是否生效
SELECT Id, UserName, RealName, Email, PhoneNumber, UpdatedAt
FROM Users
WHERE UserName = 'doctor1'
ORDER BY UpdatedAt DESC;
```

---

## Task 5.5: 边界测试（20分钟）

### 1. 并发修改测试

#### 1.1 测试场景
1. 用户A打开个人资料对话框
2. 用户B（或另一个客户端）修改同一用户的资料
3. 用户A尝试保存

**预期结果**：
- ✅ 显示冲突提示："资料已被其他用户修改，请刷新后重试"
- 或 ✅ 自动刷新最新数据

### 2. 网络异常测试

#### 2.1 测试场景
1. 打开个人资料对话框
2. 断开网络连接
3. 修改资料并尝试保存

**预期结果**：
- ✅ 显示网络错误提示
- ✅ 数据不丢失，可以在网络恢复后重试

### 3. 数据完整性测试

#### 3.1 测试场景
1. 修改真实姓名为特殊字符：`张三\李四`
2. 修改邮箱为超长字符串（>255字符）

**预期结果**：
- ✅ 服务器返回400错误
- ✅ 显示友好的错误提示

### 4. 权限测试

#### 4.1 测试场景
1. 普通医生尝试修改其他用户的资料（通过修改API请求）

**预期结果**：
- ✅ 服务器返回403禁止访问
- ✅ 客户端显示权限错误

---

## 验证完成标准

### 必须满足的条件
- [ ] sysadmin模式：对话框标题为"修改密码"
- [ ] sysadmin模式：API调用 `POST /api/auth/sysadmin/change-password`
- [ ] 普通用户模式：对话框标题为"个人资料"
- [ ] 普通用户模式：API调用 `PUT /api/v1/users/{userId}/profile`
- [ ] 密码修改功能正常工作
- [ ] 个人资料修改功能正常工作
- [ ] 所有验证规则生效
- [ ] 错误处理正确

### 可选验证
- [ ] 头像上传/移除功能
- [ ] 并发修改处理
- [ ] 网络异常恢复

---

## 问题记录

### 发现的问题
（记录实际测试中发现的任何问题）

### 解决方案
（记录问题的解决方法）

---

**验证人**：______________________

**验证日期**：2025-11-07

**验证结果**：□ 通过  □ 未通过

**备注**：
