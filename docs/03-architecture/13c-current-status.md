# LYBTZYZS 当前状态
> 版本: v1.3 | 日期: 2026-09-23

> 由 [13-project-master-plan.md §四/§五](13-project-master-plan.md) 拆出（2026-08-04 规则体系优化 E-03），内容原样迁移：Desktop 视图实现现状 + 已知问题清单。**本文件是 Build/测试/已知问题的唯一权威。**

## 三、当前状态速览

| 项 | 值 |
| ---- | ----- |
| Build | 0 错误 **0 警告** |
| 架构测试 | **107/107 pass**（2026-09-23 实测；B-06 追加 `BaseBackupController` 双端共享基类白名单 P02/P09c） |
| Server 测试 | **851/851 pass**（2026-09-23 收尾批次 2 实测；847 基线见 #148，本批次 +4 条患者电话盲索引真实 SQL 测试——见 #150） |
| Desktop 测试 | **1014/0 失败/24 Skip（1038 用例）**；**分层执行（2026-09-23 确立，见 `tests/AGENTS.md`）**——L0 快速（Architecture+Unit）约 31s / L1 集成（+Server+Desktop Integration）约 2m42s / L2 全量约 **2m30s**（原 25m40s，共享 LocalDB+Respawn 提速 14×）；24 Skip 全部为 `Integration/RemoteApi/**`（需运行中的远程 WebAPI localhost:5000，均带 `Skip="PRE-EXISTING: Remote E2E requires live remote WebAPI…"` 理由，前置条件见 `tests/AGENTS.md`） |
| 最新迁移 | `20260923120000_AddPatientPhoneSearchHash`（R-6 手机号盲索引——见 #150；前序 `20260917120000_AddPatientIdCardHash`） |
| 前端导航切片 | **N1–N7 已实施（2026-09-27 收尾）**——见下「前端导航修复记录」 |

### 三.1 前端导航修复记录（2026-09-27）

> 设计 SSOT: 导航设计已交付并同步至 `02-desktop.md`（原设计稿已清理，git 历史可查）

| 项 | 状态 | 说明 |
| ---- | ---- | ---- |
| N1–N7 导航切片 | ✅ 已实施 | MedicalCaseNav 参数契约 + MedicalCaseWorkspace 守卫扩权（四角色）+ VM 单门面（INavigationCoordinator）+ 后退 Journal fallback + 角色 RequiredModules 含 Home 模块 + 对话框核心收敛 + 正式文档/架构测试同步 |
| 医案工作台参数契约 | ✅ 已统一 | `MedicalCaseNav` / `PatientManagementNav` / `RegistrationListNav` 工厂入 `LYBT.Desktop.Contracts/Models/Navigation/`；生产方（PatientSelection/RegistrationList/ClinicalWorkspace）对齐 ForExistingCase/ForNewCase |
| 对话框收敛 | ✅ 核心路径已完成 | UserNotificationService/NotificationService/Control 委托 IDialogManager/IToastService；**ToastService.ShowMessageBoxFallback 有意保留**（无主窗口/启动早期兜底，已 XML 注释文档化） |
| 架构守卫 | ✅ 4 项已入测试 | `NavParams_ContractKeys_ConsumedByTargetViewModel` / `ViewRoleAccess_CoversAllRegisterForNavigationViews` / `RoleRequiredModules_ContainHomeViewModule`（DesktopNavigationArchTests）+ `ViewModels_MustNot_RequestNavigate_Directly`（DesktopLayerArchTests） |
| Desktop Model 层 | ✅ 核心已落地 | Registration Models（DetailModel+EditContext）、Formula Herbs `FormulaHerbItemModel`、MedicalCase `MedicalCaseEditContext`+`PrescriptionItemModel`+`MedicalCaseEditSession`——16-desktop-architecture-spec 已标「部分落地」 |
| **MedicalCaseWorkspace 角色 vs 服务端策略** | ⚠️ 有意不一致 | **客户端** ViewRoleAccess 含 Receptionist/Admin/SuperAdmin（可进工作台查看）；**服务端**双控制器树类级 `DoctorOrAdmin`（**不含 Receptionist**）、Create `DoctorOnly`、close `AdminOrSuperAdmin`。前台进工作台调医案 API 会 403。若需前台查看医案，须产品确认后同步双端类级策略为 `DoctorOrAdminOrReceptionist`（写操作保持 DoctorOnly）并更新 `01-product/04-permissions.md`。 |

## 四、Desktop 视图（代码实际定义）

> **审计依据（2026-08-29）**：desktop-view-design-audit 报告（已清理，git 历史可查）。判定：XAML 存在 + VM + 导航/对话框注册 + 运行时入口。角色 Home 以 *RoleDefinition.HomeViewName 为准。

### 4.1 Shell / Auth

| 视图 | 文件 | 功能 | 问题 |
| ------ | ------ | ------ | ------ |
| MainWindow | Shell/Views/ | 主窗口（ContentRegion） | — |
| LoginView | Modules/LYBT.Desktop.Auth/Views/ | 登录界面 | — |
| InitializationWizardView | Modules/LYBT.Desktop.Auth/Views/ | 首次初始化向导（5 步：模式选择/模式配置/诊所信息/初始管理员/校验完成；`RegisterForNavigation` + `RegisterDialog` 双入口） | ✅ 已实现（2026-09-23 B-07） |
| ServerConfigView | Modules/LYBT.Desktop.Auth/Views/ | 服务器地址配置（RegisterDialog） | — |
| AccountSettingsView | Shell/Views/ | **个人资料**（姓名/密码/头像/会话超时），所有角色可用 | — |
| SessionTimeoutWarningDialog | Shell/Dialogs/Views/ | 会话超时倒计时提醒（US-AUTH-014） | ✅ 已实现（2026-09-23 收尾批次；更正：原标注「US-AUTH-005」有误——US-AUTH-005 实为令牌验证） |

### 4.2 管理员 / Sysadmin 角色

| 视图 | 文件 | 功能 | 问题 |
| ------ | ------ | ------ | ------ |
| AdminHomeView | Roles/LYBT.Desktop.Admin/Views/ | 管理员首页（Admin Home） | — |
| UserManagementView | Roles/LYBT.Desktop.Admin/Views/ | 用户管理 | — |
| SystemSettingsView | Roles/LYBT.Desktop.Admin/Views/ | **诊所设置**（诊所名称/地址/电话等业务信息） | 仅读取，无编辑 |
| SysadminHomeView | Roles/LYBT.Desktop.Admin/Sysadmin/Views/ | **运维设置**（系统运行配置主页，SuperAdmin Home） | — |
| LogLevelControlView | Roles/LYBT.Desktop.Admin/Sysadmin/Views/ | 日志级别控制 | — |
| DeploymentView | Roles/LYBT.Desktop.Admin/Sysadmin/Views/ | 部署视图 | 仅上传+重启 |
| BackupManagementView | Roles/LYBT.Desktop.Admin/Sysadmin/Views/ | 备份恢复（US-SHELL-013） | — |
| SecurityAuditLogView | Roles/LYBT.Desktop.Admin/Sysadmin/Views/ | 安全审计日志查看（US-SHELL-014） | ✅ 2026-08-29 实现；仅远程可查，本地空态 |
| ConfigExportImportView | Roles/LYBT.Desktop.Admin/Sysadmin/Views/ | 配置包导出/导入（US-SHELL-016） | ✅ 已实现（2026-09-23 收尾批次）；业务数据 JSON 导入导出仍在各 MasterDetail |

### 4.3 临床 / 前台角色

| 视图 | 文件 | 功能 | 问题 |
| ------ | ------ | ------ | ------ |
| ClinicalWorkspaceView | Roles/LYBT.Desktop.Clinical/Views/ | **医生真实 Home**（DoctorRoleDefinition.HomeViewName） | — |
| ClinicalHomeView | Roles/LYBT.Desktop.Clinical/Views/ | 临床首页 | ⚠️ 半死：仅 RoleRegistry.DefaultHomeView fallback，无角色绑定 |
| PatientManagementView | Roles/LYBT.Desktop.Clinical/Views/ | 患者管理 | — |
| PatientSelectionView | Roles/LYBT.Desktop.Clinical/Views/ | 患者选择（身份证读卡）+ **内嵌待诊队列** | 队列功能在此，无独立 PendingQueue 页 |
| HerbManagementView | Roles/LYBT.Desktop.Clinical/Views/ | 药材管理 | — |
| FormulaManagementView | Roles/LYBT.Desktop.Clinical/Views/ | 验方管理 | — |
| MedicalCaseManagementView | Roles/LYBT.Desktop.Clinical/Views/ | 医案管理 | — |
| MedicalCaseWorkspaceView | Roles/LYBT.Desktop.Clinical/Views/ | 医案工作台（核心） | — |
| ReceptionistHomeView | Roles/LYBT.Desktop.Clinical/Receptionist/Views/ | 前台首页（Receptionist Home） | — |

> **已删除（2026-08-29 审计清理）**：PendingQueueView.xaml(.cs) — 未导航注册的死页；队列 UI 由 PatientSelectionView 内嵌 PendingQueueViewModel 实现。PendingQueueViewModel 保留。

### 4.4 医疗模块

| 视图 | 文件 | 功能 | 问题 |
| ------ | ------ | ------ | ------ |
| MedicalCaseMasterDetailView | Modules/LYBT.Desktop.MedicalCase/Views/ | 医案主从详情 | — |
| AuditLogView | Modules/LYBT.Desktop.MedicalCase/Views/ | **医案**审计日志（US-MC-017，非安全审计） | — |
| ReportsHomeView | Modules/LYBT.Desktop.MedicalCase/Reports/Views/ | 报表首页 | TODO: 待完善 |

### 4.5 挂号

| 视图 | 文件 | 功能 | 问题 |
| ------ | ------ | ------ | ------ |
| RegistrationListView | Modules/LYBT.Desktop.**Registrations**/Views/ | 挂号列表 | 项目名复数（A-25） |

---

## 五、统一/合并遗留项登记（2026-08-10 建立）

> **规则**：每轮统一/合并任务完成后，把「本轮有意保留项」显式登记于此（项 + 原因 + 计划批次），作为后续扫描判断「遗留 vs 新问题」的依据。避免分阶段统一的有意决策只留在 compose 报告里、被后续轮次当作新发现反复提出。

| 登记日期 | 来源任务 | 有意保留项 | 原因 | 计划批次 |
| --------- | --------- | ----------- | ------ | --------- |
| 2026-08-10 | D1 Catalog 双轨统一 | FormulaDetailModelMapper 若接线后发现仍零调用 → 删除；Herb 轨补 Mapperly | D1 按扫描报告统一，执行中复核 | D1 内收口 |
| 2026-08-10 | D4 命名统一 | ~~Service 前缀统一方向待定~~ **已执行**：统一为 Remote* 前缀（RemoteUserService/RemoteRegistrationService/RemoteHerbService/RemoteFormulaService/RemotePatientService）；契约层 IXxxService 无前缀为接口惯例，实现类 Remote 标识 HTTP 数据服务 | 4:1 既定模式（D1 确立 RemoteFormulaService），仅 PatientService 需改名，改动面 1 类+Module+测试 | ✅ 9748db4b4 |
| 2026-08-10 | D4 命名统一 | **Manager 后缀保留**：DesktopCacheManager/DialogManager/SessionManager/TokenManager/LoginStateManager/StatusBarManager/NavigationManager/MenuManager/LoadingStateManager/EventSubscriptionManager/WorkspaceStateManager/SessionLifecycleManager 共 12 个——全部为「生命周期/会话状态/UI 基础设施」管理职责，与数据业务 Service 职责明确不同；契约层 IXxxService 与 IXxxManager 各自统一 | A-08 曾报告；D4 核实 Manager 有明确职责差异（Session/Token/Cache/UI 状态管理），改名无收益 | ✅ 9748db4b4 |
| 2026-08-10 | D4 命名统一 | **SearchProvider 保留**：I{Herb | Formula}SearchProvider 为 D5-3 跨模块门面（MedicalCase/Formula 消费，Catalog 实现委托 I{Herb | Formula}Service）——与 Service 明确分工：Service=模块内数据服务，SearchProvider=跨模块解耦（防 MedicalCase→Catalog 编译期依赖，P07 合规） | 合并会破坏模块解耦；接口注释已文档化 D5-3 意图 | ✅ 9748db4b4 |
| 2026-08-10 | D4 命名统一 | **BreadcrumbItem 同名不改**：Contracts record（LYBT.Desktop.Contracts.UI，导航架构数据 Title/ViewName/IsCurrent）vs Controls class（LYBT.Desktop.Controls.Controls，BreadcrumbBar 渲染模型 Label/Level/IsCurrent/IsLast/NavigateCommand）——两个 namespace 封闭使用、零桥接（BreadcrumbBar 经 NavigationPath/NavigateCommand DP 绑定），无文件同时 using 两处 | 核实不冲突；重命名仅同名巧合，无实际歧义 | ✅ 9748db4b4 |
| 2026-08-10 | D5 MedicalCase 双体系 | ~~DTO 门面缓存（Cached*）收敛到 EditContext 新路径~~ **已执行**：CachedMedicalCase/CachedConsultation/CachedPrescription/ClearCache 删除；LoadDetailsAsync 内部改走 LifecycleService.InitializeAsync（DTO 快照单一持有于 LifecycleService，门面统一为 Current/CurrentConsultation/CurrentPrescription/CurrentDetail）；AggregateSaveAsync 保存后 UpdateSnapshot 前移 EditContext 基线；消费方（MasterDetail VM/Workspace VM/PrescriptionPrintHandler/测试）全部切换 | 用户已拍板方案 A（收敛）；EditContext 新路径由死代码变为真实接线（CommandService.SaveAsync 会话可用） | ✅ 待提交 |
| 2026-08-10 | O1 死代码 | ~~PatientDetailDisplayModel / TokenManager 无写入路径 / PrintOptions 死选项~~ **已执行**：PatientDetailDisplayModel 删除（零引用+测试）；TokenManager 删除（无写入路径恒 null，真实 token 存储为 ITokenStorageService，SignalR 改匿名连接行为等价）；PrintOptions.Orientation/DuplexPrinting + PrintOrientation 枚举删除 | 扫描报告 P2 确认零引用 | ✅ 待提交 |
| 2026-08-10 | O3 卫生 | ~~Controls 控件层 PrescriptionItemDto 交换形状~~ **保留**（内部适配层，非 UI 编辑路径）；wpftmp csproj 已删（git 忽略，报告「已追踪」过时）；obj 陈旧 UnfinishedCaseDialogViewModel 生成物已清 | B1 已解耦外部 DP 为 IEnumerable | ✅ 待提交 |
| 2026-08-11 | T1 测试审查（只读） | 结论：Architecture 83 规则高质量；Desktop 形式主义集中（50 纯交互断言/恒真断言/MasterDetail 模板复制/假业务 Integration）；Server 零 mock 健康但 _Infrastructure 9 文件 SQL 集成基建零消费者、AGENTS.md 宣称 1185 tests 与实际脱节。报告见 `docs/compose/archive/test-code-review-2026-08-11.md` | P0-06 根因确认（148 VM 测试挂 LocalDB + 146 Integration 挂 localhost:5000）；建议 T2 批次跟进 | ✅ 1129d6f81（报告） |
| 2026-08-11 | T2 测试修复 | ~~T2-1 Server _Infrastructure 零消费者 / T2-2 VM 测试去 LocalDB / T2-3 假业务测试~~ **已执行**：T2-1 删 Server_Infrastructure 11 文件+TestDataBuilders（零消费者）、AntiMockRuleTests AM01/02 改引 Server 测试类型、AM03 删除（保护对象已不存在）、tests/AGENTS.md 数字校正；T2-2 UserJourneyTestBase 去 IClassFixture<UserJourneyFixture>/LocalDB（12 类纯 VM 测试不再建库，FrameworkVerificationTests 改直连 fixture）；T2-3 MedicalCaseTests GetPermissions/GetAuditLogs 补真实 API 调用+断言、GetPendingCases 死测试删除 | P0 结构性发现修复；34 个残留失败全为存量（STA/mock 具体类/断言，stash 基线实证） | ✅ 2ee9143ac |
| 2026-08-11 | T3 测试质量 | ~~T3-1~T3-5~~ **已执行**：T3-1 MasterDetail 3 文件（MedicalCase/Patients/Formula）装配改走基类 CreateMasterDetailServicesMock（消除 ~90 行重复，保留 Formula 定制+字段引用）；T3-2 LoadListAsync 补状态断言（Items/TotalCount），50 个交互断言甄别后多为转发方法契约（合理保留）；T3-3 恒真断言改真实验证（WPF 资源）、Dispose 测试补 NotThrow、空壳持久化测试改状态断言；T3-4 JwtService 注入 TimeProvider（8 处 UtcNow + ValidateToken 时钟预检），过期测试去 Thread.Sleep(65s) 改假时钟推进（取消 Skip，23/23 通过）；T3-5 MedicalCase 反射扩展改 Testable 子类化（对齐 Herb 模式），LoadHerbsAsync private→protected | 每项 stash/基线实证零回归 | ✅ 817d131d1 |
| 2026-08-11 | T3-6 存量失败补丁 | ~~4 个存量失败~~ **已执行**：PrescriptionItemTests.Clear（MedicalCaseId 有意保留——断言改保留）、PrescriptionItemTests.Items_SetProperty（Items.Add 不触发 VM 通知——对齐 NotifyItemsChanged 契约）、ConsultationItemTests.IsPresentIllnessValid（测试赋值"头痛三"仅 3 字符 bug——改 5 字符）、PrescriptionEditorViewModelTests.InitializeFromDto（SingleDosePrice 为计算属性——断言改计算值 17.5m） | 4 个失败全为测试断言与实现意图不符（非实现 bug）；修复后 MedicalCase Unit 94/94 | ✅ 157d72903 |
| 2026-08-11 | R1 需求覆盖矩阵（只读） | 136 US 对照完成：✅64/⚠️46/🔴17/🧲📦15；基线矩阵 ~34 处状态过时；P0 缺口 4 项（远程 auth 路由疑损坏、refresh 端到端断裂、本地 refresh 无验签、导入导出端点双端全缺）+ 备份恢复整体缺失 + FeatureToggle 全库消失。报告见 `docs/compose/archive/requirements-coverage-matrix-2026-08-11.md` | 为「需求先行」治理提供基准；🔴 17 项为代码 backlog 源 | ✅ 8173230a4（报告） |
| 2026-08-14 | Entity/DTO/Model 重构第一批（DTO 移除脱节字段，任务书 entity-dto-refactor-batch1——ADR adr-entity-dto-model-refactor.md） | **已执行**：① **FormulaInputDto/DetailDto 移除幻影字段**——Description/Instructions/Preparation/Contraindications/Source（Entity 无——审计 P1）；② **FormulaHerbItemDto 清理**——移除 Price/UnitPrice/SortOrder/SpecialInstructions + HerbDetailDto 嵌套导航（改 HerbId+HerbName 冗余，HerbNames 改用 HerbName）；FormulaHerbItemInputDto Preparation 字段删除（与 ProcessingMethod 重复——P5 命名统一）；③ **ConsultationDetailDto 移除父聚合字段**（PatientId/UserId/PatientName/DoctorName——MedicalCase 载体，Enrich 同步删）；④ **PrescriptionDetailDto 移除伪字段 Status**（Entity 无——P8）；⑤ **命名统一 Notes→Remark**（PrescriptionItemDto 删别名——P9）；**Mapper 同步修复**：MedicalCaseMapper（4 IgnoreTarget+Enrich 赋值删、Status/Notes Ignore 删）、ConsultationMapper（空串归一化删）、PrescriptionMapper（VM Status 删除）、FormulaDetailModelMapper（8 个失效 Ignore 属性删 + Indication 保留）；**Desktop 模型对齐**：FormulaDetailModel.Source / ConsultationItem 4 字段 / PrescriptionItemViewModel.Status 删除；**顺手修正**：MapHerbs 原把 Preparation 位置参数误当 usage 传入（现用 h.Usage——隐 bug 修复）；LocalWebAPI CloneFormula 删 Description 赋值。**有意保留**：FormulaImportItemDto/FormulaHerbImportItemDto（老系统迁移专用 DTO——保留 Instructions/Preparation/Source 等源字段）；FormulaHerbItemInputDto.SortOrder（客户端排序语义）；PrescriptionItemDto.Role/Subtotal/TotalPrice/TotalWeight（运行时计算——非持久化）；MedicalCase.UserId vs Registration.DoctorId（ADR §5 保留差异） | 构建 0/0；Server 736/736；架构 87/87；Desktop 受影响单测 71/71（存量环境失败 STA/SysAdmin 登录——stash 基线复现一致）；任务书待删 | ✅ 已提交 30af2162e |
| 2026-08-14 | Entity/DTO/Model 重构第二批（Mapper 简化 + 跨文件模式抽取，任务书 entity-dto-refactor-batch2——mapper-chain-audit 2026-08-14） | **已执行**：① **H1 PrescriptionItemMapper 共享类**——MedicalCaseDetailModelMapper/PrescriptionMapper 完全重复的 14 字段双向映射抽取为独立静态类（ToModel/ToDto），两 Mapper + MedicalCaseCommandsViewModel 调用点改指共享类（-40 行重复）；② **H2 FormulaHerbItem 通用方法**——FormulaDetailModelMapper 3 处 FormulaHerbItemModel 构造 + 1 处 InputDto 构造抽取为 ToHerbItemModel/ToHerbItemDto/ToHerbItemInputDto 私有方法（-30 行重复）；③ **M2 PinYinCode 回退统一**——PinYinHelper.EnsurePinYinCode（`pinYin ?? GetPinYinCode(name)` 语义等价）替换 4 处 Mapper 内 `PinYinCode ?? PinYinHelper.GetPinYinCode(name)` 模式（HerbDetailModelMapper/PatientMapper/UserMapper×2）；④ **M3 Guid 空值转换统一**——GuidExtensions.OrNullIfEmpty（Guid + Guid? 双重载）替换 7 处 `== Guid.Empty ? null` 模式（3 Mapper + UserMapper + 3 VM）。**有意保留（第 4 项 Desktop Model 命名对齐不执行）**：① PrescriptionItemModel/FormulaHerbItemModel → XxxViewModel 与**已存在**的 ViewModels/Items 层同名类直接冲突（PrescriptionItemViewModel/FormulaHerbItemViewModel——编译器拒绝）；② 其余 Model（FormulaDetailModel 等 7 类）改名 = XAML 绑定/DI/测试 100+ 引用点大规模重命名零功能收益；③ Model 层为 INPC 可编辑数据模型（ObservableObject + FilterHerbs/SelectedHerb 业务逻辑），「从 DTO 派生」在 WPF 绑定下不可行；④ 审计报告本身未将 Model 改名列为建议（确认 Core+包装模式为合理设计）。**验证**：构建 0/0；Server 736/736；架构 87/87；Desktop 受影响单测 40/40（全量失败 172 全为环境性 localhost:5000 连接 + STA——第一批 stash 基线已实证）；任务书待删 | 用户指示前端测试不需全过（本次改动涉及测试通过即可） | ✅ 已提交 be1e22c84 |
| 2026-08-14 | Desktop 测试清理（P0+P1 低价值测试删除，任务书 desktop-test-cleanup——desktop-test-audit 2026-08-14） | **已执行（删 34 个测试）**：① **P0-1** PatientMasterDetailViewModelTests 删 3 个 RestoreAsync 测试（`[Fact(Skip="RestoreAsync removed")]`——实验实证：移除 Skip 后 2/3 断言失败（MasterDetailCommandGroup 命令宿主重构后失效），确为死测试；**注意**：RestoreAsync 命令/接口实际仍存在（基类 MasterDetailViewModelBase + IPatientStatusHandler）——Skip 原因措辞过时，实质是断言失效；保留同 region 的 CanRestore_ReturnsFalse_WhenNoSelection 活动测试）；② **P0-2** NotificationTypeMappingTests **删 2 个 Skip 死测试**（引用不存在的 ExceptionSeverityMapper——空方法体）——**保留 2 个有效枚举测试**（US_ERR_008_ExceptionSeverity_EnumValues/ HasExpectedMemberCount——引用存在的 ExceptionSeverity 枚举；任务书/审计「整个文件引用不存在类」前提不成立，按「删除前确认」指令不误删）；③ **P0-3** MedicalCaseTests 删 BatchDelete_MultipleCases_ReturnsOperationResult（Skip——试图为同一患者建 2 个活动医案违反业务规则）；④ **P1** 删除 BreadcrumbBarTests.cs（15——WPF 控件属性测试）+ ToastServiceTests.cs（13——纯 `Record.Exception().Should().BeNull()` 弱断言，虚假安全）。**删除前确认**：各测试引用核实行（RestoreAsync 存在但断言失效 / ExceptionSeverityMapper 不存在 / 多活动医案违反规则 / 控件+弱断言低价值）；删除后无残留引用（grep 实证）。**验证**：build 0/0；受影响 Desktop 测试 29/29（PatientMasterDetail 27 + NotificationTypeMapping 2） | 用户指示前端不需全过；审计预估 36 实删 34（保留 2 个有效枚举测试） | ✅ 已提交 dda2a0cea |
| 2026-08-14 | 验证器补全（RegistrationInputDtoValidator 缺失 + UserInputDtoValidator 检查——entity-dto-layer-audit §6.1） | **已执行**：① **新建 RegistrationInputDtoValidator**（Validators/Registration/——原目录缺失）——覆盖 PatientId/DoctorId NotEmpty、PatientName/DoctorName NotEmpty+MaxLength(100)、Source IsInEnum（任务书点名）、RegistrationFee ≥0、Remark MaxLength(500)，与 DataAnnotations + 命令级 CreateRegistrationValidator 对齐；② **UserInputDtoValidator 检查结果：文件整体缺失**（审计「有但不完整」实为不存在）——**新建**（Validators/Users/）：UserName 创建时（Id==null）必填+Length(3,32)+正则 / 更新时有值校验格式；Password Length(6,128)、ConfirmPassword 与 Password 一致；RealName 创建时必填+MaxLength(50)；PhoneNumber 正则+长度、Email 格式+长度、PinYinCode/Role/Remark 有值校验；保留用户名业务检查留在命令级 CreateUserValidator（DTO 级不做业务规则）。**注册**：两验证器位于 LYBT.Shared.Models 程序集——已被 Catalog/MedicalCase/Patients 模块的 AddValidatorsFromAssemblyContaining 自动注册，无需额外 DI 代码（DTO 级验证器与 FormulaInputDtoValidator 同模式：注册但不被管道自动调用，管道只验证 Command 类型） | 构建 0/0（纯新增文件，无行为变化） | ✅ 已提交 fcd532eb5 |
| 2026-08-14 | Desktop 死代码清理（任务书 desktop-dead-code-cleanup——desktop-dead-code-audit 2026-08-14） | **已执行**：① **P1 DC-001 删重复 StringResources.resx**（顶层 Resources/Strings/ 与 Shell/Resources/Strings/ MD5 完全一致——保留 Shell 版；验证 csproj 无旧路径 EmbeddedResource 引用（EmbeddedResource Remove 在注释块）→ 无需 csproj 改动）；② **P2 DC-002 删 PatientEvents.CreatedEvent + PatientCreatedPayload**（零引用——UpdatedEvent/PatientUpdatedPayload 在用保留）；③ **P2 DC-003 删 LoginStrings.resx**（零引用——已被 StringResources 替代）；④ **P2 DC-004 更新 Resources/AGENTS.md**（移除虚构 Dictionaries/ 引用——实际主题在 Controls/Themes/、字符串在 Shell/Resources/Strings/）；⑤ **P3 DC-005 BreadcrumbItem 统一**——Contracts record（Title/ViewName/IsCurrent）扩展 Level/IsLast/NavigateCommand（ICommand），删除 Controls 本地 class；BreadcrumbBar.xaml.cs 改用 Contracts record（UpdateBreadcrumbs 逻辑等价：Title=原 Label），XAML 绑定 Label→Title；Contracts.csproj net8.0 目标排除 UI/BreadcrumbItem.cs + Services/INavigationCoordinator.cs（ICommand 为 WPF 类型——与 SyncEvents 先例一致；INavigationCoordinator 实现全在 net8.0-windows 的 Infrastructure）。**验证**：build 0/0（含 Contracts net8.0 目标）；Desktop 全量 失败 172→135（减少 37 无新增——全为环境性 localhost:5000+STA）；任务书待删 | 功能等价（无 API/DB/WPF 线程行为变化） | ✅ 已提交 9d11eecfa |
| 2026-08-14 | Desktop DI 注册修复（任务书 desktop-di-fix——IApiClientIdentity 未注册致启动崩溃） | **已执行（启动验证逐层暴露并修复 6 项）**：① **IApiClientIdentity 注册**——UnifiedApiClientExtensions 加 `RegisterSingleton<IApiClientIdentity>(r => r.Resolve<IApiClient>().Identity)`（SwitchingApiClient 子接口转发；消费方 AuthenticationService/LogoutService/TokenLifecycleService/AuthHealthService 直接注入）；② **同类排查：其余直接注入子接口**——ConnectionModeService→IApiClientMedicalCases、DeploymentService→IApiClientDeploy、DiagnosticsService→IApiClientDiagnostics、ServerConfigurationService→IApiClientConfiguration 一并注册（启动验证第二崩点）；③ **DesktopUpdateOptions 注册缺失**——PrismConfigurationExtensions.AddLybtClientConfiguration 补 `RegisterOptions<DesktopUpdateOptions>`（系统性核对：全仓 IOptions<T> 注入点 vs 注册——唯一缺失，FeatureToggleOptions 注册无注入点为 T8 动态读正常）；④ **LocalWebApiProgram 内容根/环境**——`WebApplication.CreateBuilder` 默认按当前目录（dotnet run=仓库根）加载配置 → Jwt 节缺失 → IDX10703 key length zero；改 `WebApplicationOptions { ContentRootPath=AppContext.BaseDirectory, EnvironmentName="Development" }`（同时替代原 WebHost.UseEnvironment——CreateBuilder 后调用抛 NotSupportedException）；⑤ **LocalWebAPI 模块 DI 缺口**——补 AddMemoryCache/AddOutputCache/ICacheInvalidationService（MedicalCase 模块依赖，远程在 DatabaseServiceCollectionExtensions 注册）+ AddSignalR（NotificationService 依赖 IHubContext<RegistrationHub>）。**验证**：build 0/0；MainWindow 加载成功（原 XamlParseException/ContainerResolutionException 消除——启动管线推进至 LocalWebAPI DB 初始化）；Desktop 全量失败无新增（144 全环境性 localhost:5000+STA，0 个 LocalWebAPI/DI 相关失败）。**遗留（登记）**：LocalWebAPI 启动仍卡在数据库种子 `User security stamp cannot be null`（Identity 种子逻辑存量问题——超出本任务范围，本地模式完整可用待后续任务）→ **已修复（2026-08-17 mode-switch-analysis）**：IdentitySeedData 补 null-stamp 修复，见下 | 完成（门禁达成）；后续任务 resolved：模式切换三大问题 ✅ | ✅ 待提交 |
| 2026-08-17 | Desktop 双模式切换修复（任务书 mode-switch-analysis） | **已执行（三大问题）**：① **ConnectionSettingsService 配置持久化迁移**——bin/appsettings.json（构建 --no-incremental 覆盖）→ `%LOCALAPPDATA%/LYBTZYZS/user-settings.json`；构造优先读用户文件（RemoteUrl 显式清空保留），缺失键回退 appsettings；roundtrip 单测覆盖重启恢复；② **登录页模式刷新补齐**——ConnectionStatusViewModel.ModeChanged 订阅核查正确；ModeChanged 处理器补 ApiStatusMessage 同步（设置对话框/首次运行向导发起切换及时反映）、SwitchToLocal/SwitchToRemote 统一 async/await（去 UI 阻塞 GetResult + fire-and-forget）+ SyncModeDisplay 对齐；③ **LocalWebAPI 种子崩溃根因**——`User security stamp cannot be null` = UserManager.GetSecurityStampAsync 从存储取到 null stamp（历史裸 EF 建用户）→ EnsureUserAsync 的 GeneratePasswordResetTokenAsync 抛 InvalidOperationException → LocalWebApiStartupStep 失败；IdentitySeedData 补 `SecurityStamp 为空 → UpdateSecurityStampAsync 修复`（先于密码重置/角色校准），本地/远程共用种子双受益。**验证**：Desktop Shell/Server WebAPI build 0/0；相关测试 28/28 + AuthController 集成 8/8；新增 IdentitySeedDataStampRepairTests 2 个回归（null-stamp 存量用户重新种子不再抛 + stamp/密码修复断言） | 所有测试绿 | 完成（commit 待填） |
| 2026-08-11 | T4 P0 生产 bug 修复 | ~~6 项（4 P0 + 2 P1 安全）~~ **已执行**：P0#1 IdentityController 认证 5 端点路由改 `/` 开头绝对路径（修复合并后 `/api/v1/users/api/v1/auth/*` 组合路由损坏）；P0#2 Login/Refresh/ValidateAutoLogin 响应签发 RefreshToken（access 即刷新凭据，修复客户端刷新断链）；P0#3 本地 /refresh 改 ValidateToken 验签（原 ReadJwtToken 只解析，任意伪造 JWT 可换令牌）；P0#4 导入导出端点双端 12 个（import-template/export/export-all × 患者/药材/验方，NPOI 2.7.2 ExcelExportHelper 共享于 Infrastructure——WebAPI+LocalWebAPI）；P1#10 保留用户名清单（UserReservedNameHelper 共享，Validator+Handler 双保险）；P1#11 AutoLoginToken 服务端签发+轮换（GenerateAutoLoginToken 30 天，RememberMe 条件，ValidateAutoLogin 轮换） | 修复后 Server 认证单测 56/56、架构 87/87；备份/FeatureToggle 两 P0 未做（全新功能大工程，建议 T5） | ✅ 58f4e571a |
| 2026-08-11 | R2 需求文档校准（纯文档） | **已执行**：13-traceability-matrix.md 61 处状态校准（v1.1，含 T4 修复后状态）+ 统计汇总表重算（141 US：✅94/⚠️30/🔴10/🧲7）；12 个需求文档 123 处 US 状态段同步；WebAPI README 3 个 stale 端点（batch-details/consultations/prescriptions）删除 | 文档状态列如实反映代码现状（文档是字典不是过程）；🔴 10 项为剩余代码 backlog | ✅ 80a4a4b80 |
| 2026-08-11 | T5-1 backlog 安全修复 | ~~4 项安全缺口~~ **已执行**：#13 患者 DTO 加 [SensitiveData]（IdNumber/PhoneNumber，管道自动生效）；#12 用户管理（单删不可删自己、重置密码 sysadmin 保护、GET /users role/status 筛选接线——Repository 已支持 Service 丢参修复、REG-BR-006 有待诊挂号禁止禁用医生——跨模块 HasWaitingRegistrationsAsync）；#9 挂号服务端守卫（Cancel 仅 Waiting + 关联医案拒绝——实体层；同日重复挂号 HasSameDayWaitingAsync——REG-BR-007）；#8 MC 所有权（GetDetailDtoAsync/GetAuditLogsAsync Doctor 非本人 Forbidden、Search 按操作者过滤、close 强制关闭仅 Admin——US-MC-004/007/012/017） | 全量构建 0/0；架构 87/87；Server 568 通过（3 失败=ConfigurationLoadingTests 存量+并行竞争偶发单跑通过） | ✅ a2344feb2 |
| 2026-08-11 | T5-2 backlog 功能修复 | ~~4 项功能缺口~~ **已执行**：#17 报表 5 端点 AC（endDate 默认=startDate 单日语义 + startDate>endDate→400——US-REPORT-001~003）；#15 FLAW-F1 降级（Formula.DegradeToDraftIfAnyHerbUnvalidated——Validated 验方更新后任一药材未验证→Draft，US-FORM-010）；#14 验方单条创建/更新持久化药材（Formula.ReplaceHerbs + MapHerbs——原 Mapper 丢弃 Herbs，仅批量导入能建带药材验方，US-FORM-003/004）；#18 打印回写全链接线（Repository→聚合代理→接口→IPrintService.PreviewAsync 回调→预览窗打印按钮→PrintHandler fire-and-forget RecordPrint——原服务端完整客户端零调用，US-PRINT-004） | 全量构建 0/0；架构 87/87；Server 570 通过（1 失败=ConfigurationLoadingTests 存量） | ✅ b824332cb |
| 2026-08-11 | T5-3 业务规则/审计修复 | ~~2 项~~ **已执行**：#7 EditReason 校验（SaveAsync 路径——打印后修改/非 Admin 编辑已完成医案需提供 EditReason 否则 422；US-MC-002/016 打印保护 + Completed 编辑铁律）；#16 更新审计+字段 diff（保存成功后写 MedicalCaseAuditLog——OperationType=Update、ChangedFields/OldValues/NewValues 填充诊断 4 字段+处方条数 diff；原仅取消写审计且 diff 列从不填充，US-MC-017） | 全量构建 0/0；架构 87/87；Server 570 通过（1 失败=ConfigurationLoadingTests 存量） | ✅ ebc51a54f |
| 2026-08-11 | T6 全新功能设计（暂不实现） | backlog 剩余 #5 备份/恢复（US-SHELL-013+NFR-AVAIL-001）+ #6 FeatureToggle（US-CFG-004）按需求先行门禁深化设计：备份=ILocalDbBackupService（T-SQL BACKUP/RESTORE + 登录后 fire-and-forget 自动备份 + 7 天清理 + Sysadmin BackupManagementView 恢复工作流）；FeatureToggle=feature-toggles.json + IOptionsMonitor 热更新（沿 ClinicSettingsService 先例）+ 仅策略级 2 键（OverwriteConflicts/DuplicateHerbMergeStrategy，不回到 18 开关反模式）。设计依据见需求文档 [US-SHELL-013](../02-requirements/11a-shell.md)（备份/恢复）与 [US-CFG-004](../02-requirements/11b-configuration.md)（功能开关） | 待用户审批 4 个确认项（保留期/重启/配置位置/实施顺序）后进入 T7/T8 实现（**备份/恢复已由 B-06 于 2026-09-22 交付**，见 #146） | ✅ 134edc562（设计） |
| 2026-08-11 | T7-1 备份服务层（UI 属 T7-2） | **已执行**（用户确认：7 天保留/自动重启/串行 T7 先）：ILocalDbBackupService + LocalDbBackupService（T-SQL BACKUP/RESTORE DATABASE，%AppData%/LYBTZYZS/Backup/，SemaphoreSlim 串行，RESTORE 编排 Stop 内嵌 LocalWebAPI→恢复→自动 Start）；登录后 fire-and-forget 自动备份+7 天清理（ShellEventCoordinator）；SystemConstants.BackupRetentionDays 30→7；DI 注册（ServiceCollectionExtensions） | 全量构建 0/0；架构 87/87 | ✅ 2c7c4a471 |
| 2026-08-11 | T7-2 备份管理 UI | **已执行**：BackupManagementViewModel（状态卡片：上次备份/文件数/总大小 + 手动备份进度/失败原因 + 文件列表 + 恢复）+ BackupManagementView（MaterialDesign 卡片 + DataGrid + 恢复前 MessageBox 确认「将覆盖当前数据库」）；ViewNames.BackupManagement + SysadminModule 注册 + ModuleLazyLoader 映射 + NavigationManager SuperAdmin 菜单「备份恢复」（DatabaseBackup 图标） | 全量构建 0/0；架构 87/87；US-SHELL-013 六项 AC 全部落点完成 | ✅ 0c2fb1cef |
| 2026-08-11 | T8 FeatureToggle 实现 | **T8-1 基建已执行**：FeatureToggleOptions（OverwriteConflicts/DuplicateHerbMergeStrategy 2 键）+ IFeatureToggleService + FeatureToggleService（IConfiguration 动态读 + GetReloadToken 变更回调 → TogglesChanged 热更新事件——注意 RegisterOptions 为启动快照，服务改用 IConfiguration 动态读实现真热更新）+ feature-toggles.json（应用根目录，用户确认）+ AddJsonFile reloadOnChange + DI 单例。**T8-2 两消费点核实后记录**：DuplicateHerbMergeStrategy 唯一消费在 HerbListControlViewModel（默认 Max = 需求 v1.0 默认值，行为已正确——接线需大改两 VM 构造链且无当前差异价值）；OverwriteConflicts 无同步冲突功能（无消费场景）——配置/API（GetDuplicateMergeStrategy/GetOverwriteConflicts）就位，未来功能接入即用 | 全量构建 0/0；架构 87/87；US-CFG-004 热更新 AC 以 IConfiguration 动态读实现（优于快照先例） | ✅ 6459ad53e |
| 2026-08-11 | P1 打磨第一批（⚠️ 部分实现 6 项按业务影响） | **已执行**：MC-011 完成权限（Doctor 仅本人医案可完成，CompleteAsync 加 UnauthorizedAccessException）；MC-016 权限 DTO 补 RequiresEditReason/DenialReason（打印后修改/非 Admin 编辑 Completed 判定）；FORM-001 验方列表所有权过滤（Doctor 仅本人+共享——全链 Repository/Service/接口/双端 Controller，含导出路径）；MC 价格口径统一（Desktop 编辑器 TotalPrice 补折扣，对齐服务端 MC-D14；打印端为收费合计语义合理差异）；REG-001 前台挂号（患者存在/Enabled 校验 + 挂号费从医生带出 REG-BR-009——UserBasicDto 已有 RegistrationFee 字段 Mapperly 自动映射）；REG-004 队列当天过滤（REG-BR-012，onlyToday 可选参数——禁用检查保持全量从严） | 全量构建 0/0；架构 87/87；Server 570 通过（1 失败=ConfigurationLoadingTests 存量） | ✅ 5c41f98b7 |
| 2026-08-11 | P2 打磨第二批（⚠️ 部分实现 6 项） | **已执行**：AUTH-003 本地 auto-login 加限流（LocalLogin 5/min 策略——原无限流）；USER-004 创建用户默认密码用 DefaultPasswords.NewUserPassword 配置（原随机 GUID 不可运维，空配置回退随机）；PAT-003/004 患者电话唯一查重（ExistsByPhoneAsync Create+Update 排除自身——需求电话唯一语义原仅姓名查重；DB 唯一索引记录为后续 migration）；HERB-012 批量删除逐项引用检查（ValidateAsync 钩子——有处方/验方引用的药材拒绝并报引用数）；FORM-006 批量导入加 10000 上限（对齐药材导入）；USER-005 更新用户角色（Role 字段更新 + sysadmin 保护 + 非 SuperAdmin 不可提升 Admin/SuperAdmin 层级约束——原 Role 被忽略）。**AUTH-008 本地登出记录**：本地 JWT 无状态 1 年（无会话表），登出撤销需黑名单机制——本地威胁模型接受（G-02） | 全量构建 0/0；架构 87/87；Server 570 通过（1 失败=ConfigurationLoadingTests 存量） | ✅ 82a7e7464 |
| 2026-08-11 | P3 打磨第三批收尾（⚠️ 剩余 6 项） | **已执行**：AUTH-006 重放检测撤销该用户全部会话（RevokeAllUserSessionsAsync——原仅拒绝不撤销）；USER-010 JWT Bearer OnTokenValidated 禁用/删除用户拦截（令牌有效期内状态检查——原禁用后令牌到期前仍可用）；USER-012 批量用户 100 条上限；FORM-007 待验证列表分页（Query/Handler/双端 Controller——原全量返回）；MC-D09 验方导入禁用跳过 Toast 提示（原仅日志——「已停用、已跳过 N 味（名称）」）；PAT-010 患者批量引用计数一次查询（CountMedicalCasesBatchAsync GroupBy——原逐患者 N+1；GetRecent 5 条详情保留逐查） | 全量构建 0/0；架构 87/87；Server 568 通过（3 失败=ConfigurationLoadingTests 存量+2 并行偶发单跑通过） | ✅ 8fd8949f3 |
| 2026-08-11 | R2-补 需求文档状态列全量重扫（纯文档） | **已执行**：R2 后 14 commit（T4/T5/T7/T8/P1-P3）修复同步——traceability 40 处校准（v1.2）+ 12 文档 123 处状态段 + 统计重算（141 US：✅122/⚠️7/🔴5/🧲7，原 ✅94/⚠️30/🔴10/🧲7）；🔴 5 = MC-008/009/018 + HERB-005 + SHELL-018；⚠️ 7 = AUTH-002/HERB-006/SHELL-007/ERR-006/007/CARD-002；🧲 7 = REG-002 + SHELL 规划项 | 文档状态列如实反映 2026-08-11 当前代码（含全部新功能与打磨） | ✅ 24b8f24c5 |
| 2026-08-11 | R2-补 专项（虚构控制器引用清理，纯文档） | **已执行**：05-herbs.md/06-formulas.md/13-traceability-matrix.md 的 HerbsController.cs/FormulasController.cs 虚构引用（C3b 合并后已删）全部替换为 CatalogController.cs 实际端点行号（已核实——herbs 50/70/82/107/124/146/174/202/228/247/262/287/302/319/339；formulas 363/385/397/423/443/464/489/516/542/565/580/607/627/657/677）；全仓 02-requirements 虚构引用清零 | 替换后残留 0（grep 实证） | ✅ 3d8f8b017 |
| 2026-08-11 | R3-补 反向脱节补 US（纯文档） | **已执行**：R1 §三 6 类代码超前功能收编为 7 个新 US（标注「已实现未文档化」+ 状态 ✅）：PAT-014 身份证号查询（PatientsController:296）、FORM-014 验方克隆（LocalWebAPI:459——注记远程待补）、MC-020 医案批量删除（:178）、REPORT-004 趋势/绩效/排行/流量 5 端点（ReportsController:69-158——超越 v1.0 克制声明）、CFG-005 服务器配置 PUT/validate 端点、CFG-006 诊所信息热更新（ClinicSettingsService）。traceability 新增 6 行 + 统计重算（147 US：✅128/⚠️7/🔴5/🧲7，v1.3） | SignalR/安全审计/Restore 等 R1 第 3/4 点已由既有 US 覆盖（R2 后 ✅）不需补 | ✅ 0b5566f54 |
| 2026-08-11 | B1 backlog 小修复批（5 项） | **已执行**：HERB-005 单删引用检查（CatalogEntityCommandHandlerBase.ValidateBeforeDeleteAsync 钩子 + Herb override 处方/验方引用拒绝）；MC-008/009 患者历史聚合端点 GET /medicalcases/patients/{id}/history（复用 GetPatientRecentMedicalCasesAsync）；MC-018 批量详情 POST /medicalcases/batch-details（BatchIdsRequest ≤100 + GetByIdsWithDetailsAsync 单次 In 查询 + Doctor 所有权过滤）；AUTH-002 本地锁定对齐远程（LoginOptions.LockoutEnabled=true + Identity MaxFailedAccessAttempts=5） | 全量构建 0/0；架构 87/87；Server Catalog+MC 71/71；Desktop VM/Service 198 通过；LocalWebAPI Integration 存量失败（stash 基线实证） | ✅ 9117df395 |
| 2026-08-11 | B2 backlog 小修复批（6 项） | **已执行**：HERB-006 服务端 Excel 解析（ExcelImportHelper NPOI + 双端 POST /herbs/import-excel→BatchImportHerbsCommand）；SHELL-007 切换守卫（SetModeAsync + ModeSwitchResult：远程 URL/可达/未完成医案 ERR-70506 三重守卫，失败自动回退原模式；ConnectionStatusViewModel 阻断提示）；ERR-006/007 异常体系（Conflict 409/Unauthorized 401/ApiException/Validation 422+errors 字典/ExceptionFactory——AppException 族 handler 自动映射）；CARD-002 维持用户决定（2026-08-08 UI 设计时整体考虑，不实施）；REG-002 QuickVisit UI 接线（QuickVisitAsync 全链——Refit+Http 双轨 + QuickVisitDialog + 列表 VM 命令导航 MedicalCaseWorkspace + 医生工具栏按钮） | 全量构建 0/0；架构 87/87；Server Registration 6/6；Desktop Integration/E2E 存量失败同 B1 | ✅ 6b952836c |
| 2026-08-11 | traceability 状态同步（B1/B2 后） | **已执行**：B1 5 项 + B2 5 项状态 🔴/⚠️/🧲→✅（CARD-002 维持 ⚠️ 用户搁置）；统计重算（147 US：✅137/⚠️3/🔴1/🧲6，v1.4——🔴1 = SHELL-018 配置中心待设计批次） | 行级替换 + 统计公式重算 | ✅ a10517692 |
| 2026-08-13 | 恢复被误删的 POST /Registrations（任务书 restore-create-registration——真机 405） | **已执行**：a99619f47 删 quick-visit 时**误删了远程 Create 端点**（`[HttpPost]` + Create 一起被删——diff 证实——真机 swagger 无 POST + 405）。**恢复**：远程 RegistrationsController 恢复 Create（a67285088 原样——`[Authorize(DoctorOrReceptionist)] [HttpPost] [EnableRateLimiting]` + LogOperation + CreatedAtAction）；**本地**：Create 未被误删（存在）但补 `[HttpPost]`（消除本地 405 风险——同步双端）；**同类排查**：a99619f47 全量删除审查——Base 控制器仅删 QuickVisit ✓、本地仅删 QuickVisit override ✓、其余文件均 QuickVisit 专属 ✓——**唯一误删 = 远程 Create**。**测试**：`RegistrationsCreateRouteTests` 2（Create [HttpPost] + DoctorOrReceptionist 策略恢复 / QuickVisit 保持删除） | 构建 0/0；Server 696/696（+2）；架构 87/87；任务书待删 | ✅ 待提交 |
| 2026-08-13 | QuickVisit 两步改造（任务书 quickvisit-twostep——产品决策定案） | **已执行**：① **删 quick-visit 端点**——QuickVisitCommand/Handler/Validator + QuickVisitInputDto/ResultDto（4 文件）+ BaseRegistrationsController/远程/本地 3 控制器方法 + Desktop 全部接线（对话框 3 文件 + 8 接口/客户端/仓储/服务方法 + VM 方法 + Module 注册 + 测试 1 文件）——**代码残留 0**（仅历史注释）；② **POST /Registrations 支持 Source=Doctor**——CreateRegistrationCommandHandler 改：**一律 Waiting**（原 Source=Doctor→InProgress——一步原子遗留——与 StartVisit 域方法「仅 Waiting 可接诊」矛盾——改后统一合法）；③ 确认：POST 权限已含 Doctor（DoctorOrReceptionist）✓；doctorId=当前医生（操作者传 OperatorId——startvisit-createdby-fix 已加）；④ **文档同步**——traceability US-REG-002（两步）、08-registration（本地模式/原子性/要素/REG-BR-009）、03-users（权限决策）、04-permissions（策略表）、MedicalCases README | 构建 0/0；Server 694/694；架构 87/87；Desktop 集成测试存量环境失败（localhost:5000 服务未起/WPF headless——与本次无关）；任务书待删 | ✅ 待提交 |
| 2026-08-13 | StartVisit CreatedBy NULL（任务书 startvisit-createdby-fix——真机 start-visit 500） | **已执行**：服务器日志直达根因（新日志机制）：`[REPO] MedicalCase.SaveChanges 保存失败 → SqlException: 不能将值 NULL 插入列 CreatedBy`——`CreateFromInputDtoAsync` 创建块缺 CreatedBy（BaseEntity 可空但 MedicalCaseConfiguration 28-29 强制 NOT NULL）。**修复**：`CreatedBy = currentUserId`（创建者=操作医生——接诊即建 US-REG-005+US-MC-001 核心路径）。**同类排查**：① 唯一 NOT NULL CreatedBy = MedicalCase（配置强制——已修）；② Registration.CreatedBy 可空（不 500）但语义缺失——补（`CreateRegistrationCommand` 加 OperatorId + Handler 设 CreatedBy——双端 Controller 传 GetOperator——QuickVisit 已有先例）；③ Formula/Herb 工厂有 createdBy 参数 ✓。**测试策略**：MedicalCaseCommandService 依赖链重（8+ 接口——本地单元测试不可行）——以全量回归 + 真机 start-visit 200 为验证 | 构建 0/0；Server 694/694；架构 87/87；api-manual-test Bug 3；任务书待删 | ✅ 待提交 |
| 2026-08-13 | 软删唯一索引冲突（任务书 softdelete-uniqueindex-fix——真机创建 testdoctor 稳定 500） | **已执行**：**系统性 grep 全唯一约束**——ApplicationUser.UserName（UserNameIndex）+ Email（EmailIndex）为软删实体唯一索引；Token/PatientId/MedicalCaseId 唯一（非软删——不受影响）；业务实体（Herb/Formula/Patient）无 DB 唯一索引（业务查重——已友好）。**根因链**（SQL 实证）：软删用户仍占索引 + FindByNameAsync 走 QueryFilter（不含软删）→ 认为可用 → INSERT 撞索引 → 500。**策略**：方案 B（软删不释放索引）——创建查重**含软删**（IgnoreQueryFilters——UserName + Email）→ **422 友好提示**（「已被删除——请先恢复或更换」）——恢复路径验证安全（UserName 不变——索引行是自己的——无冲突）。**测试**：`SoftDeleteUniqueIndexTests` 3（真实 UserStore + SQLite——软删同名 422 / 软删 Email 422 / 软删可恢复 + 恢复后同名 422）+ 层级测试改真实 UserStore（Fake 无法模拟 EF Users 查询） | 构建 0/0；Server 694/694（+3）；架构 87/87；US-USER-006 软删语义补充；任务书待删 | ✅ 待提交 |
| 2026-08-13 | 分层日志级别落地（任务书 log-level-implementation——US-LOG-000 5 项缺口） | **已执行**：① **P0-1 Repository 异常日志**——BaseRepository GetByIdAsync/GetByIdIncludingDeleted/AddAsync 加 try-catch `LogError("[REPO] {EntityType}.{Op}({Id}) 查询/保存失败")`（@x 含 InnerException + CorrelationId Enricher 自动）+ SaveChangesAsync Error 加 [REPO] 前缀；Formula override（Include Herbs 查询——绕过基类）补 2 处；② **P0-2 MedicalCases 写操作**——远程 8 端点 LogInformation→LogOperation（统一 + 脱敏——Create/Update/Delete/Flag/Status/Close/Suspend/Cancel）+ 本地 LocalWebAPI 7 端点补 LogOperation（15 处总）；③ **P1-3 Service 业务决策**——Formula 降级 Draft 检测 + Warning（US-FORM-010）；④ **P1-4 Users/Auth**——审计确认已全覆盖（BaseUsersController 7 端点 + Auth 锁定 Warning 已有）——无需补；⑤ **P1-5 422/403 级别校准**——422/400 业务拒绝确认不打 Error（静默）✓ + `ForbidResponse`（403）补统一 Warning（Path/User 上下文） | 构建 0/0；Server 691/691；架构 87/87；任务书待删 | ✅ 待提交 |
| 2026-08-13 | 可观测性完善（任务书 observability-completion——US-LOG-008/009 + 定位指南） | **已执行**：① **US-LOG-008 启动版本日志**——`LoggingBootstrap.GetInformationalVersion/GetCommitSha`（AssemblyInformationalVersion——SDK 注入 +sha）+ Server（Program.Main）与 Desktop（App.xaml.cs）启动首条日志含版本/commit/env/pid——**本地启动实测**：`[启动] LYBT.WebAPI v1.0.0+21cc2ecbe... (commit 21cc2ecbe...) env=Production pid=41212`；② **US-LOG-009 单实例日志文件**——主 File sink + bootstrap sink 加 `shared: true`（多进程共享同文件）——**实测**：同日两次重启仍单文件（bootstrap-20260813.log 无 _001）；③ **定位指南**——`docs/06-operations/03-troubleshooting.md`（US-ERR-009——四步工作流：版本首行→CorrelationId 关联→`--->` 内层根因→当前日志文件 + grep 示例 + 生产屏蔽不破坏诊断说明）；④ **flaky 根治**——Serilog 全局 ReloadableLogger 与多宿主测试并行冲突（freeze——~1/3 概率）→ 宿主测试类同 `[Collection("WebApiHostTests")]` + **程序集级禁并行**（AssemblyInfo）——连续 2 次串行全量稳定 | 构建 0/0；Server 691/691（串行稳定）；架构 87/87；任务书待删 | ✅ 待提交 |
| 2026-08-13 | CreateUser 层级校验（任务书 createuser-hierarchy-fix——USER-D04 真机缺口） | **已执行**：真机实测 sysadmin 创建 Doctor → 200（应拒绝）——根因 `CreateUserCommandHandler` 仅 `IsAdmin` 粗粒度检查（漏 USER-D04 层级）。**修复**：command `IsAdmin`→`OperatorRole`（UserRole——Controller 已有 currentRole）；Handler 加层级校验——Sysadmin 仅可创建 Admin（创建 Doctor/Receptionist → Forbidden 403「系统管理员仅可创建 Admin 角色」）；Admin 仅可创建 Doctor/Receptionist（创建 Admin → 403「Admin 仅可创建 Doctor/Receptionist 角色」）；任何操作者禁创建 SuperAdmin（系统唯一账号）；非 Admin → Unauthorized。**测试**：`CreateUserHierarchyTests` 7 用例（任务书 4 + 补充 3——Receptionist 拒绝/SuperAdmin 拒绝/非管理员 401）——FakeUserManager 手写替身 | 构建 0/0；Server 691/691（+7）；架构 87/87；矩阵 C7 已修标注 + US-USER-004 状态；任务书待删 | ✅ 待提交 |
| 2026-08-13 | 移除全局 SplitQuery（任务书 splitquery-fix——真机失败真正根因） | **已执行**：根因链——`DatabaseServiceCollectionExtensions.cs:105` 全局 `UseQuerySplittingBehavior(SplitQuery)` → 含 Include 查询拆多条 SQL 独立连接 → 远程 SQL（243，延迟 ~0.42s）多连接失败/超时 → 种子失败/保存异常（PUT formula 500 真正根因——4 轮本地修复全过但真机全败）。**修复**：移除全局 SplitQuery（改 EF 默认 SingleQuery——JOIN 一条 SQL）；**评估**：唯一两级集合 Include（MedicalCaseRepository Prescription.Items——数据量小）保持 SingleQuery（远端多连接更糟）；模块 DbContext（AddModuleDbContext——原本 SingleQuery）不受影响 | 构建 0/0；Server 684/684；架构 87/87；踩坑清单 #11 | ✅ 待提交 |
| 2026-08-13 | 真机深挖（任务书 formula-deep-fix——Dev 非空库权威 + 大范围调整授权） | **已执行**：① **Dev 真实数据复现（LYBTDB_Dev）**——FormulaDevReproTests 真实 formula 更新 **REPRO-NEGATIVE**（SQL 证据：DELETE 旧→UPDATE Formulas RowVersion→INSERT 新）——当前代码在 Dev 正确；② **部署滞后铁证**——真机 dll 03:52 vs bca7bccf0 11:49——真机 500/201/500FK 全为旧 dll；③ **重写 FormulaRepository.UpdateAsync**（显式 DbSet 模式：RemoveRange 旧 + Add 新——根治 EF item 状态误判；父行走 base 乐观语义）；④ 校验链——本地 DI 注册测试证明 Validator 已注册（真机 201 = 旧 dll）；⑤ 测试——Dev 真实数据（Repro）+ Test 库真实 SQL（RealSqlUpdate 2）+ Unit（Reference 3/ReplaceHerbs 2/DI 2） | 构建 0/0；Server 684/684；架构 87/87；任务书待删 | ✅ 待提交 |
| 2026-08-13 | 真实 SQL 修复（任务书 formula-realsql-fix——PUT formula 真机并发 + 引用校验缺口） | **已执行**：① 并发——`FormulaRealSqlUpdateTests` 2 用例连 **192.168.190.243 LYBTDB_Test**（TEST_DB_CONNECTION 激活）**2/2 通过**（764ms 真实执行——ReplaceHerbs 新 item 真实 SQL 上 INSERT 正确无并发冲突 + Detached 场景验证）；② 引用校验缺口——空 herbs → Validator `NotEmpty`（400）；herbId 不存在/已删除 → Base 新钩子 `ValidateBeforeSaveAsync`（Create/Update 前）+ Formula override（按 Id 逐个查 Herb——不存在/软删 → `FormulaValidationFailed` 60004 → **422**）——`FormulaReferenceValidationTests` 3 用例 | 构建 0/0；Server 681/681（+5）；架构 87/87；任务书待删 | ✅ 待提交 |
| 2026-08-13 | ReplaceHerbs 第 2 层根因修复（真机 PUT formula 仍 500——任务书 replaceherbs-rowversion-fix） | **已执行**（BaseRepository 修复后 PUT herb 200 但 PUT formula 仍 500——Formula 特有子集合）：**根因实证（EF SQL 日志铁证）**——ReplaceHerbs 的新 FormulaHerbItem 被 EF **误标 Modified**（非 Added）→ SaveChanges 发 `UPDATE FormulaHerbItems WHERE Id=新Guid` → **0 rows → DbUpdateConcurrencyException**（SQLite 复现——与真机一致）。**修复**：① ReplaceHerbs 孤儿删除模式（逐个 Remove 旧 + 只设 FormulaId 不设 Formula 导航——导航赋值到已跟踪父会把 Detached 子 Attach 成 Modified）；② **FormulaRepository.UpdateAsync override**——Herbs 集合中 Detached/Modified 的 item 强制 `EntityState.Added`（ReplaceHerbs 语义=全换新 Guid——显式 INSERT）；③ BaseRepository.UpdateAsync 并发重试（包装异常匹配 InnerException）——真并发仍抛（乐观语义保持）。**复现测试**——FormulaReplaceHerbsTests 2（真实 DbContext+SQLite：更新+改组成正常落库 / 同组成干净替换——旧 herbs 孤儿删除验证） | 构建 0/0；Formula/Herb 并发 4/4；Server 全量 676/676；架构 87/87；任务书待删 | ✅ 待提交 |
| 2026-08-13 | UpdateHandler RowVersion 并发修复（真机 PUT 500——任务书 updatehandler-rowversion-fix） | **已执行**（真机：PUT /formulas/{id} 500——DbUpdateConcurrencyException 0 rows）：**根因实证**——`BaseRepository.UpdateAsync` 的 `_dbSet.Update(entity)` 对**已跟踪实体**（HandlerBase.GetByIdAsync 加载后）是反模式：全属性标记 Modified（含子集合/RowVersion）——UPDATE WHERE RowVersion 值错误 → 0 rows。**修复（倾向 A 根治）**——UpdateAsync 检查 `_context.Entry(entity).State`：Attached/Modified → **只 SaveChanges**（EF 变更跟踪器精确标记实际变更列——RowVersion 仅 WHERE 值正确）；Detached → 保留 Update() 语义（外部兼容）。**复现测试**——HerbUpdateRowVersionTests 2（真实 DbContext+SQLite concurrency token：Attached 更新正常落库 / 并发修改仍抛乐观异常——防覆盖语义保持；Detached 无真实场景删除）。影响面：通用 HandlerBase——Herb/Formula 全量回归覆盖（Server 674/674） | 构建 0/0；RowVersion 2/2；Server 全量 674/674；架构 87/87；任务书待删 | ✅ 待提交 |
| 2026-08-13 | PostProcessor+ConfigurationManager 补丁（df49d7ba9 后） | **已执行**：① 新真实类型测试缺 `using System.Text`（命名空间冲突）——补；② **Reload 与 AddJsonStream 冲突**（StreamConfigurationProviders 流二次加载崩）——测试改用 AddInMemoryCollection（模拟 JSON 层——真实 ConfigurationManager + Reload 安全——Reload 修复本身正确：文件 JSON provider 重读 OK）；③ 新测试加 `[Collection("EnvIsolated", DisableParallelization)]`（env 并行污染——DeploymentConfigTests 同 collection 迁移） | PostProcessor 14/14；Server 全量 672/672；构建 0/0 | ✅ 待提交 |
| 2026-08-13 | PostProcessor+ConfigurationManager 兼容修复（真机 PUT 500——任务书 postprocessor-configmanager-fix） | **已执行**（真机测试发现：PUT /formulas/{id} 稳定 500——连接串读到未展开占位符 `${DB_SERVER}`——环境变量值正确）：**根因**——ConfigurationPostProcessor.Process 在 ConfigurationManager（生产类型）上 `Providers.ToList()` 快照不含刚 `AddEnvironmentVariables` 的 provider（Add 延迟重建）→ 误判无有效值 → 回退 JSON 占位符。**修复**——Process 开头 `root.Reload()`（强制 provider 重建 + 值刷新——Reload 后 Providers 含全部）。**测试盲区**——旧测试用自定义 ConfigurationRoot（≠生产类型）→ 补 **3 个真实 ConfigurationManager 测试**（env 有效保留 / env 占位回退 / env 空串回退——复现原 bug：JSON 占位 + env 有效 → env 值保留）。ConfigurationPostProcessorTests 11/11 | 构建 0/0；PostProcessor 11/11；Server 全量 669/669；架构 87/87；任务书待删 | ✅ 待提交 |
| 2026-08-13 | sysadmin 生命周期回归设计（移除 ForceReset——.hermes-lifecycle，任务书 sysadmin-lifecycle-redesign） | **已执行**（用户权威：无强制重置密码——空库建/有数据不建/遗忘仅工具恢复）：① **移除 ForceResetOnStartup 密码重置**——DatabaseInitializationService.EnsureSystemAdminExistsAsync 删 UserManager 重置分支（构造/字段/using 同步移除）；SystemAdminOptions.ForceResetOnStartup 属性移除（注释说明保留）；② **边界场景**——软异常（sysadmin 存在但 Deleted/Disabled）LogWarning 不阻断 + 登录明确提示「系统管理员账号异常，请联系运维使用密码初始化工具恢复」（LoginCommandHandler sysadmin 特殊化——软删用户查询已滤走通用路径）；硬删（数据存在但 SuperAdmin 缺失——`Users.Any(Role != SuperAdmin)` 信号）LogCritical + 抛异常禁止启动；空库首次正常；③ **测试**——删 3 个 ForceReset 测试 + FakeUserManager（服务不再需）→ 新增 4 个生命周期边界测试（空库/有数据跳过/软异常警告/硬删 Fatal——需 Development env 到达检查）；DatabaseInitializationServiceTests 22/22；④ **文档**——05-security ForceReset 段改移除说明 + 决策表/流程图 + 11a-shell US-SHELL-017 业务规则 1 | 构建 0/0；生命周期 22/22；Server 全量 669/669；架构 87/87；任务书待删 | ✅ 待提交 |
| 2026-08-13 | sysadmin 逻辑完整审核（任务书 7 项） | **已执行**：逐项验证文档 vs 代码——**6 项代码正确**（#1 初始密码配置/环境变量 K4 ✓、#4 ResetPassword 拒绝 sysadmin ✓、#5 PasswordHashGenerator PBKDF2+仅生成哈希（人工 SQL 更新）✓、#6 锁定统一 ✓）；**3 处文档过时已修**（#2 ForceReset「生产始终忽略」→ 新语义：开发直接+非开发 token 门控；#3 铁律#4 LastLoginAt==null → 不存在才创建幂等；#7 05-security BCrypt 描述 → PBKDF2——line 5/15/174/220 修 + 决策表 ForceReset 行）。报告 sysadmin-logic-audit-2026-08-13.md | 构建 0/0；IdentitySeed/DBInit 21/21；架构 87/87；纯文档（代码零差异） | ✅ 待提交 |
| 2026-08-12 | 配置集中实施（ADR-0019——Server 端 config/ 目录，.hermes-config-central.md） | **已执行**：① **文件移入**——appsettings*.json ×4 + clinic.config.json git mv 至 `src/Server/Services/LYBT.WebAPI/config/`（源=产物镜像——发布路径一致）；② **csproj** Content `config/**` CopyToOutputDirectory（Remove 默认 appsettings Content 防 NETSDK1022）——输出验证 bin/config/ 5 文件；③ **Program 加载链**——CreateBuilder 后移除默认 json providers → 加载 `{BaseDir}/config/appsettings.json + env.json`（保持 CFG-BATCH2 优先级/CFGCLOSE 自动生成/PostProcessor——runtime-overrides 同 config/）；④ **EnsureEnvironmentConfigFiles** 生成到 config/（加 baseDir 参数可测）；⑤ **AppDbContextFactory** config/ 路径；⑥ **测试基建**——EnvironmentConfigFileClosureTests 适配 config/（临时 baseDir）+ DeploymentConfigTests helper 路径；⑦ **真机验证**——dotnet run 读 config/ 启动 + /health 200（启动慢为 DB 初始化超时环境问题非本次）；⑧ ADR-0019 状态 → 已实施 + Runbook 配置集中段 | 构建 0/0；Server 全量 668/668；架构 87/87；Desktop 配置 9/9；任务文件待删 | ✅ 待提交 |
| 2026-08-12 | 文档修复（sysadmin 密码设计对齐——DefaultPasswordService 虚构引用清理，.hermes-sysadmin-docs-fix.md） | **已执行**（用户权威确认：初始密码从配置文件读取——K4 已实现；DefaultPasswordService 代码 0 命中为虚构）：修 5 处任务文档 + 3 处同类残留——03-users:97、11a-shell:343/344/350/354（ValidateSetupToken 引用改为 DatabaseInitializationService 真实位置）、13-traceability:182、12-permissions:69（旧风险描述→✅ K4 已修复 + 处置列）、0005-ADR:42、02-configuration:15/135、webapi-publish-readiness:23（活文档）。**保留历史记录**（02-configuration 变更日志 v1.4 / coverage-matrix compose 历史——历史不可改）。验证：活文档残留 0（grep 实证——仅剩历史变更记录） | 纯文档（0 代码）；grep 验证 | ✅ 待提交 |
| 2026-08-12 | ForceResetOnStartup 根本性修复（.hermes-forcereset-fix.md） | **已执行**：① **解除 IsDevelopment 绑定**——ForceReset 生效条件 = 开发直接触发 / 非开发需 InitialSetupToken 验证（安全门控——与 AllowAutoCreate 同模式）；② **门控重构**——非开发分支：`AllowAutoCreate+token` 或 `ForceReset+token` 任一通过（ForceReset 只重置不创建——不要求 AllowAutoCreate）；③ **真正重置密码**——注入 UserManager，`GeneratePasswordResetTokenAsync + ResetPasswordAsync`（PBKDF2 哈希——新密码 DefaultPasswords:SysAdminPassword；占位/空则仅重置状态+警告）；④ **Options 注释修正**（「仅开发/测试环境有效」→ 明确门控语义）；⑤ **单测 3**（Dev 重置/Prod 无效 token 不重置/Prod 有效 token 重置——手写 FakeUserManager（零 mock 约定）+ 预插 sysadmin）——DatabaseInitializationServiceTests 21/21 | 构建 0/0；ForceReset 3/3；Server 全量 668/668；架构 87/87；任务文件已删 | ✅ 待提交 |
| 2026-08-12 | CFG-BATCH2（配置优先级修正 + 占位符/空值处理——边界决策已确认） | **已执行**：① **Provider 顺序修正**（决策 1）——CreateBuilder 内置 env provider 移除 → runtime-overrides 追加 → 环境变量重建（最高）——目标顺序 `环境变量 > runtime-overrides.json > appsettings.{env}.json > appsettings.json`；② **ConfigurationPostProcessor**（决策 2/3——独立 helper + 自动生成逻辑并存）——已知键清单（Jwt:SecretKey/DefaultConnection/密码×2/Token/FeedUrl）遍历——最高 provider 值无效（空串/`${...}` 占位）→ 回退下一级有效值（ConfigurationManager 覆盖层注入）；全部无效保持（校验器拦截）；未知键不触碰。单测 11（有效性判定×6/env 优先/空串回退/占位回退/全无效保持/未知键不动） | 构建 0/0；后处理 11/11；Server 全量 665/665；架构 87/87 | ✅ 待提交 |
| 2026-08-12 | 配置闭环（环境配置文件缺失自动生成） | **已执行**（用户原则：尽量保证正常顺利启动）：Program.EnsureEnvironmentConfigFiles（CreateBuilder 前）——appsettings.json/appsettings.{env}.json 缺失时自动生成默认模板（内嵌——含双下划线占位符——占位符=运维注入名）；已存在文件不覆盖（自定义优先）；失败仅告警不阻塞启动；单测 3（生成含占位符/不覆盖/Development 模板——临时 CWD + 禁并行 collection 防 CWD 污染）；Runbook 加配置闭环段 | 构建 0/0；配置闭环 3/3；Server 全量 654/654；架构 87/87 | ✅ 待提交 |
| 2026-08-12 | 配置唯一化（SYSADMIN_PASSWORD 兼容分支删除） | **已执行**（用户原则：配置跟着变量走——每环境内部唯一一致）：IdentitySeedData.ResolveSysAdminPassword 删 `SYSADMIN_PASSWORD` 兼容分支（统一 `DefaultPasswords__SysAdminPassword`——错误消息同步）；appsettings.Production.json 全部占位符统一为**真实双下划线变量名**（`${SYSADMIN_PASSWORD}`→`${DefaultPasswords__SysAdminPassword}`、`${NEWUSER_PASSWORD}`→`${DefaultPasswords__NewUserPassword}`、`${JWT_SECRET}`→`${Jwt__SecretKey}`、`${LYBT_INITIAL_SETUP_TOKEN}`→`${SystemAdmin__InitialSetupToken}`——占位符即运维注入名——踩坑 #1 对齐）；docs 01-deployment/03-webapi-deployment-summary 同步；踩坑 #1 的 JWT_SECRET 示例保留（问题描述语义——有意示例） | 构建 0/0；IdentitySeed/DBInit 测试通过；残留 grep 0（除有意示例） | ✅ 待提交 |
| 2026-08-12 | 存量失败修复（ConfigurationLoadingTests） | **已执行**：`LocalJwtOptions.SectionName` 断言 `"LocalJwt"` → `"Jwt"`——本地 JWT 与远程同构读共享 Jwt 节（LocalWebApiProgram 绑定 `LocalJwtOptions.SectionName`="Jwt"——消费链实证）；测试断言为过时历史假设。**Server 全量 651/651 全绿（0 失败）——多年存量失败清除** | 全量 651/651（重跑实证——首次 2 失败为并行 env 偶发） | ✅ 待提交 |
| 2026-08-12 | L2-L4 分层测试实施（WebAPI 后端——方案全落地） | **已执行**：按 webapi-test-strategy §五+§七——**L3 系统层** WebApiSystemTests 8（启动冒烟/全量路由 swagger.json/FallbackPolicy 401/CorrelationId/安全头×2/下载页——拦截 #1 #3 #9 #10）+ WebApiLoginFlowTests（env-gated 登录）；**L2 集成层** DbContextMappingSmokeTests 6（4 DbContext SQLite 建表 + LastLoginAt 列映射 + 真实 SQL env-gated——拦截 #2）+ 引 Microsoft.EntityFrameworkCore.Sqlite（社区校准 §7.1）；**L4 部署层** DeploymentConfigTests 3（纯校验器——非法 JWT/占位符/合规通过——拦截 #4 #5 #7——WebApplicationFactory 无法注入 Main 构建前校验 → env/纯校验器方案；Serilog freeze 多 host 限制发现——Production 启动冒烟移除由 Runbook 发布验证覆盖）。**测试库 env 驱动**（TEST_DB_CONNECTION——不猜测凭据）。Runbook 加发布前门禁（决策 4——L3 冒烟第 0 步） | L2-L4 18/18 + Server 全量 650/651（1 存量 ConfigurationLoadingTests）+ 架构 87/87 | ✅ 待提交 |
| 2026-08-12 | RETRO（开发过程深度沉淀——10 天 454 commits） | **已执行**：深度回顾 08-02~08-12——4 阶段（Desktop 统一→测试重建→需求对齐/backlog 清零→发布准备）；度量（docs 58%/净代码 +0.3%——收敛特征；架构 83→87/Server 单测 360→620/AC 守护 0→147 US）；5 教训（测试自洽≠守护需求/文档滞后=技术债/复制漂移/手工替换破坏/环境测试分层）+ 6 工作模式固化；报告 dev-process-retrospective-2026-08-12.md | git 实证（454 commits 统计）+ 批次记录交叉 | ✅ 待提交 |
| 2026-08-12 | LSP 最终定案（用户纠正：C# 默认 **csharp-ls**——omnisharp 首次索引太慢实测后弃用） | **定案**：lsp.json C# 指向 `C:\Users\player\.dotnet\tools\csharp-ls.exe`（stdio——0.12.0 全局工具）+ rootMarkers `*.sln`/`*.csproj`；omnisharp v1.39.11 **保留安装**（`~/.omnisharp/omnisharp-bin/`——不注册 lsp.json——实测 75s+ sln 握手无响应弃用）；验证：lsp status **csharp-ls (ready)** + capabilities 完整（references/rename/codeAction/semanticTokens/callHierarchy/inlayHint——前次实证） | csharp-ls ready ✓（status 实证）；omnisharp 未注册不冲突 | ✅ 待登记 |
| 2026-08-12 | LSP 启用（环境配置——非仓库代码） | **已执行**：C# 语言服务启用——`~/.omp/agent/lsp.json` 注册 csharp-ls（全局 dotnet 工具 0.12.0——PATH `C:\Users\player\.dotnet\tools`——所有 agent 共享）；rootMarker `*.sln`/`*.csproj`（LYBTZYZS.sln 命中）；验证：lsp status 4 服务器全 ready（csharp-ls + vscode-json/html/css）；csharp-ls capabilities 完整（references/rename/codeAction/semanticTokens/callHierarchy/inlayHint）；`csharp-ls-adapter`（csharplspadapter 1.0.2）探明为无标准 LSP 输出的辅助工具——lsp.json 用标准 csharp-ls。后续 C# 跨文件改名/引用/诊断走 LSP（替代手工 grep） | lsp status 实证 ready + capabilities 全；仓库零代码变更（lsp.json 在用户目录） | ✅ 待登记 |
| 2026-08-12 | RULES-UPDATE（项目规则更新——文档同步审计） | **已执行**：读取新规则（代码-文档一致性红线/发布标准流程/文档地位深化——lybtzys-coder-rules v1.0）。**文档同步审计**：01-deployment.md 踩坑清单 10 条与本 session 全部修复核对——5/6/7/8/9 已含我的 commit（1f4f54a91/a3ab01417/8d02ed365/SWAGGER-TOGGLE）；踩坑 8 CSP 豁免（b1c2bf2bb——SecurityHeadersMiddleware SWAGGER-CSP-FIX）代码核查存在；**修正踩坑 5 表述**（「相对路径」→「绝对路径 / 开头」——与 1f4f54a91 实际修复一致——相对会拼出错误语义）；踩坑 1/2/3（双下划线/Base64/密码）与诊断实测一致 | 纯文档（0 代码）；grep 核对 10 条踩坑与代码实现一致 | ✅ 待提交 |
| 2026-08-12 | SWAGGER-ANON（/swagger 401 补丁——FallbackPolicy 拦截） | **已执行**：验收发现 /swagger 401——FallbackPolicy（RequireAuthenticatedUser）对无端点的 swagger 请求返回 401。修复过程：① UseWhen 认证豁免方案**实测破坏管线**（health 也 401）→ 回滚；② 最终方案——**Swagger 启用时注册匿名兜底端点** `MapGet("/swagger/{**path}") AllowAnonymous → 404`（SwaggerUI 中间件短路优先 200；异常时 404 而非 401——授权豁免锚）。开发实测：swagger 200/health 200/formulas 401（业务正常）/login 401；生产启动受完整环境变量链约束（JWT/Token/密码——发布时运维注入——诊断中途确认） | 全量构建 0/0；架构 87/87；开发实测管线恢复 | ✅ 待提交 |
| 2026-08-12 | SWAGGER-TOGGLE（主页跳转 Swagger + 配置开关，方案 2+3） | **已执行**：① 配置开关——SwaggerOptions.Enabled（默认 false——生产默认关）+ ConfigureSwaggerMiddleware 条件化（非生产 OR Swagger:Enabled=true——测试发布可在线启用，SHELL-018 配置中心/环境变量 Swagger__Enabled 切换）；② 主页 Swagger 入口——DownloadController 下载页加「API 文档」按钮（指向 /swagger——Swagger:Enabled 或非生产时显示） | 全量构建 0/0；架构 87/87；生产默认关闭（安全默认——生产配置未写入） | ✅ 待提交 |
| 2026-08-12 | HEALTHCHECK-FALLBACK-FIX（发布验证：连接串 fallback 不一致） | **已执行**：SqlServerHealthCheck 只读 `DatabaseOptions.ConnectionString`（Database 节）——空则 Unhealthy「连接字符串未配置」；但实际运行连接串经 `ConnectionStrings:DefaultConnection`（环境变量 ConnectionStrings__DefaultConnection 注入）→ **健康检查误报 + 与注册链不一致**。修复：抽共享 `DatabaseConnectionResolver`（统一 3 级 fallback：Database 节 → ConnectionStrings:DefaultConnection → CONNECTION_STRING 环境变量）——SqlServerHealthCheck + DatabaseServiceCollectionExtensions 注册处**同源引用**（防复制漂移——本次 bug 正是两处复制）。单测 5（优先级/环境变量覆盖/全空/回退） | 全量构建 0/0；架构 87/87；resolver 5/5 | ✅ 待提交 |
| 2026-08-12 | IDENTITY-DBCONTEXT-FIX（测试发布发现登录崩溃 P0） | **已执行**：IdentityDbContext.OnModelCreating 漏 `ApplyConfiguration(new UserConfiguration())`——ApplicationUser.LastLoginTime 未映射 Users 表 LastLoginAt 列 → EF 按属性名查 LastLoginTime → SqlException → 登录 500。修复：补挂载 UserConfiguration（Infrastructure/Data/Configurations——与 SecurityAuditLogConfiguration 同源，模块引用 Infrastructure 先例）。回归守卫：IdentityDbContextMappingTests 2 用例（LastLoginTime→LastLoginAt 列映射 + RealName 映射——EF Model 级断言） | 全量构建 0/0；架构 87/87；映射测试 2/2 | ✅ 待提交 |
| 2026-08-12 | CATALOG-ROUTE-FIX（测试发布发现启动崩溃 P0） | **已执行**：CatalogController 路由 bug——类级 `Route("api/v{version}/herbs")` + 15 处 formulas 动作路由写完整前缀（相对模板）→ 拼接后 version 参数两次 → ControllerActionDescriptorProvider 启动崩溃。修复：15 处动作路由改**绝对路径** `/api/v{version:apiVersion}/formulas...`（/ 开头覆盖类级前缀——对齐 IdentityController T4 先例）。**启动实测**（有效密码环境变量启动）：进程正常启动无崩溃、`/api/v1/formulas`/`/herbs`/`/formulas/import-template` 均 401（路由正确注册——FallbackPolicy 认证拦截）；health 503 为 DB 未连（本地开发无 SQL Server——非本次问题）。回归守卫：CatalogControllerRoutesTests 2 用例（类级单 version + 15 动作绝对路径/version 不重复） | 全量构建 0/0；架构 87/87；路由测试 2/2；启动实测通过 | ✅ 待提交 |
| 2026-08-12 | PUBLISH-ASSESS（WebAPI 发布就绪度评估，只读） | **已执行**：评估 60.190.215.86:5000 发布就绪——10 项检查 8/10 通过（端口 5000 已配/CORS 目标地址已预配/Swagger 生产关闭/生产门控 AllowAutoCreateInProduction=false + 占位符拦截/密钥环境变量注入机制）。**3 项发布必做动作**：① 环境变量注入（DB_SERVER/DB_USER/DB_PASSWORD/JWT_SECRET/SYSADMIN_PASSWORD/NEWUSER_PASSWORD/LYBT_INITIAL_SETUP_TOKEN——验证器拦未展开占位符）；② EF 迁移执行（EnsureCreatedInDevelopment=false——生产走 migrations）；③ DesktopUpdate.FeedUrl 占位符替换（your-server.example.com → 60.190.215.86:5000）。风险非阻塞：HTTP 明文（测试可接受/公网须 HTTPS ADR-0014）、库名 LYBTDB_Dev 语义、SQL 可达性（P0-06 历史）。验证闭环：validate+health+登录冒烟。报告 webapi-publish-readiness-2026-08-12.md | 只读（零代码） | ✅ 待提交 |
| 2026-08-12 | VELOPACK（Velopack 打包框架——US-SHELL-010 剩余 AC） | **已执行**：Velopack 1.2.0（2026-06-03 最新稳定）引入——Directory.Packages.props + Desktop.Shell/Foundation 引用；`scripts/velopack-pack.ps1`（dotnet publish win-x64 自包含单文件 → vpk pack：Setup.exe+RELEASES+nupkg）；`scripts/sync-to-server.ps1`（scp/SMB 同步更新源到服务器 ReleasesPath）；`DesktopUpdateService`（UpdateManager——SimpleWebSource(FeedUrl)+UpdateOptions，1.2.0 API 适配：2 参 ctor/ApplyUpdatesAndRestart(asset)/NotesMarkdown）；`DesktopUpdateStartupStep`（Order 400 后台检查——非阻塞 + 提示下载/重启）；DI 注册 + AppStartupOrchestrator 接线；DesktopUpdateOptions 补 FeedUrl + 生产 appsettings；部署文档 01-deployment.md 旧 zip 手动升级段 → Velopack 流程；单测 3（未启用/无 FeedUrl/下载禁用） | 全量构建 0/0；架构 87/87；更新服务单测 3/3；traceability US-SHELL-010 ⚠️→✅（Setup.exe 实机打包由运维跑脚本产出——标注） | ✅ 待提交 |
| 2026-08-12 | SHELL-010-DL（下载主页 + 发布包静态服务，决策 A） | **已执行**：① `/releases/` 静态服务**核实已有**（UnifiedMiddlewareConfiguration 条件启用——DesktopUpdate:Enabled+ReleasesPath，生产 appsettings 已配 C:\Services\LYBT-releases）；② 补 **GET / 下载页**（DownloadController——公开 AllowAnonymous + 类级 Authorize 豁免模式、极简 HTML：下载按钮/版本号（Setup-*.exe 文件名解析）/更新时间/文件列表 Setup.exe+RELEASES+.nupkg 过滤）+ LocalWebAPI 双端同步（本地模式提示页）。架构守卫适配：P09 类级 Authorize+方法级豁免 / P09b 公开根页 `""` 特例（与 health 同型）/ P19b IL 解码器健壮性（IndexOutOfRange 边界忽略——DownloadController 新 IL 触发手工解码越界，非代码违规）。单测 3（公开性/路由/前缀） | 全量构建 0/0；架构 87/87；下载页单测 3/3；traceability US-SHELL-010 🧲→⚠️（打包 4 项属 Velopack 工程） | ✅ 待提交 |
| 2026-08-11 | WEBAPI-DOC（WebApi 层文档漂移修复 + CorrelationId 核实） | **已执行**：① README 漂移大修——端点概览表/文件表/章节对齐实际 10 控制器：删 SyncController 章节（全仓 0 命中——已移除但文档全留）、AuthController+UsersController→IdentityController（继承 BaseUsersController——A-31-C3a 合并）、HerbsController+FormulasController→CatalogController（A-31-C3b 合并）、补 Registrations/Reports/Configuration/Deploy 4 控制器、文件表 12→10、端点节标题 4 处、更新记录追加。② CorrelationId 核实——**链完整无缺口**：UseLybtCorrelationId 中间件（生成/透传/写响应头 X-Correlation-ID）+ Serilog CorrelationIdEnricher + SystemLogs.CorrelationId 索引 + ProblemDetails 错误响应注入 + A-31-C1 单点注册 | 构建 0/0；README 旧控制器名残留 0（grep 实证——5 处均为合法描述）；docs/04-api-reference/09-sync.md 已正确标注（v2.0 规划——无漂移） | ✅ 待提交 |
| 2026-08-11 | ARCH-REVIEW（资深架构师深度评测，只读） | **已执行**：全 solution 深度评测（32 项目/39,619 行/87 架构测试/618 Server 单测/17 ADR）——7 维评分：愿景 9.0/分层 9.2/核心决策 8.6/质量 8.8/测试 8.5/安全 8.9，**综合 8.8**。亮点：87 条机器强制架构守卫、ADR 决策可追溯、测试从自洽→守护需求演进（P0 闭环）、收敛主导技术栈。主要扣分：集成测试环境依赖（P0-06 最短板）、项目数偏多（29）、Shell 职责集中苗头。演化建议 3 项（集成测试容器化/域门面演进/Shell 拆分）。报告 architecture-deep-review-2026-08-11.md | 只读（零代码） | ✅ 待提交 |
| 2026-08-11 | DEPLOY-PERM（部署权限收紧安全补丁） | **已执行**（用户派发安全 P0 主项——RISK-CLOSE 漏项）：DeployController 双端类级 `AdminOrSuperAdmin` → `SysAdminOnly`（PolicyConstants 既有——部署属运维操作，Admin 业务管理员无部署能力，US-SHELL-020 AC 注闭环）。新增权限守卫测试 2 个（Server 远程 DeployControllerPermissionTests + Desktop 本地 LocalDeployControllerPermissionTests——反射断言类级策略防回归）；traceability + 11a-shell US-SHELL-020 状态标注 | 全量构建 0/0；双端权限测试 1+1；架构 87/87 | ✅ 待提交 |
| 2026-08-11 | RISK-CLOSE（剩余风险完善） | **已执行**：核查论证报告 §4.3 剩余 5 项风险可完善项——SHELL-003（LoginCoordinator 旁路 🔴 唯一）**核实已清除**（C7 批次 2026-08-09 删死代码 HandleLoginSuccessAsync/GetDiagnostics/LoginFlowDiagnostics——代码 0 命中实证 + traceability 已校准 ✅ + 🔴 全库清零 151 US：✅141/⚠️3/🔴0/🧲7）；其余 4 项均为不可完善的边界决策（CARD-002 用户搁置/数据孤立 N1/Integration 环境依赖 P0-06/固件接口）。**修正 ARGUMENT 报告 §4.3**（剩余风险 5→4 + 统计更新） | 代码零改动（核实类任务）；ARGUMENT 报告过时数据修正 | ✅ 待提交 |
| 2026-08-11 | ARGUMENT（需求理解 + 方案完整性双角色论证，只读） | **已执行**：双角色（需求分析专家/方案设计专家）对话模式论证——需求理解度：领域模型还原准确（MedicalCase 唯一聚合根/4 角色边界/核心旅程）+ 语义深度（QuickVisit 双职能/双模式数据孤立 N1/配置热更新边界）；方案完整性：四层无空白（架构 87 守卫/CQRS 模块自治/安全五面闭环/测试分层守护）；五轮质询全站住（聚合根合理性/双模式一致性/refresh=access 凭据/重启体验/测试环境依赖）；剩余 5 项风险均为已记录边界决策（CARD-002 搁置/SHELL-003 待对齐/数据孤立 N1/P0-06/固件接口） | 只读（零代码）；报告 requirements-design-argumentation-2026-08-11.md | ✅ 待提交 |
| 2026-08-11 | DOCS-CONVERGE-2（全面收敛——过程文档价值迁移后删除） | **已执行**（价值迁移三步：读文件提取 → 内联/改引用 → 删除）：删除 55 个已完成批次的过程产物——specs 任务书 26（task-a17~a31 + structure-audit task-spec + auth-users-merge research + registration-workflow-redesign）、reports 批次执行报告 22（a18×2/a21/a23/a27/a28/a29/a31-c0~c8×9/arch-test-refactor/auth-users-merge/mimo-verify）、plans 过期计划 7（v1.0-completion/arch-test-refactor/batch-handler/p02-q02/method-audit/code-gap-fix-list/desktop-mc-edit-context/shell018-design）。**引用迁移**：11-business-flows:7 R10 spec 引用 → ADR-0002/0010（双模式分野价值已内联句子）；总账 6 处任务书引用 → 「已归档」。**保留 5 活文档**：backup-featuretoggle-design（13c 引用）+ 4 报告（coverage-matrix/ac-test-mapping/test-code-review/requirements-first-campaign——活文档） | 非 compose 断链引用 0（grep 实证）；55 个文件价值核查：A 批次结论在总账 A 行表（commit SHA）+ 当前态代码；架构测试重构=87 测试运行中；策略补丁=控制器当前态 | ✅ 待提交 |
| 2026-08-11 | COMPOSE-CLEAN（compose 收敛——文档是字典不是过程） | **已执行**：删除 30 个一次性扫描/审计中间产物（method-audit×7、structure-audit×9、namespace-consistency×2、dead-code×2、webapi-deep×3、desktop/server class scan×3、data-pipeline、dto-entity、code-review、desktop-layer-review）。**删除前逐文件核查关键结论已沉淀**：A-16 结构审计结论→13 总账行+ADR-0010；A-30 方法审计 S0-S4 统计→15-solution-integration-plan（已删除） 表；A-26 收敛清单→总账行；S-04 模糊匹配修复/Q-03 命名空间/403 映射审查→总账行。同步清理 4 份正式文档 16 处死链引用（08-shared/14-blueprint/15-plan/13-master-plan——文件名引用替换为「已归档」） | 正式文档残留引用 0（grep 实证）；compose 内部 specs↔reports 互引属归档区容忍 | ✅ 待提交 |
| 2026-08-11 | AC-TEST-P23（AC→测试落地——优先级 2+3） | **已执行**：P2 全闭环——MedicalCaseHistoryQueryTests 5（真实仓储+EF InMemory：倒序/Take/Doctor 所有权/空）；HerbReferenceCheckTests 5（手写 fake 仓储：引用拒绝/放行软删/404）；QuickVisitDialogViewModelTests 4（门控/确认/失败）。P3——ConfigurationWritePolicyTests 22（IsAllowed 白名单/拒绝 + IsSensitive 掩码）；备份保留期标注跳过（静态 AppData 路径污染）；MC-020/FORM-014 标注端点级（集成/手动验收守护） | 全量构建待验；Server 32/32 + Desktop 4/4 新测试 | ✅ 待提交 |
| 2026-08-11 | P0-TEST（AC→测试落地——P0 逃逸闭环，优先级 1） | **已执行**：3 组 15 用例补测——`IdentityControllerRoutesTests`（5 端点绝对路径 + users 双重前缀回归守卫——P0#1）；`RefreshTokenIssuanceTests`（签发非空/access=refresh/身份保留/令牌旋转/非法拒绝——P0#2）；`ExcelImportHelperTests`（表头跳过/数字格式化/行数上限/空行跳过/非法文件——P0#4）。4 个 P0 全部有测试守护（P0#3 本地验签 LocalTokenValidatorTests 既有） | 全量构建 0/0；架构 87/87；Server 587 通过（1 存量 ConfigurationLoadingTests）；新 17/17 | ✅ 待提交 |
| 2026-08-11 | AC-TEST（需求 AC → 测试映射，只读调研） | **已执行**：建立「需求验收条件 → 测试守护」映射表（147 US × 15 域）——域级守护 12 强/1 中/0 无；P0 逃逸复盘（4 个 P0 中 2 个修复后仍无守护：远程 auth 路由/导入导出）；近批缺口清单（B1/B2/R3-补 新 US 无测试：MC-008/009/018/020、HERB-005/006、REG-002、FORM-014）；建议 3 优先级 9 项补测（随批外科式补，不设独立大批） | 只读调研（零代码）；报告 requirements-ac-test-mapping-2026-08-11.md | ✅ 待提交 |
| 2026-08-11 | SHELL-019（读卡器诊断测试工具） | **已执行**（用户确认有硬件可实测）：`ICardReader.GetDeviceInfo()` 默认实现 + `CardReaderDeviceInfo` record；`ICardReaderDiagnostics` 接口 + `CardReaderDiagnosticReport`（探测/握手/固件/读卡 4 步 + Messages）；`CardReaderDiagnosticsService`（测试模式编排——CreateReader 独立实例不触医生会话 + Connect 探测 + 链路握手 + 固件（驱动未暴露→提示）+ Detect/ReadCard 测试 + 断开清理）；`CardReaderDiagnosticsViewModel`（厂家下拉 HuaDaHD100/Auto + 手动参数覆盖校验 + 运行诊断 + 报告逐行 + 持久化 CardReader 节含 ReaderType）+ SysadminHome 组合 + XAML 面板（替换 Phase 2 占位）；DI 注册；单测 4（Mock 通过路径/连接失败/工厂异常/参数透传） | 全量构建 0/0；架构 87/87；诊断单测 4/4；串口测试=USB 链路握手（HD100 无独立串口协议——如实） | ✅ 待提交 |
| 2026-08-11 | SHELL-018 Phase 3（双模式布局整合） | **已执行**：`ServerConfigSectionViewModel`（远程服务端面板——节列表 6 节加载 GET sections/编辑保存 PUT/二次确认重启 POST restart——IServerConfigurationService 门面扩展 3 方法 + IApiClientConfiguration/Refit/Http 双轨同步）；SysadminHomeView TabControl 模式感知（远程：配置 + 服务端配置；本地：配置 + 备份恢复嵌入 BackupManagementView）；本地重启按钮（决策 B——LocalWebAPI restart 端点，OfflineModeOptions 地址）；SysadminHomeViewModel 注入 IConnectionModeService + ServerConfig 组合（ModeChanged 联动加载）；测试构造同步 | 全量构建 0/0；架构 87/87；Desktop VM 161 + 新 6/6；XAML 编译验证（含 BackupManagementView 嵌入 + Cvt） | ✅ 待提交 |
| 2026-08-11 | SHELL-018 Phase 2（配置中心客户端面板） | **已执行**：`ClientConfigurationStore`（节级原子写 + .bak 备份 + IConfiguration.Reload——文件路由 ClinicSettings→clinic-settings.json、FeatureToggles→feature-toggles.json、其余→appsettings.json）；`ConfigurationCenterViewModel`（5 组可编辑：诊所 6 项/会话 3 项校验区间/连接 BaseUrl+Timeout+测试连通/安全 2 项密码框/功能开关 2 项热更新 + 读卡器只读占位决策 E + 状态条）；SysadminHomeViewModel 组合 + View 分组卡片布局（含 PasswordBox 参数化命令）；VM 单测 6 个（加载/校验拒绝/保存路由） | 全量构建 0/0；架构 87/87；Desktop VM 161 通过 + 新增 6/6 | ✅ 待提交 |
| 2026-08-11 | SHELL-018 Phase 1（配置中心服务端 API 扩展） | **已执行**（用户确认 A-E 后）：`SysAdminOnly` 策略（PolicyConstants + RequireRole(SuperAdmin)）；`ConfigurationWritePolicy.IsSensitive` 脱敏判定（Forbidden 集 + 密钥启发式）；`ISystemConfigurationService.GetSectionAsync/UpdateSectionAsync`（白名单逐键 → JsonFileConfigurationStore → Reload → `ConfigUpdateResultDto`：FeatureToggles/ClinicSettings hot 即时生效其余 restart——决策 A 落实）；WebAPI `GET/PUT sections/{section}`（SysAdminOnly——路由加 `sections/` 前缀避 `{key}` 歧义）+ `POST restart`（滑动窗口限频每小时 3 次 + 30s 延迟 StopApplication）+ 审计接线（ConfigGet/ConfigUpdate/ConfigRestart → SecurityAuditService）；LocalWebAPI 双端同步（同签名 + SuperAdmin 角色检查 + 同限频）；traceability SHELL-018 🧲→⚠️（Phase 2-3 客户端面板待实施） | 全量构建 0/0；架构 87/87；Server Configuration 55 测试 54 通过（1 存量 ConfigurationLoadingTests LocalJwtOptions 断言失败非引入）；设计文档偏差小记（sections/ 路由） | ✅ 待提交 |

## 六、已知问题（代码实际状态）

### 🔴 P0 — 必须修复

| ID | 问题 | 位置 | 影响 |
| ---- | ------ | ------ | ------ |
| P0-01 | Shell 登出状态机错误 | Shell/LoginCoordinator | ✅ 已修复 (2026-08-04, B-01) |
| P0-02 | 并发登录竞态 | Shell/LoginCoordinator | ✅ 已修复 (2026-08-04, B-01) |
| P0-03 | 异常时事件未发布 | Shell/ShellEventCoordinator | ✅ 已修复 (2026-08-04, B-01) |
| P0-04 | Sync-over-Async 死锁 | Desktop 多处 `.GetAwaiter().GetResult()` | ✅ 已核实无残留 (2026-08-04, B-01) |
| P0-05 | 明文密码泄露 | appsettings.json (SSH/SA/JWT SecretKey) | ✅ 已修复 (2026-08-04, B-01) |
| P0-06 | 104 个 Desktop 测试失败 | tests/LYBT.Tests.Desktop | ✅ 根因已修复 (2026-08-11, T2-2)——148 纯 VM 测试解耦 LocalDB、146 Integration 独立化；残留失败为存量 STA/网络环境项（见 13c §五 T2 登记） |
| P0-07 | 配置无法 API 修改 | ConfigurationController 无 PUT | ✅ 已实现 (A-18 P1-1 + SHELL-018 Phase 1-3)——IApiClientConfiguration + 完整配置中心（GET 脱敏/节级 PUT/restart/审计，SysAdminOnly 策略，双端同步 2026-08-11） |

### 🟡 P1 — 应修复

| ID | 问题 | 位置 | 影响 |
| ---- | ------ | ------ | ------ |
| P1-01 | 9 个 Build 警告 | 多处 (CA1001/CS8603/CS0168/CS4014) | ✅ 已修复 (2026-08-10, B1/B2/B3 后 build 0 警告门禁) |
| P1-02 | 8 个 TODO 残留 | MedicalCase/Shell/Reports | 技术债务 |
| P1-03 | 5 个超大类型 (>600行) | MedicalCaseCommandService/Repository/HttpClientApiClient/NavigableViewModelBase/PrescriptionPrintService | ✅ 已解决 (2026-08-10: B2 重写后 5 个文件均 <600 行) |
| P1-04 | 22 个 MediatR trivial Handler | MedicalCase Application/ | 过度设计 |
| P1-05 | 实体双模型 | Domain/ vs Shared/ (6 对双胞胎) | 维护成本翻倍 |
| P1-06 | **Excel 导入/导出** ✅ 患者已 Excel 化（2026-09-13 B-12——Desktop 前端 .xlsx，后端 JSON 契约不变；见 13c #133）；Herbs/Formula 前端仍为 JSON 格式（功能可用——导入/导出/模板均已接线，2026-08-19 #129/#130） | Desktop Patients/Herbs/Formula | 原「缺失」已消除：三模块均有模板/导入/导出 UI；格式策略=后端 JSON + 前端 Excel（患者已落地，药材/验方按需推广） |
| P1-07 | 报表功能严重不足 | ReportsController (仅 3 个日统计) | 数据分析能力弱 |
| P1-08 | 6 个 NotSupportedException 桩 | Desktop Foundation Http/Clients | Desktop 功能不完整 |
| P1-09 | 处方价格刷新未实现 | MedicalCasePrescriptionService TODO | 价格不自动更新 |

### 🔵 P2 — 可后续完善

| ID | 问题 | 位置 | 影响 |
| ---- | ------ | ------ | ------ |
| P2-01 | 自动更新 | Shell (Velopack) | 运维依赖 |
| P2-02 | **数据备份/恢复** ✅ 已完成（2026-09-22——B-06 结案，见 #146） | — | 运维依赖（原「Desktop 专用+远程靠 Agent」；现双端共享引擎 + 应用内恢复） |
| P2-03 | SignalR 实时通知 | — | 体验增强 |
| P2-04 | 离线同步 v2.0 | 旧分支已放弃 | 大功能 |
| P2-05 | **Swagger/OpenAPI** ✅ 已完成（2026-09-12——B-16 结案，见 13-project-master-plan B-16/§九 `6792904101`） | WebAPI 文档 | 全量端点 XML 摘要 + DTO 字段描述 + 逐操作 Bearer security（Try it out 带 Token）+ 生产默认关闭（`Swagger:Enabled=false`，测试发布注入 `Swagger__Enabled=true`）+ 开关判定 SSOT（`SwaggerAvailability`）；L3/L4 契约测试守卫 |
| P2-06 | 排班管理 | Registration | 业务增强 |
| P2-07 | **多进程/端口占用防护** ✅ 已实现（2026-08-13——见 #116） | start.sh + Program | 部署可靠性：start.sh 仅 pkill+sleep 2 无端口检查/无 PID 文件/无启动成功验证——实测旧进程 741060 未释放端口导致新进程 741734 启动失败（06:18 日志 `${DB_SERVER}` 连接失败 + 双 dotnet 进程并存）。设计需覆盖：端口释放确认、PID 文件、启动成功探测（/health）、孤儿进程清理、单实例 Mutex（US-SHELL-001 已有 Desktop 侧） |
| P2-08 | **API 单一职能全面审查** ✅ 已完成（2026-08-14——P1-7/8/9 + P1-1/4/5/6 全部落地；剩余端点已无 Controller 内业务逻辑；见 #118/#119/#120） | 全部 Controller | QuickVisit 两步改造暴露的设计原则（用户定案）：API 单一职能、组合编排放前端 VM。API 测试完成后审查全部端点是否有类似复杂设计（一个端点做多件事/跨模块编排/隐式联动），考虑拆分。参考：StartVisit 接诊即建（已原子化保留）、quick-visit（已删）、经验方导入（前端组合） |
| P2-09 | **HTTP/HTTPS 双协议支持** ✅ 已实现（2026-08-14——见 #121）——原登记（2026-08-13，待讨论） | WebAPI 宿主 | 用户提出：API 地址 http 和 https 是否可同时支持，通过配置开启/关闭某一个。需讨论：Kestrel 多端点配置（<http://0.0.0.0:5000> + <https://0.0.0.0:5001）、证书来源（dev> 自签/prod 正式）、配置开关（Http:Enabled / Https:Enabled）、CSP/安全头影响、发布 Runbook 更新 |
| P2-10 | **死代码删除误伤防护** ✅ 已固化（2026-08-14——见 #122；端点审计 98=98 一致） | 重构流程 | 教训：a99619f47（QuickVisit 收敛）删除 quick-visit 端点时**误删了 POST /Registrations 创建端点**（Create 方法与 quick-visit 代码相邻，被当死代码连带删除）——真机 405 才发现。防护：删除死代码前先确认方法归属（grep 端点/接口/前端调用），删除后立即验证端点清单（swagger 对照） |
| 109 | 2026-08-13 | Consultation CreatedBy NULL 两连击完结（任务书 consultation-createdby-fix——真机 start-visit 500 复发） | **已执行**：pi 只读调查发现 + 真机验证——服务器日志直达根因：`SqlException: 不能将值 NULL 插入列 'CreatedBy'，表 'LYBTDB_Dev.dbo.Consultations'`。**修复**：`CreateFromInputDtoAsync` 创建 Consultation 补 `CreatedBy = currentUserId`（聚合根 1:1 共享主键，与 MedicalCase.CreatedBy 对称——上次 startvisit-createdby-fix 漏了级联插入的 Consultation）。**同类排查（全实体）**：① **Prescription.CreatedBy**（PrescriptionConfiguration 同样 NOT NULL——医生带处方建案同样会 500）——`CreateNewPrescriptionAsync` 补 currentUserId 参数（调用链：CreateFromInputDtoAsync 创建路径 + HandlePrescriptionUpdateAsync 保存路径）；② **MedicalCaseAuditLog**（更新审计/取消审计 2 处）补 CreatedBy=操作者；③ **MedicalCasePrintLog** 补 CreatedBy=operatorId；④ PrescriptionItem 无 CreatedBy（不继承 BaseEntity——无列）✓；⑤ PrescriptionDetailDto 补 CreatedBy（Mapperly 同名自动映射——审计可观测）。**测试**：`StartVisit_MedicalCaseAndConsultation_CreatedByPopulated` + `CreateMedicalCase_WithPrescription_CreatedByFieldsPopulated`（断言三实体 CreatedBy=操作者——Desktop E2E 基建需 localhost:5000，真机等价验证替代） | 构建 0/0；Server 696/696；架构 87/87；**真机 start-visit 200**（原 500——InProgress + 医案 Active MC20260813001 + CreatedBy 非空）；api-manual-test Bug 4；07-medical-cases US-MC-001 补审计字段规则；任务书待删 | ✅ 已提交 18a6f676e |
| 110 | 2026-08-13 | Configuration 权限隔离（任务书 config-permission-fix——真机 testadmin 200 应 403） | **已执行**：真机测试发现 testadmin（业务管理员）GET /api/v1/configuration → 200（应 403）——配置管理设计为 sysadmin 专属（US-SHELL-018「角色: sysadmin」）但类级 AdminOrSuperAdmin 误放行 Admin（App/ConnectionStrings/Jwt 等敏感配置泄露面）。**修复**：① **远程** ConfigurationController 类级 AdminOrSuperAdmin→SysAdminOnly（统一覆盖全部 7 端点GET/PUT/GET key/PUT key/sections×2/restart/validate——原 3 处方法级 SysAdminOnly 冗余移除）；② **本地** LocalWebAPI 同步类级收紧 + Set（PUT key）手工检查 Admin/SuperAdmin→仅 SuperAdmin（双端一致）；③ **策略注册补齐**——LocalJwtConfig 缺 SysAdminOnly 注册（本地 DeployController 已用该类级策略但未注册——实际请求会抛 InvalidOperationException，本次补注册一并修复）；④ **测试**——ConfigurationControllerPermissionTests（远程反射 2：类级 SysAdminOnly + 无方法级 AdminOrSuperAdmin 覆盖）+ ConfigurationPermissionBehaviorTests（WebApplicationFactory + TestAuthHandler 真实授权中间件：Admin→403 / SuperAdmin→200）+ LocalConfigurationControllerPermissionTests（本地反射 2）；⑤ **文档同步**——10-configuration v1.5、API README（配置/部署行 + 策略说明 6→7 项补 SysAdminOnly）、14-deploy（DEPLOY-PERM 滞后同步）、12-permissions-matrix（系统设置行加 Configuration 明确）、04-permissions（Controller 映射 + 策略表）、11a-shell US-SHELL-018（🧲→✅ 滞后校准 + 权限注）、05-dual-mode（策略数 6→7）、13c | 构建 0/0；Server 700/700（+4）；架构 87/87；Desktop 本地反射 2/2；任务书已删 | ✅ 已提交 3fa497d3e |
| 111 | 2026-08-13 | 处方明细金额快照（任务书 prescription-price-snapshot-fix——真机 item.subtotal/totalPrice=0） | **已执行**：真机保存处方后处方级 totalPrice=15.0 ✅ 但明细 item.subtotal=0（应 10g×0.3=3.0）、item.totalPrice=0。**根因**：`MedicalCaseMapper.ToPrescriptionItemDto` 对 Subtotal/TotalPrice/TotalWeight 全部 `[MapperIgnoreTarget]`（实体无同名属性，Mapperly 忽略）——但实体 `PrescriptionItem.Amount` 是只读计算属性（UnitPrice×Dosage，DB 无列——A2 快照语义 = UnitPrice 持久化即可推导），修复应在 Enrich 层补算而非加 DB 列。**修复**：`EnrichPrescriptionDetailDto` Items 映射后补——① `Subtotal = item.Amount`（= UnitPrice×Dosage，单帖小计，文档 §721 Amount 定义）；② `TotalPrice = item.Amount × DosageCount`（该味药帖剂总价——折扣为处方级 MC-D14 整体概念不摊入明细）；③ 保存路径核实无需改（`CreatePrescriptionItemsAsync` 持久化 UnitPrice，不读客户端传入 Subtotal——服务端权威计算）。**测试**：`MapToMedicalCaseDetailDto_WithPrescription_ShouldComputeItemPriceSnapshot`（柴胡/白芍 10g×5.0，DosageCount=7：Subtotal=50/TotalPrice=350，处方级 700）+ `..._ItemPriceExcludesDiscount`（Discount=0.9：明细不变，处方级 630）——原有裸 `ToPrescriptionItemDto_ShouldIgnoreComputedFields` 仍绿（裸方法忽略行为保留）。**文档同步**：07-medical-cases §处方价格计算补明细快照两行；13c #111 | 构建 0/0；Server 702/702（+2）；架构 87/87；任务书已删 | ✅ 已提交 4bfc70849 |
| 112 | 2026-08-13 | 导入导出改 JSON（任务书 importexport-json——用户决策：后端不涉及 Excel，保持通用性） | **已执行**：① **患者** import-template/export 双端改 JSON（模板=字段说明+示例+必填；导出=JSON 数组，SensitiveDataJsonConverterFactory 管道自动脱敏 PhoneNumber）；② **药材** 服务端 Excel 解析路径 `import-excel` 双端删除（只留 DTO/JSON `batch-import` 唯一路径——US-HERB-006）；import-template/export-all 双端改 JSON（**注：此批次“双端”表述滞后——药材 `export`/`import-template`/`export-all` Local 实际缺失，Desktop 本地模式仍 404；见 #129 P1 补齐**）；③ **验方**（ExcelExportHelper 使用点——为满足「移除依赖无残留」一并改）import-template/export 双端改 JSON；④ **移除 NPOI 2.7.2**（ExcelExportHelper/ExcelImportHelper 2 文件 + ExcelImportHelperTests 1 文件删除；csproj/props 引用移除——全仓 NPOI 残留 0）；⑤ **测试**——`ImportExportJsonTests`（远程 6：患者模板/导出 JSON、药材模板/导出 JSON、验方模板/导出 JSON、无 import-excel、batch-import 保留）+ `LocalImportExportJsonTests`（本地 4）；⑥ **文档同步**——03-patients（v2.0 规划节→已实现 JSON）、04-herbs 注意段、06-formulas US-FORM-013（验收标准/双模式 Excel→JSON）、traceability（US-PAT-011/012/US-HERB-006/US-FORM-013 状态列）、13c #112。保留：`ErrorCode.HerbImportExcelError` 错误码（业务语义兼容，无实际抛出点）；`SystemConstants.Export=".xlsx"` 死常量零使用不动 | 构建 0/0；Server 703/703（+6 新、-5 删）；架构 87/87；Desktop 本地 JSON 守卫 4/4；任务书已删 | ✅ 已提交 850c601a0 |
| 113 | 2026-08-13 | 患者电话唯一约束缺失（任务书 patient-phone-unique-fix——真机同电话可重复创建） | **已执行**：真机创建两个同电话患者都 201（需求 US-PAT-003/004「PhoneNumber 唯一，重复返回 409」）。**根因**：① 查重逻辑存在但失败错误码用 PatientNotFound（404 语义）且走 BusinessFail 恒 422——需求要 409；② **BatchImport 只按姓名去重，完全没查电话**；③ 拼音同类排查：API 直调未传 PinYinCode → 拼音搜索 0 条。**修复**：① ErrorCodeExtensions `PatientPhoneDuplicate` 400→409 段；② Create/Update Handler 错误码改对；③ BatchImport 补电话查重（行内互查 + 系统查重）；④ 拼音兜底在 Handler 层（P05 架构守卫）。**测试**：PatientPhoneUniqueTests 6 + ErrorCodeTests 409 行。**文档**：04-patients US-PAT-003 实现注；13c #113 | 构建 0/0；Server 710/710（+7）；架构 87/87；任务书已删 | ✅ 已提交 231d15326 |
| 114 | 2026-08-13 | 电话唯一 409 状态码修复（任务书 patient-phone-409-fix——真机 422 应 409） | **已执行**：ErrorCodeExtensions 已映射 PatientPhoneDuplicate→409 但真机仍 422。**根因**：`Result.Failure(ErrorCode)` 不设 ModuleErrorCode——`HandleResult` 只认 ModuleErrorCode → 落 `BusinessFail` 恒 422；Controller 层双路径不一致。**修复**：① `HandleResult<T>` 在 ModuleErrorCode 为空时回退 ErrorCode 映射；② 双端 PatientsController 创建/更新/批量导入失败分支改 `HandleResult(useAuthMapping: true)`。**同类检查**：IdentityController 已手写正确；409 错误码（MedicalCaseLocked 等）代码零消费无路径可测。**测试**：ControllerBaseExtensionsTests 3。**文档**：04-patients US-PAT-003 409 注；13c #114 | 构建 0/0；Server 713/713（+3）；架构 87/87；任务书已删 | ✅ 已提交 48278e512 |
| 115 | 2026-08-13 | UpdateUser 层级约束缺失（任务书 updateuser-hierarchy-fix——真机 testadmin 更新 Admin 200 应拒绝） | **已执行**：CreateUser 有层级校验（USER-D04）但 Update 漏了——Admin 可更新另一个 Admin。**同类排查**：Delete/ToggleStatus/BatchDelete 用 bool IsAdmin 粗粒度——同缺口；Restore 已完整。**修复**：① 抽共享 `UserHierarchyGuard`；② Update/Delete/ToggleStatus/BatchDelete 命令 IsAdmin bool → OperatorRole，Handler 统一走 guard；③ BaseUsersController 4 端点传 OperatorRole + Forbidden→403；④ UpdateUser 保留原越级拦截 + 补 sysadmin 不可设 SuperAdmin。**测试**：UpdateUserHierarchyTests 7。**文档**：03-users US-USER-005 实现注；13c #115 | 构建 0/0；Server 720/720（+7）；架构 87/87；任务书已删 | ✅ 已提交 63674dc40 |
| 116 | 2026-08-13 | Server 单实例与端口防护（任务书 p2-07-single-instance——US-SHELL-024 定案全做） | **已执行**：① **start.sh 四层增强**（服务器 60.190.215.86 直接改+上传，备份 start.sh.bak）——PID 文件（校验 /proc/PID/cmdline 防 PID 复用）+ 端口释放确认（≤30s，15s 后 kill -9）+ health 探测（≤60s）+ 部署日志；② **Program.cs Mutex**（Global\LYBTZYZS_WebAPI_Instance）——Linux 下 .NET named Mutex 映射 /tmp 文件锁（flock），Global 前缀在非 Windows 被忽略等价全局；AbandonedMutexException 自动接管；获取失败降级警告；③ **测试**——SingleInstanceMutexTests 3（首次获取/二次拒绝/释放后可重取）；④ **真机验证**——start.sh 连续两次单进程；手动双开被 Mutex 拒绝（`[FTL] 已有实例...拒绝启动` exit 1）；health 200；⑤ **文档**——01-deployment Runbook（start.sh v3 + 踩坑 #12）、11a-shell US-SHELL-024 状态 ✅、13c #116 + P2-07 ✅ | 构建 0/0；Server 720/720（+3）；架构 87/87；任务书已删 | ✅ 待提交 |
| 117 | 2026-08-14 | Local ReportsController 分层修复（P0-1 API 单一职能——直注 IReportRepository 跳过 Service 层） | **已执行**：LocalWebAPI ReportsController 原直注 `IReportRepository`（DTO 组装在 Controller——3-Layer 违规，Remote 已注入 `IReportService`）。**修复**：Local 改注入 `IReportService`——DTO 组装（DailyIncomeDto TotalIncome 相加/DailyConsultationDto ByDoctor/DailyHerbUsageDto Items）移入 Service 层（ReportService 已有同逻辑实现——与 Local 原组装完全一致）；Local 3 端点同步补 T5-2 #17 语义（endDate 默认=startDate；startDate>endDate→400）。**测试**：ReportsControllerLayeringTests（远程反射 1：注入 IReportService 非 IReportRepository）+ LocalReportsControllerLayeringTests（本地 1）——防回归守卫。**文档**：13c #117 | 构建 0/0；Server 723/723；架构 87/87；Desktop 报表 4/4 | ✅ 待提交 |
| 118 | 2026-08-14 | 所有权检查模式统一（P1-7/8/9——Get+ValidateOwnership+SendCommand 移入 CommandHandler） | **已执行**：3 Controller（Catalog Herbs/Formulas + Patients）的 Update/Delete/ToggleStatus 原在 Controller 内做 GetByIdAsync+ValidateOwnership+SendCommand 重复编排——违反 API 单一职能。**修复**：① **Command 加 OperatorRole**（UpdateEntityCommand/DeleteEntityCommand/ToggleEntityStatusCommand + UpdatePatientCommand/DeletePatientCommand/TogglePatientStatusCommand——共 6 个，Handler 无 HttpContext 需显式传角色）；② **Handler 内做所有权检查**——CatalogEntityCommandHandlerBase 新增 ValidateOwnership 私有辅助（Admin/SuperAdmin 放行，Doctor 仅 CreatedBy==operatorId，失败 Forbidden）；患者 3 Handler 同逻辑；③ **Controller 移除 Get+ValidateOwnership 前置检查**（双端 Catalog 6 端点 + Patients 6 端点——Remote+Local 同步，失败 Forbidden→403）；④ **死代码清除**——Remote PatientsController CheckOwnershipAsync 私有辅助删除（零调用）。**测试**：OwnershipCheckLayeringTests（远程 5：无 CheckOwnershipAsync 辅助/Update/Delete/Toggle 端点保留返回 Task<IActionResult>）+ LocalOwnershipCheckLayeringTests（本地 3）；HerbReferenceCheckTests 5 处 + PatientPhoneUniqueTests 1 处 Command 补 OperatorRole（Admin 通过所有权）。**文档**：13c #118 | 构建 0/0；Server 724/724；架构 87/87；Desktop 4/4；任务书已删 | ✅ 待提交 |
| 119 | 2026-08-14 | MedicalCasesController 状态端点单一职能（P1-10/11） | **已执行**：① **Completed 分支路由移入 StateService**——UpdateStatus 端点原在 Controller 内 if(request.Status==Completed) 分支路由到 CompleteAsync（Controller 编排业务分派——违反单一职能）；现 `MedicalCaseStateService.UpdateStatusAsync` 加 operatorId/isAdmin 参数统一处理（Completed→委托 CompleteAsync，含工作流验证+权限+挂号联动；原「CompletedBlocked 抛异常」Guard 移除——语义改为统一分派）；`IMedicalCaseStateService` 接口签名同步；双端 Controller UpdateStatus 移除分支仅编排。② **close 权限改方法级 Authorize**——CloseMedicalCase 原 Controller 内 if(!isAdmin) return Forbid；改为 `[Authorize(Policy=AdminOrSuperAdmin)]` 方法级（强制关闭仅限 Admin——US-MC-012）；CompleteAsync 传 isAdmin:true（Authorize 已保证）；双端同步（Local CloseCase）。**测试**：MedicalCaseStatusEndpointTests（远程 2：Close 方法级 Authorize/UpdateStatus 保留）+ LocalMedicalCaseStatusEndpointTests（本地 2）。**文档**：13c #119 | 构建 0/0；Server 729/729；架构 87/87；Desktop 2/2；任务书已删 | ✅ 待提交 |
| 120 | 2026-08-14 | API 单一职能剩余 P1（4 个独立端点业务逻辑移出 Controller） | **已执行**：① **P1-1 Configuration 限频**——Restart 端点 TryAcquireRestartSlot 滑动窗口 + Task.Run 延迟重启移入 `ISystemConfigurationService.ScheduleRestartAsync`（限频静态字段 + 30s 调度在 Service；Controller 仅编排 + 审计）；② **P1-4 Deploy 上传**——新建 `IDeployService`/`DeployService`（扩展名校验+目录创建+文件保存移入），Controller 注入调用；③ **P1-5 Diagnostics 校验**——`LoggingLevelManager.EnableDebugMode` 新增 string 重载（枚举转换 verbose/debug/information + durationMinutes 120 上限移入），双端 Controller 移除转换逻辑；④ **P1-6 Download 生成**——新建 `IDownloadService`/`DownloadService`（ExtractVersion/BuildHtml/FormatSize + 文件扫描移入），Controller 仅调用 BuildDownloadPageHtml。每处改动 <30 行，外科手术式；Local 侧同步（Deploy/Download 为桩不需改）。**测试**：ApiSingleResponsibilityGuardTests 3（Configuration 无 TryAcquireRestartSlot / Deploy 注入 IDeployService / Download 注入 IDownloadService 非 IConfiguration——防回归）。**文档**：13c #120 | 构建 0/0；Server 731/731（+3）；架构 87/87；任务书已删 | ✅ 待提交 |
| 121 | 2026-08-14 | HTTP/HTTPS 双协议（P2-09 US-SHELL-025——Kestrel 多端点 + 配置开关） | **已执行**：① **Program.cs ConfigureKestrel 多端点**——Http 5000 默认开 + Https 5001 默认关，读取 `Server:Endpoints` 段（**非 Kestrel:Endpoints**——避开 ASP.NET Core 内建端点绑定，实测双重监听 bind 失败根因）；Http 开关 `Http:Enabled`、Https 开关 `Https:Enabled` + 证书 `Https:Certificate:Path/Password`（空 Path 用 dev-cert）；启动日志 `[启动] Listening on http://0.0.0.0:5000`；② **配置**——appsettings.Production.json + 内嵌模板加 Server:Endpoints 段（Https 默认关）；③ **Mutex 崩溃修复**（真机发现）——非持锁线程 ReleaseMutex 抛 Object synchronization → 进程崩溃；finally 加 try-catch 忽略（进程退出自动释放）；④ **测试**——KestrelEndpointConfigTests 3（默认 Http 开 Https 关/证书读取/模板含 Https 段）；⑤ **真机验证**——部署后 health 200 + `[启动] Listening on http://0.0.0.0:5000` + 单进程 + 仅 5000 监听（Https 默认关）；⑥ **文档**——01-deployment 配置注意事项 HTTPS 指南（开关/证书/验证）、13c P2-09 ✅ + #121 | 构建 0/0；Server 737/737（+3）；架构 87/87；真机 health 200；任务书已删 | ✅ 待提交 |
| 122 | 2026-08-14 | 死代码删除误伤防护固化 + 端点审计（P2-10——a99619f47 教训：删 quick-visit 误删 POST /Registrations Create） | **已执行**：① **流程固化**（代码+文档）——删除死代码前 grep 确认归属（区分死代码 vs 相邻代码）；删除后立即用 swagger 端点清单对照；编译门禁后跑相关端点测试（不能只看编译）；traceability 同步标记删除端点。② **端点审计**（临时测试 WebApplicationFactory 导出 swagger.json vs 反射 Controller 路由，规范化 diff）——**代码端点 98 = swagger 路径 98 完全一致**：无未暴露端点（代码有 swagger 无）、无幽灵端点（swagger 有代码无）；diff 仅 `{id:guid}` vs `{id}` 参数约束规范化差异（swagger 把 :guid 约束规范化掉——同端点）。③ **traceability 同步**——补 US-SHELL-024（P2-07 ✅）/US-SHELL-025（P2-09 ✅）两行（新增 US 未入矩阵——补齐）；P2-08（API 单一职能审查 ✅——P1 系列全落地）、P2-10（✅ 固化）。④ 临时审查测试用后即删（任务书无新增测试——纯流程/规范加固）。**文档**：traceability +2 行、13c P2-08/P2-10 ✅ + #122 | 构建 0/0；架构 87/87；任务书已删 | ✅ 待提交 |
| 123 | 2026-08-14 | 架构 P10 修复 + 命名 P0 统一（P10-1 + N-01 + N-03；N-02 评估后不做） | **已执行**：① **P10-1 UserService 直注 IdentityDbContext → Repository**——4 个凭证/登录状态方法（GetByUsernameAsync/UpdateLoginFailureAsync/ResetLoginStateAsync + GetByIdAsync 复用）移入 IUserRepository/UserRepository（EF 查询在 Repository 层）；UserService 移除 _context 注入（Service 不直连 DbContext——P10 合规）；连带 UpdateUserHierarchyTests.FakeUserRepository 补 3 方法。② **N-01 IdentityController → UsersController**——git mv 文件 + 类名 + 引用（IdentityControllerRoutesTests 改引用 UsersController）；路由不变（/users）；Local 端 AuthController+UsersController 不动。③ **N-03 路由声明统一**——5 个 Server Controller（MedicalCases/Configuration/Deploy/Diagnostics/Reports）类级路由改 `[controller]`（类名复数 = 原硬编码路径——路径零变化，仅声明方式统一；HealthController 保持硬编码）。④ **N-02 Formulas 硬编码路径改相对——评估后不做**：前端 Refit 硬编码 `/api/v1/formulas` 15 处 + herbs——改相对（类级 herbs 前缀）会拼成 /herbs/formulas（错）；改类级 catalog 会破坏 /herbs 前端调用。任务书前提「确认前端无直接调用硬编码路径」实测不成立（前端有大量调用）——外科手术原则：不破坏 API 为命名风格买单。**测试**：路由测试 9/9（UsersController 改名后）+ Server 全量 737/737。**文档**：13c #123 | 构建 0/0；Server 737/737；架构 87/87；任务书已删 | ✅ 待提交 |
| 124 | 2026-08-14 | 医案分页 TotalCount 修复 + 命名 P1（P1-2 + N-04 + N-05） | **已执行**：① **P1-2 医案分页总数 DB 下推**——`SearchMedicalCasesAsync` 原在 Service 内存过滤 `paged.Items.Where(c => c.CreatedBy == operatorId)`（TotalCount 偏大——分页总数不准）；`QueryPagedAsync` 加 doctorId/isAdmin 参数，`m.UserId == doctorId` 过滤下推 DB（TotalCount 随之准确）；接口 + Repository + Service 同步，内存过滤移除。② **N-04 {id:guid}→{id} 统一**——4 文件 19 处（Remote MedicalCases/Patients/Registrations + Local Patients）：统一 `{id}`（模型绑定自动验证 Guid 格式——前端 Refit 无 :guid 约束，安全零破坏）。③ **N-05 Server/Local 方法名对齐**——Local ConfigurationController（GetAll→GetConfiguration/Get→GetValue/Set→SetValue/Validate→ValidateProduction）+ Local HealthController（GetHealth→Get/GetDetails→GetDetailedHealth——Get 保留 async 因方法体 await）——Server 权威源，HTTP 路由不变。**测试**：Server 737/737（P1-2 签名变更无测试连带——MedicalCaseTests 为 E2E HTTP 级）；架构 87/87。**文档**：13c #124 | 构建 0/0；Server 737/737；架构 87/87；任务书已删 | ✅ 待提交 |
| 125 | 2026-08-14 | Reports GetDoctorPerformanceAsync 4次查询合并为1次（相关子查询模式——初版 LEFT JOIN 预聚合在 EF InMemory 翻译缺陷 Nullable must have value，改用 SelectMany 相关子查询（decimal?）Sum ?? 0——InMemory/SQL 均可翻译；14/14 测试含组合计数/平均处方价/零处方） | pi 实施，Hermes 修复 + 验证 | ✅ 已提交 907bd392b + 相关子查询修复 |
| 126 | 2026-08-19 | 架构重构 Review + L4-L0 死代码清理（任务书 l4-l0-cleanup——修复 Review 4 问题 + 全层清理） | **R1 README/AGENTS 旧名同步**：Users/Registrations/Patients 3 个模块 README + Registrations AGENTS 共 11 处 `Remote*Service` → `*Service`（P1-2 重命名残留）；src/Client/Desktop 下 `Remote\w+Service` 引用清零。**R4 CardReaderPureTests 存量失败修复**：`PrivacyHelper.MaskIdNumber` 边界 `<=10`→`<10`（10 位号也脱敏，匹配测试期望 `1234567890`→`123456****7890`）+ null 原样返回（原 `?? string.Empty` 吞 null）+ 返回类型 `string`→`string?`（3 个透传 VM 同步）——CardReaderPureTests 全绿。**R3 补单测**：AuditLogServiceTests 3（成功/空分页/异常→Failed）+ ReportServiceTests 6（3 报表×成功/失败/异常）——P0-2 行为变化纳入覆盖。**L4/L3 清理**：删 `HttpApiClientBase.WrapSuccess<T>` 泛型重载（零调用）+ `PostRawAsync<T>`（零调用）+ IMedicalCaseApi/IRegistrationApi/IApiClientRegistrations 3 处空文档注释（无对应方法残留）。**L2 清理**：`ApiClientRepositoryBase.HandleException` 删冗余 `args` 参数与恒等三元分支。**L0 清理**：删死字段 `MedicalCaseMasterDetailViewModel._loggerFactory`、`AdminHomeViewModel._dialogService`、`ClinicalHomeViewModel._dialogService`（均只赋值不读取）+ 死属性 `ConfirmationDialogViewModel.IsSoftDeleteSelected`（零读取）+ `MessageDialogViewModel.IconSource/IconColor` 计算属性（XAML 未绑定，仅 DataTrigger 用 MessageType）+ DataGridSelectionBehavior 空 catch 补注释。**评估保留**：空 catch 4 处（ThemeService×2/MainWindow/App 有注释说明的有意静默）；Repository/Service 层 BatchImport/Export 等公共接口零调用但对应 Server 端点（不删——契约完整性）；MedicalCaseService 门面死成员（量大关联深，登记后续专项）；XAML 坏绑定 5 处（QuickVisitCommand/ImportCommand/DownloadTemplateCommand/Consultation/Prescription/BoolToVis——修复需 XAML 改动，登记报告）。**验证**：构建 0 错误 0 警告；新增+受影响单测 23/23 通过；任务书已删 | 报告 desktop-l4-l0-cleanup-2026-08-19.md | ✅ 待提交 |
| 127 | 2026-08-19 | Desktop 架构收敛（任务书 desktop-convergence——C-5 机制收敛 + C-6/C-7 死代码清理 + Review 遗留） | **① 机制收敛（ADR-0020）**：`MedicalCaseRepository.CancelMedicalCaseAsync` 恒 null bug 修复（原成功也返回 null → 取消永远报失败；签名 `MedicalCaseDetailDto?`→`bool` 写操作契约，LifecycleService 适配）+ 补 2 回归测试（成功→true / 失败→false）。**② MedicalCaseService 门面死成员删除**（上批登记遗留项）：删非接口成员死方法 4 个（SetPrescriptionFlagAsync/UpdateStatusAsync/SuspendViaApiAsync/DeleteMedicalCaseAsync——全仓零调用）+ 清理 using。**③ XAML 死绑定清理**（上批登记 5 处坏绑定中修复 4 类 6 处）：DeploymentView `StaticResource BoolToVis`→`x:Static Cvt.BoolToVis`（资源不存在坏绑定）；RegistrationListView 删 QuickVisitCommand 死按钮（QuickVisit 删除残留）；UserMasterDetailControl 删 ImportCommand/DownloadTemplateCommand 死按钮（UserImportExportHandler 已删）；PatientMasterDetailControl 删 ImportCommand/ExportCommand/DownloadTemplateCommand 死菜单（零命令对应）。**④ 架构守卫存量修复**：ReportRepository 继承 ApiClientRepositoryBase（P0-2 引入时漏继承——A02 架构守卫 86/87 存量失败，修复后 87/87）。**⑤ 文档过时修正**：Users AGENTS.md（IUserService 死代码/ChangePasswordAsync placeholder 过时声明删除——实际已完整实现并注册；IUserImportExportHandler 已删；IUserDataSource 已不存在）。**⑥ TODO 清理**：MedicalCaseWorkspaceViewModel 超大类型 TODO 删除（引用文档 code-review-duplicates.md 已归档、579 行 <600 限制、薄壳+子 VM 拆分已落地）；其余 5 处保留（功能待实现/P1-3 约束）。**评估保留**：Converter 13 个全在用（x:Static Cvt 模式）；Core Controls 全被引用；空 catch 4 处有意静默；Consultation/Prescription 绑定有效（非坏绑定——上批误登记）。**验证**：构建 0 错误 0 警告；架构 87/87；MedicalCaseRepositoryTests 6/6（+2 新增） | 报告 desktop-convergence-2026-08-19.md | ✅ 待提交 |
| 128 | 2026-08-19 | Desktop 深度代码审查（任务书 desktop-deep-review——基于最新扁平化需求文档 + 追溯矩阵双端分列 + f5e0b4236 批量导入/导出 UI，只审查不修改代码） | **结论**：UI 接线质量良好（build 0/0 + 架构 87/87 本次独立实测），但暴露 **P1 路由断裂 ×2**——① 药材「导出」双模式 404（Desktop 契约调 `GET /herbs/export`，Remote 只有 `/herbs/export-all`，Local 无端点）；② 药材「下载模板」Local 模式 404（Local 无 `GET /herbs/import-template`）；③ 验方模板 Example.Herbs 为字符串但导入 DTO 是 List（照抄模板必失败）+ 模板 Fields 含 DTO 不存在的 Category；测试盲区：LocalImportExportJsonTests 未覆盖药材、无契约↔路由对齐守卫。**P2**：权限口径（文档 Admin vs 代码 DoctorOrAdmin 类级继承 + 模块间不一致）、导出保存 ApiResponse 包装体（非文档「JSON 数组」且不可回灌导入）、验方导出 category/keyword 参数错位 + 无药材组成明细、分类搜索「分类:xxx」当 keyword 永不命中（既有）。**矩阵 v1.9 失真**：HERB-007/013 Desktop ✅ 乐观标注（未真机验证）。**文档滞后**：05-herbs 验收标准仍写 .xlsx、04-patients US-PAT-011/012 状态 🔧、06-formulas AllowAnonymous 矛盾 + 虚构接口 IHerbImportExportService/IPatientImportExportService。修复任务书 `.hermes-task-fix-desktop-import-export-routes.md`（P1 可直接派发，P2 先过需求） | 报告 docs/compose/archive/desktop-deep-code-review-2026-08-19.md | ✅ 本批次（审查任务，无代码改动） |
| 129 | 2026-08-19 | 导入导出 P1 修复（任务书 .hermes-task-p1-fixes.md——desktop-deep-review #128 派单） | **① 药材导出路由对齐（双模式 404 修复）**：Remote CatalogController 新增 `GET /api/v1/herbs/export`（关键词筛选，对齐患者模式——Desktop 契约此前调 /export 而服务端只有 /export-all 导致远程 404）；LocalWebAPI 补 `GET /api/v1/herbs/import-template` + `/export` + `/export-all`（此前全缺导致本地模式「下载模板」「导出」404——13c #112 声称的「双端」此前不成立）。**② 验方模板 DTO 对齐（US-FORM-006/013）**：`FormulaImportItemDto` 补 `Category` 属性（实体/Formula.Create 已支持——导入分类不再静默丢弃；模板 Fields 保留 Category 字段）+ 模板 Example.Herbs 改对象数组 `[{"HerbName":"人参","Dosage":10,"Unit":"g"}]`（照抄模板此前必反序列化失败）——Remote + Local 双端模板同步。**③ 同类路由修复（新发现）：LocalWebAPI 验方路由前缀错位**——新建路由对齐守卫测试发现 CatalogController 16 个验方 action 路由模板缺少前导 `/`（"api/v1/formulas/..."）→ 属性路由与类级 `Route("api/v1/herbs")` 拼接成 `/api/v1/herbs/api/v1/formulas/*`——离线模式全部验方操作（查询/CRUD/导入/导出/模板/克隆/校验/批量）404（对齐 Remote 的 CATALOG-ROUTE-FIX 1f4f54a91 先例，15 处改绝对路径）。**④ 测试补齐（防回归）**：新增 `ImportExportRouteParityTests`（Desktop 测试项目——Refit 契约路径 ↔ Remote/Local 控制器路由表反射比对，覆盖患者/药材/验方 batch-import/import-template/export 9 路径 + 双端路由面一致性）；`LocalImportExportJsonTests` 补 LocalHerbTemplateAndExport（药材模板/导出/export-all）断言；`ImportExportJsonTests`（Server）HerbTemplateAndExport 补 HerbExport 断言。**⑤ 文档同步**：追溯矩阵 v1.10（HERB-007/013 Desktop ⚠️→✅——修复后双端成立；FORM-013 Desktop ✅→⚠️——导出缺药材组成明细 + 分类筛选参数对齐 P2 待办）；需求文档 US-PAT-011/012 状态 🔧→✅、US-HERB-007/013 验收标准去 Excel 残留（改 JSON 数组/模板）、删虚构接口引用（IHerbImportExportService/IFormulaImportExportService）、US-FORM-013 删 AllowAnonymous 矛盾规则；04-api-reference/04-herbs.md 注意段补 /export + 双端清单。**验证**：build 0 错误 0 警告（--no-incremental）；架构守卫 87/87；ImportExportRouteParityTests 2/2 + LocalImportExportJsonTests 5/5 + ImportExportJsonTests 6/6 全绿 | 构建 0/0；架构 87/87；路由对齐守卫 2/2、Local JSON 守卫 5/5、Server JSON 守卫 6/6 | ✅ 已提交 |
| 130 | 2026-08-19 | P2 修复 + 文档同步（任务书 .hermes-task-p2-fixes-doc-sync.md） | **① 权限口径对齐（T3）**：药材/验方 `batch-import`/`import-template`/`export`/`export-all` 双端补显式 `[Authorize(Policy=AdminOrSuperAdmin)]`（对齐患者 `batch-import` `AdminOrSuperAdmin` 口径；原类级 `DoctorOrAdmin` 放行 Doctor 导入/导出）。**② 验方导出筛选参数对齐 + 明细补全（T5）**：服务端 `FormulaExport` 筛选参数 `keyword`→`category` 对齐客户端 `IFormulaApi:71` `?category=`（原错位导致传参永不生效）；导出 DTO 由 `List<FormulaListDto>`→`List<FormulaDetailDto>` 含 `Herbs` 明细（满足 US-FORM-013 “每行含药材组成详情”）；`ICatalogQueryService` 新增 `ExportDetailsAsync` + `GetPagedAsync(category)` 直通仓储 `Category` 过滤 + 所有权。**③ 文档同步（T7）**：`06-formulas.md` US-FORM-013 ⚠️→✅ + “导出 Excel”→“导出 JSON”；`13-traceability-matrix.md` v1.11（FORM-013 ⚠️→✅ 142/2，HERB-007/013 保持 ✅）；`13c` #112“双端”表述修正（滞后注记…
| 134 | 2026-09-13 | Shell 绑定修复 + SSOT 架构收敛（任务书：前端设计深化优化——基于 View/VM 绑定审计） | **① P1 修复（三控件 AW 静默失效）**：`HeaderControl/SideNavControl/FooterControl` 的 Prism 约定名（`HeaderControlViewModel`/`SideNavControlViewModel`/`FooterControlViewModel`）与实际 VM 类名（`HeaderViewModel`/`SideNavViewModel`/`FooterViewModel`）不匹配且未显式登记 → Prism `AutoWireViewModelChanged` 在解析不到时**直接 return**（源码语义），控件静默继承宿主 MainWindowViewModel → 侧栏导航列表（`GroupedNavigationItems`）/深色开关（`IsDarkMode`）、底栏全部 4 个绑定失效。修复：新增 `ShellViewMappings`（视图→VM 显式映射表，5 条）作为唯一真相源，`App.ConfigureViewModelLocator` 调 `Register()`；三 VM 同步显式 DI 注册（显式优于约定、不依赖 DryIoc 具体类型兜底）。**② P2 修复**：`ClinicalModule` 显式注册 `ClinicalWorkspaceViewModel`（此前靠 DryIoc 兜底构造，与其他导航 VM 不一致）。**③ 架构深化（SSOT 收敛，合理性优先）**：a) **侧栏状态 SSOT**——新增 `ISidebarStateManager`/`SidebarStateManager`（Shell 单例，宽度由展开态 + ShellConstants 推导），宿主与侧栏 VM 均改为只读/可写代理 → 修复既有审查 #6「双份状态不同步」（此前 Ctrl+M（宿主）与汉堡按钮（侧栏）各持一份状态）；b) **主题 SSOT**——`SideNavViewModel.IsDarkMode` 由自有字段改为代理 `IThemeService.IsDarkMode`（构造即与当前主题一致，设置即 `ApplyTheme`，并订阅广播）→ 修复 #34 后半「IsDarkMode 未按当前主题初始化」；c) **底栏状态文本绑定化**——`FooterViewModel.ApiStatusText`（Healthy/Unhealthy/Checking → 已连接/未连接/检测中）替换 XAML 硬编码「API 已连接」+ 固定绿色（断连时曾恒显示已连接）；d) **登出唯一入口**——新增 `IShellLogoutService`/`ShellLogoutService`（活跃医案 `RequestLeaveAsync` 守卫 + 二次确认 → `PerformLogoutAsync`，返回 `LogoutOutcome`），宿主与侧栏退出按钮共用 → 修复 #34 前半「侧栏 LogoutAsync 绕过活跃医案离开守卫」（数据丢失风险）；e) **AppShell 列宽**改绑宿主 VM 的 `SidebarWidth` 代理（去掉 `DataContext.SidebarWidth, ElementName=SideNav` 跨元素遍历，符合 WPF 绑定最佳实践）；f) 清理：删 `MainWindowViewModel.EditProfileCommand`、`SideNavViewModel.ToggleSidebarCommand`/`EditProfileCommand` 等零消费代理；`SideNavViewModel.LogoutCommand` 改用 `[RelayCommand]` 源生成（CommunityToolkit 规范）。**④ 对话框核查**：9 个对话框 VM 均经 `DialogViewModelBase : NavigableViewModelBase, IDialogAware` 完整实现生命周期（Title/OnDialogOpened/CanCloseDialog/OnDialogClosed/RequestClose + `OnDialogOpenedCore` 钩子），`RegisterDialog` 9 处齐备——无缺口。**⑤ 守卫**：新增 `ShellViewViewModelBindingTests`（架构测试项目 2 条：AW 视图 VM 可解析「约定或显式映射」+ 映射表 VM 具象性/绑定面成员契约），已用「临时摘除一条映射」验证非空转（预期 FAIL → 复原后 PASS）；新增 `ShellViewModelBindingTests`（Desktop 单测 13 条：Ctrl+M↔汉堡同源、导航文字/宽度通知、主题代理、底栏状态文本与通知、Header 代理、分组视图、侧栏状态管理器 + 登出守卫 5 条）。**验证**：`build Shell --no-incremental` 0/0；全 solution 0/0；架构 Shell 相关 8/8；Desktop Unit/Shell 13/13（既有 Startup 27 条保持全绿）。**运行时观察（附带发现，未修）**：实机启动 Desktop Shell 抓 UIA 元素树（52 元素 = 首次运行向导 + 登录页；三控件因未登录被覆盖未入树）；日志暴露既有缺陷 `LocalWebAPI 首启种子失败：User security stamp cannot be null`（`IdentitySeedData.EnsureUserAsync → AddToRoleAsync`，本地模式首启不可用，非本次改动引入，登记待修） | 构建 0/0；架构 8/8；Desktop Unit/Shell 13/13 | ✅ 已提交 95c39466e |
| 135 | 2026-09-13 | 本地模式首启种子失败修复（任务书：本地模式首启种子失败——#134 附带发现） | **根因（复现后定位）**：`IdentitySeedData.EnsureUserAsync` 建号路径**忽略 `UserManager.CreateAsync` 返回值**。Identity `CreateAsync(user, password)` 在「密码不满足策略」时先 `UpdatePasswordHash(validatePassword: true)` 返回失败，**尚未写入 SecurityStamp**；代码继续 `AddToRoleAsync` → `UpdateUserAsync` → `GetSecurityStampAsync` 见 null → 抛 `InvalidOperationException("User security stamp cannot be null.")`——**与真因无关的误导性错误**。真因：本地模式 `Shell/appsettings.json` 的 `DefaultPasswords:*` 为占位符 `__REPLACE__`（未注入），本地无 Program 侧密码校验门禁，故静默走到种子才炸。**修复（仅种子文件）**：① 建号路径校验 `CreateAsync` 结果，失败即抛可执行错误（含 Identity 原始错误 + 配置键 `DefaultPasswords:SysAdminPassword`/环境变量 `DefaultPasswords__SysAdminPassword` + 密码策略提示 + 占位符说明）；② 抽共享 `EnsureSecurityStampAsync`（原有 2026-08-17 自愈逻辑 + 建号路径新增「stamp 非空」防御），两条路径统一在角色分配前保证 stamp 非空；③ 角色/密码重置/sysadmin 标记逻辑不变（远程/本地同源一致）。**验证**：新增 `IdentitySeedDataTests` 3 条（离线 InMemory + 真实 AppDbContext/Identity，与 Program 同密码策略）——**修复前**占位符密码用例 FAIL（实测抛出 `"User security stamp cannot be null."`，与真机一致，构成复现证据）→ **修复后 3/3 通过**（占位符密码得可执行错误、合规密码建号+stamp+角色+幂等、存量无 stamp 用户自愈）；`build LYBT.WebAPI --no-incremental` 0/0；全 solution 0/0。**实机复验**：Desktop Shell 首启两条日志对比——未注入：`[SEED] 创建用户 sysadmin 失败：Passwords must have at least one digit…请检查默认密码配置…`（可执行）；注入 `DefaultPasswords__SysAdminPassword` 后：`[LOCAL-API] Embedded LocalWebAPI started on http://localhost:5300` + `[HEALTH-MON] Healthy/Connected` + `[启动] API 连通性检查通过` → 种子成功、本地模式恢复可用。**文档**：`05-development/01-setup.md` 本地模式补「首次运行必须注入默认密码」步骤（含 PowerShell 片段与安全语义）；`06-operations/01-deployment.md` 踩坑 #13 | 构建 0/0；IdentitySeedDataTests 3/3；实机本地模式 Healthy | ✅ 已提交 2e692fc25 |
| 136 | 2026-09-13 | 前端设计文档全面复盘与完善（任务书：前端设计文档全面复盘与完善——UI/UX 文档与代码对齐）。**Phase 1-3 结果**：① 事实基准改为机器扫描（View 30 / Control 33 / Dialog 7 / Root 1 / ViewModel 55；XAML 82 = 视图 71 + 资源模板 11，口径写入各文档）；② `desktop-ui-detailed-design.md` v2.1（封面 25/24/51+ → 30/33/55；§1.3 去重并补全 16 共享控件 + 新增 §1.3.1 模块控件 17；§4 改为 30 个 View 并补 §4.4.1 Shell 组件、§4.18-21、§4.22 未建视图对照、§4.23 覆盖核对表）；③ `desktop-ui-requirements.md` v1.2 + `second-level-design-checklist.md` 重写（37 行可勾选清单 = 30 View + 7 Dialog × 8 判定列）；④ `desktop-view-inventory.md` v1.0 按代码重建（每个 View 含真实路径/VM/进入点/US 依据；删除「侧栏仅全局操作」错误结论 → 侧栏承载角色导航矩阵，每角色 3 项、Group∈临床/目录/管理；Doctor 首页改 `ClinicalWorkspaceView`；新增 drawio 同步差异清单 30 条，不修改 drawio）；⑤ `DESKTOP_ARCHITECTURE_STANDARD.md` v1.1（新增 §13.4 对话框规范 9 处 RegisterDialog + `DialogViewModelBase`/`IDialogAware` 生命周期 + Prism 自动装配；新增 §13.5 数量口径与校验；幽灵示例名标注）；⑥ 新增 4 份 UX 文档（用户旅程/交互规范/错误处理/加载状态）+ `07-ui-ux/README` 索引 11 份；⑦ 模块文档刷新（Auth/Patients/Users/Registrations/MedicalCase/Clinical/Admin README + 各自 AGENTS.md 与 Roles/Modules AGENTS.md）并**新建 Catalog README**（此前缺失 → `FormulaEditorViewModel`/`HerbEditorViewModel`/`FormulaHerbItemViewModel`/`FormulaValidationItemViewModel` 无文档描述）；⑧ `docs/README.md` 目录计数按「含子目录 *.md」规则重算（151→214，原口径不可复现）并声明规则。**Phase 4 验证（独立于书写 agent 的复算）**：71 个 `x:Class` 在 inventory 与 detailed-design 全覆盖（差集空）；**55/55 ViewModel 均有文档描述**；30 个改动文档相对链接 **0 失效**（另修 3 处真实失效链接：`Desktop/Core/Modules` 的 `Shared/AGENTS.md` 相对深度错误）；`git status` 确认 25 改 + 5 增**全部为 .md，0 代码文件**。**未修（超出「只改文档」约束，登记为代码侧发现）**：`LoginView.xaml` 硬编码诊所名 ×4（未绑定 `ClinicSettings`）；`designs/*.pen` 部分稿系统名沿用旧称；`InitializationWizardView`/`SessionTimeoutWarningDialog`/配置导出导入页仍未建 | 文档数即可核对：事实基准统一为机器扫描 + 口径显式声明，杜绝「文档说 25 个 View、代码有 30 个」类漂移；幽灵视图一律标注真实承载者而非静默删除；缺口（VM 无文档）通过补 Catalog README 一次补齐 | 技术总监 |
| 137 | 2026-09-14 | ⚠️ US 补 E2E 测试（任务书：按追溯矩阵 ⚠️ US 补充 E2E——US-FORM-014/US-SHELL-007/US-CFG-006/US-ERR-006+007）。**① 新增 E2E（15 条，全部通过）**：`ShellFlow/ModeSwitchE2ETests`（4：远程↔本地切换成功、模式持久化重载、双库数据隔离、未配置远程地址阻断 NO_REMOTE_URL；**双真实后端**——本类自启嵌入式 LocalWebAPI 固定 `localhost:5300`（`IsLocalUrl` 仅认 :5300）+ 基类随机端口服务器作远程替身，各自独立 LocalDB；端口被占即失败，绝不误连真实服务）；`ErrorFlow/ExceptionMappingE2ETests`（6：400/404/401/403/422 状态码 + ApiResponse 六字段规范 + 成功响应同构）；`FormulaCrudE2ETests.Clone_*`（4：组成逐字段复制、名称 `" (副本)"` 后缀、原验方不变、Draft 初始）；`ConfigurationE2ETests.UpdateClinicSettings_PersistsAndReloads`（1：诊所信息落盘 + 重载可读 + 其他配置节不受影响，含原文件备份/恢复）。**② 修复 2 个被测路径缺陷（E2E 前置阻断，已随包提交）**：a) `LocalWebAPI/Controllers/FormulasController` 克隆漏拷 `ProcessingMethod`（炮制方法）→ 补字段并断言（需求 US-FORM-014「药材组成与源验方一致」）；b) 同文件 `GetFormulaById` **未 override** 基类虚方法（路由 `{id}` 与基类 `{id:guid}` 并存 → 约束更具体的基类动作胜出 → `GET /api/v1/formulas/{guid}` 恒 500「此资源不支持 GetById 操作」，桌面 `FormulasHttpApiClient.GetFormulaByIdAsync` 全场景失败）→ 改 `override GetById` + `CreatedAtAction(nameof(GetById))`，与 Herbs/Patients/MedicalCases/Users 既有约定一致。**③ 取证登记缺口（不修，进 backlog）**：a) **患者关键词检索 500**：`GET /api/v1/patients?keyword=…` 在部分关键词下 500「invalid escape character … LIKE predicate」——EF 对加密列 `PhoneNumber` 的 `Contains` 生成 `LIKE @p ESCAPE N'\'`，值转换器把 ESCAPE 常量也加密成密文（`ToQueryString` 取证）——**已修复（2026-09-23 收尾批次 2，见 #150）：电话分支改走 `PhoneSearchHash` HMAC 盲索引等值匹配（姓名/拼音保持前缀）**；b) **US-CFG-006 过度声明**：`ClientConfigurationStore` 文件链✅（原子写 + `IConfigurationRoot.Reload`），但生产读取方注入 `PrismConfigurationExtensions.RegisterOptions` **启动期静态 `IOptions` 快照** → 同进程不热更新；UI（`SystemSettingsViewModel`→`IClinicSettingsService.SaveSettingsAsync`）写 `Directory.GetCurrentDirectory()` 与 Shell 读 `AppContext.BaseDirectory` 可能非同一文件；本地 `PUT /configuration/sections/ClinicSettings` 被 `ConfigurationWritePolicy` 白名单 422 拦下（ClinicSettings/FeatureToggles 均不在 `AllowedSections`，故 `effectiveMode:"hot"` 分支不可达）；c) **US-SHELL-007 ERR-70506 未阻断**：`FireAndForgetRemoteProbeAsync` 仅记 warning，`ModeSwitchResult.PendingCasesBlocked` 有定义无调用 → 需求 AC「未完成医案阻断切远程」不成立；d) **本地无 409 生产者**：`PatientPhoneDuplicate` 等 409 码无抛出点，电话唯一查重因 `PhoneNumber` AES-GCM 非确定性加密使 SQL 等值比较恒不命中（实测同号两次创建均 201，US-PAT-003/004 语义失效）——**已修复（2026-09-23 收尾批次 2，见 #150）：`ExistsByPhoneAsync` 改走 `PhoneSearchHash` 盲索引等值，`PatientPhoneDuplicate`→409 恢复可达**；e) **US-ERR-006 双轨**：模型校验走 `[ApiController]` 默认 `InvalidModelStateResponseFactory`（`SharedHost.AddSharedControllers` 未进入 LocalWebAPI 自建 builder 链路）→ 400 **ProblemDetails**，业务失败才走 ApiResponse。**④ 文档同步**：需求状态列四处校准（US-SHELL-007 ✅→⚠️、US-CFG-006 ✅→⚠️ 含三条细分、US-ERR-006/007 补实测映射、US-FORM-014 记修复）、追溯矩阵 5 行更新 + 变更记录、`05-development/04-testing.md` 补「E2E 套件清单（US → 测试类）」并把失真的 Desktop 测试目录树改为实际结构 | ⚠️ US 需要 E2E 覆盖，且状态列必须与代码实测一致；测试写不下去的地方即真实缺陷——本次 2 个修、5 项登记，避免「✅ 已实现」掩盖缺口。**⑤ 验证**：`dotnet build LYBTZYZS.sln --no-incremental` **0 错误 0 警告**；`Integration/E2E` 全目录 **101/101**（含本批次 15 条 + 既有 86 条回归）；`Integration/LocalApi` + Unit 78/78；**架构守卫 98/99——`DP_M1_ViewModels_Must_Not_Hold_Dto_As_Editable_Property` 失败：`Roles/LYBT.Desktop.Admin/Sysadmin/ViewModels/SecurityAuditLogViewModel.SelectedLog`（`SecurityAuditLogDto`），系 2026-09-09 提交 `47ba24e73`（US-SHELL-014）引入的**既有违规**，非本批次改动，登记待修** | 技术总监 |
| 138 | 2026-09-14 | ViewModel 层清理批次 1-2（任务书：按 `docs/07-ui-ux/viewmodel-layer-design.md` 渐进式迁移——Shell + Auth）。**① Shell 模块 5 个 VM 核查：无需迁移（0 改动）**——`MainWindowViewModel`/`SideNavViewModel`/`HeaderViewModel`/`FooterViewModel`/`AccountSettingsViewModel` 均已用 `[ObservableProperty]`/`[RelayCommand]`；15 个 `public ICommand` 全部委托 `IShellServices.Menu`（菜单命令 SSOT），手写 `OnPropertyChanged(...)` 仅用于「子 VM/服务状态 → 宿主绑定面」代理广播与派生属性（Title/IsSidebarExpanded/SidebarWidth/GroupedNavigationItems/ApiStatusText 等），**迁 `[ObservableProperty]` 会复制状态、破坏 #134 确立的 SSOT（侧栏/主题/登录态单一真相源）**，故按「合理性优先」保留。Shell 对话框 VM（Confirmation/Input/MessageDialogViewModel）亦已全量 Toolkit 化。**② Auth 模块：迁移 `LoginViewModel` 4 处手写命令实例化 → `[RelayCommand]`**——`LoginCommand`（原 `new AsyncRelayCommand(ExecuteLoginAsync, 谓词)` → `[RelayCommand(CanExecute = nameof(CanLogin))] LoginAsync()`，谓词逐字保留 `用户名/密码非空 && !IsLoading`，P2-14-1 防重入与 `NotifyCanExecuteChanged()` 调用点全部不变）、`CloseApplicationCommand`/`OpenSettingsCommand`/`ForgotPasswordCommand` 同类迁移（生成命令名与 XAML 绑定名一一对应，XAML 零改动）；`RetryApiCheck/SwitchToLocal/SwitchToRemote` 3 个代理命令改为表达式体属性（保持与子 VM **同一命令实例**，其 CanExecute 通知直达 UI）；代理属性（Username/Password/ApiStatus…）状态 SSOT 在 `LoginCredentialsViewModel`/`ConnectionStatusViewModel`，**不迁**（迁即双份状态）并已在代码注释写明依据；`IsAutoLogin`（无消费者订阅的纯 UI 状态）、`ClinicName`（构造期只读）有意保留。其余 Auth VM（`LoginCredentialsViewModel`/`ConnectionStatusViewModel`/`ConnectionTestViewModelBase`/`FirstRunSetupViewModel`/`ServerConfigViewModel`）核查后已符合最佳实践。**③ 文档**：`viewmodel-layer-design.md` v1.1——§1.3 统计改为可复算口径（实测：`[ObservableProperty]` 238 / `[RelayCommand` 174 / `OnPropertyChanged|SetProperty` 调用 828（含代理广播，非「待迁移量」）/ `public ICommand` 声明 60 / **手写命令实例化 34 = 真正迁移目标**），§4.1 迁移顺序标注完成状态 + 剩余落点（MedicalCase 2 VM、Shell `MenuManager`/`NavigationManager`、Controls code-behind 2），新增 §八 迁移记录（逐文件核查结论/变更/验证证据）与变更记录。**④ 验证**：`dotnet build LYBTZYZS.sln --no-incremental` 0 错误 0 警告（`src/Client/Desktop/` 下无独立 sln，按仓库门禁用根 sln；另单独 `Shell`/`Auth` 项目 `--no-incremental` 均 0/0）；`--filter FullyQualifiedName~Auth` 89/89；`--filter FullyQualifiedName~Shell` 35/35；架构守卫 98/99（唯一失败仍为既有 `SecurityAuditLogViewModel` DTO 属性违规，见 #137，非本批次引入） | 迁移价值判据应是「本 VM 自有字段 + 手写 setter/命令实例化」，而非 `OnPropertyChanged` 调用点数量；Proxy/派生属性必须保留手写通知，否则 SSOT 与绑定同步双双受损 | 技术总监 |
| 139 | 2026-09-14 | ViewModel 层清理批次 3（任务书：继续迁移——Patients 模块）。**① 迁移 2 处手写属性**：`PatientEditorViewModel.Patient`（`get => _patient; set => SetProperty(ref _patient, value)` → `[ObservableProperty] private PatientEditContext _patient = PatientEditContext.CreateNew()`，属性名/可写性与 `Context` 重写、`SubscribeContext()` 调用点全部不变）；`PatientCardReaderViewModel.IsReadingCard`（`private set => SetProperty(...)` → `[ObservableProperty] private bool _isReadingCard`，写入点仍仅 `ReadCardAsync`，**未**追加 `NotifyCanExecuteChangedFor` 以保持 `ReadCardCommand` CanExecute 时机与原来一致；副作用：生成属性 setter 由 private 变 public，属该工具包迁移的标准结果，无外部写入点）。**② 核查无需改动**：`PatientMasterDetailViewModel`（6 个命令全 `[RelayCommand]` 源生成、属性全部来自基类/子 VM 代理、零手写命令实例化）；`ViewModels/Handlers/*` 无 MVVM 手写模式。**③ 有意不迁（登记）**：`Models/PatientDetailModel.cs` + `Models/Items/PatientEditContext.cs` 共 12 处 `SetProperty(...)`——它们是 **Model 非 VM**，且多处带联动副作用（`if (SetProperty(ref _birthDate, value)) { 重算 Age }`），迁 `[ObservableProperty]` 需改写为 `OnXxxChanged` 分部方法，收益低风险高，另立批次评估。**④ 验证与既有缺陷取证**：`dotnet build src/Client/Desktop/Modules/LYBT.Desktop.Patients/ --no-incremental` 0/0；全 sln `--no-incremental` 0/0；`--filter FullyQualifiedName~Patients` **30/31**——唯一失败 `Integration/RemoteApi/DoctorRoleTests.SearchPatients_ByKeyword`（远程患者关键词检索 500）经 **`git stash` 摘除本批次改动后对照运行仍失败**，证明为既有缺陷（#137③a 记录的同一根因：`PatientRepository.GetPagedAsync` 的 keyword 谓词含 `PhoneNumber.Contains(kw)`，加密列使 EF 生成 `LIKE @p ESCAPE N'<密文>'` → SQL「invalid escape character … LIKE predicate」），与 VM 迁移无关（**2026-09-23 已修复——见 #150**）。**⑤ 文档同步**：`viewmodel-layer-design.md` v1.2（§4.1 标注 Patients ✅、§1.3 复测计数、§八.3 批次记录 + 验证证据）；`04-patients.md` **US-PAT-001 状态 ✅→⚠️ 部分实现**（姓名/拼音✅、电话筛选 500，附根因与复现）；追溯矩阵 US-PAT-001 行同步；`PatientSearchE2ETests` 头注释纠正——**原注释「手机号不参与搜索」与 US-PAT-001 AC「支持按姓名、电话、拼音首字母筛选」矛盾，已按需求改正**并注明缺陷 | 一致性优先：VM 迁移不得顺带改动业务行为（故不追加 CanExecute 通知、不迁 Model 层 SetProperty）；测试红灯必须给出「是否本批次引入」的对照证据，并回到需求文档裁决（本次需求判定「电话搜索应有」→ 不删谓词、改登记缺陷 + 状态列降级） | 技术总监 |
| 140 | 2026-09-14 | ViewModel 层清理批次 4-6（任务书：Catalog → MedicalCase → Shell 服务层与 Controls，一次性跑完）。**批次 4 Catalog（3 属性）**：`HerbEditorViewModel.Herb`、`FormulaEditorViewModel.Formula`、`FormulaHerbItemViewModel.Remark` 手写 `SetProperty` → `[ObservableProperty]`（后者类加 `partial`）；`FormulaValidationItemViewModel` 已合规；两个 MasterDetail VM 仅剩**派生属性**通知（`IsNameEditable`/`DetailTitle`/`IsValidationDetail`）有意保留；验证 模块 build 0/0 + 测试 69/69。**批次 5 MedicalCase（9 命令 + 2 属性）**：`Workspace/MedicalCaseCommandsViewModel` 9 处手写命令实例化 + 9 个 `IRelayCommand` 声明 → 9 个 `[RelayCommand]`（5 个 `CanExecute = nameof(CanX)` 谓词逐字等价、`RefreshCanExecute()` 6 处通知不变、生成命令名与 XAML `Commands.*` 绑定一致）；`ConsultationEditorViewModel.Consultation` → `[ObservableProperty]`；`PrescriptionEditorViewModel.Prescription`（**带副作用 setter**：切换 `Items.CollectionChanged` 订阅 + `HasItems` 通知）→ `[ObservableProperty]` + `partial void OnPrescriptionChanged`（生成器仅在值变化时调用，与 `SetProperty` 返回 true 时机一致，等价迁移）；**`Items/PrescriptionItemViewModel` 有意不迁**——类内 2026-08-21 TODO 记载实验证据：`[ObservableProperty]` 生成成员对 Mapperly 不可见 → `PrescriptionMapper` 的 `[MapperIgnoreSource/Target(nameof(...))]` 报 RMG004/RMG021/RMG066 → 空映射 → 运行时丢数据，需专项解决生成器互操作；验证 模块 build 0/0 + 测试 190/190。**批次 6 Shell 服务层 + Controls**：`Controls/SearchBox.xaml.cs` 的 `ClearCommand`（Prism `DelegateCommand`）→ `[RelayCommand]`（控件经 `DataContext={Binding ElementName=Root}` 绑自身，属性名不变）；**`Shell/Services/MenuManager.cs` 尝试迁移失败并已回退**——`IMenuManager` 将 8 个命令声明为 `ICommand`、2 个导航命令声明为 Prism `DelegateCommand`，源生成属性类型为 `IRelayCommand`/`IAsyncRelayCommand`，接口实现要求类型精确匹配 → 实测 **CS0738**（改接口则违反「不修改接口定义」）；且导航命令依赖 Prism `CommandManager` 重新求值语义 + 显式 `RaiseCanExecuteChanged()`（`MenuManager.cs:153-154`）；`NavigationManager.cs` 的 10 处 `new RelayCommand` 是 `NavigationItem` **数据构造**（含循环内捕获 `definition.HomeViewName`），源生成无参命令无法表达，需改数据模型 + XAML；`Controls/BaseDetailContainer.xaml.cs` 的 `DelegateCommand` 是**按实例包装 DP 注入命令**（未保存确认 + 重入哨兵 + 回写依赖属性），类级源生成命令无法表达——三者均「有意保留」并记录硬证据；验证 Shell build 0/0 + Shell/Controls 测试 35/35。**验证汇总**：架构守卫 98/99（唯一失败仍为既有 `SecurityAuditLogViewModel` DTO 属性违规，见 #137）；全量 Desktop 与 Server 套件结果见 #141 | 「保持现有功能不变」优先于迁就工具包：副作用 setter 用 `OnXxxChanged` 分部方法等价迁移、被 Mapperly/接口契约/依赖属性阻塞处一律保留并留证，而不是硬改成编译通过 | 技术总监 |
| 141 | 2026-09-14 | ViewModel 迁移批次 4-6 全量回归（承接 #140）。**运行**：`tests/LYBT.Tests.Desktop/`（仓库 `.runsettings` 的 `TestSessionTimeout=1200000`（20 分钟）不足以覆盖全量 Desktop 套件——首次运行 873 通过后被 VSTest 中止，改用临时 runsettings 放宽至 90 分钟后完整跑完）、`tests/LYBT.Tests.Server/`、`tests/LYBT.Tests.Architecture/`。**结果**：Desktop **912/914**、Server **828/828**、Architecture **98/99**。**两处失败均为既有缺陷、与本批次无关**：① `Integration/RemoteApi/DoctorRoleTests.SearchPatients_ByKeyword`（关键词「张」）② `Integration/RemoteApi/ReceptionistRoleTests.GetPatientByIdNumber_FindsPatient`——后者名字虽为「按身份证号查询」，实际调用 `PatientApi.GetPatientsAsync(1, 10, keyword: idNumber)`，与①同一路径（患者关键词检索）。两者根因同 #137③a：`PatientRepository.GetPagedAsync` keyword 谓词含 `PhoneNumber.Contains(kw)`，加密列使 EF 生成 `LIKE @p ESCAPE N'<密文>'` → SQL「invalid escape character … LIKE predicate」500。**既有时序证据**：`PatientRepository.cs`（2026-08-22 `01a5b1625`）/`ReceptionistRoleTests.cs`（2026-08-20 `7717148b5`）自 8 月后未改动，而本批次提交全部落在 Desktop VM/控件与文档；架构守卫唯一失败为既有 `SecurityAuditLogViewModel` DTO 属性违规（#137 已登记）。**副产物登记**：仓库 `.runsettings` 的 20 分钟会话上限低于全量 Desktop 套件实际耗时（约 24 分钟），全量回归需临时放宽——若希望 CI/本地一条命令跑完，建议把该值上调（属配置治理，未擅自改） | 全量回归必须给出真实分母与失败归类：2/914 与 1/99 均为既有缺陷且已留证，避免把既有红当成迁移引入 | 技术总监 |
| 142 | 2026-09-14 | ViewModel 重构后验证审计 + 11 组问题修复（任务书：全部修复导航/事件/命令审计发现的 11 个问题）。**审计**：9 个只读切片（导航/弹窗/事件/命令/XAML 绑定×3/转换器/迁移等价性）覆盖 82 XAML、180 处 `[RelayCommand]`、9 对话框注册、14 事件类型、23 ViewNames；报告归档 `docs/compose/reports/vm-layer-postrefactor-audit-2026-09-14.md`。**迁移专项**：10 文件/28 成员逐条等价（0 失效），仅 2 处无语义影响差异（`IsReadingCard` setter 可见性放宽、`PrescriptionEditor` 通知顺序）。**修复（P0）**：① 手动读卡按钮绑 `CardReader.ReadCardCommand`→VM 只生成 `ManualReadCardCommand`（按钮无反应）→ 改绑定；② 部署上传 `UploadCommand` 三态依赖零通知（上传不可达）→ 补 `[NotifyCanExecuteChangedFor]`；③ 验方导入回传 `FormulaListDto` 而消费端按 `FormulaDetailDto` 读（导入失败）→ 回传 `SelectedFormulaDetail` + `CanConfirm` 前置；④ 配置节编辑绑 `Dictionary` 迭代项（只读 `KeyValuePair.Value` → 保存不生效）→ 新增 `ServerConfigEntry` 可编辑条目 + XAML 改绑 + 保存改从 `Entries` 组装；⑤ 确认框传参键大小写不匹配（`message`/`title` vs `Message`/`Title` → 全部确认框显示默认文案）+ 5 处实参顺序颠倒 → 键改 PascalCase + 顺序修正（同步修正 `FormulaMasterDetailViewModelTests` 断言）。**修复（P1，命令通知 29/29 全覆盖）**：⑥ `MasterDetailViewModelBase` 12 个 XAML 绑定代理命令的通知此前只发往 `MasterDetailCommandGroup` 内部同名实例 → 新增 `NotifyOwnCrudCommands`/`NotifyOwnPaginationCommands` 在 4 个既有通知点同步转发 + 子类扩展点 `OnCrudCommandStateChanged()`（SearchText 变化亦回调）；⑦ 15 个模块自定义命令补通知（Herb 2 / Formula 4 / User 3+筛选 / Patient 3+子 VM 订阅 / CardReader 1 / HerbList 1 / 部署 2 / 配置节 2）。**修复（P2）**：⑧ `HerbItems` TwoWay→只读源（2 处宿主）改 OneWay；患者卡改 `Patient=` DP 绑定并新增 `SelectedPatientDisplayModel` 投影（原只设 DataContext → 卡片恒空）、删除无数据源的「就诊次数」行与 `ShowVisitCount` DP；删除 `Formula.Source` 无效行；⑨ 改密成功补 `PasswordChangedEvent` 发布（Issue #1906 改密后强制重新登录，新增回归测试 2/2）；⑩ 清死代码：`PatientEvents`/`CaseEvents`/`PendingLogoutsClearedEvent`（+载荷）/`EventSubscriptionManager.Publish<>` 泛型帮助方法（零调用）；附：宿主订阅 `INavigationManager.PropertyChanged` 修复面包屑停留。**修复（P3，5 文件文档漂移）**：`Auth/AGENTS.md`、`Auth/README.md`、`MedicalCase/README.md`、`docs/03-architecture/02-desktop.md`（事件清单按代码重写并记录删除项）、`Core/…/Controls/README.md`（转换器清单按 `Cvt` 13 成员重写）。**验证**：`dotnet build LYBTZYZS.sln --no-incremental` 每阶段 0 错误 0 警告；P0 91/91、P1 136/136、P2 183/183、新增 I-11 回归 2/2；**全量** Desktop 914/916（2 个失败＝既有患者关键词检索 500，见 #137/#141）、Server 828/828、Architecture 98/99（既有 `SecurityAuditLogViewModel` DTO 属性违规）。**有意保留（登记于审计报告 §四）**：7 个「只发布无订阅」事件与 `LogoutService` 死服务（测试断言中，删除需产品决策）、Session 层 .NET 事件、`HerbListControl` 回写投影丢字段、`NullToVis` 命名陷阱、`SearchBox` 清除按钮可见性与 `AlternationIndex`（2 处 1 行修复待确认）、导航 `Action`/`DefaultRoleFilter` 未接线 | 审计必须区分「迁移引入」与「既有缺陷」：本次 37 个无效/28 待修复项**无一是迁移引入**，修复按 P0→P3 分批且每批独立 build+测试；对需产品决策/接口变更的项一律保留并留证据，不做「顺手删」 | 技术总监 |

| 143 | 2026-09-16 | 前端深度架构审查（HTTP → ViewModel 六层；报告已清理，git 历史可查） | **已执行（P0×1 + P1×9 批次）**：**① P0 本地传输不可用**——`HttpClientApiClient` 的本地 `IHttpClientFactory` 既无 `AuthorizationMessageHandler`（本地端点受 `[Authorize]` 保护 → 恒 401）又返回**同一个** `HttpClient`，而 `HttpApiClientBase.SendAsync` 用 `using var client` 释放它 → 第二次调用 `ObjectDisposedException`（两条均以真实 LocalWebAPI 集成测试实测复现）。新增 `LocalApiHttpClientFactory`（共享 handler 链 + 每次 `CreateClient` 返回新包装 `disposeHandler:false` + 配置超时），删 `LocalWebApiHttpClientFactory`。**② L1 双端一致性**——Local `FormulasController` 的 Delete/ToggleStatus/BatchDelete/Restore 改为 `override`（原以新方法名 + 绝对路由声明，batch-delete 模板与基类完全相同 → `AmbiguousMatchException`；另三条被基类 `{id:guid}` 抢占 → `NotSupportedException`，四条路径恒 500）；Local 注册 `ApiCalls` 限流策略（共享 `BaseUsersController` 的 batch-enable/disable 标注该策略名，缺失即 500）；Local 补 `FallbackPolicy`（原 fail-open）；Local 认证端点失败码 `422`→`401`（双模式语义对齐）；Local `Login` 限流改按 IP 分区 + 结构化 429（原全局窗口 + 空体 429）；Local MedicalCases 创建返回 201、`query` 补 `ValidatePagination`；`BaseCrudController` 的 ToggleStatus/Restore/BatchDelete 默认实现由 `NotSupportedException`（500）改 404；Remote 补 `GET /medicalcases/pending`（契约已声明、本地已实现、远程 404）；Remote `cancel` 标注 204→200；Remote `/auth/refresh` 补 `Login` 限流、`/auth/validate` 补 `[AllowAnonymous]`（原被类级 `[Authorize]` 拦截，自解析头的 401 分支不可达）；Remote 挂号 `start-visit`/`cancel` 去掉与基类重复的路由属性。**③ L2**——`TokenRefreshHandler` 刷新/降级自动登录改打**当前连接 URL**（原固定 appsettings `BaseUrl`：用户改 `RemoteUrl` 后刷新请求打到错误主机，跨服务器提交 RefreshToken）。**④ L3**——`EntityApiClientRepositoryBase` 的 `GetPaged/GetById` 区分「服务端判定失败」（`Success=false` → 抛 `InvalidOperationException`）与「无数据」（`Data=null` → 空分页/null），修正原「失败静默折叠为空」导致的 `CrudServiceBase` 把权限/业务拒绝翻译成「不存在」。**⑤ L4（P10）**——`CancelRegistrationCommandHandler` 移除直注 `AppDbContext`：实体层 `Registration.Cancel()` 已更严格地覆盖「已关联医案不可取消」，`RegistrationStateGuard.EnsureCanCancel` 收敛为仅校验状态。**⑥ L5**——`RegistrationListViewModel` 轮询 CTS「Cancel→等循环退出→Dispose」+ 新旧循环交接守卫；`PatientSelectionViewModel` 对称释放 `PendingQueue`；`MedicalCaseWorkspaceViewModel` 订阅先 `-=` 再 `+=`、`KeepAlive` 与 `IsNavigationTarget` 取一致组合、去重复通知、释放 `Commands`；`LoginViewModel` 匿名 lambda 订阅改具名方法可退订；`ConnectionStatusViewModel` 补 `[NotifyPropertyChangedFor]`。**⑦ L6**——补 `DarkOpacityBrush`（Surfaces.xaml）与 `HoverBackgroundBrush`/`SelectedBackgroundBrush`（改引用 MDIX 既有语义键）；`HerbListControl` 将 `ScrollViewer` 移入 `ItemsControl.Template` 使 `VirtualizingStackPanel` 真正生效（保留容器类型与全部交互契约）；删 `AccountSettingsControl`/`LoginView` 的 `PasswordChanged` code-behind 同步（`BoundPassword` 行为已是双向唯一通道）。**验证**：`dotnet build --no-incremental` 0/0；Architecture **99/99**（含修复存量 `DP_M1`——`SecurityAuditLogViewModel.SelectedLog` 为 `DataGrid.SelectedItem` 只读选中项持有者，其 `Logs` 集合已在守卫只读白名单内，并入既有 `SelectedPatient`/`SelectedHerb` 先例，**需架构负责人确认该分类**）；Desktop 定向套件 25/25 + UserManagement 9/9（含 3 条新增回归）；新增 `LocalModeTransportTests`（3）、`FormulaCrudE2ETests.FormulaWriteEndpoints_AreNotShadowedByBaseCrudRoutes`、`UserManagementE2ETests.BatchDisableAndEnable_RoundTripsUserStatus`。**未执行项已登记**：契约层 `CancellationToken` 全链、`AddHttpClient`/弹性（属技术引入 T3，须先过治理）、领域错误层统一、缓存层定案、L6 主题 Token 化/样式提取/i18n、跨聚合事务 | 双端 API 树与安全语义按「默认拒绝 + 同一传输契约 + 同一错误码」收敛；审计发现 60 项按 P0/P1/P2/P3 分级，高优先级已修并全部有可复现证据，未执行项在报告 §四 明列而非静默跳过 |
| 144 | 2026-09-16 | 架构优化 6 项（承接 #143 审查 §四 未执行项；报告与设计稿已清理（git 历史可查），ADR-0028/0029/0030） | **已执行（6 项中 5 项完整落地，1 项部分落地且有据未做）**：**① CancellationToken 全链**——契约层 226 个 `Task` 方法 + 适配器 202 + 仓储基类 7 处「声明却丢弃」+ 模块仓储 69 处透传（统一末位 `ct = default`）。实施中发现并修复 **12 处 `ct` 静默落入 `object? body` 形参**（`Post/PutAndWrapAsync(url, ct)` → 序列化 `CancellationToken` 抛 `NotSupportedException`），改具名实参并全仓脚本复扫剩余 0；DIM 转发不可有可选参数（CS1066）→ 实现侧去默认值。**② 领域错误层统一**——`AppException.GetHttpStatusCode()` 增加 Category 兜底映射（此前无类型化错误码一律 500，致 3 处 `NotFoundException(msg)` 误报 500）+ 新增 `ForbiddenException`(403)；客户端新增 `ApiClientException`（`ErrorCode`+`ServerMessage`，**继承 `HttpRequestException` 以兼容既有 catch 站点**）+ `ApiErrorEnvelope` 共用解析，本地/远程抛同一类型；Mapper 删除 Refit 反射分支并修 `TaskCanceledException` 排在 `TimeoutException` 前导致「超时显示为操作被取消」。**③ 跨聚合事务**——实测 `StartVisitCommandHandler` 的「原子事务」声明**不成立**（事务在 `RegistrationDbContext`，医案写走 `MedicalCaseDbContext`，跨连接）；按 ADR-0030 改为「同上下文显式事务 + 跨上下文幂等/补偿」：幂等键落在挂号侧（`InProgress && MedicalCaseId.HasValue` 短路，并明确禁止按 patientId 判重），回滚后新增 `CompensateInMemory` 防 ChangeTracker 残留复活已回滚写；`RecordPrintAsync` 同库多聚合补显式事务。**④ 缓存层定案**——初稿把生产者放 `HttpApiClientBase`，落地改为传输层 `CachingHttpMessageHandler`（读写一体，覆盖全部出站请求）：GET 2xx 缓存（键含用户域后缀、显式 `Size=1`、目录 5min/事务 30s），非 GET 2xx 按域前缀失效（含跨域依赖表）；新增 `DesktopCacheKeyRegistry` 去反射前缀失效；补 `Registrations`/`Reports`/`All` 失效域。**⑤ AddHttpClient + 池化/弹性（T3 技术引入，ADR-0028）**——桌面主容器为 Prism.DryIoc 无法直接 `AddHttpClient`，改由 `DesktopHttpTransportFactory` 用私有 `ServiceCollection` 构建传输层 `IHttpClientFactory`（具名 `RemoteApi`/`LocalApi`/`RefreshToken`）后注入 DryIoc；`SocketsHttpHandler` + `PooledConnectionLifetime`/`SetHandlerLifetime` 2min；Polly 3 次指数退避**仅幂等方法**（POST 不重试）；删除「`IsRegistered<IHttpClientFactory>()` 恒 false」死分支与失真注释；删被取代的 `LocalApiHttpClientFactory`。**⑥ L6 主题/样式/i18n（部分）**——✅ 死资源清理（Spacing.xaml/5 图标几何/Converters.xaml）+ 样式提取（ValidationStyles.xaml、TcmTags.xaml，9 处本地重复删除）+ 30 处页面底色字面量 `#FBF7F3` 收敛到 `{DynamicResource SurfaceLevel0Brush}`（`ThemeService` 会切换该 Color，此前字面量使 26 个页面深色模式失效；亮色 ΔRGB 1/1/2）+ `AutomationProperties` 1→55 处；⚠️ **设计文档给的 3 组 token 映射经实测全部不等值**（`#FBF7F3`≠`SurfaceLevel0Brush`、`MaterialDesign.Brush.Outline` 在 MDIX 5.3.2 不存在、`Foreground="Black"` 已不存在）→ 按「不合理不替换」保留并登记；i18n（`StringResources` 接入）因「缺 key 静默空白 + 无法视觉验收」列为独立批次。**验证**：`dotnet build --no-incremental` 0/0；Architecture **99/99**；Server **828/828**（期间修复由 #143 变更引起的 Swagger 安全声明守卫——`/auth/validate` 按守卫要求显式登记匿名端点）；Desktop **918/920**（仅剩 `13c` #141 记录的 2 项既有 RemoteApi 失败，与本批次无关）。**未执行项已登记**：i18n、打印模板样式合并、`White` 背景逐点判定、模块仓储 `catch(Exception)` 吞 `OperationCanceledException`、Server 侧 `RemoveByPrefix` 反射实现、跨聚合失败注入测试 | 6 项从「可选项」变为「有设计、有 ADR、有实现、有验证证据」；两处关键技术约束（DryIoc 非 MS DI、跨 DbContext 无单事务）在设计中如实记录并改变了实现形态 |
| 145 | 2026-09-16 | B-08/B-09 Desktop 发布包 + 自动更新（Velopack；发布流程 `docs/06-operations/12-desktop-release.md`） | **已执行**：**① 复核推翻「已实现」结论**——`scripts/velopack-pack.ps1`/`sync-to-server.ps1` 已在 `eb8612658` 被删除，且原脚本用 `vpk pack --releaseDir`（**非有效参数**，实为 `--outputDir`）、从未产出过安装包。**② 重建打包脚本**——`--outputDir`/`--packId`/`--packVersion` 等正确参数；**刻意 `PublishSingleFile=false`**（单文件会把应用压成一个大文件 → 按文件差分的增量包退化为全量）；`-p:Version` 与程序集同源（安装包版本==应用内版本）；产出 Setup.exe + `releases.<channel>.json` + full/delta nupkg + SHA256SUMS + 末尾**馈源自检**（清单必须含本次版本，否则报错退出）。**③ 修复发布阻塞缺陷 `NETSDK1152`**——Shell 与 LocalWebAPI 各持一份 `appsettings.json`，发布输出同相对路径冲突（此前的 publish 命令就因此不可用）；根因是内嵌宿主与独立调试宿主共用文件名，处置：LocalWebAPI 的独立宿主配置改名 `appsettings.localwebapi.json`（独立宿主显式加载；内嵌场景仍读 Shell 的 appsettings.json），并 `CopyToPublishDirectory=Never`。**④ 用户数据迁出安装目录（满足「更新不丢用户数据」）**——连接设置与数据库备份原在 `%LOCALAPPDATA%\LYBTZYZS\`，**与 Velopack 每用户安装根重合**；迁至 `%LOCALAPPDATA%\LYBT\Desktop\`（既有约定目录，收敛为 `SystemConstants.UserDataDirectory`）并带**一次性幂等迁移**；业务库在 LocalDB 实例目录，本就不受影响。**⑤ 生命周期接入**——`VelopackApp.Build().Run()`（SetArgs/SetAutoApplyOnStartup(false)/Serilog 日志适配/首次运行/更新后/卸载前钩子）置于 `App.OnStartup` 中 **单实例互斥量之前**（安装/卸载期安装器会拉起第二个进程，先抢互斥量会导致安装器超时）。**⑥ 更新源**——Velopack 无内置 Gitee 源，新增 `GiteeReleaseSource`（仅覆写 API 基地址 `{host}/api/v5/`，复用 GiteaSource 的解析；**实测 Velopack git 源要求 Release 附带 `releases.<channel>.json` 清单资产**，否则报 `Could not find asset called 'releases.win.json'`）；`IUpdateSourceFactory` 按 `SourceKind`（Server/Gitee）选源；`DesktopUpdateService` 缓存待应用更新（原「检查→下载→应用」触发三次馈源往返）、`No VelopackLocator has been set` 等管理器初始化失败降级为不可用且不影响启动。**验证**：`dotnet build --no-incremental` 0/0；连续打包 v1.0.0→v1.0.3 成功，**增量包 1.5–1.6 MB vs 全量 118 MB**（约 1/78）；产物馈源经 `SimpleFileSource` 验证可被 Velopack 解析（含 Delta 资产）；新增单测 15（Gitee 源解析 6 + 更新服务/源选择 9）全过；受影响套件 65/65（Unit.Foundation / Unit.Shell / ShellFlow / LocalApi）。**未做**：安装包实机安装与端到端升级（需目标机执行 Setup.exe 与真实更新源）；Gitee Release 实测上传（仓库为私有且 Gitee API 对私有仓库要求 access_token，公开网络不可探测）；CI（Gitee Go 凭据链未验证，文档列为可选） | 从「声明已实现但实际脚本缺失」纠正为可复现的打包与更新链路；发布体积的杠杆确定为增量包（WPF 不支持 PublishTrimmed，运行时程序集无法安全裁剪） |
| 146 | 2026-09-22 | B-06 数据备份/恢复交付（共享 SQL Server 备份引擎 + 双端 BackupController；US-SHELL-013 / NFR-AVAIL-001） | **已执行**：**① 共享引擎**——新增 `LYBT.Infrastructure/Services/Backup/`（`IBackupService`/`SqlServerBackupService`/`BackupFileEncryption`/`BackupManifestStore`/`BackupJobTracker`/`BackupSchedulerService`/`BackupPaths` + `AddBackupServices(IConfiguration, string defaultBackupDirectory)`），远程 WebAPI（本机 SQL Server）与嵌入式 LocalWebAPI（LocalDB）共享同一引擎，取代旧 Desktop 专用 `ILocalDbBackupService`（已迁移移除）；连接串经 `ConnectionStringResolver.GetEffectiveConnectionString`，库名取 `SqlConnectionStringBuilder.InitialCatalog`。**② 双端 API**——`BaseBackupController`（类级 `[Authorize]` + 逐方法 `SysAdminOnly`；`auto` 仅需认证）派生双端 `BackupController`，同路由 8 端点：`GET /api/v1/backup`、`GET /api/v1/backup/status`、`GET /api/v1/backup/tables`、`POST /api/v1/backup`、`POST /api/v1/backup/{id}/restore`、`DELETE /api/v1/backup/{id}`、`POST /api/v1/backup/cleanup`、`POST /api/v1/backup/auto`；响应统一 `ApiResponse<T>`，业务失败走 `BusinessFail`（422）。**③ 能力**——全量/差异备份（`WITH DIFFERENTIAL` 无基准报错）、恢复前保护性备份（`Kind=PreRestore`，失败不阻断但返回 Warning）、可选文件级 AES-256-CBC + HMAC-SHA256（encrypt-then-MAC，PBKDF2-SHA256 210000 迭代，落盘 `*.bak.enc`）、整库恢复（`WITH REPLACE`，差异先基准 NORECOVERY 再 RECOVERY，单用户切换 + `ClearAllPools`）、选择性恢复（临时库 `<db>_LYBT_SELRESTORE` + `WITH MOVE`，按表/记录回写，回写期禁用全部外键并以 `WITH NOCHECK` 重启——不校验，提示 `DBCC CHECKCONSTRAINTS`）、删除（拒绝删除被差异引用的全量）、保留期清理（保护最新全量与差异链）、进度（`sys.dm_exec_requests.percent_complete` → `BackupJobTracker` → `/status`）。**④ 自动备份**——登录成功后 fire-and-forget `POST /api/v1/backup/auto`；`BackupSchedulerService` 宿主调度仅在 `Backup:AutoBackup:Enabled=true` 时启动；两者共用 `IntervalHours=24` 间隔判定（未满为空操作）。**⑤ 缺陷修复**——旧实现每次列举备份派生随机 Guid → 「选中 → 恢复/删除」必失配（恢复必失败）；改由 `{文件名}.manifest.json` 侧车清单提供稳定 Id，无清单的历史 `.bak` 由文件名确定性派生。**⑥ 配置/目录**——新增 `BackupOptions`（`SectionName="Backup"`）+ `AppDataPaths`（`DesktopDataDirectory` 唯一权威，`SystemConstants.UserDataDirectory` 改为派生）；本地默认备份目录 `%LOCALAPPDATA%\LYBT\Desktop\Backup`（安装目录之外，规避 Velopack 更新/卸载清理），服务端默认 `{应用基目录}/backup`。**⑦ 客户端守卫**——`NavigationCoordinator.ViewRoleAccess[ViewNames.BackupManagement]` 收紧为仅 `SuperAdmin`（原 Admin+SuperAdmin），与服务端 `SysAdminOnly` 一致；`BackupManagementView` 为 DP_M1 合规的展示模型（`BackupFileModel`/`RestorableTableModel`）承载格式化。**文档**：11a-shell US-SHELL-013、12-nfr NFR-AVAIL-001/002（「每次登录」→「登录触发 + 24 小时间隔判定」）、追溯矩阵 US-SHELL-013 行、13-project-master-plan B-06 + §九、03-server 备份服务表、13b 端点、05-dual-mode 覆盖率、06-operations/06-backup-recovery、04-api-reference/15-backup（新）、07-ui-ux 两份、08-uat X.1 | 双轨真共享（同一引擎双宿主，ADR-0010/0023）替代「本地专用服务 + 远程靠 SQL Server Agent」；需求内部矛盾（逐次登录 vs 7 天 7 个文件）按交付语义修正；备份 Id 跨列举稳定是恢复链路可用性的前提；**另修复 6 处 HEAD 存量编译错误**（`MedicalCaseNav`/`CommandResult` 的枚举/类型被同名常量与属性遮蔽、`IResult.ExecuteAsync` 双参重载在 net8 不存在 ×2、两个对话框 VM 把静态类 `DialogParams` 当实例用、测试 `PrescriptionPrintHandler` 形参错位、P02/P09c 基类白名单）——HEAD 原处于**不可编译**状态（前端导航/对话框收敛批次提交未过构建门禁），B-06 的「build 0 错误 0 警告」验收不可达；✅ a7a4d4d24 |
| 147 | 2026-09-23 | B-07 首次初始化向导交付（5 步向导；US-SHELL-011） | **已执行**：**① 向导 View/VM**——新增 `Modules/LYBT.Desktop.Auth/Views/InitializationWizardView.xaml` + `ViewModels/InitializationWizardViewModel.cs`（派生既有 `ConnectionTestViewModelBase` ← `DialogViewModelBase : NavigableViewModelBase, IDialogAware`）+ `Models/InitializationWizardStep.cs`（5 步枚举：Welcome/Connection/Clinic/Administrator/Finish）+ `Models/WizardChecklistItemModel.cs`（第 5 步校验清单项）；**同一 View/VM 双入口**——`RegisterForNavigation`（可导航）+ `RegisterDialog`（模态），一份实现两种呈现（旧单屏 `FirstRunSetupView`/`FirstRunSetupViewModel` 已删除，避免两条并行首次运行入口）。**② 5 步流程**——① 欢迎 + 模式选择（本地全栈 / 远程服务器）② 模式相关配置（本地：数据库连接 LocalDB/SQL Server + 「测试连接」；远程：服务器地址 + 「测试连接」）③ 诊所信息（名称/科室/地址/电话）④ 初始管理员账号创建（用户名/姓名/密码/确认密码，「创建管理员」；已存在同名账号时可跳过）⑤ 配置校验清单 + 完成（写入首次运行标记）；上一步/下一步/完成按钮，第 2 步远程分支须连接测试成功、第 4 步须创建成功方可继续。**③ 模式应用时机**——模式只在第 2 步「下一步」应用（**先测试后切换**：远程先 `CheckRemoteAvailableAsync` 通过），因此第 4 步创建的 admin 作用于所选后端。**④ 触发**——`ShellEventCoordinator.OnLoginSucceeded`（登录用户为 sysadmin 且 `IFirstRunStateService.IsFirstRun`）弹出（管理员创建需 sysadmin 会话，故不再在登录前弹出）+ `SysadminHomeView` 手动入口可随时重跑；完成写 `%LOCALAPPDATA%\LYBT\Desktop\first_run_done.flag`（沿用既有路径，既有安装的已完成状态延续），**未完成不写标记且不阻塞进入主界面**（下次登录再次提示）。**⑤ 新增核心服务**——`LYBT.Desktop.Contracts/Services/ILocalDatabaseSettingsService.cs`（`%LOCALAPPDATA%\LYBT\Desktop\local-database.json`；默认 `(localdb)\MSSQLLocalDB` + 库 `LYBTDesktop`；SQL Server 分支支持 Windows 认证或 User ID/Password + `Encrypt=True`；口令 DPAPI（CurrentUser）加密 + Base64，JSON 无明文）、`IFirstRunStateService.cs`（标记读写，检测方与写入方共用）、Auth 模块 `Services/IInitialAdminService.cs`（DP10 门面，封装 `IApiClientIdentity.CreateUserAsync` 建 `Role=UserRole.Admin`）；嵌入式 `Shell/Services/EmbeddedLocalWebApiService.cs` 改为消费 `ILocalDatabaseSettingsService.Current` 构造连接串。**⑥ 顺带修复**——a) `ClinicSettingsService.SaveSettingsAsync` 原写入**进程当前目录**（非应用目录，配置可能丢失）→ 改走 `IClientConfigurationStore.SaveSectionAsync`（原子写 + `.bak` + `IConfiguration.Reload`），`GetSettings()` 原读启动快照 → 改读**实时** `IConfiguration`（兑现实现类注释声称的热更新）；b) `LoginView.xaml` 4 处硬编码「中医诊所管理系统」（副标题/版权/登录提示/版本行）改为绑定 VM 属性，产品名由 `ClinicSettings.ClinicName` 驱动（默认诊所名 → 「凌隐宝堂中医诊所管理系统」）。**文档同步**：11a-shell US-SHELL-011（含 v1.4 变更记录）、追溯矩阵 v1.16（US-SHELL-011 行 + Shell/合计统计）、13-project-master-plan B-07（B 类表/Phase 3/§八/§九）、本文件 §4.1、07-ui-ux 四份（`FirstRunSetupView`→`InitializationWizardView`）、08-uat Narrative 0 新增向导用例 | ✅ 1d91a9ef0 |
| 148 | 2026-09-23 | 存量测试失败清零（Desktop 49 + Server 5，两组均经 HEAD 基线复现为存量） | **已执行**：**① 根因一：测试宿主缺 `Security__AesKey`（占 Desktop 48/49）**——患者电话/身份证经 `AesGcmValueConverter` 透明加解密，密钥解析**仅读环境变量**且非 Development 环境缺失即 fail-fast（R-5：禁止生产静默用测试密钥）；测试宿主环境名为 `Test` 却从未注入密钥 → 任何触碰加密列的读写以「敏感数据解密失败」（ERR-00013 → HTTP 422）失败。**实测取证**：`POST /api/v1/patients` 返回 ProblemDetails `{"title":"敏感数据错误","status":422,"detail":"敏感数据解密失败，请联系管理员","errorCode":"ERR-00013"}`。**修复=测试基建（非产品）**：两测试项目新增 `TestEnvironmentSetup.cs`，`[ModuleInitializer]` 注入 `Security__AesKey`（与生产部署同机制；外部已注入则不覆盖）→ Desktop 失败 49→1、`PatientEncryptionTests` 转绿、LocalApi 族 24/24。**② 根因二：`INavigationCoordinator.NavigateTo<TParams>` 泛型重载吞掉字典载荷（真实产品缺陷，占 Desktop 1/49）**——C# 重载解析对 `Dictionary<string,object>` 实参优先选泛型重载（恒等转换优于接口转换），其「按属性反射展开」实现把 Dictionary 自身 `Comparer/Count/Keys/Values` 当作导航参数 → 目标 VM 取不到 `*Nav` 契约键；影响全部带参导航 10 处（接诊→医案工作台、患者选择、读卡、待诊队列、临床工作台、医案审计日志、账户设置、用户管理默认角色筛选、MenuManager 患者新增等）。**修复**：删除该重载（全仓零显式调用，仅被字典实参误绑）+ 接口/实现 XML 注释记录「禁止再引入」+ `05-development/standards/SHELL-BEST-PRACTICES.md` §1.2 改为 `*Nav` 字典契约口径（原文档正倡导泛型重载，是缺陷来源）+ `07-ui-ux/desktop-ui-detailed-design.md` §P2-57 同步。**③ Server 5 项逐条定位与修复**：a) `PatientEncryptionTests.AesGcm_RoundTrip`（同①）；b) `CatalogControllerRoutesTests.FormulaActionRoutes_UseAbsolutePath_NoDoubleVersion`——N-03 路由声明统一后动作路由改相对路径 → 断言改测**真不变量**「version 占位符全链仅一次」并覆盖 Herbs/Formulas 双控制器；c) `MedicalCaseHistoryQueryTests.GetPatientRecentMedicalCasesAsync_...`——审计自动化在 Added 状态**强制覆盖** `CreatedAt`（`DbContextAuditExtensions.SetAuditFields`）致三条记录时间戳相同、排序退化 → 测试改为保存后回填历史时间线（Modified 仅强制 UpdatedAt）；d) `HerbRepositoryTests.GetPagedAsync_WithKeywordMatchingMultiple_...`——P2-7 前缀匹配走索引 + US-HERB-001「按名称、拼音首字母筛选」→ 断言改前缀语义并**锁定「后缀不匹配」为有意设计**（非缺陷）；e) `PatientSqlIntegrationTests.SoftDelete_And_Restore_ShouldToggleQueryVisibility`——审计以**环境上下文操作者**为准强制写 `UpdatedBy`（防伪冒），无 HttpContext 时归 System 用户（`GetCurrentUserId` P1-3 兜底）→ 测试注入带 `NameIdentifier` 声明的 `IHttpContextAccessor`，使断言真正验证「当前操作者留痕」。**验证**：`dotnet build --no-incremental` 0 错误 0 警告；**Server 847/847**；**Desktop 982 通过 / 0 失败 / 24 Skip**（25m42s；24 项为 `Integration/RemoteApi/**`，均带 `Skip="PRE-EXISTING: Remote E2E requires live remote WebAPI (localhost:5000…)"`）；**架构 107/107**；`tests/AGENTS.md` 同步（测试宿主密钥前置 + 本地 E2E 自举说明 + 计数） | 54 项失败**无一项以放宽断言收场**：48+1 项分别归因于「测试基建缺前置密钥」与「真实产品缺陷（导航参数被静默改写，影响全部带参导航）」；Server 5 项按「环境依赖 / 断言过期 / 真实缺陷」分类后分别修测试基建、改测真不变量、注入真实上下文；RemoteApi 24 项为环境依赖，按任务口径保留显式 Skip + 前置条件文档化。**遗留（已登记未修）**：`PatientRepository` 关键词检索对加密列 `PhoneNumber.Contains` 的 EF 翻译生成 `LIKE @p ESCAPE N'<密文>'`，部分关键词触发 SQL「invalid escape character」500（ModeSwitchE2ETests 头注取证）——需产品侧决策（等值检索改 HMAC 列/去加密列 LIKE），非测试失败项。→ **已闭环（2026-09-23 收尾批次 2，见 #150）**：按 R-6 盲索引方案落地——电话分支改 `PhoneSearchHash`（HMAC-SHA256）等值（完整号码精确匹配），并连带修复 `ExistsByPhoneAsync` 因随机 nonce 恒不相等导致的查重失效（US-PAT-003/004）。 |
| 149 | 2026-09-23 | 收尾批次：会话超时预警 + 配置导入导出 + 设计稿品牌统一 + `IFileDialogService` 扩展 | **已执行**：**① 会话超时预警（US-AUTH-014）**——新增 `Shell/Dialogs/Views/SessionTimeoutWarningDialog.xaml` + `Dialogs/ViewModels/SessionTimeoutWarningDialogViewModel.cs`（`DialogViewModelBase`）+ `Core/LYBT.Desktop.Contracts/Services/ISessionTimeoutMonitor.cs` + `Shell/Services/Session/SessionTimeoutMonitor.cs`：1s `PeriodicTimer` 轮询 `IUserActivityTracker.TimeUntilInactive`，剩余 >0 且 ≤ `ClientSession:WarningBeforeTimeoutMinutes`（默认 2 分钟，0=关闭）且无对话框打开时经 Prism `ShowDialog("SessionTimeoutWarningDialog")`（注册于 `Shell/App.xaml.cs`，与既有 3 个对话框并列）在 UI 线程弹出；对话框从同一来源显示 mm:ss 倒计时，「续期」= `ResetActivity()` + 尽力 `TryRefreshTokenAsync()` → close OK，「退出」→ close Cancel，打开期间 `SessionExpired` → close Abort（对话框绝不残留于登录页之上）；每个不活动窗口最多弹一次。`ShellEventCoordinator` 在登录成功时启动监控器 + `IApplicationTickService`（此前从未在任何地方启动——追踪器的不活动到期依赖其 tick），登出/会话过期时停止；监控器 `LogoutRequested` 复用既有会话过期路径。测试：对话框 VM 9 + 监控器 8。**② 配置导出/导入（US-SHELL-016）**——新增 `Roles/LYBT.Desktop.Admin/Sysadmin/Views/ConfigExportImportView.xaml` + `Sysadmin/ViewModels/ConfigExportImportViewModel.cs`（`RegisterForNavigation`、`ViewNames.ConfigExportImport`、`NavigationCoordinator.ViewRoleAccess` = SuperAdmin、`SysadminHomeView` 第 7 个功能卡「配置导入导出」）+ `Roles/LYBT.Desktop.Admin/Services/IConfigurationPackageService.cs` + `Sysadmin/Services/ConfigurationPackageService.cs`：导出写**单个缩进 camelCase JSON 包**（`packageVersion` "1.0" / `exportedAt` / `application` / `clinicSettings` / `connection`（模式 + 远程地址 + 本地数据库，口令写 `***`）/ `rolePermissions`（快照）/ `featureToggles`）；**密钥永不导出**（无 `Jwt:SecretKey`/`DefaultPasswords`/数据库口令/令牌），故 US-SHELL-016 规则 3「保留当前 Jwt:SecretKey」由构造保证；导入先校验（JSON 解析失败或 `packageVersion` 缺失/不受支持 → 中文拒绝 + 零服务调用），再应用诊所信息（`IClinicSettingsService`）/连接（远程 URL/模式 `IConnectionSettingsService`、本地库 profile `ILocalDatabaseSettingsService` 保留当前口令）/功能开关（`IClientConfigurationStore`），返回报告（已应用/已跳过/`RequiresRestart`）并报告角色权限快照差异（权限为编译期 `ViewRoleAccess`/`IRoleDefinition`，导入只校验不应用）。测试：服务/VM 22（含真实临时文件 round-trip + 密钥缺席断言）。**③ 设计稿品牌统一**——`designs/*.pen` 系统名统一为「凌隐宝堂中医诊所管理系统」。**④ `IFileDialogService.ShowOpenFileDialog`**——契约新增 `ShowOpenFileDialog(filter, defaultExt, fileName = null)` + `FileDialogService` 实现；导入 VM 消费之。**已知不一致（登记）**：5 个 VM 仍直接用 `Microsoft.Win32.OpenFileDialog`（`FormulaMasterDetailViewModel`/`HerbMasterDetailViewModel`/`PatientMasterDetailViewModel`/`DeploymentViewModel`/`SystemSettingsViewModel`）——本批次**未**迁移，留待后续统一。→ **已闭环（2026-09-23 收尾批次 2，见 #150）**：5 个 VM 全部改走 `IFileDialogService.ShowOpenFileDialog`（过滤器/行为不变，测试构造点同步）；全仓仅 `FileDialogService` 自身仍引用 Win32 类型（抽象实现，属预期）。 | 收尾批次交付 4 项；文档同步：`02-auth.md` US-AUTH-014、`11a-shell.md` US-SHELL-016、`13-traceability-matrix.md`、本文档、`02-desktop.md`、`desktop-view-inventory.md`、`desktop-ui-*`、`second-level-design-checklist.md`、`13-project-master-plan.md` §九 |
| 150 | 2026-09-23 | 收尾批次 2：患者加密列 LIKE 搜索 500 修复（R-6 电话盲索引）+ 5 个 VM 文件对话框收敛 + 无用 `RootDialog` 脚手架删除 | **已执行**：**① 患者加密列 LIKE 搜索 500（真实产品缺陷，US-PAT-001）**——根因：`PatientRepository.GetPagedAsync` 关键词谓词含 `p.PhoneNumber.Contains(kw)`（AES-GCM 加密列），EF 把参数一并加密后生成 `LIKE @p ESCAPE N'<密文>'`，密文含非法转义字符时 SQL Server 报「invalid escape character … LIKE predicate」→ 患者搜索 500（**按姓名关键词检索同样 500**——同一谓词命中电话分支）；同时 `ExistsByPhoneAsync` 对加密列做等值比较，随机 nonce 下恒不相等 → 电话查重恒 false（US-PAT-003/004 语义失效）。**修复（R-6 盲索引，镜像既有 `IdCardHash` 模式）**：新增 `Patient.PhoneSearchHash`（`SensitiveDataHashHelper.ComputeHmacSha256Hex(trim 后电话)`，与加密同密钥）+ 非唯一筛选索引 `IX_Patients_PhoneSearchHash`；`PatientRepository` 在 Add/Update 写入；关键词检索电话分支改 `PhoneSearchHash == HMAC(kw)`（**完整号码精确匹配**——盲索引不可做前缀/片段；姓名/拼音保持前缀，口径见 `04-patients` 规则 5-7）；`ExistsByPhoneAsync` 改盲索引等值；手写迁移 `20260923120000_AddPatientPhoneSearchHash`（snapshot 同步）；启动回填泛化为两列（`DatabaseInitializationService` / `LocalWebApiSeedData`）。**② 5 个 VM 文件对话框收敛（闭环 #149 登记）**——`FormulaMasterDetailViewModel`/`HerbMasterDetailViewModel`/`PatientMasterDetailViewModel`/`DeploymentViewModel`/`SystemSettingsViewModel` 由 `Microsoft.Win32.OpenFileDialog` 改走 `IFileDialogService.ShowOpenFileDialog`（过滤器/行为不变，测试构造点同步）；全仓仅 `FileDialogService` 自身仍引用 Win32 类型（抽象实现，属预期）。**③ `Shell/Views/MainWindow.xaml` 删无用 MDIX `DialogHost Identifier="RootDialog"` 包裹层**——全仓零 `DialogHost.Show`/`CloseDialogCommand` 消费者（4 个 Admin 视图各自持有自包含 DialogHost），`xmlns:materialDesign` 保留给 Snackbar；会话超时对话框仍为 Prism dialog（不引入第二套对话框机制）。**④ 测试基建修复（连带）**——`tests/LYBT.Tests.Server/_Infrastructure/IntegrationTestBase.cs` 建库前先 `EnsureDeletedAsync()`：共享测试库 `LYBT_Test` 残留旧 schema 致模型新增列缺失、「Invalid column name」掩盖真实回归。**⑤ 测试**——`tests/LYBT.Tests.Server/Integration/SqlCore/PatientSqlIntegrationTests.cs` 新增 4 条真实 SQL 测试（姓名关键词「张」不再抛异常＝原失败复现；完整电话命中 + 片段不命中且不抛；`ExistsByPhoneAsync` 查重 + 自身排除；hash 创建/更新/清空）。**文档同步**：`04-patients`（状态/AC/规则）、`04-data-model`（列/索引/变更记录 v2.5）、`13-traceability-matrix`（US-PAT-001 ⚠️→✅ + US-PAT-003/004 盲索引注 + US-ERR-007 校准）、`13-project-master-plan` §九、本文件（§三 基线/最新迁移 + #137③a/③d、#139、#148、#149 登记项闭环）、`Shell/README.md`（删 `DialogHostService`/`SnackbarService` 幽灵条目 + AppShell 的 RootDialog 描述） | Server **851/851**（847 基线 + 4 条新测试，见 #148）；架构/Desktop 全量未在本批次重跑（Desktop 基线沿用 #148 记录）。**仍滞后（报告未改，非本批次范围）**：`07-ui-ux/desktop-ui-detailed-design.md` §4.20 与 `desktop-ui-requirements.md` 宿主描述、`DESKTOP_ARCHITECTURE_STANDARD.md` §13 仍称 `DialogHost Identifier="RootDialog"` 包裹 AppShell；`07-ui-ux/desktop-ux-loading-states.md` 患者导入导出仍写 `OpenFileDialog`；`02-requirements/11c-error-handling.md` US-ERR-007 仍称电话查重恒不命中 |
| 151 | 2026-09-23 | 测试分层策略 L0/L1/L2 确立 + Desktop 集成测试提速（共享 LocalDB + Respawn；规范落 `tests/AGENTS.md`） | **已执行**：**① 根因**——`LocalWebApiTestBase` 以 `IAsyncLifetime` 挂在测试类上，`InitializeAsync` 按**测试方法**执行 → 每个测试都建库+建表+4 角色种子+启动 Kestrel（≈11s/测试），Desktop 全量 1038 用例需 25m42s（Unit 3s + Integration 6m12s + E2E 19m22s）。**② 共享 LocalDB + Respawn**——每测试进程一库 `LYBTZYZS_LocalApiShared_{pid}`（首次 `EnsureDeleted`+`EnsureCreated`，`ProcessExit` 删除，异常退出由下次同名删除兜底）；每测试类首次运行 Respawn 清库 + **确定性重建**身份/业务种子（不依赖 Respawn 的 `TablesToIgnore` 名单——其按 schema+name 匹配，仅给表名会漏配导致身份表被清 → 登录 401，实测踩坑后改为重建）；**每个测试仍启动独立宿主**（限流桶/内存状态互不串扰，登录限流 5 次/分钟不受跨测试影响）。实测：Desktop Integration **6m12s→23s**、E2E **19m22s→1m23s**、全量 **25m42s→1m43s**（1014 通过/0 失败/24 Skip 不变）。**③ 命名空间校正**——`Unit/**` 下 69 个文件原声明扁平根命名空间 `LYBT.Tests.Desktop`，`--filter` 只能切出 89/966 用例（其余不可分层）→ 统一改为与目录一致的 `LYBT.Tests.Desktop.Unit`（嵌套命名空间仍解析父命名空间类型，无需改 using）；Unit 层由此从「部分 89 用例」变为「完整 877 用例」。**④ collection 并行**——`tests/LYBT.Tests.Desktop/xunit.runner.json` 的 `parallelizeTestCollections` 由 `false` 改 `true`（`parallelizeTestMethods` 保持 `false`）；必须串行的 collection 保持 `DisableParallelization = true` 并在 `tests/AGENTS.md` 注明原因（进程级 `ASPNETCORE_URLS`/`ASPNETCORE_ENVIRONMENT`、`ModeSwitch` 固定端口 5300、RemoteApi 外部依赖、Server 端环境变量/配置文件/静态状态）。**⑤ 三层规范（`tests/AGENTS.md` 强制）**——L0 快速（Architecture+Desktop Unit，**31s**，每次改动自检）/ L1 集成（+Server 全量+Desktop Integration，**2m42s**，commit 前）/ L2 全量（**3m40s**，仅收尾/发布前），含可直接复制命令、namespace↔层切分表、并行/串行说明。**⑥ 核查结论**——`Integration/E2E/**`、`Integration/LocalApi/**` 基建**无固定硬等待**（`Task.Delay`/`Thread.Sleep` 零处，仅 HttpClient 60s 超时），无需引入就绪探测。**验证**：全量 1014/0/24 Skip（1m43s）、Integration 34/0、Unit 877/0、Server 851/851、Architecture 107/107、build 0 错误 0 警告 |
