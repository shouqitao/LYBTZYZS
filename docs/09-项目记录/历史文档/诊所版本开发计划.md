# 诊所版本开发计划

## 一、开发目标

**实现一个简化的中医诊所管理系统，专注于医生看诊和开方的核心功能。**

## 二、现有代码评估

### 2.1 可直接使用的模块（无需修改）
| 模块 | 完成度 | 说明 |
|------|--------|------|
| Auth | 95% | 认证系统完整，可直接使用 |
| Herbs | 90% | 药材管理完整，满足需求 |
| Infrastructure | 100% | 基础设施层完整 |

### 2.2 需要优化的模块
| 模块 | 当前状态 | 需要的工作 |
|------|----------|-----------|
| Patients | 70% | 优化患者快速录入界面 |
| Prescriptions | 40% | 简化处方流程，添加打印功能 |
| Consultation | 30% | 重构为看诊开方模块 |

### 2.3 需要新增的功能
| 功能 | 优先级 | 预计工时 |
|------|--------|---------|
| 验方管理（Formula） | 高 | 2天 |
| 处方打印 | 高 | 1天 |
| 快速开方界面 | 高 | 2天 |

### 2.4 可以删除的模块
- Registration（挂号）
- Queueing（排队）
- Cashier（收费）
- Billing（账单）
- Pharmacy（药房）
- TreatmentRoom（治疗室）
- Sync（同步）

## 三、详细开发任务

### 第1天：验方模型和后端实现
```csharp
// 1. 创建验方模型
public class FormulaModel {
    public Guid Id { get; set; }
    public string Name { get; set; }  // 方名
    public string Category { get; set; }  // 分类
    public string Effect { get; set; }  // 功效
    public string Indication { get; set; }  // 主治
    public string Usage { get; set; }  // 用法
    public List<FormulaItemModel> Items { get; set; }
}

// 2. 创建验方服务
public interface IFormulaService {
    Task<List<FormulaDto>> GetAllAsync();
    Task<FormulaDto> GetByIdAsync(Guid id);
    Task<FormulaDto> CreateAsync(FormulaCreateDto dto);
    Task<bool> UpdateAsync(Guid id, FormulaUpdateDto dto);
    Task<bool> DeleteAsync(Guid id);
    Task<List<FormulaDto>> SearchAsync(string keyword);
    Task<PrescriptionDto> ConvertToPrescription(Guid formulaId);
}
```

**具体任务：**
- [ ] 创建 FormulaModel 实体
- [ ] 创建 FormulaItemModel 实体
- [ ] 更新 AppDbContext
- [ ] 创建数据库迁移
- [ ] 实现 FormulaService
- [ ] 实现 FormulaRepository
- [ ] 创建 FormulaController
- [ ] 编写API测试

### 第2天：验方管理前端界面
**界面设计：**
```
验方管理
├── 验方列表
│   ├── 搜索框
│   ├── 分类筛选
│   └── 验方卡片
├── 验方详情
│   ├── 基本信息
│   ├── 药材组成
│   └── 使用说明
└── 验方编辑
    ├── 信息编辑
    ├── 药材选择
    └── 保存按钮
```

**具体任务：**
- [ ] 创建 FormulaManagementView.xaml
- [ ] 创建 FormulaManagementViewModel.cs
- [ ] 创建 AddFormulaDialog.xaml
- [ ] 创建 EditFormulaDialog.xaml
- [ ] 实现药材选择组件
- [ ] 实现验方搜索功能
- [ ] 集成到主菜单

### 第3天：看诊开方界面重构
**界面布局：**
```
┌────────────────┬──────────────────┐
│   患者信息区    │    验方选择区     │
├────────────────┼──────────────────┤
│                │                  │
│   诊断输入区    │    药材列表区     │
│                │                  │
├────────────────┴──────────────────┤
│          处方编辑区                │
├────────────────────────────────────┤
│     [保存] [打印] [清空] [关闭]     │
└────────────────────────────────────┘
```

**具体任务：**
- [ ] 创建 ConsultationView.xaml（新版）
- [ ] 创建 ConsultationViewModel.cs（新版）
- [ ] 实现患者快速选择/创建
- [ ] 实现验方快速应用
- [ ] 实现药材手动添加
- [ ] 实现剂量调整功能
- [ ] 实现医嘱输入

### 第4天：处方打印功能
**打印模板：**
```
处方打印
├── 打印预览
├── 打印设置
└── 打印输出
```

**具体任务：**
- [ ] 设计处方打印模板
- [ ] 实现打印预览功能
- [ ] 实现 PrintService
- [ ] 添加打印设置（纸张、边距）
- [ ] 测试打印效果
- [ ] 处理打印异常

### 第5天：集成测试和优化
**测试场景：**
1. 新患者看诊流程
2. 老患者复诊流程
3. 验方应用和修改
4. 处方打印测试

**具体任务：**
- [ ] 完整流程测试
- [ ] 性能优化
- [ ] 界面优化
- [ ] 快捷键设置
- [ ] Bug修复

### 第6-7天：部署和文档
**部署任务：**
- [ ] 数据库部署脚本
- [ ] 应用程序打包
- [ ] 安装程序制作
- [ ] 部署测试

**文档任务：**
- [ ] 用户操作手册
- [ ] 安装部署手册
- [ ] 常见问题解答
- [ ] 版本发布说明

## 四、技术实现要点

### 4.1 数据库修改
```sql
-- 删除不需要的表
DROP TABLE IF EXISTS Registrations;
DROP TABLE IF EXISTS QueueItems;
DROP TABLE IF EXISTS Bills;
DROP TABLE IF EXISTS PaymentRecords;

-- 新增验方表
CREATE TABLE Formulas (
    Id UNIQUEIDENTIFIER PRIMARY KEY,
    Name NVARCHAR(100) NOT NULL,
    Category NVARCHAR(50),
    Effect NVARCHAR(500),
    Indication NVARCHAR(500),
    Usage NVARCHAR(200),
    CreateTime DATETIME2,
    CreatedBy NVARCHAR(50)
);

CREATE TABLE FormulaItems (
    Id UNIQUEIDENTIFIER PRIMARY KEY,
    FormulaId UNIQUEIDENTIFIER FOREIGN KEY REFERENCES Formulas(Id),
    HerbId UNIQUEIDENTIFIER FOREIGN KEY REFERENCES Herbs(Id),
    Dosage DECIMAL(10,2),
    Unit NVARCHAR(10),
    Note NVARCHAR(50)
);
```

### 4.2 配置简化
```json
// appsettings.json 简化配置
{
  "AppSettings": {
    "ClinicName": "凌隐宝堂中医诊所",
    "Version": "诊所版 V1.0",
    "DefaultDoctor": "李医生",
    "PrinterName": "默认打印机"
  }
}
```

### 4.3 界面快捷键
| 快捷键 | 功能 |
|--------|------|
| F2 | 新建患者 |
| F3 | 搜索患者 |
| F4 | 选择验方 |
| F5 | 添加药材 |
| F6 | 保存处方 |
| F7 | 打印处方 |
| ESC | 关闭对话框 |

## 五、风险和解决方案

| 风险 | 影响 | 解决方案 |
|------|------|---------|
| 打印兼容性 | 处方无法正确打印 | 使用标准打印接口，提供多种模板 |
| 数据迁移 | 现有数据丢失 | 保留原表，只添加新表 |
| 性能问题 | 开方速度慢 | 预加载常用数据，使用缓存 |
| 操作复杂 | 用户学习成本高 | 提供默认值，简化操作流程 |

## 六、验收标准

### 6.1 功能验收
- [ ] 能够快速创建/选择患者
- [ ] 能够管理验方（增删改查）
- [ ] 能够通过验方快速开方
- [ ] 能够手动组方
- [ ] 能够打印处方
- [ ] 能够查看历史处方

### 6.2 性能验收
- [ ] 患者搜索 < 0.5秒
- [ ] 验方应用 < 1秒
- [ ] 处方保存 < 1秒
- [ ] 处方打印 < 3秒

### 6.3 用户体验验收
- [ ] 界面简洁清晰
- [ ] 操作流程顺畅
- [ ] 支持键盘快捷操作
- [ ] 错误提示友好

## 七、项目里程碑

| 里程碑 | 日期 | 交付物 |
|--------|------|--------|
| M1 | Day 2 | 验方管理功能完成 |
| M2 | Day 4 | 看诊开方功能完成 |
| M3 | Day 5 | 处方打印功能完成 |
| M4 | Day 7 | 诊所版V1.0发布 |

## 八、后续优化（V1.1）

1. **数据统计**
   - 日处方量统计
   - 常用药材统计
   - 常用验方统计

2. **批量操作**
   - 批量打印处方
   - 批量导出数据

3. **模板管理**
   - 诊断模板
   - 医嘱模板
   - 用法模板

4. **备份恢复**
   - 自动备份
   - 一键恢复
   - 云端备份（可选）

---
*计划制定日期：2025-01-30*  
*预计完成日期：2025-02-07*  
*负责人：开发团队*