# 2025-09-24 服务器端审计字段测试适配任务

## 任务背景
- BusinessService 第一阶段重构已完成（参见 docs/tasks/completed/2025-09-24-server-BusinessService重构-summary.md），核心服务已切换为 Repository 模式。
- 审计字段自动化已落地（参见 docs/development/audit-field-automation-implementation-summary.md），所有实体继承 BaseEntity 并引入 CreatedAt/UpdatedAt 字段。
- 当前测试与部分仓库代码仍停留在旧字段（CreatedTime/UpdateTime）命名，引发编译错误并阻塞持续集成。

## 当前问题审计
- tests/UnitTests/Entities/LYBT.Entities.Tests/Users/UserModelTests.cs:31/32 等处仍访问 `CreatedTime`、`UpdateTime`，导致 `dotnet build` 直接报错 CS1061。
- tests/UnitTests/Entities/LYBT.Entities.Tests/Patients/PatientModelTests.cs:40 等处同样引用 `UpdateTime`，无法编译。
- tests/UnitTests/Modules/Users.UnitTests/Services/UserQueryServiceTests.cs 及 Users.UnitTests/UserRepositoryTests.cs 仍使用旧字段并直接初始化 `AppDbContext`，未注入 IHttpContextAccessor，影响新审计逻辑。
- tests/UnitTests/Modules/Users.UnitTests/Mapping/UserMappingProfileTests.cs 等多处断言依赖旧字段，导致逻辑失真。
- 解决方案级搜索显示仍有大量 `CreatedTime`/`UpdateTime` 文本（例如 src/Server/Modules/LYBT.Module.Prescriptions/Services/PrescriptionQueryService.cs 的注释与排序逻辑），必须统一到新字段命名。
- tests/UnitTests/Entities/LYBT.Entities.Tests/LYBT.Entities.Tests.csproj 出现重复 `coverlet.collector` 引用（NU1504），需要一起清理以保证任务完成后编译无警告。

## 任务目标
1. 调整所有实体、DTO、AutoMapper 配置以及仓库/服务层中残留的 `CreatedTime`、`UpdateTime` 等旧字段命名，统一改为 `CreatedAt`、`UpdatedAt`（必要时补充 `CreatedBy`、`UpdatedBy`）。
2. 全量修复受影响的单元测试与集成测试，更新断言、测试数据构造与排序逻辑，确保覆盖新审计字段行为。
3. 在测试项目中为 `AppDbContext` 引入可选的 `IHttpContextAccessor` stub 或工厂，验证审计字段在无 HttpContext 场景下的默认行为。
4. 清理重复的包引用、无效注释与临时兼容代码，保持项目遵循中央依赖管理策略。
5. 重新梳理 README/文档中涉及审计字段说明的段落，确保描述与最新实现一致（由 Thinker 负责同步 README 体系）。

## 工作范围
- 代码层面：`src/Server/Core/LYBT.Entities`, `src/Server/Modules/LYBT.Module.*`, `src/Server/Core/LYBT.Infrastructure`, 所有引用旧命名的测试项目。
- 文档层面：README 及与实体审计行为相关的开发文档需要 Thinker 配套更新。
- 构建脚本：确保 `Directory.Packages.props` 仍是唯一版本源，删除测试项目中重复声明的版本号。

## 交付与验收标准
- `dotnet build LYBT.All.sln -c Release --no-restore` 通过且无审计相关警告。
- `dotnet test tests -c Release --no-build` 全部通过，新增或更新的测试覆盖审计字段自动化场景。
- 关键文件无残留 `CreatedTime`/`UpdateTime` 等旧命名（允许历史文档、迁移脚本中保留变更说明）。
- 更新后的测试项目确保在缺省 HttpContext 情况下依旧可以运行。
- 任务完成后由 Coder 在 `docs/tasks/completed` 目录撰写同名总结，附上受影响文件列表与测试截图/日志。

## 风险与提示
- 需谨慎处理 AutoMapper 映射和 DTO 序列化字段，避免 API 契约发生非预期变更。
- 更新测试时注意与审计字段相关的时间断言，优先使用 `BeCloseTo` 等容错断言，避免脆弱测试。
- 若发现业务服务层仍直接引用 `CreatedTime`/`UpdateTime`，需同步适配并回归验证。
- 变更范围广，建议分模块提交并及时运行对应模块测试，避免回归。

## 计划排期
- 预计工作量：2 人日。
- 建议顺序：代码检索与批量替换 → 核心实体/DTO 修订 → AutoMapper 与业务服务校验 → 测试更新 → 构建验证 → 文档同步。

