# 2025-09-24 服务器仓储测试重构任务

## 任务背景
- 审计字段测试适配任务已完成（docs/tasks/completed/2025-09-24-server-审计字段测试适配-summary.md），大部分模块及测试已统一使用 `CreatedAt/UpdatedAt`。
- 代码审计发现 `tests/UnitTests/ServerRepositories/UserRepositoryTests.cs` 仍大量引用 `CreatedTime/UpdateTime`，导致编译失败（`CS0117`）。
- 该测试项目坚持真实 SQL Server 依赖，无法在标准 CI 环境稳定运行，同时存在重复的 `coverlet.collector` 包引用（`NU1504`）。

## 当前问题审计
- `tests/UnitTests/ServerRepositories/UserRepositoryTests.cs:63-75`：测试数据生成仍访问 `User.CreatedTime/UpdateTime`，与当前实体不符。
- `tests/UnitTests/ServerRepositories/LYBT.ServerRepositories.Tests.csproj:12-22`：重复声明 `coverlet.collector`，触发 NU1504 警告。
- 项目构建需本地 SQL Server（`Server=localhost;…`），破坏可重复构建；同样的仓储用例已在 `tests/UnitTests/Modules/Users.UnitTests/Repositories/UserRepositoryTests.cs` 覆盖，后者使用 InMemory 数据库且符合新审计字段。
- 该项目仍包含 24K+ 行过度冗长的测试用例，涵盖业务逻辑而非纯仓储，存在大量与当前实现不一致的断言。

## 任务目标
1. **统一审计字段命名**：移除 `CreatedTime/UpdateTime` 等旧字段引用，改用 `CreatedAt/UpdatedAt`；同步调整断言与数据构造逻辑。
2. **简化/合并测试项目**：评估 `tests/UnitTests/ServerRepositories` 的必要性，优先迁移有效用例至模块级测试后删除冗余项目；或最少将其改造为独立、可配置的轻量测试（使用 InMemory/SQLite）。
3. **修复包引用与构建配置**：移除重复 `coverlet.collector`，确认所有依赖遵循集中管理策略。
4. **消除环境耦合**：去除对本地 SQL Server 的硬依赖，改用 EF Core 可移植 provider，并补充必要的测试基类/fixture。
5. **验证一致性**：`dotnet test tests/UnitTests/Modules/Users.UnitTests` 与（若保留）`tests/UnitTests/ServerRepositories` 均需在 Release 下通过，无审计字段相关警告。

## 工作范围
- 代码：`tests/UnitTests/ServerRepositories/*`、相关迁移/共享测试工具；如决定迁移用例，目标为 `tests/UnitTests/Modules/*`。
- 文档：更新测试策略说明（README/testing 节或 docs/development/testing-handbook.md），记录仓储测试新的执行方式。
- 构建：确保 `LYBT.All.sln` 构建流程不再依赖实例化 SQL Server。

## 交付与验收标准
- 解决方案级构建：`dotnet build LYBT.All.sln -c Release --no-restore` 通过且无 NU1504。
- 相关测试：`dotnet test tests/UnitTests/Modules/Users.UnitTests -c Release --no-build` 通过；如保留 ServerRepositories 项目，同样通过并不连接外部 SQL Server。
- 全仓库不再出现 `CreatedTime`/`UpdateTime` 的编译级引用（迁移脚本/历史文档除外）。
- 任务完成后在 `docs/tasks/completed` 创建同名总结，附测试日志与处理策略。

## 风险与提示
- 如直接删除冗余项目，需确认覆盖率与回归范围由模块级测试承担，避免功能盲点。
- 若改造为 SQLite Provider，需要处理 `DateTime` 精度差异及事务语义差别。
- 迁移大体量测试时注意保留关键断言，可考虑按功能拆分为多文件提升可维护性。

---
文件：docs/tasks/pending/2025-09-24-server-仓储测试重构.md
