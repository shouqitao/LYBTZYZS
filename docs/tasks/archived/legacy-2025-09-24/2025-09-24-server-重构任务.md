# 任务：Server 端服务分层与代码一致性重构（更新版）

- **发布日期**：2025-09-24
- **发布人**：Thinker（ChatGPT）

## 背景
基于最新《2025-09-23-gemini-code-review-server.md (v2)》的审计：
1. PatientBusinessService 仍直接依赖 AppDbContext，违反 Service→Repository 分层规范。
2. 服务器端注释与术语仍存在“看诊”等旧称，需统一为“诊疗”等最新术语。
3. PatientsController 中保留未使用的 isActive 参数。
4. 业务层手动维护审计字段（CreatedAt/UpdateTime），建议统一由 DbContext 处理。
5. 仍使用 StyleCop.Analyzers 1.2.0-beta.556，需评估稳定版本。
6. PatientRepository 仍在使用预编译查询，需清理过时注释而非删除实现。

## 目标
- 恢复服务层与仓储层的职责边界。
- 统一服务器端术语、注释和 API 定义。
- 制定审计字段自动化及 StyleCop 版本策略。
- 更新 README/文档，确保与实现保持一致。

## 工作内容
1. **服务层重构**
   - 重构 PatientBusinessService 等业务服务，通过 IPatientRepository 进行数据访问，移除对 AppDbContext 的直接依赖。
   - 排查其他 BusinessService 是否存在相同问题并修复。

2. **术语与注释统一**
   - 将 AppDbContext、实体注释等位置的“看诊”等旧称替换为“诊疗”等最新术语。
   - 更新 PatientRepository 中关于预编译查询的注释，使其与实际实现一致（保留实现、修改注释）。

3. **控制器接口清理**
   - 移除 PatientsController.GetList 中未使用的 isActive 参数，保持 API 签名与实现一致。

4. **审计字段策略**
   - 调研在 AppDbContext 中统一设置 CreatedAt/UpdatedAt（如实现 IAuditableEntity）的可行方案。
   - 在任务总结中输出结论，若未实现需说明后续计划。

5. **StyleCop 版本评估**
   - 调研最新稳定版本，评估升级或继续使用 beta 版的影响，输出建议。

## 交付物
- 重构后的服务与仓储代码及相应单元测试。
- 术语、注释与 README/相关文档的同步更新。
- 控制器参数清理的提交记录。
- 审计字段与 StyleCop 的处理结论（可落实或列入后续任务）。

## 验收标准
- dotnet build LYBT.Server.sln -c Release 通过。
- Service 层不再直接调用 AppDbContext。
- 术语和注释统一，无“看诊”等旧称。
- PatientsController 不再暴露未使用的参数。
- 提供审计字段及 StyleCop 的处理结果或后续计划。
- README 及相关文档更新到位。

> 任务完成后，请在 docs/tasks/completed/2025-09-24-server-重构任务-summary.md 中总结改动、测试结果与后续建议。

