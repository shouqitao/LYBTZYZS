# 2025-09-24 Users 模块业务逻辑测试攻坚任务

## 任务背景
- `2025-09-24-users-模块单元测试契约同步-summary.md` 显示目前已完成映射契约、依赖注册和密码策略类问题修复，但仍有 65 个失败用例集中在服务业务逻辑与仓储缓存实现。
- Cache SizeLimit 异常、用户创建流程缺失持久化、批量操作规则不匹配等问题阻碍了 Users 模块测试全面通过。
- 需以业务逻辑场景为核心，快速清零剩余失败测试，支撑后续中期（SQLite In-Memory）与长期（测试基建）计划落地。

## 目标
1. 修复所有业务逻辑相关失败测试（服务层、仓储层、批量操作、唯一性验证）。
2. 统一 `ServiceResult` 返回契约，确保成功/失败语义与测试断言一致。
3. 完成一次完整的 `LYBT.Module.Users.Tests` 运行并达到 0 失败。

## 工作项
1. **缓存异常修复**
   - 调整 `UserRepository` 中所有 `_cache.Set` 调用，统一使用带 `SetSize(1)` 的缓存写入（建议封装 `SetCacheSafely`）。
   - 保留 `MemoryCacheOptions.SizeLimit` 配置，回归测试确认无异常。

2. **用户创建流程完善**
   - `CreateUserAsync` 在新增用户后调用 `_userRepository.SaveChangesAsync()`。
   - 新增手机号唯一性校验（仓储层提供 `ExistsByPhoneNumberAsync` 或查询实现）。
   - 更新相关测试，验证 `ServiceResult` 的 Data/Message。

3. **批量操作契约对齐**
   - 在 `BatchDisableAsync/BatchEnableAsync` 中检查 `UserOptions.MaxBatchOperationSize`、空集合、重复 ID、状态已一致等场景，返回统一错误消息。
   - 调整测试断言，使成功返回实际变更数量，失败返回具体提示。

4. **测试基建临时增强**
   - 提取最小可用的 `UsersTestFixture`：统一创建 `AppDbContext`、真实 `MemoryCache`、AutoMapper 配置。
   - 引入 `UserBuilder` 构造常用测试数据，减少重复初始化。

## 验收标准
- `dotnet build LYBT.All.sln -c Release --no-restore` 通过。
- `dotnet test tests/UnitTests/Modules/Users.UnitTests/LYBT.Module.Users.Tests.csproj -c Release --no-build` 0 失败。
- 缓存异常（SizeLimit）不再出现，批量操作/创建用户相关测试全部通过。
- 完成任务后在 `docs/tasks/completed/2025-09-24-users-模块业务逻辑测试攻坚-summary.md` 写入总结，并附最新测试日志。

## 风险提示
- 若新增仓储接口需同步 API/服务依赖；如影响范围较大需在总结说明。
- 批量操作规则变化需与业务方确认，避免影响现有使用场景。
- Fixture 引入需保证测试隔离，避免数据库/缓存污染。

---
文件：docs/tasks/pending/2025-09-24-users-模块业务逻辑测试攻坚任务.md