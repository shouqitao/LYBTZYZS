# Login 模块全面重构任务

> 目标：对 Login 相关的 View、ViewModel、Service 进行一次完整重构，统一规范、修复已知问题、提升代码质量

## 涉及文件

### View 层（4 个）
- `Modules/LYBT.Desktop.Auth/Views/LoginView.xaml` + `.cs`
- `Modules/LYBT.Desktop.Auth/Views/ServerConfigView.xaml` + `.cs`
- `Modules/LYBT.Desktop.Auth/Views/FirstRunSetupView.xaml` + `.cs`
- `Shell/Views/AccountSettingsView.xaml` + `.cs`

### ViewModel 层（6 个）
- `Modules/LYBT.Desktop.Auth/ViewModels/LoginViewModel.cs`
- `Modules/LYBT.Desktop.Auth/ViewModels/LoginCredentialsViewModel.cs`
- `Modules/LYBT.Desktop.Auth/ViewModels/ConnectionStatusViewModel.cs`
- `Modules/LYBT.Desktop.Auth/ViewModels/ServerConfigViewModel.cs`
- `Modules/LYBT.Desktop.Auth/ViewModels/FirstRunSetupViewModel.cs`
- `Shell/ViewModels/AccountSettingsViewModel.cs`

### Service 层（7 个）
- `Shell/Services/Login/LoginCoordinator.cs`
- `Shell/Services/Login/LoginStateManager.cs`
- `Foundation/Services/ConnectionModeService.cs`
- `Infrastructure/Services/ConnectionSettingsService.cs`
- `Foundation/Security/CredentialVault.cs`
- `Foundation/Security/UsernameStorageService.cs`
- `Contracts/Services/ILoginCoordinator.cs`

### 控件层（2 个）
- `Shell/Controls/AccountSettingsControl.xaml` + `.cs`

## 重构维度

### D1: 远程/本地切换（已知问题）
- 切换卡顿：已改为先切 URL 再后台探测，需验证
- URL 不更新：已绑定 CurrentServerUrl，需验证
- 模式标签移除：已完成，需验证
- **验证项**：切换瞬间完成，URL 实时变化，无 UI 卡顿

### D2: 字体与样式统一
- 检查所有 Login 相关 View 的字体：
  - 标题：Noto Sans SC, 24-28px, Bold
  - 正文：Noto Sans SC, 13-14px, Regular
  - 辅助文本：Noto Sans SC, 11-12px, Regular
- 检查颜色是否使用设计 Token（PenPrimaryBrush, PenTextSecondaryBrush 等）
- 检查间距是否一致（Margin/Padding 是否对齐 4px 网格）

### D3: 绑定一致性
- 检查所有 Binding 是否指向正确的 VM 属性
- 检查 DataContext 是否正确设置
- 检查 ElementName binding 是否有效
- 检查 UpdateSourceTrigger 是否正确（Password 需 PropertyChanged）

### D4: 命令与交互
- 检查所有 Command 绑定是否有对应的 VM 命令
- 检查 CanExecute 是否正确反映状态
- 检查错误提示是否友好（中文，不暴露技术细节）
- 检查加载状态（IsLoading/IsBusy）是否正确显示

### D5: DI 注入
- 检查所有 ViewModel 的构造函数注入是否完整
- 检查可选依赖（`?`）是否正确处理 null
- 检查 IDisposable 实现是否完整（事件退订、CTS 取消）

### D6: 异常处理
- 检查所有 async void 方法是否有 try-catch
- 检查所有 async Task 命令是否有顶层异常处理
- 检查网络错误是否友好提示（不暴露 HttpRequestException）

## 输出要求

1. 逐文件审计表（文件 | 问题 | 修复）
2. 统一修复所有问题
3. `dotnet build` 验证 0 错误 0 警告
4. 产出审计报告到 `docs/compose/reports/login-refactor-2026-08-29.md`

## 约束

- 不改变公共 API 签名
- 不改变登录流程逻辑
- 保持向后兼容
- 每次修改后 build 验证
