# ViewModel 层全面审计与优化任务

> 目标：对 53 个 ViewModel 进行系统性审计，发现并修复逻辑缺陷、性能问题、一致性问题
> 前次审查（2026-08-26）P0×5 已全部修复，P1×8 中 P1#6/P1#7 已修复，本次需复查并覆盖新维度

## 审计维度（6 维）

### D1: 异常处理完整性
- 检查所有 `async void` 方法是否有 try-catch（已知 4 处）
- 检查 `async Task` 命令方法是否有顶层 try-catch
- 检查 `OnNavigatedTo` 是否有异常保护

### D2: 资源泄漏
- 事件订阅（`.Changed +=`）是否有对应退订
- `IDisposable` 实现是否完整（Dispose 方法是否取消订阅/释放资源）
- `CancellationTokenSource` 是否在适当时机 Dispose
- `Timer`/`Polling` 是否在导航离开时停止

### D3: 状态管理一致性
- `IsBusy`/`IsLoading`/`IsNotLoading` 使用是否一致
- 多个 VM 共享状态时是否通过 EventAggregator 或共享服务同步
- 命令 `CanExecute` 是否正确反映状态

### D4: UI 线程安全
- 后台任务结果是否正确调度到 UI 线程
- `ObservableProperty` 更新是否在 UI 线程
- `IEventAggregator` 订阅是否在 UI 线程

### D5: 业务逻辑正确性
- API 调用参数是否正确
- 分页逻辑是否正确（PageNumber/PageSize/HasMore）
- 搜索/筛选逻辑是否正确
- 空状态/错误状态处理是否完整

### D6: 代码质量
- 构造函数注入数是否合理（>5 个参数需评估）
- 魔法数字是否提取为常量
- 重复代码是否提取为基类方法
- 命名是否符合 C# 规范

## 输出要求

1. 逐 VM 审计表（VM 名称 | 维度 | 问题 | 严重级 | 修复建议）
2. 汇总统计（P0/P1/P2/P3 数量）
3. 修复计划（按优先级排序，预估工时）
4. 修复执行（按优先级从高到低修复）

## 约束

- 仅修改 ViewModel 层代码，不改 View/XAML
- 每次修改后 `dotnet build` 验证 0 错误 0 警告
- 保持向后兼容，不改变公共 API 签名
- 修复完成后产出审计报告到 `docs/compose/reports/vm-layer-audit-2026-08-29.md`

## 文件范围

`src/Client/Desktop/` 下所有 `*ViewModel.cs`（排除 obj/ 目录），共 53 个文件。
