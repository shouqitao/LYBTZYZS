 问题结论先说

  - 报错信息显示：在启动时解析接口 LYBT.Desktop.Shell.Services.Bootstrap.IApplicationBootstrapper 失败（DI 解析异常），导致桌面壳程序弹框崩溃。
  - 根因并非未注册该接口本身（代码中已注册），而是 IApplicationBootstrapper 的构造链中某一依赖没有被正确注册或其注册时序在它之后，导致 Prism/DryIoc 在解析 IApplicationBootstrapper 时抛出异常。

  关键证据与依赖链

  - 解析点
      - App 在 OnInitialized 中解析 _bootstrapper = Container.Resolve<IApplicationBootstrapper>()，随后调用其方法 src/Client/Desktop/Shell/App.xaml.cs:79-116。
      - IApplicationBootstrapper 注册在 RegisterTypes 里，且位于 containerRegistry.RegisterAllServices() 之后：src/Client/Desktop/Shell/App.xaml.cs:61-90, 100-116, 119-135。顺序正确（先注册依赖，再注册
    bootstrapper）。
  - IApplicationBootstrapper 的依赖列表（构造函数参数）
      - IApplicationInitializationService
      - IStartupOptimizationService
      - LYBT.Desktop.Services.ErrorHandling.IErrorHandlingService
      - IModuleManager
      - IModuleCatalog
      - IEventAggregator
      - ILogger<ApplicationBootstrapper>
        路径：src/Client/Desktop/Shell/Services/Bootstrap/ApplicationBootstrapper.cs:23-39
  - 这些依赖的注册现状
      - IApplicationInitializationService 已在 RegisterBootstrapServices 中注册为 ApplicationInitializationService
          - src/Client/Desktop/Shell/Extensions/ServiceCollectionExtensions.cs:108-118
      - IStartupOptimizationService：在 RegisterBootstrapServices 中注册了 StartupOptimizationService（早期注释称也在 RegisterPerformanceServices 中注册，但当前 RegisterPerformanceServices 实际是空壳，未注册任
    何服务）
          - 注册：src/Client/Desktop/Shell/Extensions/ServiceCollectionExtensions.cs:115-123
          - 实现存在：src/Client/Desktop/Core/LYBT.Desktop.Services/Performance/StartupOptimizationService.cs
      - 错误处理服务（Services 层接口）：LYBT.Desktop.Services.ErrorHandling.IErrorHandlingService -> UnifiedErrorHandlingService 已注册
          - src/Client/Desktop/Shell/Extensions/ServiceCollectionExtensions.cs:103-112
      - IModuleManager / IModuleCatalog / IEventAggregator：由 Prism 框架注入，正常。
      - Logging：ILoggerFactory 与 ILogger<> 已注册
          - src/Client/Desktop/Shell/Extensions/ServiceCollectionExtensions.cs:186-203
  - 另一个容易踩坑的类型：MainWindowViewModel
      - 其构造函数依赖用户通知接口 LYBT.Desktop.Infrastructure.Interfaces.IUserNotificationService，该接口已注册为 Infrastructure.Services.UserNotificationService
          - 依赖：src/Client/Desktop/Shell/ViewModels/MainWindowViewModel.cs:40-54
          - 注册：src/Client/Desktop/Shell/Extensions/ServiceCollectionExtensions.cs:93-101
      - 说明：目前 ViewModel 这块看起来能解析通过。

  高概率问题点（组合根处最易出错的两类）

  1. 报错接口名与截图一致：IApplicationBootstrapper。根据我们的检查，这个接口本身已注册，时序也在依赖之后。因此更可能是其构造链里的某个服务未能解析或解析过程中抛异常。
      - 首要怀疑目标：IStartupOptimizationService
          - 原因：文件顶部注释“Issue #837: IStartupOptimizationService 已在 RegisterPerformanceServices 中注册, 移除重复注册”，但 RegisterPerformanceServices 函数当前是空实现（未注册任何东西）src/Client/
    Desktop/Shell/Extensions/ServiceCollectionExtensions.cs:555-583。幸好我们看到它实际上在 RegisterBootstrapServices 中注册了一次 StartupOptimizationService。这一处“注释与现实不符”容易导致历史分支或本地缓存中没
    有注册，或被误删。若你本地分支与当前提交不同步，这里最容易缺注册。
      - 次要怀疑目标：LYBT.Desktop.Services.ErrorHandling.IErrorHandlingService
          - 它的实现 UnifiedErrorHandlingService 依赖 INotificationService（已注册）。如果 INotificationService 注册或其使用路径在 UI 线程访问失败，也可能在构造或初始化时抛异常。
  2. 另一路径：App.CreateShell 中我们增加了“诊断解析 MainWindowViewModel”的代码，如果你本地与我们仓库的不同步，或 ViewModel 依赖的 Facade/Commands/ModuleLoadingService 任意一个未注册，也会先期报错。但截图显示的
    接口名是 IApplicationBootstrapper，而不是 ViewModel，所以当前错误发生在 OnInitialized 里解析 Bootstrapper 时。

  如何快速定位（不用改代码，运行态调试）

  - 在 App.xaml.cs 的 OnInitialized 中，Container.Resolve<IApplicationBootstrapper>() 这一行捕获的 ex.InnerException 信息能显示到底是哪个类型没解析到（DryIoc 会给出具体类型）。你可以在本地加一行 Debug 输
    出 ex 或 ex.InnerException 的 Message/ToString 已经有了：src/Client/Desktop/Shell/App.xaml.cs:35-57, 82-95。请把 InnerException 的完整消息贴出来；通常会是 “Unable to resolve IStartupOptimizationService” 或
    “IErrorHandlingService ctor failed …”。

  修复方案（按优先级，二选一即可）

  - 方案 A（推荐，最小化且与现结构一致）
      1. 确认 IStartupOptimizationService 的注册存在于你的分支
          - 确保代码包含：containerRegistry.RegisterSingleton<IStartupOptimizationService, StartupOptimizationService>();
          - 文件：src/Client/Desktop/Shell/Extensions/ServiceCollectionExtensions.cs:115-123
      2. 确认 IErrorHandlingService 与 INotificationService 的注册在它之前（已满足，当前顺序正确）。
      3. 保持 IApplicationBootstrapper 的注册在 RegisterAllServices() 之后（当前已满足：App.xaml.cs:100-116）。
      4. 重新运行，若仍报错，把 InnerException 文本贴出，我们继续针对具体类型补注册。
  - 方案 B（兜底，避免时序问题）
      - 将所有与 IApplicationBootstrapper 构造相关的注册集中到同一个方法，保证顺序：INotificationService → IErrorHandlingService → IStartupOptimizationService → IApplicationInitializationService →
    IApplicationBootstrapper。
      - 即使未来调整 RegisterPerformanceServices，也不影响组合根解析。

  额外对齐建议（非必须，但能减少未来崩溃）

  - 错误处理抽象统一：ViewModel 使用 Infrastructure 的 IUserNotificationService，Shell/Bootstrapper 依赖 Services 的 IErrorHandlingService，这两个职责不同但名称相近，容易引发误解。建议在 CLAUDE.md 中固化“UI 交
    互提示走 IUserNotificationService；系统级错误链路走 IErrorHandlingService”的约束，避免误替换接口。
  - 删除误导性注释：把“已在 RegisterPerformanceServices 注册”这类已过时注释清理干净，以免后续维护时误删正确注册点。

  总结

  - 你看到的弹框“An unexpected error occurred while resolving 'IApplicationBootstrapper'”的根因是它的构造依赖链中至少有一个服务未注册或注册顺序不当。根据仓库现状，首要检查 IStartupOptimizationService 的注册是否
    在你的本地分支存在，其次检查 IErrorHandlingService/INotificationService 的注册。
  - 按方案 A 确认/恢复注册顺序后再运行，若仍失败，把异常的 InnerException 完整文本发我，我据此给出针对性的下一修复点。