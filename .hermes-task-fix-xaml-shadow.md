# 修复 SysadminHomeView DI/XAML 崩溃

## 问题
Shell 登录后导航到 SysadminHomeView 时报错：
`An unexpected error occurred while resolving 'System.Object', with the service name 'SysadminHomeView'`

## 根因（已通过日志确认）

完整异常堆栈：
```
Prism.Ioc.ContainerResolutionException: An unexpected error occurred while resolving 'System.Object', with the service name 'SysadminHomeView'
 ---> System.Windows.Markup.XamlParseException: "设置属性'System.Windows.Media.Effects.DropShadowEffect.Color'时引发了异常。", 行号为"30", 行位置为"77"。
 ---> System.InvalidOperationException: "#FF4B2C20"不是属性"Color"的有效值。
```

**根本原因是 XAML 解析错误，不是 DI 注册问题。**

文件：`src/Client/Desktop/Roles/LYBT.Desktop.Admin/Sysadmin/Views/SysadminHomeView.xaml` 第 30 行：
```xml
<DropShadowEffect BlurRadius="16" Opacity="0.2" ShadowDepth="4" Color="{DynamicResource MaterialDesign.Brush.Primary.Dark}" />
```

`DropShadowEffect.Color` 属性类型是 `System.Windows.Media.Color`，但 `{DynamicResource MaterialDesign.Brush.Primary.Dark}` 返回的是 `Brush` 类型，不是 `Color`。WPF XAML 解析器在 InitializeComponent 时就崩溃了，导致 Prism 无法创建 View 实例，呈现为 DI 解析失败的假象。

## 修复方案

### 修复 1：XAML Color 属性（必须）
将第 30 行的 Brush 资源替换为 **Color 类型**的 MDIX 主题资源。`DropShadowEffect.Color` 属性类型是 `System.Windows.Media.Color`，不能接受 Brush 类型资源。

查找 MDIX 中对应的 Color 资源键（如 `MaterialDesign.Color.Primary`、`MaterialDesignColor.PrimaryDark` 等），使用正确的 Color 类型资源。不要硬编码颜色值。

参考第 22 行的 `Color="Black"` 写法——如果找不到合适的 Color 资源，也可以使用 `Color="Black"` 作为折中方案（保持与同文件一致）。但优先查找 MDIX 的 Color 资源。

### 修复 2：SysadminModule 补注册（建议，顺带修）
`src/Client/Desktop/Roles/LYBT.Desktop.Admin/Sysadmin/SysadminModule.cs` 缺少 `IServerConfigurationService` 注册（当前仅在 AdminModule 注册，SuperAdmin 不加载 AdminModule）。

在 `RegisterTypes` 方法中添加：
```csharp
containerRegistry.Register<Services.IServerConfigurationService, Services.ServerConfigurationService>();
```

### 修复 3：NavigationCoordinator 增强日志（建议，顺带修）
`src/Client/Desktop/Core/LYBT.Desktop.Infrastructure/Navigation/NavigationCoordinator.cs` 第 128-129 行只记录了 `result.Error?.Message`，丢失了完整堆栈。改为：
```csharp
var ex = result.Error;
_logger.LogError(ex, "导航失败：{ViewName}，异常完整信息：{ExFull}", viewName, ex?.ToString() ?? "未知错误");
var errorMessage = ex?.Message ?? "未知错误";
```

## 举一反三
排查所有 XAML 文件中的 `DropShadowEffect.Color` 属性，确保不使用 `DynamicResource` 或 `StaticResource` 绑定 Brush 类型资源到 Color 属性。Color 属性只能接受颜色字面量（如 `Black`、`#6D4C41`）或 Color 类型资源。

## 验证
1. `dotnet build src/Client/Desktop/Shell/LYBT.Desktop.Shell.csproj` 必须 0 错误 0 警告
2. 启动 Shell，sysadmin 登录，SysadminHomeView 正常打开无弹窗
3. 检查其他 View 是否有同样的 DropShadowEffect.Color + DynamicResource 问题

## 约束
- 只改任务要求的代码，不顺手改相邻代码
- 不要创建临时项目验证
- 连续执行至全部完成无需确认
