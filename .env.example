# =================================================================
# 凌隐宝堂中医诊所管理系统 - 环境变量配置指引
# =================================================================
#
# 本项目有两个独立的 .env.example 模板，用途不同：
#
# 1. WebAPI 开发/运行时配置（权威模板）:
#    src/Server/Services/LYBT.WebAPI/.env.example
#    - 由 Program.cs 通过 DotNetEnv 直接加载
#    - 使用 ASP.NET Core __ 分隔符命名（如 Lybt__Jwt__SecretKey）
#    - 复制为 .env.development（开发）或 .env（生产）放在 WebAPI 目录
#
# 2. IIS 部署脚本配置:
#    deploy/iis/.env.example
#    - 由 deploy.ps1 写入机器级环境变量
#    - 通过 appsettings.Override.json 的 ${VAR} 模板替换
#    - 仅包含 3 个密钥: JWT_SECRET, SYSADMIN_PASSWORD, NEWUSER_PASSWORD
#
# 快速开始:
#   cd src/Server/Services/LYBT.WebAPI
#   cp .env.example .env.development
#   # 编辑 .env.development 填写实际值
#   dotnet run
#
# 详细配置文档: docs/03-architecture/configuration.md
# =================================================================
