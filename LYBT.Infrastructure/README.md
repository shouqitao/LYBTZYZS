# LYBT.Infrastructure

This project hosts Entity Framework Core migrations.

## Applying migrations

Use the `UpdateIdFieldsToGuid` migration to initialize the database:

```bash
# From the repository root
./scripts/update_db.sh
```

