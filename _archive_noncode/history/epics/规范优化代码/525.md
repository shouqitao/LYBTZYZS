---
name: MedicalCaseService现代化
epic: 规范优化代码
status: ready
created: 2025-09-05T14:00:04Z
updated: 2025-09-05T14:12:46Z
priority: high
estimate: 0.5 day
dependencies: [523, 524]
parallel: true
github: https://github.com/shouqitao/LYBTZYZS/issues/525
---

# Task: MedicalCaseService现代化

## Overview

将 MedicalCaseService 从传统构造函数升级为 C# 12主构造函数，遵循 PatientService 现代化的成功模式。

## Target File

`src/Server/Modules/LYBT.Module.MedicalCase/Services/MedicalCaseService.cs`

## Technical Approach

### 升级策略
基于 PatientService 现代化的经验，应用相同的升级模式：

1. **保持构造函数逻辑不变**
2. **仅改变语法形式为 C# 12主构造函数**
3. **保留所有异常检查逻辑**
4. **确保与 UserService 风格一致**

### 预期变更模式

```csharp
// 升级前：传统构造函数
public class MedicalCaseService : IMedicalCaseService
{
    private readonly IMedicalCaseQueryService _queryService;
    private readonly IMedicalCaseBusinessService _businessService;
    
    public MedicalCaseService(
        IMedicalCaseQueryService queryService,
        IMedicalCaseBusinessService businessService)
    {
        _queryService = queryService ?? throw new ArgumentNullException(nameof(queryService));
        _businessService = businessService ?? throw new ArgumentNullException(nameof(businessService));
    }
}

// 升级后：C# 12主构造函数
public class MedicalCaseService(
    IMedicalCaseQueryService queryService,
    IMedicalCaseBusinessService businessService) : IMedicalCaseService
{
    private readonly IMedicalCaseQueryService _queryService = queryService ?? throw new ArgumentNullException(nameof(queryService));
    private readonly IMedicalCaseBusinessService _businessService = businessService ?? throw new ArgumentNullException(nameof(businessService));
}
```

## Deliverables

- [ ] 分析 MedicalCaseService 当前构造函数实现
- [ ] 应用 C# 12 主构造函数语法升级
- [ ] 保持异常检查和依赖注入逻辑不变
- [ ] 编译验证和功能测试
- [ ] 代码风格与标准对比检查

## Acceptance Criteria

- ✅ MedicalCaseService 使用 C# 12 主构造函数语法
- ✅ 代码风格与 UserService、PatientService 完全一致
- ✅ 所有异常检查逻辑保留
- ✅ 编译通过，零错误零警告
- ✅ 医案相关功能测试全部通过
- ✅ 与前端 MedicalCase 模块集成正常

## Notes

依赖 PatientService 现代化完成，可以参考其成功经验。这个任务验证了构造函数现代化模式的可重复性。