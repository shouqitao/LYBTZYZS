---
name: PatientService现代化
epic: 规范优化代码
status: ready
created: 2025-09-05T14:00:04Z
updated: 2025-09-05T14:12:46Z
priority: high
estimate: 0.5 day
dependencies: [523]
parallel: true
github: https://github.com/shouqitao/LYBTZYZS/issues/524
---

# Task: PatientService现代化

## Overview

将 PatientService 从传统构造函数模式升级为 C# 12主构造函数风格，与 UserService 黄金标准保持一致。这是构造函数现代化的示范任务。

## Target File

`src/Server/Modules/LYBT.Module.Patients/Services/PatientService.cs`

## Technical Approach

### 当前状态分析
PatientService 使用传统构造函数模式：

```csharp
public class PatientService : IPatientService
{
    private readonly IPatientQueryService _queryService;
    private readonly IPatientBusinessService _businessService;
    
    public PatientService(
        IPatientQueryService queryService,
        IPatientBusinessService businessService)
    {
        _queryService = queryService ?? throw new ArgumentNullException(nameof(queryService));
        _businessService = businessService ?? throw new ArgumentNullException(nameof(businessService));
    }
}
```

### 目标状态（参考 UserService 标准）
升级为 C# 12 主构造函数：

```csharp
public class PatientService(
    IPatientQueryService queryService,
    IPatientBusinessService businessService) : IPatientService
{
    private readonly IPatientQueryService _queryService = queryService ?? throw new ArgumentNullException(nameof(queryService));
    private readonly IPatientBusinessService _businessService = businessService ?? throw new ArgumentNullException(nameof(businessService));
}
```

## Deliverables

- [ ] 分析当前 PatientService 构造函数实现
- [ ] 升级为 C# 12 主构造函数语法
- [ ] 保持相同的异常检查逻辑
- [ ] 验证编译通过且功能正常
- [ ] 运行相关测试确保无回归

## Acceptance Criteria

- ✅ PatientService 使用 C# 12 主构造函数语法
- ✅ 保持与 UserService 一致的代码风格
- ✅ 异常检查逻辑完全保留
- ✅ 编译零错误零警告
- ✅ 功能测试通过，无业务逻辑变更
- ✅ 代码风格与 UserService 黄金标准一致

## Notes

这是构造函数现代化的第一个任务，将作为其他服务类的参考模板。必须确保与 UserService 的风格完全一致。