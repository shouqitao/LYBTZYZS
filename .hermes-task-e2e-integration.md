# E2E 集成测试实现任务

## 目标
实现端到端集成测试，验证 **ViewModel → IApiClient → API → DB** 全链路，覆盖 4 个角色的核心业务流程。

## 参考文档
- 计划：`docs/compose/plans/e2e-integration-test-plan.md`
- 基类参考：`tests/LYBT.Tests.Desktop/Integration/LocalApi/LocalWebApiTestBase.cs`
- WebAPI基类：`tests/LYBT.Tests.Desktop/Integration/Infrastructure/WebApiE2ETestBase.cs`
- API Client接口：`src/Client/Desktop/Core/LYBT.Desktop.Foundation/Http/Clients/`
- ViewModel实现：`src/Client/Desktop/Modules/*/ViewModels/`

## 关键约束
1. **不 mock**：用 WebApplicationFactory + LocalDB 启动真实 API
2. **独立数据库**：每个测试类使用独立 LocalDB（EnsureCreated + EnsureDeleted）
3. **真实 API Client**：通过 DI 获取 IApiClient 实例，不 mock
4. **数据隔离**：UniqueUsername/UniquePhone 保证不冲突
5. **权限验证**：每个角色测试其可见端点，403 作为断言
6. **不要创建临时项目验证**

## 实施步骤

### Phase 1：基础设施
1. 创建 `tests/LYBT.Tests.Desktop/Integration/E2E/Infrastructure/E2ETestBase.cs`
   - 继承 IAsyncLifetime
   - WebApplicationFactory 启动 LocalWebAPI
   - LocalDB 独立数据库
   - DI 注入真实 IApiClient
   - 种子数据（4 角色用户）
   - Token 管理（登录/注销）
2. 创建 `tests/LYBT.Tests.Desktop/Integration/E2E/Infrastructure/E2ECollectionFixture.cs`
3. 更新 `tests/LYBT.Tests.Desktop/LYBT.Tests.Desktop.csproj` 添加必要引用

### Phase 2：核心业务流程
1. `AuthFlow/LoginLogoutE2ETests.cs` — 登录→Token→Claims→注销
2. `AuthFlow/TokenRefreshE2ETests.cs` — 过期→刷新→继续使用
3. `PatientFlow/PatientCrudE2ETests.cs` — 创建→分页→详情→更新→删除
4. `PatientFlow/PatientSearchE2ETests.cs` — 姓名/手机号/身份证搜索
5. `RegistrationFlow/RegistrationE2ETests.cs` — 创建挂号→查询→状态
6. `RegistrationFlow/RegistrationPermissionE2ETests.cs` — 前台可创建/医生只读
7. `MedicalCaseFlow/MedicalCaseE2ETests.cs` — 创建→诊断→处方→完成
8. `MedicalCaseFlow/PrescriptionE2ETests.cs` — 添加药材→保存→查询
9. `MedicalCaseFlow/ConsultationE2ETests.cs` — 主诉→现病史→舌脉→辨证

### Phase 3：目录+管理流程
1. `HerbFormulaFlow/HerbCrudE2ETests.cs` — 药材 CRUD + 搜索
2. `HerbFormulaFlow/FormulaCrudE2ETests.cs` — 验方 CRUD + 导入
3. `AdminFlow/UserManagementE2ETests.cs` — 4 角色 CRUD
4. `AdminFlow/ConfigurationE2ETests.cs` — 系统配置读写
5. `SysadminFlow/BackupE2ETests.cs` — 备份操作
6. `SysadminFlow/DiagnosticsE2ETests.cs` — 系统诊断

### Phase 4：权限+边界
1. 跨角色权限验证（admin 不能访问 doctor 端点）
2. 数据隔离验证（A 角色不能修改 B 角色数据）
3. 并发安全验证（同时创建不冲突）

## 验收标准
1. `dotnet build` → 0 错误 0 警告
2. `dotnet test` → 全部通过
3. 测试数量：60-80 个
4. 运行时间：< 5 分钟
5. 覆盖 4 角色 × 核心业务流程

## 回滚
如实现有问题，删除 `tests/LYBT.Tests.Desktop/Integration/E2E/` 目录即可。
