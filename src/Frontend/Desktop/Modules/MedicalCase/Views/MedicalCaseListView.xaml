<UserControl x:Class="LYBT.WPF.Client.Modules.MedicalCase.Views.MedicalCaseListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Background="{StaticResource PrimaryBrush}" Padding="16">
            <TextBlock Text="医疗案例管理" Style="{StaticResource PageTitleStyle}" Foreground="White"/>
        </Border>

        <!-- 工具栏 -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Background="{StaticResource BackgroundBrush}" Margin="8">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- 搜索区域 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBox x:Name="SearchBox" 
                             Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ModernTextBoxStyle}"
                             ToolTip="搜索患者姓名或案例编号..."
                             Width="250" Margin="4"/>
                
                    <!-- 状态筛选 -->
                    <ComboBox x:Name="StatusFilter" 
                              SelectedItem="{Binding FilterStatus}"
                              Width="120" Margin="4">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <Button Content="搜索" 
                            Command="{Binding SearchCommand}" 
                            Style="{StaticResource PrimaryButtonStyle}"
                            Margin="10,0,0,0"/>
                </StackPanel>
                
                <!-- 操作按钮区域 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="+ 新建案例" 
                            Command="{Binding AddCommand}" 
                            Style="{StaticResource SuccessButtonStyle}"
                            Margin="0,0,8,0"/>
                    <Button Content="刷新" 
                            Command="{Binding RefreshCommand}" 
                            Style="{StaticResource BaseButtonStyle}"
                            Background="{StaticResource SecondaryBrush}"
                            Foreground="White"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 数据列表 -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding MedicalCases}"
                  SelectedItem="{Binding SelectedMedicalCase}"
                  Style="{StaticResource ModernDataGridStyle}"
                  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                  IsReadOnly="True"
                  Margin="8">
            
            <DataGrid.Columns>
                <!-- 案例编号 -->
                <DataGridTextColumn Header="案例编号" 
                                    Binding="{Binding CaseNumber}" 
                                    Width="120"/>
                
                <!-- 患者姓名 -->
                <DataGridTextColumn Header="患者姓名" 
                                    Binding="{Binding PatientName}" 
                                    Width="100"/>
                
                <!-- 性别 -->
                <DataGridTextColumn Header="性别" 
                                    Binding="{Binding PatientGender}" 
                                    Width="60"/>
                
                <!-- 年龄 -->
                <DataGridTextColumn Header="年龄" 
                                    Binding="{Binding PatientAge}" 
                                    Width="60"/>
                
                <!-- 主诉 -->
                <DataGridTextColumn Header="主诉" 
                                    Binding="{Binding ChiefComplaint}" 
                                    Width="*"/>
                
                <!-- 医生 -->
                <DataGridTextColumn Header="医生" 
                                    Binding="{Binding DoctorName}" 
                                    Width="100"/>
                
                <!-- 状态 -->
                <DataGridTextColumn Header="状态" 
                                    Binding="{Binding StatusText}" 
                                    Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="Registered">
                                    <Setter Property="Foreground" Value="Orange"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="InConsultation">
                                    <Setter Property="Foreground" Value="Blue"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="Completed">
                                    <Setter Property="Foreground" Value="Green"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                    <Setter Property="Foreground" Value="Red"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <!-- 创建时间 -->
                <DataGridTextColumn Header="创建时间" 
                                    Binding="{Binding CreateTimeText}" 
                                    Width="150"/>
                
                <!-- 操作列 -->
                <DataGridTemplateColumn Header="操作" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="查看" 
                                        Command="{Binding DataContext.ViewDetailCommand, 
                                                 RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Padding="8,4"
                                        FontSize="12"
                                        Margin="2"/>
                                
                                <Button Content="看诊" 
                                        Command="{Binding DataContext.StartConsultationCommand, 
                                                 RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource BaseButtonStyle}"
                                        Background="{StaticResource AccentBrush}"
                                        Foreground="White"
                                        Padding="8,4"
                                        FontSize="12"
                                        Margin="2"/>
                                
                                <Button Content="编辑" 
                                        Command="{Binding DataContext.EditCommand, 
                                                 RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource SuccessButtonStyle}"
                                        Padding="8,4"
                                        FontSize="12"
                                        Margin="2"/>
                                
                                <Button Content="删除" 
                                        Command="{Binding DataContext.DeleteCommand, 
                                                 RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource DangerButtonStyle}"
                                        Padding="8,4"
                                        FontSize="12"
                                        Margin="2"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 分页控件 -->
        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Background="{StaticResource BackgroundBrush}" Margin="8">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 页面大小选择 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="每页显示:" VerticalAlignment="Center" Margin="4"/>
                    <ComboBox SelectedItem="{Binding PageSize}" Width="80" Margin="4">
                        <ComboBoxItem Content="10"/>
                        <ComboBoxItem Content="20"/>
                        <ComboBoxItem Content="50"/>
                        <ComboBoxItem Content="100"/>
                    </ComboBox>
                </StackPanel>

                <!-- 页码信息 -->
                <TextBlock Grid.Column="1" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           FontSize="14"
                           Foreground="{StaticResource TextSecondaryBrush}">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="第 {0} 页，共 {1} 条记录">
                            <Binding Path="CurrentPage"/>
                            <Binding Path="TotalCount"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>

                <!-- 分页按钮 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="上一页" 
                            Command="{Binding PreviousPageCommand}"
                            Style="{StaticResource BaseButtonStyle}"
                            Background="{StaticResource SecondaryBrush}"
                            Foreground="White"
                            Padding="12,6"
                            Margin="2"
                            FontSize="12"/>
                    <Button Content="下一页" 
                            Command="{Binding NextPageCommand}"
                            Style="{StaticResource BaseButtonStyle}"
                            Background="{StaticResource SecondaryBrush}"
                            Foreground="White"
                            Padding="12,6"
                            Margin="2"
                            FontSize="12"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 加载遮罩 -->
        <Grid Grid.Row="0" Grid.RowSpan="4" 
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
              Background="#80000000">
            <Border Background="{StaticResource CardBackgroundBrush}"
                    CornerRadius="5"
                    Padding="20"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <ProgressBar Width="20" Height="20"
                                 IsIndeterminate="True"
                                 Margin="0,0,10,0"/>
                    <TextBlock Text="加载中..." 
                               VerticalAlignment="Center"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>