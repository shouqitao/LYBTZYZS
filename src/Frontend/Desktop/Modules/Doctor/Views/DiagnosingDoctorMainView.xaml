<UserControl x:Class="LYBT.WPF.Client.Modules.Doctor.Views.DiagnosingDoctorMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">
    
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#4A90E2" Padding="20,15">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="诊疗工作台" FontSize="24" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    <TextBlock Text="Diagnosis Workstation" FontSize="14" Foreground="#B3D9F2" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="今日就诊：" Foreground="White" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding TodayPatientCount}" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    <TextBlock Text=" 人" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 主内容区域 -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 左侧功能区 -->
            <Grid Grid.Column="0" Margin="0,0,20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- 快捷功能 -->
                <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="快捷功能" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                        <Button Content="开始接诊" Height="45" Margin="0,0,0,10"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Command="{Binding StartConsultationCommand}"/>
                        <Button Content="查看候诊队列" Height="45" Margin="0,0,0,10"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding ViewQueueCommand}"/>
                        <Button Content="历史病历查询" Height="45"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding SearchRecordsCommand}"/>
                    </StackPanel>
                </Border>
                
                <!-- 今日统计 -->
                <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20">
                    <StackPanel>
                        <TextBlock Text="今日统计" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="已接诊：" Margin="0,0,0,10"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding CompletedCount}" FontWeight="Bold" Margin="0,0,0,10"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="待接诊：" Margin="0,0,0,10"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding WaitingCount}" FontWeight="Bold" Foreground="#FF9800" Margin="0,0,0,10"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="处方数：" Margin="0,0,0,10"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding PrescriptionCount}" FontWeight="Bold" Margin="0,0,0,10"/>
                            
                            <TextBlock Grid.Row="3" Grid.Column="0" Text="平均就诊时长："/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding AverageConsultationTime}" FontWeight="Bold"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
            
            <!-- 右侧内容区 -->
            <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="20">
                <ContentControl prism:RegionManager.RegionName="DoctorContentRegion">
                    <!-- 默认显示内容 -->
                    <Grid>
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="欢迎使用诊疗工作台" FontSize="24" Foreground="#999999" HorizontalAlignment="Center"/>
                            <TextBlock Text="请点击'开始接诊'按钮开始工作" FontSize="16" Foreground="#BBBBBB" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Grid>
                </ContentControl>
            </Border>
        </Grid>
    </Grid>
</UserControl>