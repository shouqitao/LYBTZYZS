<Window x:Class="LYBT.WPF.Client.Modules.SystemManagement.Users.Views.UserAddEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding WindowTitle}" 
        Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <!-- Boolean to Visibility Converter -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Inverted Boolean to Visibility Converter -->
        <Style x:Key="InvertedBooleanToVisibilityStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsNewUser}" Value="True">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- String to Visibility Style -->
        <Style x:Key="StringToVisibilityStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ValidationMessage}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ValidationMessage}" Value="">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="{Binding WindowTitle}" 
                   FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <!-- 表单内容 -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 用户名 -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="用户名：" 
                       VerticalAlignment="Center" Margin="0,0,10,10"/>
            <TextBox Grid.Row="0" Grid.Column="1" 
                     Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"
                     IsEnabled="{Binding IsNewUser}"
                     Margin="0,0,0,10" Height="25"/>

            <!-- 真实姓名 -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="真实姓名：" 
                       VerticalAlignment="Center" Margin="0,0,10,10"/>
            <TextBox Grid.Row="1" Grid.Column="1" 
                     Text="{Binding RealName, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10" Height="25"/>

            <!-- 角色 -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="角色：" 
                       VerticalAlignment="Center" Margin="0,0,10,10"/>
            <ComboBox Grid.Row="2" Grid.Column="1" 
                      ItemsSource="{Binding Roles}"
                      SelectedItem="{Binding SelectedRole}"
                      DisplayMemberPath="DisplayName"
                      SelectedValuePath="Value"
                      Margin="0,0,0,10" Height="25"/>

            <!-- 邮箱 -->
            <TextBlock Grid.Row="3" Grid.Column="0" Text="邮箱：" 
                       VerticalAlignment="Center" Margin="0,0,10,10"/>
            <TextBox Grid.Row="3" Grid.Column="1" 
                     Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10" Height="25"/>

            <!-- 电话 -->
            <TextBlock Grid.Row="4" Grid.Column="0" Text="电话：" 
                       VerticalAlignment="Center" Margin="0,0,10,10"/>
            <TextBox Grid.Row="4" Grid.Column="1" 
                     Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10" Height="25"/>

            <!-- 是否启用 -->
            <TextBlock Grid.Row="5" Grid.Column="0" Text="是否启用：" 
                       VerticalAlignment="Center" Margin="0,0,10,10"
                       Style="{StaticResource InvertedBooleanToVisibilityStyle}"/>
            <CheckBox Grid.Row="5" Grid.Column="1" 
                      IsChecked="{Binding IsActive}"
                      Content="启用"
                      VerticalAlignment="Center"
                      Margin="0,0,0,10"
                      Style="{StaticResource InvertedBooleanToVisibilityStyle}"/>

            <!-- 验证消息 -->
            <TextBlock Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                       Text="{Binding ValidationMessage}"
                       Foreground="Red"
                       TextWrapping="Wrap"
                       Margin="0,10,0,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ValidationMessage}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ValidationMessage}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <!-- 按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="确定" Width="80" Height="30" 
                    Command="{Binding SaveCommand}"
                    IsDefault="True" Margin="0,0,10,0"/>
            <Button Content="取消" Width="80" Height="30" 
                    Command="{Binding CancelCommand}"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>