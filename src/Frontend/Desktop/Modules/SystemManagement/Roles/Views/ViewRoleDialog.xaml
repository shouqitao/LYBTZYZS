<Window x:Class="LYBT.WPF.Client.Modules.SystemManagement.Roles.Views.ViewRoleDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        Title="角色详情" 
        Height="600" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <Border Grid.Row="0" Background="#2E86AB" Padding="20,15">
            <TextBlock Text="角色详细信息" FontSize="18" FontWeight="Bold" Foreground="White"/>
        </Border>

        <!-- 详情内容 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- 基本信息 -->
                <Border Background="#F8F9FA" CornerRadius="5" Padding="15" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="基本信息" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333"/>
                        
                        <!-- 角色名称 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="角色名称：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding Role.RoleName}" FontSize="16" FontWeight="Bold" Foreground="#2E86AB"/>
                        </Grid>

                        <!-- 角色类型 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="角色类型：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding RoleTypeDescription}" FontSize="14"/>
                        </Grid>

                        <!-- 角色状态 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="角色状态：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding Role.StatusDescription}" FontSize="14" FontWeight="Bold" Foreground="#28A745"/>
                        </Grid>

                        <!-- 用户数量 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="用户数量：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding Role.UserCountDescription}" FontSize="14"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 描述信息 -->
                <Border Background="#F8F9FA" CornerRadius="5" Padding="15" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="描述信息" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333"/>
                        
                        <!-- 角色描述 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="角色描述：" FontWeight="Bold" Foreground="#666" VerticalAlignment="Top"/>
                            <TextBlock Grid.Column="1" Text="{Binding Role.Description}" FontSize="14" TextWrapping="Wrap"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 权限信息 -->
                <Border Background="#F8F9FA" CornerRadius="5" Padding="15" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="权限信息" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333"/>
                        
                        <!-- 权限摘要 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="权限摘要：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding Role.PermissionSummary}" FontSize="14" FontWeight="Bold" Foreground="#007BFF"/>
                        </Grid>

                        <!-- 可访问模块 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="可访问模块：" FontWeight="Bold" Foreground="#666" VerticalAlignment="Top"/>
                            <TextBlock Grid.Column="1" Text="{Binding PermissionModulesText}" FontSize="14" TextWrapping="Wrap"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 时间信息 -->
                <Border Background="#F8F9FA" CornerRadius="5" Padding="15" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="时间信息" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333"/>
                        
                        <!-- 创建时间 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="创建时间：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding CreateTimeDescription}" FontSize="14"/>
                        </Grid>

                        <!-- 更新时间 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="更新时间：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding UpdateTimeDescription}" FontSize="14"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- 按钮栏 -->
        <Border Grid.Row="2" Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="0,1,0,0" Padding="20,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="打印" 
                        Command="{Binding PrintCommand}"
                        Width="100" Height="36"
                        Background="#17A2B8"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="0,0,10,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
                
                <Button Content="关闭" 
                        Command="{Binding CloseCommand}"
                        Width="100" Height="36"
                        Background="#6C757D"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        IsDefault="True">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>