<Window x:Class="LYBT.WPF.Client.Modules.SystemManagement.Records.Views.ViewRecordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        Title="病历详情" 
        Height="750" Width="650"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <Border Grid.Row="0" Background="#2E86AB" Padding="20,15">
            <TextBlock Text="病历详细信息" FontSize="18" FontWeight="Bold" Foreground="White"/>
        </Border>

        <!-- 加载指示器 -->
        <ProgressBar Grid.Row="1" IsIndeterminate="True" 
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                     Height="4" VerticalAlignment="Top"/>

        <!-- 详情内容 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <ScrollViewer.Style>
                <Style TargetType="ScrollViewer">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsLoading}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ScrollViewer.Style>
            
            <StackPanel>
                <!-- 基本信息 -->
                <Border Background="#F8F9FA" CornerRadius="5" Padding="15" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="基本信息" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333"/>
                        
                        <!-- 患者姓名 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="患者姓名：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding Record.PatientName}" FontSize="14"/>
                        </Grid>

                        <!-- 病历时间 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="病历时间：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding Record.RecordTime, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" FontSize="14"/>
                        </Grid>

                        <!-- 创建时间 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="创建时间：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding Record.CreateTime, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" FontSize="14"/>
                        </Grid>

                        <!-- 创建医生 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="创建医生：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding Record.CreatedBy}" FontSize="14"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 病情信息 -->
                <Border Background="#F8F9FA" CornerRadius="5" Padding="15" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="病情信息" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333"/>
                        
                        <!-- 主诉 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="主诉：" FontWeight="Bold" Foreground="#666" VerticalAlignment="Top"/>
                            <TextBlock Grid.Column="1" Text="{Binding Record.ChiefComplaint}" FontSize="14" TextWrapping="Wrap"/>
                        </Grid>

                        <!-- 现病史 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="现病史：" FontWeight="Bold" Foreground="#666" VerticalAlignment="Top"/>
                            <TextBlock Grid.Column="1" Text="{Binding Record.PresentIllness}" FontSize="14" TextWrapping="Wrap"/>
                        </Grid>

                        <!-- 诊断内容 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="诊断内容：" FontWeight="Bold" Foreground="#666" VerticalAlignment="Top"/>
                            <TextBlock Grid.Column="1" Text="{Binding Record.Diagnosis}" FontSize="14" TextWrapping="Wrap" FontWeight="Bold" Foreground="#2E86AB"/>
                        </Grid>

                        <!-- 辨证结果 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="辨证结果：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding DiagnosisResultsText}" FontSize="14" TextWrapping="Wrap"/>
                        </Grid>

                        <!-- 诊疗建议 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="诊疗建议：" FontWeight="Bold" Foreground="#666" VerticalAlignment="Top"/>
                            <TextBlock Grid.Column="1" Text="{Binding Record.TreatmentAdvice}" FontSize="14" TextWrapping="Wrap"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 处方信息 -->
                <Border Background="#F8F9FA" CornerRadius="5" Padding="15" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="处方信息" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333"/>
                        
                        <!-- 处方状态 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="处方状态：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding PrescriptionStatusText}" FontSize="14" FontWeight="Bold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Record.PrescriptionId, Converter={StaticResource NullToBooleanConverter}}" Value="False">
                                                <Setter Property="Foreground" Value="#28A745"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Record.PrescriptionId, Converter={StaticResource NullToBooleanConverter}}" Value="True">
                                                <Setter Property="Foreground" Value="#6C757D"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>

                        <!-- 验方模板 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="验方模板：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding FormulaTemplateText}" FontSize="14"/>
                        </Grid>

                        <!-- 药材组成 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="药材组成：" FontWeight="Bold" Foreground="#666" VerticalAlignment="Top"/>
                            <TextBlock Grid.Column="1" Text="{Binding HerbalFormulaText}" FontSize="14" TextWrapping="Wrap"/>
                        </Grid>

                        <!-- 治疗方案 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="治疗方案：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding TreatmentPlansText}" FontSize="14"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 共享信息 -->
                <Border Background="#F8F9FA" CornerRadius="5" Padding="15" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="共享信息" FontSize="16" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333"/>
                        
                        <!-- 共享状态 -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="共享状态：" FontWeight="Bold" Foreground="#666"/>
                            <TextBlock Grid.Column="1" Text="{Binding ShareStatusText}" FontSize="14" FontWeight="Bold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Record.IsShared}" Value="True">
                                                <Setter Property="Foreground" Value="#17A2B8"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Record.IsShared}" Value="False">
                                                <Setter Property="Foreground" Value="#6C757D"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- 按钮栏 -->
        <Border Grid.Row="2" Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="0,1,0,0" Padding="20,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="打印" 
                        Command="{Binding PrintCommand}"
                        Width="100" Height="36"
                        Background="#17A2B8"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="0,0,10,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
                
                <Button Content="导出" 
                        Command="{Binding ExportCommand}"
                        Width="100" Height="36"
                        Background="#FFC107"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="0,0,10,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
                
                <Button Content="关闭" 
                        Command="{Binding CloseCommand}"
                        Width="100" Height="36"
                        Background="#6C757D"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        IsDefault="True">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- NullToBooleanConverter -->
        <Style x:Key="NullToBooleanConverter" TargetType="FrameworkElement">
            <Style.Triggers>
                <DataTrigger Binding="{Binding}" Value="{x:Null}">
                    <Setter Property="Tag" Value="True"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
</Window>