<UserControl x:Class="LYBT.WPF.Client.Modules.SystemManagement.Patients.Views.PatientManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <!-- 使用应用程序统一样式 -->
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Background="{StaticResource BackgroundBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 搜索区域 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="搜索：" Style="{StaticResource SectionTitleStyle}"/>
                    <TextBox Style="{StaticResource ModernTextBoxStyle}"
                             Width="250"
                             Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <Button Content="搜索" 
                            Margin="10,0,0,0"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding SearchCommand}"/>
                </StackPanel>

                <!-- 操作按钮区域 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="+ 新增患者" 
                            Margin="0,0,10,0"
                            Style="{StaticResource SuccessButtonStyle}"
                            Command="{Binding AddCommand}"/>
                    <Button Content="刷新" 
                            Style="{StaticResource BaseButtonStyle}"
                            Background="{StaticResource SecondaryBrush}"
                            Foreground="White"
                            Command="{Binding RefreshCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 数据表格 -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem}"
                  Style="{StaticResource ModernDataGridStyle}"
                  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
            <DataGrid.Columns>
                <!-- 患者编号 -->
                <DataGridTextColumn Header="患者编号" 
                                    Binding="{Binding PatientCode}" 
                                    Width="100"/>
                
                <!-- 姓名 -->
                <DataGridTextColumn Header="姓名" 
                                    Binding="{Binding Name}" 
                                    Width="100"/>
                
                <!-- 性别 -->
                <DataGridTextColumn Header="性别" 
                                    Binding="{Binding GenderText}" 
                                    Width="60"/>
                
                <!-- 年龄 -->
                <DataGridTextColumn Header="年龄" 
                                    Binding="{Binding Age}" 
                                    Width="60"/>
                
                <!-- 手机号 -->
                <DataGridTextColumn Header="手机号" 
                                    Binding="{Binding PhoneNumber}" 
                                    Width="120"/>
                
                <!-- 身份证号 -->
                <DataGridTextColumn Header="身份证号" 
                                    Binding="{Binding IdNumber}" 
                                    Width="180"/>
                
                <!-- 地址 -->
                <DataGridTextColumn Header="地址" 
                                    Binding="{Binding Address}" 
                                    Width="200"/>
                
                <!-- 过敏史 -->
                <DataGridTextColumn Header="过敏史" 
                                    Binding="{Binding AllergyHistory}" 
                                    Width="150"/>
                
                <!-- 操作 -->
                <DataGridTemplateColumn Header="操作" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="查看" 
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Padding="8,4"
                                        FontSize="12"
                                        Margin="2"
                                        Command="{Binding DataContext.ViewCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="编辑" 
                                        Style="{StaticResource SuccessButtonStyle}"
                                        Padding="8,4"
                                        FontSize="12"
                                        Margin="2"
                                        Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="删除" 
                                        Style="{StaticResource DangerButtonStyle}"
                                        Padding="8,4"
                                        FontSize="12"
                                        Margin="2"
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 状态栏和分页 -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Background="{StaticResource BackgroundBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 统计信息 -->
                <TextBlock Grid.Column="0" 
                           VerticalAlignment="Center"
                           FontSize="14"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Text="{Binding StatusText}"/>

                <!-- 分页控件 -->
                <StackPanel Grid.Column="1" 
                            Orientation="Horizontal" 
                            VerticalAlignment="Center">
                    <Button Content="首页" 
                            Style="{StaticResource BaseButtonStyle}"
                            Background="{StaticResource SecondaryBrush}"
                            Foreground="White"
                            Padding="12,6"
                            Margin="2"
                            FontSize="12"
                            Command="{Binding FirstPageCommand}"/>
                    
                    <Button Content="上一页" 
                            Style="{StaticResource BaseButtonStyle}"
                            Background="{StaticResource SecondaryBrush}"
                            Foreground="White"
                            Padding="12,6"
                            Margin="2"
                            FontSize="12"
                            Command="{Binding PreviousPageCommand}"/>
                    
                    <Border Background="{StaticResource BorderBrush}"
                            CornerRadius="3"
                            Padding="16,6"
                            Margin="8,0">
                        <TextBlock FontWeight="Bold" FontSize="13">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0} / {1}">
                                    <Binding Path="CurrentPage"/>
                                    <Binding Path="TotalPages"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </Border>
                    
                    <Button Content="下一页" 
                            Style="{StaticResource BaseButtonStyle}"
                            Background="{StaticResource SecondaryBrush}"
                            Foreground="White"
                            Padding="12,6"
                            Margin="2"
                            FontSize="12"
                            Command="{Binding NextPageCommand}"/>
                    
                    <Button Content="末页" 
                            Style="{StaticResource BaseButtonStyle}"
                            Background="{StaticResource SecondaryBrush}"
                            Foreground="White"
                            Padding="12,6"
                            Margin="2"
                            FontSize="12"
                            Command="{Binding LastPageCommand}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>