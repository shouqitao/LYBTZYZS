<UserControl x:Class="LYBT.Desktop.Shell.Views.SimpleHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:LYBT.Desktop.Shell.ViewModels">
    
    <UserControl.DataContext>
        <vm:SimpleHomeViewModel />
    </UserControl.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#2E86AB" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="凌隐宝堂中医诊所" 
                              FontSize="24" 
                              FontWeight="Bold" 
                              Foreground="White"/>
                    <TextBlock Text="{Binding SubTitle}" 
                              FontSize="16" 
                              Foreground="#B3D9F2" 
                              VerticalAlignment="Center" 
                              Margin="20,0,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="{Binding WelcomeMessage}" 
                              Foreground="White" 
                              VerticalAlignment="Center" 
                              Margin="0,0,20,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主内容区 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="40">
                <!-- 系统管理入口 -->
                <Border Background="White" 
                       BorderBrush="#E0E0E0" 
                       BorderThickness="1" 
                       CornerRadius="5"
                       Margin="0,0,0,20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" 
                                  Text="系统管理中心" 
                                  FontSize="20" 
                                  FontWeight="Bold"
                                  Margin="20,15,20,10"/>
                        
                        <TextBlock Grid.Row="1" 
                                  Text="欢迎进入凌隐宝堂中医诊所管理系统" 
                                  FontSize="14" 
                                  Foreground="Gray"
                                  Margin="20,0,20,10"/>
                        
                        <!-- 快捷功能区 -->
                        <Grid Grid.Row="2" Margin="20,10,20,20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Text="基础功能" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                            
                            <UniformGrid Grid.Row="1" Rows="2" Columns="3">
                                <Border Background="#52C41A" CornerRadius="5" Margin="5" Cursor="Hand">
                                    <StackPanel Margin="15">
                                        <TextBlock Text="👤" FontSize="24" HorizontalAlignment="Center"/>
                                        <TextBlock Text="用户管理" FontSize="12" HorizontalAlignment="Center" Foreground="White" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </Border>
                                
                                <Border Background="#FAAD14" CornerRadius="5" Margin="5" Cursor="Hand">
                                    <StackPanel Margin="15">
                                        <TextBlock Text="👥" FontSize="24" HorizontalAlignment="Center"/>
                                        <TextBlock Text="患者档案" FontSize="12" HorizontalAlignment="Center" Foreground="White" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </Border>
                                
                                <Border Background="#2E86AB" CornerRadius="5" Margin="5" Cursor="Hand">
                                    <StackPanel Margin="15">
                                        <TextBlock Text="🌿" FontSize="24" HorizontalAlignment="Center"/>
                                        <TextBlock Text="药材管理" FontSize="12" HorizontalAlignment="Center" Foreground="White" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </Border>
                                
                                <Border Background="#722ED1" CornerRadius="5" Margin="5" Cursor="Hand">
                                    <StackPanel Margin="15">
                                        <TextBlock Text="📜" FontSize="24" HorizontalAlignment="Center"/>
                                        <TextBlock Text="验方管理" FontSize="12" HorizontalAlignment="Center" Foreground="White" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </Border>
                                
                                <Border Background="#F5222D" CornerRadius="5" Margin="5" Cursor="Hand">
                                    <StackPanel Margin="15">
                                        <TextBlock Text="⚙️" FontSize="24" HorizontalAlignment="Center"/>
                                        <TextBlock Text="系统设置" FontSize="12" HorizontalAlignment="Center" Foreground="White" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </Border>
                                
                                <Border Background="#1890FF" CornerRadius="5" Margin="5" Cursor="Hand">
                                    <StackPanel Margin="15">
                                        <TextBlock Text="💾" FontSize="24" HorizontalAlignment="Center"/>
                                        <TextBlock Text="数据备份" FontSize="12" HorizontalAlignment="Center" Foreground="White" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </Border>
                            </UniformGrid>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- 状态栏 -->
        <Border Grid.Row="2" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid Margin="10,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}"/>
                <TextBlock Grid.Column="2" Text="{Binding CurrentDateTime}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>