<Window x:Class="LYBT.WPF.Client.Shell.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" 
        Height="800" Width="1200" 
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="White">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <!-- 登录界面 -->
        <Grid Visibility="{Binding IsNotLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}}"
              Background="#F5F5F5">
            <Border Background="White" 
                    CornerRadius="8" 
                    Margin="0"
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Width="400"
                    Height="500">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="5" Opacity="0.3" BlurRadius="10"/>
                </Border.Effect>
                
                <!-- 登录内容区域 -->
                <ContentControl prism:RegionManager.RegionName="LoginRegion" Margin="0"/>
            </Border>
        </Grid>

        <!-- 登录后的主界面 -->
        <Grid Visibility="{Binding IsLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>

            <!-- 顶部工具栏 -->
            <Border Grid.Row="0" Background="#2E86AB" >
                <Grid Margin="20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="凌隐宝堂中医诊所管理系统" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding CurrentUser.RealName}" FontSize="14" Foreground="White" VerticalAlignment="Center" Margin="0,0,20,0"/>
                        <Button Content="退出登录" Background="Transparent" Foreground="White" BorderThickness="1" BorderBrush="White" 
                                Padding="10,5" FontSize="12" Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                                        BorderThickness="{TemplateBinding BorderThickness}" 
                                                        CornerRadius="3">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#40FFFFFF"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 主内容区域 -->
            <Grid Grid.Row="1" Background="#F8F9FA">
                <Border Background="White" Margin="20" CornerRadius="8">
                    <Border.Effect>
                        <DropShadowEffect Color="Gray" Direction="270" ShadowDepth="2" Opacity="0.2" BlurRadius="5"/>
                    </Border.Effect>
                    
                    <!-- 动态内容区域 -->
                    <ContentControl prism:RegionManager.RegionName="ContentRegion" Margin="20"/>
                </Border>
            </Grid>

            <!-- 底部状态栏 -->
            <Border Grid.Row="2" Background="#E9ECEF" BorderBrush="#DDD" BorderThickness="0,1,0,0">
                <Grid Margin="20,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="就绪" FontSize="12" Foreground="#666" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="2024-01-15 10:30:00" FontSize="12" Foreground="#666" VerticalAlignment="Center"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>

</Window>