<UserControl x:Class="LYBT.WPF.Client.Shell.Views.TestHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    
    <Grid Background="LightYellow">
        <ScrollViewer Margin="20">
            <StackPanel>
                <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
                <TextBlock Text="依赖注入调试信息：" FontSize="16" FontWeight="Bold"/>
                <TextBlock Text="{Binding DebugInfo}" FontSize="14" FontFamily="Consolas" Margin="0,10,0,0"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>