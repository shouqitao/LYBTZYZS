<Window x:Class="LYBT.Desktop.Shell.Dialogs.Views.ErrorDetailsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d" 
        Title="错误详情" 
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="400" MinWidth="600">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 错误摘要 -->
        <Border Grid.Row="0" 
                Background="#FFF3E0" 
                BorderBrush="#FF9800" 
                BorderThickness="1" 
                CornerRadius="4" 
                Padding="12" 
                Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="{Binding UserMessage}" 
                           FontWeight="SemiBold" 
                           FontSize="14" 
                           TextWrapping="Wrap" 
                           Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="错误类型: " FontWeight="SemiBold" Foreground="#666"/>
                    <TextBlock Text="{Binding Category}" Margin="0,0,16,0"/>
                    <TextBlock Text="严重程度: " FontWeight="SemiBold" Foreground="#666"/>
                    <TextBlock Text="{Binding Severity}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                    <TextBlock Text="发生时间: " FontWeight="SemiBold" Foreground="#666"/>
                    <TextBlock Text="{Binding Timestamp, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 建议操作 -->
        <GroupBox Grid.Row="1" Header="建议操作" Margin="0,0,0,16"
                  Visibility="{Binding HasSuggestedActions, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ItemsControl ItemsSource="{Binding SuggestedActions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,2">
                            <Ellipse Width="6" Height="6" Fill="#FF9800" Margin="0,2,8,0"/>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </GroupBox>

        <!-- 技术详情 -->
        <TabControl Grid.Row="2">
            <TabItem Header="异常详情">
                <ScrollViewer>
                    <TextBox Text="{Binding TechnicalDetails}"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             FontSize="11"
                             Background="#F8F8F8"
                             Padding="8"/>
                </ScrollViewer>
            </TabItem>
            
            <TabItem Header="上下文信息">
                <ScrollViewer>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="操作名称:" FontWeight="SemiBold" Margin="0,4"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Context.OperationName}" Margin="8,4"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="模块名称:" FontWeight="SemiBold" Margin="0,4"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Context.ModuleName}" Margin="8,4"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="视图名称:" FontWeight="SemiBold" Margin="0,4"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Context.ViewName}" Margin="8,4"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="用户:" FontWeight="SemiBold" Margin="0,4"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Context.Username}" Margin="8,4"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="错误ID:" FontWeight="SemiBold" Margin="0,4"/>
                        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Id}" IsReadOnly="True" 
                                 BorderThickness="0" Background="Transparent" Margin="8,4"
                                 FontFamily="Consolas" FontSize="11"/>

                        <TextBlock Grid.Row="5" Grid.Column="0" Text="附加数据:" FontWeight="SemiBold" Margin="0,4" VerticalAlignment="Top"/>
                        <ItemsControl Grid.Row="5" Grid.Column="1" ItemsSource="{Binding ContextData}" Margin="8,4">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,2">
                                        <TextBlock Text="{Binding Key}" FontWeight="SemiBold" MinWidth="100"/>
                                        <TextBlock Text=": " Margin="4,0"/>
                                        <TextBlock Text="{Binding Value}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- 分隔线 -->
        <Separator Grid.Row="3" Margin="0,16"/>

        <!-- 操作按钮 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="复制错误信息" 
                    Padding="12,6" 
                    Margin="0,0,12,0"
                    Command="{Binding CopyErrorCommand}"/>
            
            <Button Content="重试" 
                    Padding="12,6" 
                    Margin="0,0,12,0"
                    Command="{Binding RetryCommand}"
                    Visibility="{Binding CanRetry, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            
            <Button Content="关闭" 
                    Padding="12,6" 
                    IsDefault="True"
                    Command="{Binding CloseCommand}"/>
        </StackPanel>
    </Grid>
</Window>