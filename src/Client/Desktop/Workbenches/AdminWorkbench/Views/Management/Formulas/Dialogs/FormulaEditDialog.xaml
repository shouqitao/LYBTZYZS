<Window x:Class="LYBT.Desktop.Workbench.Admin.Views.Management.Formulas.Dialogs.FormulaEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="验方编辑" 
        Height="700" Width="800"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 验方名称 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="验方名称:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtName" Height="32" FontSize="14"/>
        </StackPanel>
        
        <!-- 功效 -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="功效:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtEffect" Height="32" FontSize="14"/>
        </StackPanel>
        
        <!-- 用法 -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="用法:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtUsage" Height="32" FontSize="14"/>
        </StackPanel>
        
        <!-- 适应症 -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="适应症:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtIndications" Height="60" FontSize="14" 
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>
        
        <!-- 禁忌症 -->
        <StackPanel Grid.Row="4" Margin="0,0,0,15">
            <TextBlock Text="禁忌症:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtContraindications" Height="60" FontSize="14" 
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>
        
        <!-- 制备方法 -->
        <StackPanel Grid.Row="5" Margin="0,0,0,15">
            <TextBlock Text="制备方法:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtPreparation" Height="60" FontSize="14" 
                     TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>
        
        <!-- 是否共享 -->
        <StackPanel Grid.Row="6" Margin="0,0,0,15">
            <CheckBox x:Name="ChkIsShared" Content="设为共享验方" FontSize="14"/>
        </StackPanel>
        
        <!-- 药材组成 -->
        <GroupBox Grid.Row="7" Header="药材组成" FontWeight="SemiBold" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- 添加药材按钮 -->
                <Button Grid.Row="0" x:Name="BtnAddHerb" Content="添加药材" 
                        HorizontalAlignment="Left" Margin="0,10,0,10"
                        Width="100" Height="30" Click="BtnAddHerb_Click"/>
                
                <!-- 药材列表 -->
                <DataGrid Grid.Row="1" x:Name="DgHerbs" AutoGenerateColumns="False" 
                          CanUserAddRows="False" Height="200">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="药材名称" Binding="{Binding HerbName}" Width="150" IsReadOnly="True"/>
                        <DataGridTextColumn Header="用量" Binding="{Binding Quantity}" Width="80"/>
                        <DataGridTextColumn Header="制法" Binding="{Binding Preparation}" Width="100"/>
                        <DataGridTextColumn Header="用法" Binding="{Binding Usage}" Width="100"/>
                        <DataGridTemplateColumn Header="操作" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="删除" Click="BtnRemoveHerb_Click" 
                                            Tag="{Binding}" Width="50" Height="25"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>
        
        <!-- 按钮区域 -->
        <StackPanel Grid.Row="8" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="BtnSave" Content="保存" Width="80" Height="35" 
                    Margin="0,0,10,0" Click="BtnSave_Click" IsDefault="True"/>
            <Button x:Name="BtnCancel" Content="取消" Width="80" Height="35" 
                    Click="BtnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>