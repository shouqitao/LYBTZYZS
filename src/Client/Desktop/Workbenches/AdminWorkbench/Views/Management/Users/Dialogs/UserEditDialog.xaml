<Window x:Class="LYBT.Desktop.Workbench.Admin.Views.Management.Users.Dialogs.UserEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="用户编辑" 
        Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 用户名 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="用户名:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtUsername" Height="32" FontSize="14"/>
        </StackPanel>
        
        <!-- 真实姓名 -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="真实姓名:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtRealName" Height="32" FontSize="14"/>
        </StackPanel>
        
        <!-- 电话号码 -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="电话号码:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtPhoneNumber" Height="32" FontSize="14"/>
        </StackPanel>
        
        <!-- 角色 -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="角色:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <ComboBox x:Name="CmbRole" Height="32" FontSize="14">
                <ComboBoxItem Content="Admin" Tag="Admin"/>
                <ComboBoxItem Content="Doctor" Tag="Doctor"/>
                <ComboBoxItem Content="Nurse" Tag="Nurse"/>
                <ComboBoxItem Content="User" Tag="User" IsSelected="True"/>
            </ComboBox>
        </StackPanel>
        
        <!-- 密码（仅新增时显示） -->
        <StackPanel Grid.Row="4" x:Name="PasswordPanel" Margin="0,0,0,15">
            <TextBlock Text="密码:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <PasswordBox x:Name="TxtPassword" Height="32" FontSize="14"/>
        </StackPanel>
        
        <!-- 确认密码（仅新增时显示） -->
        <StackPanel Grid.Row="5" x:Name="ConfirmPasswordPanel" Margin="0,0,0,15">
            <TextBlock Text="确认密码:" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <PasswordBox x:Name="TxtConfirmPassword" Height="32" FontSize="14"/>
        </StackPanel>
        
        <!-- 按钮区域 -->
        <StackPanel Grid.Row="7" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="BtnSave" Content="保存" Width="80" Height="35" 
                    Margin="0,0,10,0" Click="BtnSave_Click" IsDefault="True"/>
            <Button x:Name="BtnCancel" Content="取消" Width="80" Height="35" 
                    Click="BtnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>