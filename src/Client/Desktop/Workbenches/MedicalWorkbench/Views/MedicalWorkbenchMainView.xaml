<UserControl x:Class="LYBT.Desktop.Workbench.Medical.Views.MedicalWorkbenchMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:workflow="clr-namespace:LYBT.Desktop.Workbench.Medical.Views.Workflow"
             xmlns:management="clr-namespace:LYBT.Desktop.Workbench.Medical.Views.Management"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../../Themes/Design/Workbenches/WorkbenchStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Tab样式 -->
            <Style x:Key="WorkbenchTabItem" TargetType="TabItem">
                <Setter Property="Height" Value="45"/>
                <Setter Property="MinWidth" Value="150"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderThickness="0,0,0,3"
                                    BorderBrush="Transparent">
                                <ContentPresenter x:Name="ContentSite"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Center"
                                                  ContentSource="Header"
                                                  Margin="15,0"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Border" Property="BorderBrush" Value="#007BFF"/>
                                    <Setter Property="Foreground" Value="#007BFF"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#F0F4F8"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- 标题栏 -->
            <RowDefinition Height="*"/>    <!-- 内容区 -->
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="White" BorderBrush="#DEE2E6" BorderThickness="0,0,0,1">
            <Grid Height="60">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                    <TextBlock Text="诊疗工作台" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
                    <TextBlock Text="{Binding CurrentUserName}" FontSize="14" Foreground="#6C757D"
                              VerticalAlignment="Center" Margin="20,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="20,0">
                    <Button Content="📊 今日统计" Style="{StaticResource {x:Type Button}}"
                           Background="Transparent" BorderThickness="0" Margin="10,0"
                           Command="{Binding ShowStatisticsCommand}"/>
                    <Button Content="⚙️ 设置" Style="{StaticResource {x:Type Button}}"
                           Background="Transparent" BorderThickness="0" Margin="10,0"
                           Command="{Binding ShowSettingsCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主内容区 - 使用TabControl分离职责 -->
        <TabControl Grid.Row="1"
                   SelectedIndex="{Binding SelectedTabIndex}"
                   Background="White"
                   BorderThickness="0">

            <!-- Tab: 诊疗流程 -->
            <TabItem Header="诊疗流程" Style="{StaticResource WorkbenchTabItem}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🩺" FontSize="16" Margin="0,0,5,0"/>
                            <TextBlock Text="诊疗流程" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <workflow:MedicalWorkflowView/>
            </TabItem>

            <!-- Tab: 数据管理 -->
            <TabItem Header="数据管理" Style="{StaticResource WorkbenchTabItem}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📁" FontSize="16" Margin="0,0,5,0"/>
                            <TextBlock Text="数据管理" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <management:MedicalManagementView/>
            </TabItem>

            <!-- Tab: 快速查询 -->
            <TabItem Header="快速查询" Style="{StaticResource WorkbenchTabItem}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔍" FontSize="16" Margin="0,0,5,0"/>
                            <TextBlock Text="快速查询" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <ContentControl prism:RegionManager.RegionName="MedicalWorkbenchQueryRegion"/>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
