<Window x:Class="LYBT.Desktop.Shell.Views.SplashScreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="凌隐宝堂中医诊所"
        Height="440" Width="640"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.4" />
        </Storyboard>
    </Window.Resources>

    <Border CornerRadius="12" ClipToBounds="True">
        <Border.Background>
            <ImageBrush ImageSource="pack://application:,,,/LYBT.Desktop.Shell;component/Assets/Images/Backgrounds/img-login-background.jpg"
                        Stretch="UniformToFill" />
        </Border.Background>

        <Grid>
            <Border Background="#60000000" />

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image Source="pack://application:,,,/LYBT.Desktop.Shell;component/Assets/Images/logo.png"
                       Width="100" Height="100" Margin="0,0,0,20" HorizontalAlignment="Center" />

                <TextBlock Text="大医精诚"
                           FontFamily="pack://application:,,,/LYBT.Desktop.Shell;component/Assets/Fonts/#LiSu"
                           FontSize="64" FontWeight="Bold"
                           Foreground="#F5E6C8" HorizontalAlignment="Center" Margin="0,0,0,12">
                    <TextBlock.Effect>
                        <DropShadowEffect BlurRadius="12" Opacity="0.4" ShadowDepth="3" Color="Black" />
                    </TextBlock.Effect>
                </TextBlock>

                <TextBlock Text="凌隐宝堂中医诊所"
                           FontSize="18" Foreground="#D7CCC8" HorizontalAlignment="Center" Margin="0,0,0,40" />
            </StackPanel>

            <StackPanel VerticalAlignment="Bottom" Margin="0,0,0,44">
                <TextBlock x:Name="StatusText"
                           Text="正在初始化系统..."
                           FontSize="13" Foreground="#BCAAA4" HorizontalAlignment="Center" Margin="0,0,0,10" />
                <ProgressBar x:Name="ProgressBar"
                             Width="340" Height="4"
                             IsIndeterminate="True" HorizontalAlignment="Center">
                    <ProgressBar.Foreground>
                        <SolidColorBrush Color="#8D6E63" />
                    </ProgressBar.Foreground>
                    <ProgressBar.Background>
                        <SolidColorBrush Color="#33FFFFFF" />
                    </ProgressBar.Background>
                </ProgressBar>
            </StackPanel>

            <TextBlock Text="© 2026 凌隐宝堂 · 版本 1.0.0"
                       FontSize="11" Foreground="#80BCAAA4"
                       HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,16" />
        </Grid>
    </Border>
</Window>
