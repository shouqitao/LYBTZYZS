<Window x:Class="LYBT.Desktop.Shell.Views.SplashScreenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="凌隐宝堂"
        Height="280" Width="420"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.4" />
        </Storyboard>
    </Window.Resources>

    <Border Background="#FFFFFF" CornerRadius="12" BorderBrush="#E0E0E0" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.15" ShadowDepth="4" Color="Black" />
        </Border.Effect>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Image Source="pack://application:,,,/LYBT.Desktop.Shell;component/Assets/Images/logo.png"
                   Width="64" Height="64" Margin="0,0,0,16" HorizontalAlignment="Center" />

            <TextBlock Text="凌隐宝堂中医诊所"
                       FontSize="20" FontWeight="Bold"
                       Foreground="#5D4037" HorizontalAlignment="Center" Margin="0,0,0,24" />

            <ProgressBar x:Name="ProgressBar"
                         Width="280" Height="3"
                         IsIndeterminate="True" HorizontalAlignment="Center">
                <ProgressBar.Foreground><SolidColorBrush Color="#8D6E63" /></ProgressBar.Foreground>
                <ProgressBar.Background><SolidColorBrush Color="#F0F0F0" /></ProgressBar.Background>
            </ProgressBar>

            <TextBlock x:Name="StatusText"
                       Text="正在加载..."
                       FontSize="12" Foreground="#AAAAAA" HorizontalAlignment="Center" Margin="0,10,0,0" />
        </StackPanel>
    </Border>
</Window>
