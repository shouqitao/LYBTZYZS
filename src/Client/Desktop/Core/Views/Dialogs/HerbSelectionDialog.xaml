<Window x:Class="LYBT.Desktop.Core.Views.Dialogs.HerbSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="600"
        Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 搜索区域 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="搜索:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14" />
            <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                     Width="200" Height="25" FontSize="14" />
            <Button Content="搜索" Command="{Binding SearchCommand}"
                    Width="60" Height="25" Margin="10,0,0,0" />
        </StackPanel>

        <!-- 中药材列表 -->
        <Border Grid.Row="1" BorderBrush="#D1D1D1" BorderThickness="1">
            <ListView ItemsSource="{Binding Herbs}"
                      SelectedItem="{Binding SelectedHerb}"
                      GridViewColumnHeader.Click="ListView_HeaderClick">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="名称" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="规格" Width="100" DisplayMemberBinding="{Binding Specification}" />
                        <GridViewColumn Header="单位" Width="60" DisplayMemberBinding="{Binding Unit}" />
                        <GridViewColumn Header="单价" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Price, StringFormat=F2}" HorizontalAlignment="Right" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="库存" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Stock}" HorizontalAlignment="Right" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="备注" Width="120" DisplayMemberBinding="{Binding Remark}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <!-- 数量设置 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,15,0,15">
            <TextBlock Text="用量:" VerticalAlignment="Center" FontSize="14" Margin="0,0,10,0" />
            <TextBox Text="{Binding Quantity}" Width="80" Height="25" FontSize="14"
                     HorizontalContentAlignment="Right" />
            <TextBlock Text="{Binding Unit}" VerticalAlignment="Center" FontSize="14"
                       Margin="5,0,0,0" />
        </StackPanel>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定"
                    Command="{Binding ConfirmCommand}"
                    Width="80" Height="30"
                    Margin="0,0,10,0"
                    IsDefault="True"
                    Background="#0078D4"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand" />

            <Button Content="取消"
                    Command="{Binding CancelCommand}"
                    Width="80" Height="30"
                    IsCancel="True"
                    Background="#F0F0F0"
                    Foreground="#323130"
                    BorderThickness="1"
                    BorderBrush="#D1D1D1"
                    Cursor="Hand" />
        </StackPanel>

        <!-- 加载指示器 -->
        <Border Grid.Row="0" Grid.RowSpan="4"
                Background="#80000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Margin="0,0,0,10" />
                <TextBlock Text="正在加载..." FontSize="14" Foreground="White" HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
