<Window x:Class="LYBT.Desktop.Core.Views.Dialogs.FormulaSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" 
        Width="800" 
        Height="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 搜索区域 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="搜索:" VerticalAlignment="Center" Margin="0,0,10,0" FontSize="14"/>
            <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                     Width="300" Height="25" FontSize="14"/>
            <Button Content="搜索" Command="{Binding SearchCommand}" 
                    Width="60" Height="25" Margin="10,0,0,0"/>
        </StackPanel>
        
        <!-- 验方列表 -->
        <Border Grid.Row="1" BorderBrush="#D1D1D1" BorderThickness="1">
            <ListView ItemsSource="{Binding Formulas}" 
                      SelectedItem="{Binding SelectedFormula}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="验方名称" Width="180">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="分类" Width="120" DisplayMemberBinding="{Binding Category}"/>
                        <GridViewColumn Header="功效" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Efficacy}" TextWrapping="Wrap" MaxWidth="190"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="组成" Width="250">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Composition}" TextWrapping="Wrap" MaxWidth="240"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="状态" Width="60">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock FontWeight="Bold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                        <Setter Property="Text" Value="启用"/>
                                                        <Setter Property="Foreground" Value="Green"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Text" Value="禁用"/>
                                                        <Setter Property="Foreground" Value="Red"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
                
                <!-- 列表样式 -->
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F8FF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#0078D4"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Border>
        
        <!-- 详细信息 -->
        <Expander Grid.Row="2" Header="验方详情" IsExpanded="True" Margin="0,15,0,15">
            <Border BorderBrush="#D1D1D1" BorderThickness="1" Padding="10" Background="White">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="组成:" FontWeight="Bold" 
                               Margin="0,0,10,5" VerticalAlignment="Top"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedFormula.Composition}" 
                               TextWrapping="Wrap" Margin="0,0,0,5"/>
                    
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="用法:" FontWeight="Bold" 
                               Margin="0,0,10,5" VerticalAlignment="Top"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedFormula.Usage}" 
                               TextWrapping="Wrap" Margin="0,0,0,5"/>
                    
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="禁忌:" FontWeight="Bold" 
                               Margin="0,0,10,5" VerticalAlignment="Top"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedFormula.Contraindications}" 
                               TextWrapping="Wrap" Margin="0,0,0,5"/>
                    
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="备注:" FontWeight="Bold" 
                               Margin="0,0,10,0" VerticalAlignment="Top"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedFormula.Remark}" 
                               TextWrapping="Wrap"/>
                </Grid>
            </Border>
        </Expander>
        
        <!-- 按钮区域 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" 
                    Command="{Binding ConfirmCommand}"
                    Width="80" Height="30"
                    Margin="0,0,10,0"
                    IsDefault="True"
                    Background="#0078D4"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CanConfirm}" Value="False">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Setter Property="Background" Value="#CCCCCC"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            
            <Button Content="取消" 
                    Command="{Binding CancelCommand}"
                    Width="80" Height="30"
                    IsCancel="True"
                    Background="#F0F0F0"
                    Foreground="#323130"
                    BorderThickness="1"
                    BorderBrush="#D1D1D1"
                    Cursor="Hand"/>
        </StackPanel>
        
        <!-- 加载指示器 -->
        <Border Grid.Row="0" Grid.RowSpan="4" 
                Background="#80000000" 
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Margin="0,0,0,10"/>
                <TextBlock Text="正在加载..." FontSize="14" Foreground="White" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>