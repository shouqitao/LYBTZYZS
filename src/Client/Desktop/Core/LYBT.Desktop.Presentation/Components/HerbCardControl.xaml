<UserControl x:Class="LYBT.Desktop.Presentation.Components.HerbCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="280">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <!-- 单行布局: 药材名 + 剂量 + 删除按钮 -->
    <Border BorderBrush="#E0E0E0"
            BorderThickness="1"
            CornerRadius="4"
            Background="White"
            Margin="3"
            Padding="6,4">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>      <!-- 药材名称 -->
                <ColumnDefinition Width="Auto"/>   <!-- 剂量区域 -->
                <ColumnDefinition Width="Auto"/>   <!-- 删除按钮 -->
            </Grid.ColumnDefinitions>

            <!-- Column 0: 药材名称输入框 -->
            <TextBox x:Name="HerbNameTextBox"
                     Grid.Column="0"
                     Text="{Binding HerbName, UpdateSourceTrigger=PropertyChanged}"
                     PreviewKeyDown="OnTextBoxPreviewKeyDown"
                     TextChanged="OnTextBoxTextChanged"
                     GotFocus="OnTextBoxGotFocus"
                     LostFocus="OnTextBoxLostFocus"
                     IsEnabled="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsEditMode}"
                     FontSize="13"
                     Height="26"
                     MinWidth="80"
                     Padding="4,2"
                     VerticalContentAlignment="Center"/>

            <!-- Column 1: 剂量区域 -->
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Margin="8,0,0,0">
                <TextBox x:Name="DosageTextBox"
                         Width="45"
                         Height="26"
                         Text="{Binding Dosage, UpdateSourceTrigger=PropertyChanged, StringFormat=F1}"
                         PreviewKeyDown="OnDosageKeyDown"
                         GotFocus="OnDosageGotFocus"
                         IsEnabled="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsEditMode}"
                         FontSize="12"
                         Padding="2"
                         VerticalContentAlignment="Center"
                         HorizontalContentAlignment="Right"/>
                <TextBlock Text="{Binding Unit, FallbackValue=g}"
                          VerticalAlignment="Center"
                          Margin="2,0,0,0"
                          FontSize="12"
                          Foreground="#666"/>
            </StackPanel>

            <!-- Column 2: 删除按钮 -->
            <Button x:Name="DeleteButton"
                    Grid.Column="2"
                    Content="x"
                    Width="22"
                    Height="22"
                    FontSize="12"
                    FontWeight="Bold"
                    Foreground="#DC3545"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    Margin="6,0,0,0"
                    Click="OnDeleteButtonClick"
                    Visibility="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}"
                    ToolTip="删除">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="11">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFF0F0"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- 建议列表Popup (挂在药材名称输入框下方) -->
            <Popup x:Name="SuggestionsPopup"
                   PlacementTarget="{Binding ElementName=HerbNameTextBox}"
                   Placement="Bottom"
                   StaysOpen="True"
                   AllowsTransparency="True"
                   PopupAnimation="Slide">
                <Border Background="White"
                        BorderBrush="#E0E0E0"
                        BorderThickness="1"
                        MaxHeight="200"
                        MinWidth="{Binding ActualWidth, ElementName=HerbNameTextBox}">
                    <ListBox x:Name="SuggestionsListBox"
                             ItemsSource="{Binding FilteredHerbs}"
                             DisplayMemberPath="Name"
                             PreviewMouseDown="OnListBoxPreviewMouseDown"
                             BorderThickness="0"
                             Background="Transparent"
                             Focusable="False"
                             IsHitTestVisible="True">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="8,4"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F0F0F0"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E3F2FD"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </Border>
            </Popup>

            <!-- 价格显示区域 (可选，通过ShowPrice控制，放在剂量后面) -->
            <StackPanel x:Name="PricePanel"
                       Grid.Column="1"
                       Orientation="Horizontal"
                       VerticalAlignment="Center"
                       Margin="60,0,0,0"
                       Visibility="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ShowPrice, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="¥"
                          VerticalAlignment="Center"
                          FontSize="11"
                          Foreground="#666"/>
                <TextBlock Text="{Binding UnitPrice, StringFormat=F2}"
                          VerticalAlignment="Center"
                          FontSize="11"
                          Foreground="#2196F3"
                          FontWeight="SemiBold"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
