<UserControl x:Class="LYBT.Desktop.Presentation.Components.PatientSelector.PatientSelectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:components="clr-namespace:LYBT.Desktop.Presentation.Components.PatientSelector"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="500">
    
      
    <UserControl.Resources>
        <!-- 转换器资源 -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- 样式资源 -->
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            Background="White"
                            CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- 搜索图标 -->
                                <Border Grid.Column="0" 
                                   Background="#F5F5F5"
                                   Padding="12,0"
                                   BorderBrush="#DDD"
                                   BorderThickness="0,0,1,0">
                                    <Path Data="M9.5,3A6.5,6.5 0 0,1 16,9.5C16,11.11 15.41,12.59 14.44,13.73L14.71,14C15.55,12.89 16,11.5 16,10A6,6 0 0,0 10,4C4,4 1,7 1,13S4,22 10,22C16,22 19,19 19,13H17A5,5 0 0,1 12,18C7.58,18 4,14.42 4,10S7.58,2 12,2A5,5 0 0,1 17,7H19.5L16,10.5L12.5,6.5H15A6,6 0 0,0 9.5,3Z"
                                      Fill="#666"
                                      Width="16"
                                      Height="16"
                                      Stretch="Uniform"/>
                                </Border>
                                
                                <!-- 文本输入区域 -->
                                <ScrollViewer Grid.Column="1" 
                                         x:Name="PART_ContentHost"
                                         Margin="12,0"
                                         VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="PatientListBoxStyle" TargetType="ListBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
        </Style>
        
        <Style x:Key="PatientItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0,0,0,0"/>
            <Setter Property="BorderBrush" Value="#EEE"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F8F9FA"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                                <Setter Property="BorderThickness" Value="2,0,0,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                            CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 搜索区域 -->
        <Border Grid.Row="0" 
                Background="White"
                Padding="16"
                BorderBrush="#EEE"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- 搜索输入框 -->
                <TextBox Grid.Column="0"
                         Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource SearchTextBoxStyle}"
                         VerticalAlignment="Center"/>
                
                <!-- 快速创建按钮 -->
                <Button Grid.Column="1"
                        Content="+ 快速创建"
                        Margin="8,0,0,0"
                        Background="#4CAF50"
                        Style="{StaticResource ActionButtonStyle}"
                        Command="{Binding ToggleQuickCreateCommand}"/>
            </Grid>
        </Border>
        
        <!-- 搜索结果区域 -->
        <Border Grid.Row="1">
            <Grid>
                <!-- 结果列表 -->
                <ListBox x:Name="ResultsList"
                         ItemsSource="{Binding SearchResults}"
                         SelectedItem="{Binding SelectedPatient}"
                         Style="{StaticResource PatientListBoxStyle}"
                         ItemContainerStyle="{StaticResource PatientItemStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0" Orientation="Vertical">
                                    <TextBlock Text="{Binding Name}"
                                             FontSize="14"
                                             FontWeight="Medium"
                                             Foreground="#333"/>
                                    <TextBlock FontSize="12" Foreground="#666">
                                        <Run Text="{Binding Gender}"/>
                                        <Run Text=" / "/>
                                        <Run Text="{Binding Age}"/>
                                        <Run Text="岁"/>
                                    </TextBlock>
                                    <TextBlock Text="{Binding PhoneNumber}"
                                             FontSize="12"
                                             Foreground="#999"
                                             Margin="0,2,0,0"/>
                                </StackPanel>
                                
                                <TextBlock Grid.Column="1"
                                         Text="选择 →"
                                         FontSize="12"
                                         Foreground="#2196F3"
                                         VerticalAlignment="Center"
                                         Margin="8,0,0,0"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                
                <!-- 无结果提示 -->
                <TextBlock Text="未找到匹配的患者，请尝试其他搜索词或快速创建新患者"
                         FontSize="14"
                         Foreground="#999"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         TextWrapping="Wrap"
                         Margin="16"
                         Visibility="{Binding HasNoResults, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </Grid>
        </Border>
        
        <!-- 快速创建面板 -->
        <Border Grid.Row="2"
                Background="#F8F9FA"
                Padding="16"
                BorderBrush="#EEE"
                BorderThickness="0,1,0,0"
                Visibility="{Binding ShowQuickCreate, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- 标题 -->
                <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="5"
                         Text="快速创建新患者"
                         FontSize="14"
                         FontWeight="Medium"
                         Foreground="#333"/>
                
                <!-- 姓名 -->
                <StackPanel Grid.Row="2" Grid.Column="0">
                    <TextBlock Text="姓名" FontSize="12" Foreground="#666" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding NewPatientName, UpdateSourceTrigger=PropertyChanged}"
                             Padding="8,6"
                             BorderBrush="#DDD"
                             BorderThickness="1"
                             Background="White"/>
                </StackPanel>
                
                <!-- 性别 -->
                <StackPanel Grid.Row="2" Grid.Column="2">
                    <TextBlock Text="性别" FontSize="12" Foreground="#666" Margin="0,0,0,4"/>
                    <ComboBox SelectedItem="{Binding NewPatientGender}"
                             Padding="8,6"
                             BorderBrush="#DDD"
                             BorderThickness="1"
                             Background="White">
                        <ComboBoxItem Content="男"/>
                        <ComboBoxItem Content="女"/>
                    </ComboBox>
                </StackPanel>
                
                <!-- 手机号 -->
                <StackPanel Grid.Row="2" Grid.Column="4">
                    <TextBlock Text="手机号" FontSize="12" Foreground="#666" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding NewPatientPhone, UpdateSourceTrigger=PropertyChanged}"
                             Padding="8,6"
                             BorderBrush="#DDD"
                             BorderThickness="1"
                             Background="White"/>
                </StackPanel>
                
                <!-- 创建按钮 -->
                <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="5"
                           Orientation="Horizontal"
                           HorizontalAlignment="Right">
                    <Button Content="取消"
                            Background="#6C757D"
                            Margin="0,0,8,0"
                            Style="{StaticResource ActionButtonStyle}"
                            Command="{Binding ToggleQuickCreateCommand}"/>
                    <Button Content="创建患者"
                            Background="#2196F3"
                            Style="{StaticResource ActionButtonStyle}"
                            Command="{Binding QuickCreateCommand}"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 错误消息 -->
        <Border Grid.Row="0" Grid.RowSpan="3"
                Background="#FFEBEE"
                CornerRadius="4"
                Padding="12,8"
                Margin="16"
                VerticalAlignment="Top"
                HorizontalAlignment="Center"
                Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}"
                     Foreground="#D32F2F"
                     FontSize="13"
                     TextWrapping="Wrap"/>
        </Border>
        
        <!-- 加载遮罩 -->
        <Grid Grid.Row="0" Grid.RowSpan="3"
              Background="#80000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="White"
                    CornerRadius="8"
                    Padding="24"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <StackPanel>
                    <ProgressBar IsIndeterminate="True"
                                Width="200"
                                Height="4"/>
                    <TextBlock Text="搜索中..."
                             FontSize="14"
                             HorizontalAlignment="Center"
                             Margin="0,12,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>