# LYBT.Desktop.Core v2.1 - 桌面客户端核心基础设施

> **WPF框架** | Prism.DryIoc 模块化 | MVVM 标准化
> 项目状态：✅ 生产可用 | 🎆 2025-09-02 架构重构完成 | 📐 标准库

## 🎯 模块定位

LYBT.Desktop.Core 为凌隐宝堂中医诊所系统的 WPF 客户端提供统一的技术底座。模块专注于“可复用的基础能力”，把通用的 MVVM 支撑、配置体系、主题与对话框服务集中在一个可维护的包中，供 8 个业务模块与多个工作台复用。

**核心价值**
- 🏗️ 架构统一：提供 CoreViewModel 等基类，确保业务模块遵循一致的 MVVM 规范。
- ⚡ 开发提速：内置通知、错误处理、导航、配置等公共服务，避免重复封装。
- 🎨 一致体验：主题、对话框与全局状态栏等 UI 组件保持视觉与交互一致。
- 🧩 易于扩展：目录与命名遵循约定，可按需在业务侧扩充特定能力。

## 核心功能

### 🏗️ 架构基础
- Prism.DryIoc 依赖注入与模块化导航。
- CoreViewModel / ServiceViewModel 等基类封装事件聚合、异步命令、统一错误处理。
- DialogViewModelBase、NavigationViewModel 等扩展基类，覆盖对话框、区域导航等场景。

### ⚙️ 配置与跨切服务
- 、 等多环境配置读取与热更新。
- 、、、 等跨模块公共服务。
- 关键服务注册集中在 Shell 的 ，便于统一维护与演进。

### 🎨 UI 与资源
- 全局状态栏、认证流程控件、错误提示窗体等通用 UI 组件。
- VirtualizedDataGrid / VirtualizedListView 等虚拟化控件，支撑大数据量展示。
- 丰富的数值、日期、状态转换器，保障绑定输出一致。

## 📦 项目结构概览



> 业务领域 ViewModel 与专用控件已迁回业务模块，Core 仅保留跨模块复用的基础设施。

## 🛠 技术栈
- .NET 8.0 + WPF
- Prism.DryIoc 8.1.97（模块化 / 依赖注入）
- AutoMapper 15、FluentValidation 12、Polly 8、Newtonsoft.Json 13 等通用库
- Microsoft.Extensions.Configuration 系列（配置绑定与热加载）

## ⚡ 推荐用法

### 继承基类


### 注册公共服务


### 分页与搜索协调


## 🔒 维护建议
1. 修改 Core 代码前评估受影响模块，必要时同步联调。
2. 新增或调整公共接口需保持向后兼容，并更新文档。
3. 与外部依赖相关的调整需同步更新 。
4. 跨模块行为变更必须附带单元 / 集成测试验证。

## 📚 相关资料
- 
- 
- 

> Core README 将持续反映实际实现状态，如发现偏差请在同一 PR 中同步修正。
