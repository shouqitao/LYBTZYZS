<!--
    LoadingOverlay - 加载遮罩控件

    功能：
    - 半透明遮罩层
    - 加载进度指示器
    - 可自定义加载文本

    使用方式：
    <Grid>
        ... 内容 ...
        <controls:LoadingOverlay
            IsLoading="{Binding IsLoading}"
            LoadingText="正在加载..."/>
    </Grid>-->
<UserControl
    x:Class="LYBT.Desktop.Controls.Controls.LoadingOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:LYBT.Desktop.Controls.Converters"
    x:Name="Root">

    <!--  转换器使用x:Static模式  -->

    <Grid Background="#20000000" Visibility="{Binding IsOverlayVisible, ElementName=Root, Converter={x:Static converters:Cvt.BoolToVis}}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <!--  加载动画  -->
            <ProgressBar
                Width="200"
                Height="4"
                BorderThickness="0"
                IsIndeterminate="True">
                <ProgressBar.Style>
                    <Style TargetType="ProgressBar">
                        <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Primary}" />
                        <Setter Property="Background" Value="{DynamicResource MaterialDesign.Brush.Outline}" />
                    </Style>
                </ProgressBar.Style>
            </ProgressBar>

            <!--  加载文本  -->
            <TextBlock
                Margin="0,16,0,0"
                HorizontalAlignment="Center"
                FontSize="14"
                Foreground="{DynamicResource MaterialDesign.Brush.BodyLight}"
                Text="{Binding LoadingText, ElementName=Root}" />
        </StackPanel>
    </Grid>
</UserControl>
