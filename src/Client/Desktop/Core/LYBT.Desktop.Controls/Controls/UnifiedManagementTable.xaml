<UserControl x:Class="LYBT.Desktop.Controls.Controls.UnifiedManagementTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:LYBT.Desktop.Controls.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- 转换器使用x:Static模式 -->

    <!-- Issue #2160: 添加Ctrl+A全选快捷键 -->
    <UserControl.InputBindings>
        <KeyBinding Key="A" Modifiers="Control" Command="{Binding SelectAllCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />
    </UserControl.InputBindings>

    <Grid>
        <!-- 数据表格-->
        <DataGrid x:Name="DataGrid"
                  ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="None"
                  HeadersVisibility="Column"
                  BorderThickness="0"
                  Background="Transparent" />

        <!-- 空状态提示 -->
        <Border Background="{DynamicResource MaterialDesignPaper}"
                Visibility="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={x:Static converters:Cvt.InverseNullToVis}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="📋"
                           FontSize="{StaticResource FontSizeXXL}"
                           HorizontalAlignment="Center"
                             Foreground="{DynamicResource MaterialDesignBodyLight}"
                           Margin="{StaticResource SpacingMD}" />
                <TextBlock Text="{Binding EmptyStateText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontSize="{StaticResource FontSizeMD}"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
