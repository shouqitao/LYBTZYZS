<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--  主功能卡片渐变（暗色值 = 亮色键名 + "Dark"，由 ThemeService 主题切换时应用）  -->
    <Color x:Key="PrimaryFunctionGradientStartColor">#D7CCC8</Color>
    <Color x:Key="PrimaryFunctionGradientEndColor">#4E342E</Color>
    <Color x:Key="PrimaryFunctionGradientStartColorDark">#4E342E</Color>
    <Color x:Key="PrimaryFunctionGradientEndColorDark">#211512</Color>

    <!--  功能卡片基础样式  -->
    <Style x:Key="FunctionCardStyle" TargetType="Border">
        <Setter Property="MinWidth" Value="200" />
        <Setter Property="MinHeight" Value="180" />
        <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesign.Brush.Outline}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="8" Opacity="0.1" ShadowDepth="2" Color="Black" />
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}" />
                <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesign.Brush.Primary}" />
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="16" Opacity="0.2" ShadowDepth="4" Color="{DynamicResource MaterialDesign.Brush.Primary.Dark}" />
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--  主功能卡片样式（渐变背景）  -->
    <Style x:Key="PrimaryFunctionCardStyle" TargetType="Border">
        <Setter Property="MinWidth" Value="320" />
        <Setter Property="MinHeight" Value="200" />
        <Setter Property="CornerRadius" Value="16" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="12" Opacity="0.25" ShadowDepth="3" Color="{DynamicResource MaterialDesign.Brush.Primary.Dark}" />
            </Setter.Value>
        </Setter>
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Offset="0" Color="{DynamicResource PrimaryFunctionGradientStartColor}" />
                    <GradientStop Offset="1" Color="{DynamicResource PrimaryFunctionGradientEndColor}" />
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  统计卡片样式  -->
    <Style x:Key="StatsCardStyle" TargetType="Border">
        <Setter Property="MinWidth" Value="200" />
        <Setter Property="MinHeight" Value="200" />
        <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}" />
        <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesign.Brush.Outline}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="8" Opacity="0.1" ShadowDepth="2" Color="Black" />
            </Setter.Value>
        </Setter>
    </Style>

    <!--  卡片图标样式  -->
    <Style x:Key="CardIconStyle" TargetType="Path">
        <Setter Property="Width" Value="48" />
        <Setter Property="Height" Value="48" />
        <Setter Property="Stretch" Value="Uniform" />
        <Setter Property="Fill" Value="{DynamicResource MaterialDesign.Brush.Primary}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Margin" Value="0,0,0,16" />
    </Style>

    <!--  卡片标题样式  -->
    <Style x:Key="CardTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
    </Style>

</ResourceDictionary>
