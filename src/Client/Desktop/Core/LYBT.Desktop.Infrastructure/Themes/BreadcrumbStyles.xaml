<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:LYBT.Desktop.Infrastructure.Converters"
                    xmlns:nav="clr-namespace:LYBT.Desktop.Infrastructure.Navigation">

    <!--
        ====================================================================
        Breadcrumb Control - Phase 2.1: Navigation Improvements
        面包屑导航控件 - 显示导航层级路径

        Usage:
        <nav:BreadcrumbControl ItemsSource="{Binding Breadcrumbs}"/>
        ====================================================================
    -->

    <!-- Breadcrumb Item Container Style -->
    <Style x:Key="BreadcrumbItemContainerStyle" TargetType="ContentControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <StackPanel Orientation="Horizontal" Margin="0,0,8,0">
                        <ContentPresenter/>
                        <TextBlock Text="›"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   Margin="8,0,8,0"
                                   FontSize="16"
                                   VerticalAlignment="Center"
                                   x:Name="Separator">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsActive, RelativeSource={RelativeSource AncestorType=ContentControl, Mode=FindAncestor, AncestorLevel=2}}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Breadcrumb Button Style (Non-Active) -->
    <Style x:Key="BreadcrumbButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                <VisualState x:Name="MouseOver">
                                    <VisualState.Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BreadcrumbBorder"
                                                        Storyboard.TargetProperty="(Border.Background).Color"
                                                        To="{DynamicResource HoverBackgroundBrush}"
                                                        Duration="0:0:0.2"/>
                                    </VisualState.Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <VisualState.Storyboard>
                                        <ColorAnimation Storyboard.TargetName="BreadcrumbBorder"
                                                        Storyboard.TargetProperty="(Border.Background).Color"
                                                        To="{DynamicResource PressedBackgroundBrush}"
                                                        Duration="0:0:0.1"/>
                                    </VisualState.Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Breadcrumb Text Style (Active) -->
    <Style x:Key="BreadcrumbTextStyle_Active" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- Breadcrumb Control Template -->
    <Style x:Key="BreadcrumbControlStyle" TargetType="ItemsControl">
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" Margin="16,12,16,12"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
    </Style>

</ResourceDictionary>
