<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        ====================================================================
        Validation Styles - 验证样式系统
        OpenSpec: ui-validation-framework
        
        提供表单验证相关的UI样式：
        - 必填标识样式
        - 验证错误消息样式
        - 带验证的输入控件样式
        ====================================================================
    -->

    <!--  错误颜色 - 使用主题资源  -->
    <SolidColorBrush x:Key="ValidationErrorBrush" Color="{DynamicResource DangerColor}" />
    <SolidColorBrush x:Key="ValidationErrorBackgroundBrush" Color="#FFF5F5" />

    <!--
        必填星号样式 - 用于标签后的必填标识
        使用方式:
        <TextBlock>
            <Run Text="用户名"/>
            <Run Text=" *" Style="{StaticResource RequiredIndicatorStyle}"/>
        </TextBlock>
    -->
    <Style x:Key="RequiredIndicatorStyle" TargetType="Run">
        <Setter Property="Foreground" Value="{StaticResource ValidationErrorBrush}" />
        <Setter Property="FontWeight" Value="Bold" />
    </Style>

    <!--
        验证错误消息样式 - 显示在输入框下方
        使用方式:
        <TextBlock Text="{Binding Errors[PropertyName]}"
                   Style="{StaticResource ValidationErrorMessageStyle}"/>
    -->
    <Style x:Key="ValidationErrorMessageStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource ValidationErrorBrush}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Margin" Value="0,4,0,0" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Visibility" Value="Collapsed" />
        <Style.Triggers>
            <!--  当Text不为空时显示  -->
            <Trigger Property="Text" Value="">
                <Setter Property="Visibility" Value="Collapsed" />
            </Trigger>
            <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="">
                <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource Self}}" Value="{x:Null}">
                <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!--
        验证错误消息样式（始终可见版本）
        用于需要始终显示区域的场景
    -->
    <Style x:Key="ValidationErrorMessageVisibleStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource ValidationErrorBrush}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Margin" Value="0,4,0,0" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="MinHeight" Value="16" />
    </Style>

    <!--
        ValidatingTextBoxStyle 已迁移至 UnifiedComponents.xaml
        以解决资源加载顺序问题（需要在EditableTextBoxStyle之后定义）
        OpenSpec: ui-validation-framework
    -->

    <!--
        ====================================================================
        Phase 1.3: 字段验证成功指示器样式
        用于显示字段验证通过后的视觉反馈（绿色对勾）
        ====================================================================
    -->

    <!--
        字段验证成功指示器样式 - 绿色对勾
        使用方式:
        <TextBlock Style="{DynamicResource FieldSuccessIndicatorStyle}"
                   Visibility="{Binding IsFieldValid, Converter={x:Static converters:Cvt.BoolToVis}}"/>
    -->
    <Style x:Key="FieldSuccessIndicatorStyle" TargetType="TextBlock">
        <Setter Property="Text" Value="✓"/>
        <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Margin" Value="8,0,0,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Visibility" Value="Collapsed"/>
    </Style>

    <!--
        已验证字段边框样式 - 绿色边框
        保留用于未来扩展（当前版本使用对勾指示器）
    -->
    <Style x:Key="ValidatedFieldBorderStyle" TargetType="Border">
        <Setter Property="BorderBrush" Value="{StaticResource SuccessBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Visibility" Value="Collapsed"/>
    </Style>

    <!--
        带验证的ComboBox样式
        验证失败时显示红色下边框和ToolTip
    -->
    <Style x:Key="ValidatingComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Validation.ErrorTemplate">
            <Setter.Value>
                <ControlTemplate>
                    <Border
                        BorderBrush="{StaticResource ValidationErrorBrush}"
                        BorderThickness="0,0,0,2"
                        CornerRadius="4">
                        <AdornedElementPlaceholder />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Validation.HasError" Value="True">
                <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--
        带验证的PasswordBox样式
        验证失败时显示红色下边框和ToolTip
    -->
    <Style x:Key="ValidatingPasswordBoxStyle" TargetType="PasswordBox">
        <Setter Property="Validation.ErrorTemplate">
            <Setter.Value>
                <ControlTemplate>
                    <Border
                        BorderBrush="{StaticResource ValidationErrorBrush}"
                        BorderThickness="0,0,0,2"
                        CornerRadius="4">
                        <AdornedElementPlaceholder />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Validation.HasError" Value="True">
                <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
