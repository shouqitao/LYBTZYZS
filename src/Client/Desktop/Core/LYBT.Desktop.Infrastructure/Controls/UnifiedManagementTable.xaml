<UserControl x:Class="LYBT.Desktop.Infrastructure.Controls.UnifiedManagementTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:LYBT.Desktop.Infrastructure.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:InverseNullToVisibilityConverter x:Key="InverseNullToVisibilityConverter" />
    </UserControl.Resources>

    <!-- Issue #2160: 添加Ctrl+A全选快捷键 -->
    <UserControl.InputBindings>
        <KeyBinding Key="A" Modifiers="Control" Command="{Binding SelectAllCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />
    </UserControl.InputBindings>

    <Grid>
        <!-- 数据表格 -->
        <DataGrid x:Name="DataGrid"
                  ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  Style="{StaticResource BaseDataGridStyle}"
                  AutoGenerateColumns="False" />

        <!-- 空状态提示 -->
        <Border Background="White"
                Visibility="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource InverseNullToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="📋"
                           FontSize="{StaticResource FontSizeDisplay}"
                           HorizontalAlignment="Center"
                           Foreground="{StaticResource NeutralLightBrush}"
                           Margin="{StaticResource SpacingMedium}" />
                <TextBlock Text="{Binding EmptyStateText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontSize="{StaticResource FontSizeBody}"
                           Foreground="{StaticResource NeutralBrush}"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
