<!--
    PatientSearchControl - 患者搜索控件
    OpenSpec: refactor-medicalcase-workspace

    功能：
    - 搜索框（支持姓名/手机号/拼音码搜索）
    - 患者列表DataGrid
    - 分页控件
    - 可选的新建患者按钮

    使用方式：
    <controls:PatientSearchControl
        SearchKeyword="{Binding SearchKeyword, Mode=TwoWay}"
        Patients="{Binding Patients}"
        SelectedPatient="{Binding SelectedPatient, Mode=TwoWay}"
        SearchCommand="{Binding SearchCommand}"
        PatientSelectedCommand="{Binding StartMedicalCaseCommand}"
        NewPatientCommand="{Binding NewPatientCommand}"
        ShowCreateButton="True"
        ShowPagination="True"
        IsBusy="{Binding IsBusy}"
        CurrentPage="{Binding CurrentPage}"
        TotalPages="{Binding TotalPages}"
        TotalCount="{Binding TotalCount}"
        PreviousPageCommand="{Binding PreviousPageCommand}"
        NextPageCommand="{Binding NextPageCommand}"/>
-->
<UserControl x:Class="LYBT.Desktop.Infrastructure.Controls.PatientSearchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/LYBT.Desktop.Infrastructure;component/Themes/UnifiedComponents.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="{StaticResource SurfaceCardBrush}"
            Padding="15"
            CornerRadius="5"
            BorderBrush="{StaticResource BorderDefaultBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- 标题 -->
                <RowDefinition Height="Auto"/>  <!-- 搜索框和按钮区 -->
                <RowDefinition Height="*"/>     <!-- 患者列表 -->
                <RowDefinition Height="Auto"/>  <!-- 分页控件 -->
            </Grid.RowDefinitions>

            <!-- 标题 -->
            <TextBlock Grid.Row="0"
                       Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue='全部患者'}"
                       FontWeight="Bold"
                       FontSize="16"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="0,0,0,15"/>

            <!-- 搜索框和按钮区 -->
            <Grid Grid.Row="1" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>    <!-- 搜索框 -->
                    <ColumnDefinition Width="Auto"/> <!-- 搜索状态指示 -->
                    <ColumnDefinition Width="Auto"/> <!-- 新建患者按钮 -->
                </Grid.ColumnDefinitions>

                <!-- 搜索框 -->
                <TextBox Grid.Column="0"
                         Text="{Binding SearchKeyword, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                         Height="35"
                         Padding="10,0"
                         VerticalContentAlignment="Center"
                         BorderBrush="{StaticResource StateDisabledBrush}"
                         BorderThickness="1">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Background" Value="{StaticResource SurfaceCardBrush}"/>
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text=" 输入姓名/手机号/拼音码搜索"
                                                               Foreground="{StaticResource StateDisabledBrush}"
                                                               FontSize="12"
                                                               Margin="10,0,0,0"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- 搜索状态指示 -->
                <TextBlock Grid.Column="1"
                           Text="搜索中..."
                           FontSize="12"
                           Foreground="{StaticResource TextTertiaryBrush}"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0"
                           Visibility="{Binding IsBusy, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- 新建患者按钮 -->
                <Button Grid.Column="2"
                        Content="+ 新建患者"
                        Command="{Binding NewPatientCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        Width="100"
                        Height="35"
                        Margin="10,0,0,0"
                        Style="{StaticResource SecondaryButton}"
                        Visibility="{Binding ShowCreateButton, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </Grid>

            <!-- 患者列表 -->
            <DataGrid Grid.Row="2"
                      ItemsSource="{Binding Patients, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      SelectedItem="{Binding SelectedPatient, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      BorderBrush="{StaticResource BorderDefaultBrush}"
                      BorderThickness="1"
                      Background="Transparent"
                      RowHeight="42"
                      AlternationCount="2"
                      FontFamily="Microsoft YaHei"
                      FontSize="13">

                <!-- 键盘导航支持 -->
                <DataGrid.InputBindings>
                    <MouseBinding Gesture="LeftDoubleClick"
                                  Command="{Binding PatientSelectedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    <KeyBinding Key="Return"
                                Command="{Binding PatientSelectedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                </DataGrid.InputBindings>

                <!-- 列头样式 -->
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="FontWeight" Value="Medium"/>
                        <Setter Property="Height" Value="36"/>
                        <Setter Property="Padding" Value="12,0"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderDefaultBrush}"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <!-- 单元格样式 -->
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="12,0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}"
                                            Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    <!-- 姓名 -->
                    <DataGridTextColumn Header="姓名" Binding="{Binding Name}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- 性别 -->
                    <DataGridTextColumn Header="性别" Binding="{Binding Gender, Converter={StaticResource EnumDescriptionConverter}}" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- 年龄 -->
                    <DataGridTextColumn Header="年龄" Width="60">
                        <DataGridTextColumn.Binding>
                            <Binding Path="Age" StringFormat="{}{0}岁"/>
                        </DataGridTextColumn.Binding>
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- 电话 -->
                    <DataGridTextColumn Header="电话" Binding="{Binding PhoneNumber}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontFamily" Value="Consolas"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- 拼音码 -->
                    <DataGridTextColumn Header="拼音码" Binding="{Binding PinYinCode}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextTertiaryBrush}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontFamily" Value="Consolas"/>
                                <Setter Property="FontSize" Value="12"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- 创建时间 -->
                    <DataGridTextColumn Header="创建时间"
                                        Binding="{Binding CreatedAt, StringFormat='{}{0:yyyy-MM-dd}'}"
                                        Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource TextTertiaryBrush}"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontSize" Value="12"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>

                <!-- 行样式 -->
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#F0F0F0"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource DataGridSelectedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource DataGridHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="AlternationIndex" Value="1">
                                <Setter Property="Background" Value="{StaticResource DataGridAlternateRowBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>

            <!-- 分页控件 -->
            <Grid Grid.Row="3" Margin="0,15,0,0"
                  Visibility="{Binding ShowPagination, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 分页信息 -->
                <TextBlock Grid.Column="0"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12">
                    <Run Text="共 "/>
                    <Run Text="{Binding TotalCount, RelativeSource={RelativeSource AncestorType=UserControl}}" FontWeight="Bold"/>
                    <Run Text=" 条记录，每页 "/>
                    <Run Text="20" FontWeight="Bold"/>
                    <Run Text=" 条"/>
                </TextBlock>

                <!-- 分页按钮 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="上一页"
                            Command="{Binding PreviousPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Width="80" Height="30" Margin="0,0,10,0"
                            Style="{StaticResource SecondaryButton}"/>

                    <TextBlock Text="{Binding CurrentPage, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               VerticalAlignment="Center"
                               FontWeight="Bold" FontSize="14"
                               Margin="10,0"/>

                    <TextBlock Text="/"
                               VerticalAlignment="Center"
                               Foreground="{StaticResource StateDisabledBrush}"
                               Margin="5,0"/>

                    <TextBlock Text="{Binding TotalPages, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               VerticalAlignment="Center"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="5,0,10,0"/>

                    <Button Content="下一页"
                            Command="{Binding NextPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Width="80" Height="30"
                            Style="{StaticResource SecondaryButton}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
