<UserControl
    x:Class="LYBT.Desktop.Infrastructure.Controls.FormFieldControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="80"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  标签行  -->
        <StackPanel
            Grid.Row="0"
            Margin="0,0,0,4"
            Orientation="Horizontal">
            <TextBlock Style="{DynamicResource FieldLabel}" Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            <TextBlock
                Margin="2,0,0,0"
                Foreground="{DynamicResource DangerBrush}"
                Text="*"
                Visibility="{Binding IsRequired, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </StackPanel>

        <!--  输入内容区域  -->
        <ContentPresenter Grid.Row="1" Content="{Binding InputContent, RelativeSource={RelativeSource AncestorType=UserControl}}" />

        <!--  验证错误信息  -->
        <TextBlock
            Grid.Row="2"
            Style="{DynamicResource ValidationErrorTextStyle}"
            Text="{Binding ErrorMessage, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Visibility="{Binding HasError, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}" />
    </Grid>
</UserControl>
