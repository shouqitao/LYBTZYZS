<!--
    InfoCard - 信息卡片控件
    OpenSpec: refactor-detail-view-container

    功能：
    - 用于查看模式下的信息分组展示
    - 卡片式布局，带标题和内容区域
    - 支持自定义内容

    使用方式：
    <controls:InfoCard Title="基本信息">
        <Grid>
            ... 内容 ...
        </Grid>
    </controls:InfoCard>
-->
<UserControl x:Class="LYBT.Desktop.Infrastructure.Controls.InfoCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:infrastructure="clr-namespace:LYBT.Desktop.Infrastructure.Converters">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/LYBT.Desktop.Infrastructure;component/Themes/UnifiedComponents.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- 卡片阴影 -->
            <DropShadowEffect x:Key="CardShadow"
                              BlurRadius="16"
                              ShadowDepth="2"
                              Opacity="0.08"
                              Color="#000000"/>
            <infrastructure:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Background="{StaticResource SurfaceBrush}"
            CornerRadius="16"
            Padding="40,32"
            Effect="{StaticResource CardShadow}"
            Margin="0,0,0,20">
        <StackPanel>
            <!-- 卡片标题 -->
            <TextBlock x:Name="TitleBlock"
                       Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource PrimaryTextBrush}"
                       Margin="0,0,0,24"
                       Visibility="{Binding ShowTitle, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <!-- 卡片内容 -->
            <ContentPresenter Content="{Binding Content, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </StackPanel>
    </Border>
</UserControl>
