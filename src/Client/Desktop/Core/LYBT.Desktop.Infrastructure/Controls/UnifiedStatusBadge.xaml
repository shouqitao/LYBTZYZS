<UserControl x:Class="LYBT.Desktop.Infrastructure.Controls.UnifiedStatusBadge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="24" d:DesignWidth="80">

    <Border Background="{Binding BadgeBackground, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Style="{StaticResource StatusBadgeBase}">
        <TextBlock Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Foreground="{Binding BadgeForeground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="{StaticResource FontSizeCaption}"
                   FontWeight="Medium"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center" />
    </Border>
</UserControl>
