<UserControl x:Class="LYBT.Desktop.Infrastructure.Controls.VirtualizedDataGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- 加载指示器样式 -->
        <Style x:Key="LoadingIndicatorStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="#666" />
            <Setter Property="Margin" Value="10" />
        </Style>

        <!-- 虚拟化数据网格样式 -->
        <Style x:Key="VirtualizedDataGridStyle" TargetType="DataGrid">
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
            <Setter Property="VirtualizingPanel.ScrollUnit" Value="Item" />
            <Setter Property="EnableRowVirtualization" Value="True" />
            <Setter Property="EnableColumnVirtualization" Value="True" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="SelectionMode" Value="Extended" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#E0E0E0" />
            <Setter Property="Background" Value="White" />
        </Style>

        <!-- 分页按钮样式 -->
        <Style x:Key="PagingButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#E0E0E0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5" />
                    <Setter Property="BorderBrush" Value="#2196F3" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="#999" />
                    <Setter Property="Background" Value="#F5F5F5" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 当前页按钮样式 -->
        <Style x:Key="CurrentPageButtonStyle" TargetType="Button" BasedOn="{StaticResource PagingButtonStyle}">
            <Setter Property="Background" Value="#2196F3" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#2196F3" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Background="#F8F9FA" Padding="10" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- 搜索框 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBox x:Name="SearchBox"
                             Width="200"
                             Padding="8,4"
                             Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="搜索..." />
                    <Button Content="搜索"
                            Command="{Binding SearchCommand}"
                            Style="{StaticResource PagingButtonStyle}"
                            Margin="5,0,0,0" />
                </StackPanel>

                <!-- 统计信息 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="{Binding TotalItemsText}"
                               VerticalAlignment="Center"
                               Margin="10,0" />
                    <Button Content="刷新"
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource PagingButtonStyle}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- 虚拟化数据网格 -->
        <DataGrid Grid.Row="1"
                  x:Name="VirtualizedGrid"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem}"
                  Style="{StaticResource VirtualizedDataGridStyle}" />

        <!-- 加载指示器 -->
        <Border Grid.Row="1"
                Background="#80FFFFFF"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="⏳ 正在加载..." Style="{StaticResource LoadingIndicatorStyle}" />
                <ProgressBar Width="100" Height="4" IsIndeterminate="True" Margin="10,0,0,0" VerticalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- 分页控制栏 -->
        <Border Grid.Row="2" Background="White" Padding="10" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- 页大小选择 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="每页显示:" VerticalAlignment="Center" Margin="0,0,5,0" />
                    <ComboBox x:Name="PageSizeComboBox"
                              Width="80"
                              SelectedValue="{Binding PageSize}"
                              VerticalAlignment="Center">
                        <ComboBoxItem Content="20" Tag="20" />
                        <ComboBoxItem Content="50" Tag="50" />
                        <ComboBoxItem Content="100" Tag="100" />
                        <ComboBoxItem Content="200" Tag="200" />
                    </ComboBox>
                    <TextBlock Text="条记录" VerticalAlignment="Center" Margin="5,0,0,0" />
                </StackPanel>

                <!-- 分页按钮 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="首页"
                            Command="{Binding FirstPageCommand}"
                            Style="{StaticResource PagingButtonStyle}"
                            IsEnabled="{Binding CanGoToPreviousPage}" />
                    <Button Content="上一页"
                            Command="{Binding PreviousPageCommand}"
                            Style="{StaticResource PagingButtonStyle}"
                            IsEnabled="{Binding CanGoToPreviousPage}" />
                    <Button Content="下一页"
                            Command="{Binding NextPageCommand}"
                            Style="{StaticResource PagingButtonStyle}"
                            IsEnabled="{Binding CanGoToNextPage}" />
                    <Button Content="末页"
                            Command="{Binding LastPageCommand}"
                            Style="{StaticResource PagingButtonStyle}"
                            IsEnabled="{Binding CanGoToNextPage}" />
                </StackPanel>

                <!-- 页面信息 -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock VerticalAlignment="Center">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="第 {0} 页，共 {1} 页">
                                <Binding Path="CurrentPage" />
                                <Binding Path="TotalPages" />
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                    <TextBlock Text="{Binding TotalItemsText}"
                               Margin="20,0,0,0"
                               VerticalAlignment="Center"
                               Foreground="#666" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>