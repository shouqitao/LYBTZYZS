<!--
    SystemSettingsView - 系统设置视图
    统一规范: 提供保存配置和重置的功能按钮操作
-->
<UserControl
    x:Class="LYBT.Desktop.Admin.Views.SystemSettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True">

    <Grid Background="{DynamicResource MaterialDesign.Brush.Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  工具栏: 提供保存配置和重置  -->
        <Border Grid.Row="0" Background="{DynamicResource MaterialDesignPaper}" BorderBrush="{DynamicResource MaterialDesignOutline}" BorderThickness="0,0,0,1" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!--  左侧: 标题  -->
                <TextBlock
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                    Text="系统设置" />

                <!--  右侧: 操作按钮  -->
                <!--  返回首页按钮已移除 - 使用导航栏的"首页"按钮统一导航  -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button
                        Margin="8"
                        Command="{Binding SaveCommand}"
                        Content="保存配置"
                        Style="{StaticResource MaterialDesignRaisedButton}" Background="#2E8B57" Foreground="White" BorderThickness="0" />
                    <Button
                        Margin="8"
                        Command="{Binding ResetCommand}"
                        Content="重置"
                        Style="{StaticResource MaterialDesignOutlinedButton}" />
                </StackPanel>
            </Grid>
        </Border>

        <!--  设置内容  -->
        <ScrollViewer
            Grid.Row="1"
            Padding="16"
            VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!--  基本设置  -->
                <Border Background="{DynamicResource MaterialDesign.Brush.Paper}" BorderBrush="{DynamicResource MaterialDesign.Brush.Outline}" BorderThickness="1" CornerRadius="8" Padding="24,16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Style="{StaticResource MaterialDesignHeadline5TextBlock}" Text="基本设置" />

                        <!--  系统名称  -->
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                Text="系统名称：" />
                            <TextBox
                                Grid.Column="1"
                                MaxWidth="400"
                                HorizontalAlignment="Left"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Text="{Binding SystemName, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>

                        <!--  医院名称  -->
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                Text="医院名称：" />
                            <TextBox
                                Grid.Column="1"
                                MaxWidth="400"
                                HorizontalAlignment="Left"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Text="{Binding HospitalName, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>

                        <!--  联系电话  -->
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                Text="联系电话：" />
                            <TextBox
                                Grid.Column="1"
                                MaxWidth="400"
                                HorizontalAlignment="Left"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Text="{Binding ContactPhone, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <!--  D2: 诊所信息配置  -->
                <Border Background="{DynamicResource MaterialDesign.Brush.Paper}" BorderBrush="{DynamicResource MaterialDesign.Brush.Outline}" BorderThickness="1" CornerRadius="8" Padding="24,16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Style="{StaticResource MaterialDesignHeadline5TextBlock}" Text="诊所信息" />

                        <!--  诊所名称  -->
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                Text="诊所名称：" />
                            <TextBox
                                Grid.Column="1"
                                MaxWidth="400"
                                HorizontalAlignment="Left"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Text="{Binding ClinicName, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>

                        <!--  科室  -->
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                Text="科室：" />
                            <TextBox
                                Grid.Column="1"
                                MaxWidth="400"
                                HorizontalAlignment="Left"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Text="{Binding ClinicDepartment, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>

                        <!--  地址  -->
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                Text="地址：" />
                            <TextBox
                                Grid.Column="1"
                                MaxWidth="400"
                                HorizontalAlignment="Left"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Text="{Binding ClinicAddress, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>

                        <!--  电话  -->
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                Text="电话：" />
                            <TextBox
                                Grid.Column="1"
                                MaxWidth="400"
                                HorizontalAlignment="Left"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Text="{Binding ClinicPhone, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>

                        <!--  邮箱  -->
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                Text="邮箱：" />
                            <TextBox
                                Grid.Column="1"
                                MaxWidth="400"
                                HorizontalAlignment="Left"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Text="{Binding ClinicEmail, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>

                        <!--  执业许可证号  -->
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                Text="许可证号：" />
                            <TextBox
                                Grid.Column="1"
                                MaxWidth="400"
                                HorizontalAlignment="Left"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Text="{Binding LicenseNumber, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <!--  数据库配置  -->
                <Border Background="{DynamicResource MaterialDesign.Brush.Paper}" BorderBrush="{DynamicResource MaterialDesign.Brush.Outline}" BorderThickness="1" CornerRadius="8" Padding="24,16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Style="{StaticResource MaterialDesignHeadline5TextBlock}" Text="数据库配置" />

                        <!--  数据库备份  -->
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                Text="自动备份：" />
                            <CheckBox
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                Content="启用数据库自动备份"
                                FontSize="14"
                                Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
                                IsChecked="{Binding AutoBackupEnabled}" />
                        </Grid>

                        <!--  备份路径  -->
                        <Grid Margin="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                Text="备份路径：" />
                            <TextBox
                                Grid.Column="1"
                                MaxWidth="400"
                                Margin="0,0,12,0"
                                HorizontalAlignment="Left"
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Text="{Binding BackupPath, UpdateSourceTrigger=PropertyChanged}" />
                            <Button
                                Grid.Column="2"
                                Command="{Binding BrowseBackupPathCommand}"
                                Content="浏览..."
                                Style="{StaticResource MaterialDesignOutlinedButton}" />
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
