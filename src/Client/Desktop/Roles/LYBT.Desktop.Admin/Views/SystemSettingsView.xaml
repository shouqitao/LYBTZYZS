<UserControl x:Class="LYBT.Desktop.Admin.Views.SystemSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <!-- 卡片阴影 -->
        <DropShadowEffect x:Key="CardShadow"
                          BlurRadius="16"
                          ShadowDepth="2"
                          Opacity="0.05"
                          Color="#000000"/>

        <!-- 标题样式 -->
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <!-- 设置项标签样式 -->
        <Style x:Key="SettingLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
            <Setter Property="MinWidth" Value="120"/>
        </Style>

        <!-- 按钮基础样式 -->
        <Style x:Key="ModernButtonBase" TargetType="Button">
            <Setter Property="Height" Value="40" />
            <Setter Property="MinWidth" Value="100" />
            <Setter Property="Padding" Value="20,0" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 主要按钮样式 -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButtonBase}">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <!-- 次要按钮样式 -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButtonBase}">
            <Setter Property="Background" Value="#6B7280"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F8FAFC">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0"
                Background="White"
                Padding="40,24"
                Margin="0,0,0,2"
                Effect="{StaticResource CardShadow}">
            <TextBlock Text="系统设置"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#1E293B"/>
        </Border>

        <!-- 内容区域 -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Padding="40,24">
            <StackPanel>
                <!-- 基本设置 -->
                <Border Background="White"
                        Padding="32"
                        Margin="0,0,0,16"
                        CornerRadius="12"
                        Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <TextBlock Text="基本设置" Style="{StaticResource SectionTitleStyle}"/>

                        <!-- 系统名称 -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="系统名称：" Style="{StaticResource SettingLabelStyle}"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding SystemName, UpdateSourceTrigger=PropertyChanged}"
                                     Height="40"
                                     Padding="12,0"
                                     FontSize="15"
                                     BorderBrush="#D1D5DB"
                                     BorderThickness="1"/>
                        </Grid>

                        <!-- 医院名称 -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="医院名称：" Style="{StaticResource SettingLabelStyle}"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding HospitalName, UpdateSourceTrigger=PropertyChanged}"
                                     Height="40"
                                     Padding="12,0"
                                     FontSize="15"
                                     BorderBrush="#D1D5DB"
                                     BorderThickness="1"/>
                        </Grid>

                        <!-- 联系电话 -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="联系电话：" Style="{StaticResource SettingLabelStyle}"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding ContactPhone, UpdateSourceTrigger=PropertyChanged}"
                                     Height="40"
                                     Padding="12,0"
                                     FontSize="15"
                                     BorderBrush="#D1D5DB"
                                     BorderThickness="1"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 数据库设置 -->
                <Border Background="White"
                        Padding="32"
                        Margin="0,0,0,16"
                        CornerRadius="12"
                        Effect="{StaticResource CardShadow}">
                    <StackPanel>
                        <TextBlock Text="数据库设置" Style="{StaticResource SectionTitleStyle}"/>

                        <!-- 数据库备份 -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="自动备份：" Style="{StaticResource SettingLabelStyle}"/>
                            <CheckBox Grid.Column="1"
                                      IsChecked="{Binding AutoBackupEnabled}"
                                      Content="启用数据库自动备份"
                                      VerticalAlignment="Center"
                                      FontSize="15"/>
                        </Grid>

                        <!-- 备份路径 -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="备份路径：" Style="{StaticResource SettingLabelStyle}"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding BackupPath, UpdateSourceTrigger=PropertyChanged}"
                                     Height="40"
                                     Padding="12,0"
                                     FontSize="15"
                                     BorderBrush="#D1D5DB"
                                     BorderThickness="1"
                                     Margin="0,0,12,0"/>
                            <Button Grid.Column="2"
                                    Content="浏览..."
                                    Style="{StaticResource SecondaryButton}"
                                    Command="{Binding BrowseBackupPathCommand}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 操作按钮 -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="保存设置"
                            Style="{StaticResource PrimaryButton}"
                            Margin="0,0,12,0"
                            Command="{Binding SaveCommand}"/>                    <Button Content="重置"
                            Style="{StaticResource SecondaryButton}"
                            Command="{Binding ResetCommand}"
                            Margin="0,0,12,0"/>
                    <Button Content="🏠 返回主页"
                            Style="{StaticResource PrimaryButton}"
                            Command="{Binding NavigateToHomeCommand}"
                            ToolTip="返回管理员主页" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
