<UserControl x:Class="LYBT.Desktop.Sysadmin.Views.AdminUserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:users="clr-namespace:LYBT.Desktop.Users.Controls;assembly=LYBT.Desktop.Users">

    <Grid Margin="16" Background="{DynamicResource MaterialDesign.Brush.Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                   Text="管理员账号管理" Margin="0,0,0,16" />

        <users:UserMasterDetailControl x:Name="UserMasterDetailControl" Grid.Row="1" />
    </Grid>
</UserControl>
