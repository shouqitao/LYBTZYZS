<UserControl x:Class="LYBT.Desktop.Sysadmin.Views.SysadminHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:LYBT.Desktop.Controls.Converters;assembly=LYBT.Desktop.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:prism="http://prismlibrary.com/">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/LYBT.Desktop.Sysadmin;component/Themes/SysadminDarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <materialDesign:DialogHost>
        <ScrollViewer VerticalScrollBarVisibility="Auto" Background="{DynamicResource MaterialDesignPaper}">
            <StackPanel Margin="24">
                <TextBlock Text="运维控制台" FontSize="{StaticResource FontSizeXXL}" FontWeight="Bold"
                           Foreground="{DynamicResource MaterialDesignBody}" Margin="0,0,0,24" />

                <!-- 状态卡片网格 -->
                <UniformGrid Columns="2" Rows="2" Margin="0,0,0,24">
                    <!-- API 状态 -->
                    <Border Background="{DynamicResource MaterialDesignBody}"
                            CornerRadius="{StaticResource RadiusMD}" Padding="{StaticResource SpacingXL}" Margin="{StaticResource SpacingSM}">
                        <StackPanel>
                            <TextBlock Text="{Binding Dashboard.ApiStatus.Title}" Foreground="{DynamicResource MaterialDesignBodyLight}" FontSize="{StaticResource FontSizeSM}" />
                            <TextBlock Text="{Binding Dashboard.ApiStatus.Value}" Foreground="{DynamicResource MaterialDesignBody}" FontSize="{StaticResource FontSizeXXL}" FontWeight="Bold" Margin="0,8,0,0" />
                            <Ellipse Width="10" Height="10" Fill="{StaticResource OpsSuccessBrush}" Margin="0,8,0,0"
                                     Visibility="{Binding Dashboard.ApiStatus.IsHealthy, Converter={x:Static converters:Cvt.BoolToVis}}" />
                        </StackPanel>
                    </Border>

                    <!-- 连接模式 -->
                    <Border Background="{DynamicResource MaterialDesignBody}"
                            CornerRadius="{StaticResource RadiusMD}" Padding="{StaticResource SpacingXL}" Margin="{StaticResource SpacingSM}">
                        <StackPanel>
                            <TextBlock Text="{Binding Dashboard.ConnectionMode.Title}" Foreground="{DynamicResource MaterialDesignBodyLight}" FontSize="{StaticResource FontSizeSM}" />
                            <TextBlock Text="{Binding Dashboard.ConnectionMode.Value}" Foreground="{DynamicResource MaterialDesignBody}" FontSize="{StaticResource FontSizeXXL}" FontWeight="Bold" Margin="0,8,0,0" />
                            <TextBlock Text="{Binding Dashboard.ConnectionMode.Status}" Foreground="{DynamicResource MaterialDesignBodyLight}" FontSize="{StaticResource FontSizeXS}" Margin="0,4,0,0" TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </Border>

                    <!-- 数据库状态 -->
                    <Border Background="{DynamicResource MaterialDesignBody}"
                            CornerRadius="{StaticResource RadiusMD}" Padding="{StaticResource SpacingXL}" Margin="{StaticResource SpacingSM}">
                        <StackPanel>
                            <TextBlock Text="{Binding Dashboard.DbStatus.Title}" Foreground="{DynamicResource MaterialDesignBodyLight}" FontSize="{StaticResource FontSizeSM}" />
                            <TextBlock Text="{Binding Dashboard.DbStatus.Value}" Foreground="{DynamicResource MaterialDesignBody}" FontSize="{StaticResource FontSizeXXL}" FontWeight="Bold" Margin="0,8,0,0" />
                            <Ellipse Width="10" Height="10" Fill="{StaticResource OpsSuccessBrush}" Margin="0,8,0,0"
                                     Visibility="{Binding Dashboard.DbStatus.IsHealthy, Converter={x:Static converters:Cvt.BoolToVis}}" />
                        </StackPanel>
                    </Border>

                    <!-- 系统信息 -->
                    <Border Background="{DynamicResource MaterialDesignBody}"
                            CornerRadius="{StaticResource RadiusMD}" Padding="{StaticResource SpacingXL}" Margin="{StaticResource SpacingSM}">
                        <StackPanel>
                            <TextBlock Text="{Binding Dashboard.SystemInfo.Title}" Foreground="{DynamicResource MaterialDesignBodyLight}" FontSize="{StaticResource FontSizeSM}" />
                            <TextBlock Text="{Binding Dashboard.SystemInfo.Value}" Foreground="{DynamicResource MaterialDesignBody}" FontSize="{StaticResource FontSizeXXL}" FontWeight="Bold" Margin="0,8,0,0" />
                            <TextBlock Text="{Binding Dashboard.SystemInfo.Status}" Foreground="{DynamicResource MaterialDesignBodyLight}" FontSize="{StaticResource FontSizeXS}" Margin="0,4,0,0" TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- 导航按钮 - 使用 MDIX 样式 -->
                <UniformGrid Columns="2" Margin="0,24,0,0">
                    <Button Content="管理员账号管理" Command="{Binding NavigateToAdminUsersCommand}"
                            Style="{StaticResource MaterialDesignFlatMidBgButton}"
                            Margin="{StaticResource SpacingSM}" Padding="{StaticResource SpacingMD}" />
                    <Button Content="诊所信息配置" Command="{Binding NavigateToClinicSettingsCommand}"
                            Style="{StaticResource MaterialDesignFlatMidBgButton}"
                            Margin="{StaticResource SpacingSM}" Padding="{StaticResource SpacingMD}" />
                    <Button Content="日志级别控制" Command="{Binding NavigateToLogLevelCommand}"
                            Style="{StaticResource MaterialDesignFlatMidBgButton}"
                            Margin="{StaticResource SpacingSM}" Padding="{StaticResource SpacingMD}" />
                </UniformGrid>
            </StackPanel>
        </ScrollViewer>
    </materialDesign:DialogHost>
</UserControl>
