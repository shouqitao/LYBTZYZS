<UserControl x:Class="LYBT.Desktop.Prescriptions.Views.PrescriptionComposerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <!-- 样式资源 -->
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="0,10,0,5" />
            <Setter Property="Foreground" Value="#2C3E50" />
        </Style>

        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#BDC3C7" />
            <Setter Property="Background" Value="White" />
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="15,0" />
            <Setter Property="Background" Value="#3498DB" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#27AE60" />
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#95A5A6" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#34495E" CornerRadius="5,5,0,0" Padding="15,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="处方开具" FontSize="20" FontWeight="Bold" Foreground="White" />
                <TextBlock Text="{Binding PatientInfo}" FontSize="14" Foreground="#BDC3C7"
                          Margin="20,0,0,0" VerticalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- 基本信息区 -->
        <Border Grid.Row="1" Background="#F8F9FA" BorderBrush="#E9ECEF" BorderThickness="1,0,1,1" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- 诊断 -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="诊断" Style="{StaticResource SectionHeaderStyle}" />
                    <TextBox Text="{Binding Diagnosis, UpdateSourceTrigger=PropertyChanged}"
                            Style="{StaticResource InputStyle}"
                            Height="60" TextWrapping="Wrap" AcceptsReturn="True" />
                </StackPanel>

                <!-- 剂数 -->
                <StackPanel Grid.Column="1" Margin="10,0">
                    <TextBlock Text="剂数" Style="{StaticResource SectionHeaderStyle}" />
                    <TextBox Text="{Binding DosageCount, UpdateSourceTrigger=PropertyChanged}"
                            Style="{StaticResource InputStyle}" />
                </StackPanel>

                <!-- 用法 -->
                <StackPanel Grid.Column="2" Margin="10,0">
                    <TextBlock Text="用法" Style="{StaticResource SectionHeaderStyle}" />
                    <ComboBox Height="32" IsEditable="True"
                             Text="{Binding Usage, UpdateSourceTrigger=PropertyChanged}">
                        <ComboBoxItem>水煎服，日一剂，分早晚服</ComboBoxItem>
                        <ComboBoxItem>水煎服，日一剂，分三次服</ComboBoxItem>
                        <ComboBoxItem>水煎服，日二剂，分四次服</ComboBoxItem>
                        <ComboBoxItem>开水冲服</ComboBoxItem>
                    </ComboBox>
                </StackPanel>

                <!-- 价格显示 -->
                <StackPanel Grid.Column="3" Margin="10,0,0,0">
                    <TextBlock Text="价格信息" Style="{StaticResource SectionHeaderStyle}" />
                    <StackPanel>
                        <TextBlock>
                            <Run Text="单剂：" />
                            <Run Text="{Binding SingleDosePrice, StringFormat=C}" FontWeight="Bold" Foreground="#E74C3C" />
                        </TextBlock>
                        <TextBlock>
                            <Run Text="总价：" />
                            <Run Text="{Binding TotalPrice, StringFormat=C}" FontWeight="Bold" FontSize="16" Foreground="#C0392B" />
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 药材列表区 -->
        <Border Grid.Row="2" BorderBrush="#E9ECEF" BorderThickness="1,0,1,1" Background="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- 药材列表标题 -->
                <Border Grid.Row="0" Background="#F1F2F6" Padding="15,10">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="处方药材" Style="{StaticResource SectionHeaderStyle}" Margin="0" />
                        <TextBlock Text="{Binding PrescriptionItems.Count, StringFormat='({0}味)'}"
                                  Margin="10,0" VerticalAlignment="Center" Foreground="#7F8C8D" />

                        <!-- 操作按钮 -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,0,0">
                            <Button Content="添加药材" Command="{Binding AddHerbCommand}"
                                   Style="{StaticResource ButtonStyle}" Margin="0,0,10,0" />
                            <Button Content="导入验方" Command="{Binding ImportFormulaCommand}"
                                   Style="{StaticResource ButtonStyle}" Margin="0,0,10,0" />
                            <Button Content="清空处方" Command="{Binding ClearAllCommand}"
                                   Style="{StaticResource SecondaryButtonStyle}" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 药材列表 (8列表格布局) -->
                <!-- Issue #1361: [ENTRY-3] 设计8列DataGrid XAML布局 -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding ItemRows}"
                         AutoGenerateColumns="False" CanUserAddRows="False"
                         GridLinesVisibility="All" HeadersVisibility="Column"
                         Background="White" AlternatingRowBackground="#F8F9FA">

                    <DataGrid.Columns>
                        <!-- 药材1 -->
                        <!-- Issue #1362: [ENTRY-4] 实现ComboBox拼音码过滤 -->
                        <DataGridTemplateColumn Header="药材1" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox IsEditable="True"
                                             ItemsSource="{Binding DataContext.FilteredHerbs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                             DisplayMemberPath="Name"
                                             Text="{Binding Item1.HerbName, UpdateSourceTrigger=PropertyChanged}"
                                             BorderThickness="0" Background="Transparent"
                                             Loaded="HerbComboBox_Loaded"
                                             x:Name="HerbComboBox1" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 用量1 -->
                        <DataGridTemplateColumn Header="用量1" Width="1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Item1.Quantity, UpdateSourceTrigger=PropertyChanged}"
                                            BorderThickness="0" Background="Transparent" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 药材2 -->
                        <!-- Issue #1362: [ENTRY-4] 实现ComboBox拼音码过滤 -->
                        <DataGridTemplateColumn Header="药材2" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox IsEditable="True"
                                             ItemsSource="{Binding DataContext.FilteredHerbs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                             DisplayMemberPath="Name"
                                             Text="{Binding Item2.HerbName, UpdateSourceTrigger=PropertyChanged}"
                                             BorderThickness="0" Background="Transparent"
                                             Loaded="HerbComboBox_Loaded"
                                             x:Name="HerbComboBox2" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 用量2 -->
                        <DataGridTemplateColumn Header="用量2" Width="1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Item2.Quantity, UpdateSourceTrigger=PropertyChanged}"
                                            BorderThickness="0" Background="Transparent" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 药材3 -->
                        <!-- Issue #1362: [ENTRY-4] 实现ComboBox拼音码过滤 -->
                        <DataGridTemplateColumn Header="药材3" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox IsEditable="True"
                                             ItemsSource="{Binding DataContext.FilteredHerbs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                             DisplayMemberPath="Name"
                                             Text="{Binding Item3.HerbName, UpdateSourceTrigger=PropertyChanged}"
                                             BorderThickness="0" Background="Transparent"
                                             Loaded="HerbComboBox_Loaded"
                                             x:Name="HerbComboBox3" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 用量3 -->
                        <DataGridTemplateColumn Header="用量3" Width="1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Item3.Quantity, UpdateSourceTrigger=PropertyChanged}"
                                            BorderThickness="0" Background="Transparent" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 药材4 -->
                        <!-- Issue #1362: [ENTRY-4] 实现ComboBox拼音码过滤 -->
                        <DataGridTemplateColumn Header="药材4" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox IsEditable="True"
                                             ItemsSource="{Binding DataContext.FilteredHerbs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                             DisplayMemberPath="Name"
                                             Text="{Binding Item4.HerbName, UpdateSourceTrigger=PropertyChanged}"
                                             BorderThickness="0" Background="Transparent"
                                             Loaded="HerbComboBox_Loaded"
                                             x:Name="HerbComboBox4" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 用量4 -->
                        <DataGridTemplateColumn Header="用量4" Width="1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Item4.Quantity, UpdateSourceTrigger=PropertyChanged}"
                                            BorderThickness="0" Background="Transparent" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- 医嘱区 -->
        <Border Grid.Row="3" Background="#F8F9FA" BorderBrush="#E9ECEF" BorderThickness="1,0,1,1" Padding="15">
            <StackPanel>
                <TextBlock Text="医嘱" Style="{StaticResource SectionHeaderStyle}" />
                <TextBox Text="{Binding Advice, UpdateSourceTrigger=PropertyChanged}"
                        Height="60" TextWrapping="Wrap" AcceptsReturn="True">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource InputStyle}">
                            <Style.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter Property="Background" Value="White" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </StackPanel>
        </Border>

        <!-- 底部操作区 -->
        <Border Grid.Row="4" Background="#2C3E50" CornerRadius="0,0,5,5" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="保存草稿" Command="{Binding SaveDraftCommand}"
                       Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" />
                <Button Content="保存处方" Command="{Binding SavePrescriptionCommand}"
                       Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0" />
                <Button Content="关闭" Command="{Binding CloseCommand}"
                       Style="{StaticResource SecondaryButtonStyle}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
