<!-- PrescriptionUnifiedView.xaml -->
<!-- 统一处方View：整合8列快速输入和列表详细编辑两种模式 -->
<!-- Epic #1701: PrescriptionView + PrescriptionEditorDialog合并 -->

<UserControl x:Class="LYBT.Desktop.Prescriptions.Views.PrescriptionUnifiedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <!-- 样式资源 -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!-- 反转布尔到可见性转换器 -->
        <Style x:Key="InverseBooleanToVisibilityStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDetailedListMode}" Value="False">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="0,10,0,5" />
            <Setter Property="Foreground" Value="#2C3E50" />
        </Style>

        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#BDC3C7" />
            <Setter Property="Background" Value="White" />
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="15,0" />
            <Setter Property="Background" Value="#3498DB" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#27AE60" />
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#95A5A6" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Row 0: 标题栏 -->
            <RowDefinition Height="Auto" /> <!-- Row 1: 基本信息区 -->
            <RowDefinition Height="*" />    <!-- Row 2: 药材列表区（动态切换） -->
            <RowDefinition Height="Auto" /> <!-- Row 3: 医嘱区 -->
            <RowDefinition Height="Auto" /> <!-- Row 4: 底部操作区 -->
        </Grid.RowDefinitions>

        <!-- Row 0: 标题栏 -->
        <Border Grid.Row="0" Background="#34495E" CornerRadius="5,5,0,0" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- 左侧：标题和患者信息 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="处方开具" FontSize="20" FontWeight="Bold" Foreground="White" />
                    <TextBlock Text="{Binding PatientInfo}" FontSize="14" Foreground="#BDC3C7"
                              Margin="20,0,0,0" VerticalAlignment="Center" />
                </StackPanel>

                <!-- 中间：状态选择器（仅编辑模式显示） -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center"
                           Visibility="{Binding ShowStatusSelector, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="状态：" FontSize="14" Foreground="#BDC3C7" VerticalAlignment="Center" />
                    <ComboBox SelectedValue="{Binding Status}" Width="120" Height="30" Margin="10,0,0,0">
                        <ComboBoxItem Content="草稿" Tag="0" />
                        <ComboBoxItem Content="已确认" Tag="1" />
                        <ComboBoxItem Content="已发药" Tag="2" />
                        <ComboBoxItem Content="已完成" Tag="3" />
                        <ComboBoxItem Content="已取消" Tag="4" />
                    </ComboBox>
                </StackPanel>

                <!-- 右侧：处方编号（Issue #1551） -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="处方编号：" FontSize="14" Foreground="#BDC3C7" VerticalAlignment="Center" />
                    <TextBlock Text="{Binding PrescriptionNumber}" FontSize="14" FontWeight="SemiBold" Foreground="#3498DB"
                              VerticalAlignment="Center" Margin="5,0,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding PrescriptionNumber}" Value="{x:Null}">
                                        <Setter Property="Text" Value="（未保存）" />
                                        <Setter Property="Foreground" Value="#95A5A6" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding PrescriptionNumber}" Value="">
                                        <Setter Property="Text" Value="（未保存）" />
                                        <Setter Property="Foreground" Value="#95A5A6" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Row 1: 基本信息区 -->
        <Border Grid.Row="1" Background="#F8F9FA" BorderBrush="#E9ECEF" BorderThickness="1,0,1,1" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- 诊断 -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="诊断" FontWeight="Bold" />
                    <TextBox Text="{Binding Diagnosis}" Height="60" TextWrapping="Wrap"
                            IsReadOnly="{Binding IsViewMode}"/>
                </StackPanel>

                <!-- 剂数 -->
                <StackPanel Grid.Column="1" Margin="10,0">
                    <TextBlock Text="剂数" FontWeight="Bold" />
                    <TextBox Text="{Binding DosageCount}" IsReadOnly="{Binding IsViewMode}"/>
                </StackPanel>

                <!-- 用法 -->
                <StackPanel Grid.Column="2" Margin="10,0">
                    <TextBlock Text="用法" FontWeight="Bold" />
                    <ComboBox IsEditable="True" Text="{Binding Usage}" IsEnabled="{Binding IsViewMode, Converter={StaticResource InverseBooleanConverter}}">
                        <ComboBoxItem>水煎服，日一剂，分早晚服</ComboBoxItem>
                        <ComboBoxItem>水煎服，日一剂，分三次服</ComboBoxItem>
                    </ComboBox>
                </StackPanel>

                <!-- 价格 -->
                <StackPanel Grid.Column="3" Margin="10,0">
                    <TextBlock Text="价格信息" FontWeight="Bold" />
                    <TextBlock>
                        <Run Text="总价：" />
                        <Run Text="{Binding TotalPrice, StringFormat=C}" FontWeight="Bold" Foreground="#C0392B" />
                    </TextBlock>
                </StackPanel>

                <!-- 布局切换按钮 -->
                <StackPanel Grid.Column="4" Margin="10,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="布局模式" FontSize="10" Foreground="Gray" Margin="0,0,0,2" />
                    <ToggleButton IsChecked="{Binding IsDetailedListMode}"
                                  ToolTip="切换输入模式（快捷键：F2）"
                                  Height="32" Width="100">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding LayoutModeIcon}" FontSize="14" Margin="0,0,5,0" />
                            <TextBlock Text="{Binding LayoutModeText}" FontSize="12" />
                        </StackPanel>
                    </ToggleButton>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Row 2: 药材列表区（动态切换） -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E9ECEF" BorderThickness="1,0,1,0">
            <Grid>
                <!-- 8列快速输入模式 -->
                <DataGrid ItemsSource="{Binding ItemRows}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          Style="{StaticResource InverseBooleanToVisibilityStyle}">
                    <DataGrid.Columns>
                        <!-- 药材1 (Issue #1362: Pinyin码过滤) -->
                        <DataGridTemplateColumn Header="药材1" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox IsEditable="True"
                                             ItemsSource="{Binding DataContext.FilteredHerbs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                             Text="{Binding Item1.HerbName, UpdateSourceTrigger=PropertyChanged}"
                                             Loaded="HerbComboBox_Loaded" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 用量1 (Issue #1363: 焦点自动跳转) -->
                        <DataGridTemplateColumn Header="用量1" Width="1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Item1.Quantity, UpdateSourceTrigger=PropertyChanged}"
                                            PreviewKeyDown="QuantityTextBox_PreviewKeyDown" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 药材2 -->
                        <DataGridTemplateColumn Header="药材2" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox IsEditable="True"
                                             ItemsSource="{Binding DataContext.FilteredHerbs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                             Text="{Binding Item2.HerbName, UpdateSourceTrigger=PropertyChanged}"
                                             Loaded="HerbComboBox_Loaded" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 用量2 -->
                        <DataGridTemplateColumn Header="用量2" Width="1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Item2.Quantity, UpdateSourceTrigger=PropertyChanged}"
                                            PreviewKeyDown="QuantityTextBox_PreviewKeyDown" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 药材3 -->
                        <DataGridTemplateColumn Header="药材3" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox IsEditable="True"
                                             ItemsSource="{Binding DataContext.FilteredHerbs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                             Text="{Binding Item3.HerbName, UpdateSourceTrigger=PropertyChanged}"
                                             Loaded="HerbComboBox_Loaded" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 用量3 -->
                        <DataGridTemplateColumn Header="用量3" Width="1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Item3.Quantity, UpdateSourceTrigger=PropertyChanged}"
                                            PreviewKeyDown="QuantityTextBox_PreviewKeyDown" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 药材4 -->
                        <DataGridTemplateColumn Header="药材4" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox IsEditable="True"
                                             ItemsSource="{Binding DataContext.FilteredHerbs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                             Text="{Binding Item4.HerbName, UpdateSourceTrigger=PropertyChanged}"
                                             Loaded="HerbComboBox_Loaded" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 用量4 -->
                        <DataGridTemplateColumn Header="用量4" Width="1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Item4.Quantity, UpdateSourceTrigger=PropertyChanged}"
                                            PreviewKeyDown="QuantityTextBox_PreviewKeyDown" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 列表详细编辑模式 -->
                <DataGrid ItemsSource="{Binding PrescriptionItems}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="{Binding IsViewMode}"
                          Visibility="{Binding IsDetailedListMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="药材名称" Binding="{Binding HerbName}" Width="150" />
                        <DataGridTextColumn Header="规格" Binding="{Binding Specification}" Width="100" />
                        <DataGridTextColumn Header="单位" Binding="{Binding Unit}" Width="60" />
                        <DataGridTextColumn Header="数量" Binding="{Binding Quantity}" Width="80" />
                        <DataGridTextColumn Header="单价" Binding="{Binding UnitPrice, StringFormat='￥{0:F2}'}" Width="80" />
                        <DataGridTextColumn Header="金额" Binding="{Binding Amount, StringFormat='￥{0:F2}'}" Width="80" />
                        <DataGridTextColumn Header="用法" Binding="{Binding Usage}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Row 3: 医嘱区 -->
        <Border Grid.Row="3" Background="#F8F9FA" BorderBrush="#E9ECEF" BorderThickness="1,0,1,0" Padding="15">
            <StackPanel>
                <TextBlock Text="医嘱" FontWeight="Bold" />
                <TextBox Text="{Binding Advice}" Height="60" TextWrapping="Wrap"
                        IsReadOnly="{Binding IsViewMode}"/>
            </StackPanel>
        </Border>

        <!-- Row 4: 底部操作区 -->
        <Border Grid.Row="4" Background="#2C3E50" CornerRadius="0,0,5,5" Padding="15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="保存草稿" Command="{Binding SaveDraftCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Visibility="{Binding ShowDraftButton, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Margin="0,0,10,0" />
                <Button Content="保存处方" Command="{Binding SavePrescriptionCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Visibility="{Binding ShowSaveButton, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Margin="0,0,10,0" />
                <Button Content="预览" Command="{Binding PreviewCommand}"
                        Style="{StaticResource ButtonStyle}"
                        Visibility="{Binding ShowPreviewButton, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Margin="0,0,10,0" />
                <Button Content="关闭" Command="{Binding CloseCommand}"
                        Style="{StaticResource SecondaryButtonStyle}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
