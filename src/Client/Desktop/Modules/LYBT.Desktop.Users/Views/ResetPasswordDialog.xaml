<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="LYBT.Desktop.Users.Views.ResetPasswordDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:LYBT.Desktop.Users.ViewModels"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d"
             d:DesignHeight="520" d:DesignWidth="540"
             d:DataContext="{d:DesignInstance Type=viewmodels:ResetPasswordDialogViewModel}">

    <UserControl.Resources>
        <!-- 现代密码框样式 -->
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Height" Value="46" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#E5E7EB" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"
                                          Margin="0" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2563EB" />
                                <Setter TargetName="border" Property="BorderThickness" Value="2" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 现代警告按钮样式 -->
        <Style x:Key="ModernWarningButton" TargetType="Button">
            <Setter Property="Height" Value="46" />
            <Setter Property="Background" Value="#F59E0B" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="contentPresenter"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Focusable="False"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#D1D5DB" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 现代次要按钮样式 -->
        <Style x:Key="ModernSecondaryButton" TargetType="Button">
            <Setter Property="Height" Value="46" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="#6B7280" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#D1D5DB" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="contentPresenter"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Focusable="False"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F9FAFB" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F3F4F6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 现代成功按钮样式 -->
        <Style x:Key="ModernSuccessButton" TargetType="Button">
            <Setter Property="Height" Value="40" />
            <Setter Property="Background" Value="#10B981" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="20,0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Focusable="False"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 现代复选框样式 -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="#374151" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <!-- 卡片阴影效果 -->
        <DropShadowEffect x:Key="CardShadow"
                          Color="#000000"
                          Direction="270"
                          ShadowDepth="2"
                          BlurRadius="16"
                          Opacity="0.05" />
    </UserControl.Resources>

    <Border Background="White"
            CornerRadius="16"
            Padding="40">
        <Border.Effect>
            <DropShadowEffect Color="#000000"
                              Direction="270"
                              ShadowDepth="4"
                              BlurRadius="32"
                              Opacity="0.1" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 标题 -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text=" 重置用户密码"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="#111827"
                           HorizontalAlignment="Center" />
                <TextBlock FontSize="15"
                           Foreground="#6B7280"
                           HorizontalAlignment="Center"
                           Margin="0,12,0,0"
                           TextAlignment="Center">
                    <Run Text="您正在为用户 " />
                    <Run Text="{Binding UserName}" FontWeight="SemiBold" Foreground="#3B82F6" />
                    <Run Text=" 重置密码" />
                </TextBlock>
            </StackPanel>

            <!-- 警告信息 - 现代样式 -->
            <Border Grid.Row="1"
                    Background="#FEF3C7"
                    BorderBrush="#F59E0B"
                    BorderThickness="2"
                    CornerRadius="12"
                    Padding="20"
                    Margin="0,0,0,24">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                Width="32" Height="32"
                                CornerRadius="16"
                                Background="#F59E0B"
                                VerticalAlignment="Top"
                                Margin="0,0,12,0">
                            <TextBlock Text="⚠"
                                       FontSize="18"
                                       Foreground="White"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>

                        <TextBlock Grid.Column="1"
                                   Text="注意事项"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="#92400E"
                                   VerticalAlignment="Center" />
                    </Grid>

                    <TextBlock TextWrapping="Wrap"
                               Foreground="#92400E"
                               FontSize="14"
                               LineHeight="24">
                        <Run Text="• 重置密码后,用户需要使用新密码登录" />
                        <LineBreak />
                        <Run Text="• 建议通知用户及时修改初始密码" />
                        <LineBreak />
                        <Run Text="• 此操作将被记录在系统日志中" />
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- 表单内容 - 卡片样式 -->
            <Border Grid.Row="2"
                    Background="#F9FAFB"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="24"
                    Margin="0,0,0,24">
                <Border.Effect>
                    <StaticResource ResourceKey="CardShadow" />
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- 新密码 -->
                    <TextBlock Grid.Row="0"
                               Text="新密码"
                               FontSize="15"
                               FontWeight="SemiBold"
                               Foreground="#374151"
                               Margin="0,0,0,8" />
                    <Grid Grid.Row="1" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <PasswordBox Grid.Column="0"
                                     x:Name="NewPasswordBox"
                                     Style="{StaticResource ModernPasswordBox}"
                                     PasswordChanged="OnPasswordChanged" />
                        <Button Grid.Column="1"
                                Content="🎲 生成随机密码"
                                Style="{StaticResource ModernSuccessButton}"
                                Command="{Binding GeneratePasswordCommand}"
                                Margin="12,0,0,0" />
                    </Grid>

                    <!-- 确认密码 -->
                    <TextBlock Grid.Row="2"
                               Text="确认密码"
                               FontSize="15"
                               FontWeight="SemiBold"
                               Foreground="#374151"
                               Margin="0,0,0,8" />
                    <PasswordBox Grid.Row="3"
                                 x:Name="ConfirmPasswordBox"
                                 Style="{StaticResource ModernPasswordBox}"
                                 PasswordChanged="OnPasswordChanged" />
                </Grid>
            </Border>

            <!-- 选项 -->
            <StackPanel Grid.Row="3">
                <CheckBox IsChecked="{Binding RequirePasswordChange}"
                          Style="{StaticResource ModernCheckBox}"
                          Margin="0,0,0,12">
                    <TextBlock Text="✓ 要求用户在下次登录时修改密码"
                               FontSize="14"
                               Foreground="#374151" />
                </CheckBox>

                <CheckBox IsChecked="{Binding SendNotification}"
                          Style="{StaticResource ModernCheckBox}"
                          Margin="0,0,0,0">
                    <TextBlock Text="📧 发送密码重置通知给用户"
                               FontSize="14"
                               Foreground="#374151" />
                </CheckBox>

                <!-- 错误提示 - 现代样式 -->
                <Border Background="#FEE2E2"
                        BorderBrush="#EF4444"
                        BorderThickness="2"
                        CornerRadius="10"
                        Padding="16"
                        Margin="0,16,0,0"
                        Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <Border Width="28" Height="28"
                                CornerRadius="14"
                                Background="#EF4444"
                                VerticalAlignment="Top"
                                Margin="0,0,12,0">
                            <TextBlock Text="⚠"
                                       FontSize="16"
                                       Foreground="White"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>
                        <TextBlock Text="{Binding ErrorMessage}"
                                   Foreground="#991B1B"
                                   FontSize="14"
                                   TextWrapping="Wrap"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- 按钮 - 现代样式 -->
            <Grid Grid.Row="4" Margin="0,32,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="✓ 确认重置"
                        Style="{StaticResource ModernWarningButton}"
                        Command="{Binding ConfirmCommand}"
                        IsDefault="True"
                        Margin="0,0,12,0" />

                <Button Grid.Column="1"
                        Content="✗ 取消"
                        Style="{StaticResource ModernSecondaryButton}"
                        Command="{Binding CancelCommand}"
                        IsCancel="True"
                        Margin="12,0,0,0" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
