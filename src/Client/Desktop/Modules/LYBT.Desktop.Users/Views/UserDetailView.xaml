<!--
    UserDetailView - 用户详情视图
    OpenSpec: extract-detail-controls Task 4.3

    重构说明：
    - 使用 BaseDetailContainer 容器实现查看/编辑模式分离
    - ViewContent: 使用 UserViewControl 预览控件
    - EditContent: 使用 UserEditControl 编辑控件
-->
<UserControl x:Class="LYBT.Desktop.Users.Views.UserDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:views="clr-namespace:LYBT.Desktop.Infrastructure.Views;assembly=LYBT.Desktop.Infrastructure"
             xmlns:userControls="clr-namespace:LYBT.Desktop.Users.Controls"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/LYBT.Desktop.Infrastructure;component/Themes/UnifiedComponents.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <views:BaseDetailContainer
        Title="{Binding PageTitle}"
        IsEditMode="{Binding IsEditMode, Mode=OneWay}"
        GoBackCommand="{Binding GoBackCommand}"
        SwitchToEditCommand="{Binding SwitchToEditModeCommand}"
        SaveCommand="{Binding SubmitCommand}"
        CancelCommand="{Binding CancelCommand}"
        SaveButtonText="{Binding SaveButtonText}"
        IsLoading="{Binding IsLoading}"
        LoadingMessage="{Binding StatusMessage}">

        <!-- ========================================
             查看模式内容 (ViewContent)
             使用 UserViewControl 预览控件
             ======================================== -->
        <views:BaseDetailContainer.ViewContent>
            <userControls:UserViewControl
                UserName="{Binding UserName}"
                RealName="{Binding RealName}"
                PhoneNumber="{Binding PhoneNumber}"
                Email="{Binding Email}"
                Role="{Binding SelectedRole}"
                Status="{Binding Status}"
                ShowStatus="{Binding IsEditOrViewMode}"/>
        </views:BaseDetailContainer.ViewContent>

        <!-- ========================================
             编辑模式内容 (EditContent)
             使用 UserEditControl 编辑控件
             ======================================== -->
        <views:BaseDetailContainer.EditContent>
            <userControls:UserEditControl
                UserName="{Binding UserName, Mode=TwoWay}"
                IsUserNameReadOnly="{Binding IsEditOrViewMode}"
                RealName="{Binding RealName, Mode=TwoWay}"
                PhoneNumber="{Binding PhoneNumber, Mode=TwoWay}"
                Email="{Binding Email, Mode=TwoWay}"
                Role="{Binding SelectedRole, Mode=TwoWay}"
                RoleOptions="{Binding RoleOptions}"
                Status="{Binding Status, Mode=TwoWay}"
                StatusOptions="{Binding StatusOptions}"
                ShowStatus="{Binding IsEditOrViewMode}"/>
        </views:BaseDetailContainer.EditContent>
    </views:BaseDetailContainer>
</UserControl>
