<UserControl x:Class="LYBT.Desktop.Users.Views.UserEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <UserControl.Resources>
        <!-- 现代化文本框样式 -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="46" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="BorderBrush" Value="#D1D5DB" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          Focusable="false"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3B82F6"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2563EB"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter Property="Background" Value="#F3F4F6"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#E5E7EB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 现代化下拉框样式 -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="46" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="BorderBrush" Value="#D1D5DB" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="toggleButton"
                                          Grid.Column="0"
                                          Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0"
                                                                  Content="{TemplateBinding Content}"
                                                                  Margin="16,0,0,0"
                                                                  VerticalAlignment="Center"
                                                                  HorizontalAlignment="Left"/>
                                                <TextBlock Grid.Column="1"
                                                           Text="▼"
                                                           FontSize="10"
                                                           Foreground="#64748B"
                                                           VerticalAlignment="Center"
                                                           Margin="0,0,16,0"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="BorderBrush" Value="#3B82F6"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="border" Property="BorderBrush" Value="#2563EB"/>
                                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="contentPresenter"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="16,0,40,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              IsHitTestVisible="False"/>
                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border Background="White"
                                        BorderBrush="#E5E7EB"
                                        BorderThickness="1.5"
                                        CornerRadius="8"
                                        Margin="0,4,0,0"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="300">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.08" Color="#000000"/>
                                    </Border.Effect>
                                    <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Height" Value="40"/>
                        <Setter Property="Padding" Value="16,0"/>
                        <Setter Property="FontSize" Value="15"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="border" Background="Transparent" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#F1F5F9"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#EFF6FF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 卡片阴影 -->
        <DropShadowEffect x:Key="CardShadow"
                          BlurRadius="16"
                          ShadowDepth="2"
                          Opacity="0.05"
                          Color="#000000"/>
    </UserControl.Resources>

    <Grid Background="#F8FAFC">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 表单区域 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="40,28">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- 顶部操作栏 -->
                <Border Grid.Row="0"
                        Background="White"
                        CornerRadius="16"
                        Padding="32,24"
                        Margin="0,0,0,28"
                        Effect="{StaticResource CardShadow}">
                    <Grid MinHeight="64">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- 返回按钮 -->
                        <Button Grid.Column="0"
                                Command="{Binding CancelCommand}"
                                Background="Transparent"
                                BorderThickness="0"
                                Foreground="#64748B"
                                FontSize="15"
                                FontWeight="Medium"
                                Cursor="Hand"
                                Padding="14,10"
                                Margin="0,0,20,0"
                                VerticalAlignment="Center">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        CornerRadius="8"
                                                        Padding="{TemplateBinding Padding}">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="◀"
                                                                   FontSize="14"
                                                                   Foreground="{TemplateBinding Foreground}"
                                                                   VerticalAlignment="Center"
                                                                   Margin="0,0,8,0"/>
                                                        <TextBlock Text="返回"
                                                                   FontSize="{TemplateBinding FontSize}"
                                                                   FontWeight="{TemplateBinding FontWeight}"
                                                                   Foreground="{TemplateBinding Foreground}"
                                                                   VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#F1F5F9" />
                                                        <Setter Property="Foreground" Value="#475569" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>

                        <!-- 标题 -->
                        <TextBlock Grid.Column="1"
                                   Text="编辑用户信息"
                                   FontSize="26"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"
                                   Foreground="#1E293B" />
                    </Grid>
                </Border>

                <!-- 基本信息 -->
                <Border Grid.Row="1"
                        Background="White"
                        CornerRadius="16"
                        Margin="0,0,0,28"
                        Effect="{StaticResource CardShadow}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- 标题栏 -->
                        <Border Grid.Row="0"
                                Background="#F9FAFB"
                                CornerRadius="16,16,0,0"
                                Padding="32,20">
                            <TextBlock Text="基本信息"
                                       FontSize="19"
                                       FontWeight="SemiBold"
                                       Foreground="#111827" />
                        </Border>

                        <!-- 内容 -->
                        <Grid Grid.Row="1" Margin="40,32">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!-- 头像 -->
                            <Border Grid.Column="0" Grid.Row="0"
                                    Width="136" Height="136"
                                    CornerRadius="68"
                                    Background="#3B82F6"
                                    Margin="0,8,48,0"
                                    VerticalAlignment="Top">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="12"
                                                      ShadowDepth="2"
                                                      Opacity="0.15"
                                                      Color="#3B82F6"/>
                                </Border.Effect>
                                <TextBlock Text="{Binding UserName, Converter={StaticResource FirstCharacterConverter}}"
                                           FontSize="52"
                                           FontWeight="Bold"
                                           Foreground="White"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center" />
                            </Border>

                            <!-- 编辑表单 -->
                            <Grid Grid.Column="1" Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="40" />
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="20" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="20" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <!-- 第一行 -->
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="用户名："
                                           Foreground="#6B7280"
                                           FontSize="15"
                                           FontWeight="Medium"
                                           VerticalAlignment="Center" />
                                <TextBox Grid.Row="0" Grid.Column="1"
                                         Text="{Binding UserName, Mode=OneWay}"
                                         IsReadOnly="True"
                                         Style="{StaticResource ModernTextBox}"
                                         VerticalAlignment="Center" />

                                <TextBlock Grid.Row="0" Grid.Column="3" Text="真实姓名："
                                           Foreground="#6B7280"
                                           FontSize="15"
                                           FontWeight="Medium"
                                           VerticalAlignment="Center">
                                    <Run Text="*" Foreground="#EF4444" FontWeight="Bold" />
                                </TextBlock>
                                <StackPanel Grid.Row="0" Grid.Column="4" VerticalAlignment="Center">
                                    <TextBox Text="{Binding RealName, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource ModernTextBox}"
                                             VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Errors[RealName]}"
                                               Foreground="#EF4444"
                                               FontSize="13"
                                               FontWeight="Medium"
                                               Margin="4,6,0,0" />
                                </StackPanel>

                                <!-- 第二行 -->
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="手机号码："
                                           Foreground="#6B7280"
                                           FontSize="15"
                                           FontWeight="Medium"
                                           VerticalAlignment="Center" />
                                <StackPanel Grid.Row="2" Grid.Column="1" VerticalAlignment="Center">
                                    <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource ModernTextBox}"
                                             VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Errors[PhoneNumber]}"
                                               Foreground="#EF4444"
                                               FontSize="13"
                                               FontWeight="Medium"
                                               Margin="4,6,0,0" />
                                </StackPanel>

                                <TextBlock Grid.Row="2" Grid.Column="3" Text="邮箱地址："
                                           Foreground="#6B7280"
                                           FontSize="15"
                                           FontWeight="Medium"
                                           VerticalAlignment="Center" />
                                <StackPanel Grid.Row="2" Grid.Column="4" VerticalAlignment="Center">
                                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                                             Style="{StaticResource ModernTextBox}"
                                             VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Errors[Email]}"
                                               Foreground="#EF4444"
                                               FontSize="13"
                                               FontWeight="Medium"
                                               Margin="4,6,0,0" />
                                </StackPanel>

                                <!-- 第三行 -->
                                <TextBlock Grid.Row="4" Grid.Column="0" Text="用户角色："
                                           Foreground="#6B7280"
                                           FontSize="15"
                                           FontWeight="Medium"
                                           VerticalAlignment="Center">
                                    <Run Text="*" Foreground="#EF4444" FontWeight="Bold" />
                                </TextBlock>
                                <ComboBox Grid.Row="4" Grid.Column="1"
                                          ItemsSource="{Binding RoleOptions}"
                                          SelectedValue="{Binding SelectedRole}"
                                          Style="{StaticResource ModernComboBox}"
                                          VerticalAlignment="Center" />

                                <TextBlock Grid.Row="4" Grid.Column="3" Text="用户状态："
                                           Foreground="#6B7280"
                                           FontSize="15"
                                           FontWeight="Medium"
                                           VerticalAlignment="Center">
                                    <Run Text="*" Foreground="#EF4444" FontWeight="Bold" />
                                </TextBlock>
                                <ComboBox Grid.Row="4" Grid.Column="4"
                                          ItemsSource="{Binding StatusOptions}"
                                          SelectedValue="{Binding Status}"
                                          Style="{StaticResource ModernComboBox}"
                                          VerticalAlignment="Center" />
                            </Grid>
                        </Grid>
                    </Grid>
                </Border>

                <!-- 状态提示 -->
                <Border Grid.Row="2"
                        Background="White"
                        CornerRadius="16"
                        Margin="0,0,0,28"
                        Effect="{StaticResource CardShadow}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- 标题栏 -->
                        <Border Grid.Row="0"
                                Background="#F9FAFB"
                                CornerRadius="16,16,0,0"
                                Padding="32,20">
                            <TextBlock Text="编辑状态"
                                       FontSize="19"
                                       FontWeight="SemiBold"
                                       Foreground="#111827" />
                        </Border>

                        <!-- 内容 -->
                        <StackPanel Grid.Row="1" Margin="40,32">
                            <!-- 未保存更改提示 - 增强版 -->
                            <Border Background="#FEF3C7"
                                    BorderBrush="#F59E0B"
                                    BorderThickness="2"
                                    CornerRadius="10"
                                    Padding="20,16"
                                    Visibility="{Binding HasChanges, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0"
                                            Width="32" Height="32"
                                            CornerRadius="16"
                                            Background="#FBBF24"
                                            VerticalAlignment="Center"
                                            Margin="0,0,16,0">
                                        <TextBlock Text="⚠"
                                                   FontSize="18"
                                                   Foreground="White"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="您有未保存的更改"
                                                   Foreground="#B45309"
                                                   FontSize="16"
                                                   FontWeight="SemiBold"
                                                   Margin="0,0,0,4" />
                                        <TextBlock Text="请确保在离开页面前保存您的修改"
                                                   Foreground="#92400E"
                                                   FontSize="14" />
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <TextBlock Text="所有标记 * 的字段为必填项"
                                       Foreground="#6B7280"
                                       FontSize="14"
                                       Margin="4,16,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- 按钮区域 - 右对齐 -->
        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#E5E7EB"
                BorderThickness="0,1,0,0"
                Padding="40,24">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="重置表单"
                        Style="{StaticResource SecondaryButton}"
                        Margin="0,0,16,0"
                        Height="48"
                        MinWidth="130"
                        FontSize="15"
                        FontWeight="Medium"
                        Command="{Binding ResetCommand}" />
                <Button Content="保存修改"
                        Style="{StaticResource SuccessButton}"
                        Height="48"
                        MinWidth="130"
                        FontSize="15"
                        FontWeight="Medium"
                        Command="{Binding SaveCommand}" />
            </StackPanel>
        </Border>

        <!-- 加载遮罩 -->
        <Grid Grid.RowSpan="3"
              Background="#90000000"
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="White"
                    CornerRadius="16"
                    Padding="40,32"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.3" Color="#000000"/>
                </Border.Effect>
                <StackPanel>
                    <ProgressBar IsIndeterminate="True"
                                 Width="240"
                                 Height="6"
                                 Background="#E5E7EB"
                                 Foreground="#3B82F6"
                                 BorderThickness="0"/>
                    <TextBlock Text="正在保存修改..."
                               Foreground="#1E293B"
                               FontSize="16"
                               FontWeight="Medium"
                               HorizontalAlignment="Center"
                               Margin="0,20,0,0" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
