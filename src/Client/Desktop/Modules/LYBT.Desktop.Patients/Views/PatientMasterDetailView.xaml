<!--
    PatientMasterDetailView - 患者Master-Detail视图
    包装PatientMasterDetailControl，供导航使用
-->
<UserControl x:Class="LYBT.Desktop.Patients.Views.PatientMasterDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:LYBT.Desktop.Patients.Controls"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <controls:PatientMasterDetailControl DataContext="{Binding}" />
    </Grid>
</UserControl>
