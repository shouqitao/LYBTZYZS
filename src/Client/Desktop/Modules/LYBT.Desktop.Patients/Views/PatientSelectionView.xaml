<UserControl x:Class="LYBT.Desktop.Patients.Views.PatientSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <UserControl.Resources>
        <!-- 标签样式 -->
        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,5,10,5"/>
        </Style>

        <!-- 按钮样式 -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5,0"/>
        </Style>

        <!-- 分页按钮样式 -->
        <Style x:Key="PageButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="3,0"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- 标题 -->
                <RowDefinition Height="Auto"/> <!-- 搜索区 -->
                <RowDefinition Height="*"/>    <!-- DataGrid -->
                <RowDefinition Height="Auto"/> <!-- 分页 -->
                <RowDefinition Height="Auto"/> <!-- 提示信息 -->
            </Grid.RowDefinitions>

            <!-- Row 0: 标题 -->
            <TextBlock Grid.Row="0"
                      Text="患者选择（Patient Selection）"
                      FontSize="18"
                      FontWeight="Bold"
                      Foreground="#2E86AB"
                      Margin="0,0,0,20"/>

            <!-- Row 1: 搜索区 -->
            <Border Grid.Row="1"
                   BorderBrush="#E0E0E0"
                   BorderThickness="1"
                   CornerRadius="5"
                   Background="White"
                   Padding="15"
                   Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="搜索患者（支持姓名/拼音码/手机号）"
                              Style="{StaticResource FieldLabelStyle}"
                              Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0"
                                Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                                FontSize="14"
                                Padding="10,8"
                                VerticalContentAlignment="Center">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>

                        <Button Grid.Column="1"
                               Content="🔍 搜索"
                               Command="{Binding SearchCommand}"
                               Style="{StaticResource ActionButtonStyle}"
                               Background="#2E86AB"
                               Foreground="White"
                               BorderThickness="0"/>

                        <Button Grid.Column="2"
                               Content="+ 新建患者"
                               Command="{Binding NewPatientCommand}"
                               Style="{StaticResource ActionButtonStyle}"
                               Background="#4CAF50"
                               Foreground="White"
                               BorderThickness="0"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Row 2: 患者列表DataGrid -->
            <Border Grid.Row="2"
                   BorderBrush="#E0E0E0"
                   BorderThickness="1"
                   CornerRadius="5"
                   Background="White"
                   Margin="0,0,0,15">
                <DataGrid ItemsSource="{Binding Patients}"
                         SelectedItem="{Binding SelectedPatient}"
                         AutoGenerateColumns="False"
                         CanUserAddRows="False"
                         CanUserDeleteRows="False"
                         IsReadOnly="True"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         SelectionMode="Single"
                         GridLinesVisibility="Horizontal"
                         HeadersVisibility="Column"
                         MinHeight="300"
                         MaxHeight="650">
                    <DataGrid.InputBindings>
                        <MouseBinding Gesture="LeftDoubleClick"
                                     Command="{Binding DoubleClickPatientCommand}"
                                     CommandParameter="{Binding SelectedPatient}"/>
                    </DataGrid.InputBindings>

                    <DataGrid.Columns>
                        <!-- 姓名 -->
                        <DataGridTextColumn Header="姓名" Binding="{Binding Name}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#2E86AB"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- 性别 -->
                        <DataGridTextColumn Header="性别" Binding="{Binding Gender}" Width="60"/>

                        <!-- 年龄 -->
                        <DataGridTextColumn Header="年龄" Width="60">
                            <DataGridTextColumn.Binding>
                                <Binding Path="Age" StringFormat="{}{0}岁"/>
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>

                        <!-- 手机号 -->
                        <DataGridTextColumn Header="手机号" Binding="{Binding PhoneNumber}" Width="140"/>

                        <!-- 最近就诊 -->
                        <DataGridTextColumn Header="最近就诊" Width="140">
                            <DataGridTextColumn.Binding>
                                <Binding Path="LastVisitTime" StringFormat="{}{0:yyyy-MM-dd}"/>
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>

                        <!-- 拼音码 -->
                        <DataGridTextColumn Header="拼音码" Binding="{Binding PinYinCode}" Width="100"/>

                        <!-- 操作按钮 -->
                        <DataGridTemplateColumn Header="操作" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="选择"
                                           Command="{Binding DataContext.SelectPatientCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           Padding="10,5"
                                           FontSize="13"
                                           Background="#2E86AB"
                                           Foreground="White"
                                           BorderThickness="0"
                                           Cursor="Hand"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Row 3: 分页控件 -->
            <Border Grid.Row="3"
                   BorderBrush="#E0E0E0"
                   BorderThickness="1"
                   CornerRadius="5"
                   Background="#F5F5F5"
                   Padding="15,10"
                   Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 左侧：统计信息 -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="共" Foreground="#666" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding TotalCount}" FontWeight="Bold" Foreground="#2E86AB" Margin="5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="条记录" Foreground="#666" VerticalAlignment="Center"/>
                        <TextBlock Text="，每页" Foreground="#666" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        <TextBlock Text="50" FontWeight="Bold" Foreground="#2E86AB" Margin="5,0" VerticalAlignment="Center"/>
                        <TextBlock Text="条" Foreground="#666" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- 右侧：分页按钮 -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Content="◀ 上一页"
                               Command="{Binding PreviousPageCommand}"
                               Style="{StaticResource PageButtonStyle}"/>

                        <StackPanel Orientation="Horizontal" Margin="10,0">
                            <TextBlock Text="第" Foreground="#666" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding CurrentPage}" FontWeight="Bold" Foreground="#2E86AB" Margin="5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="/" Foreground="#666" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding TotalPages}" FontWeight="Bold" Foreground="#2E86AB" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="页" Foreground="#666" VerticalAlignment="Center"/>
                        </StackPanel>

                        <Button Content="下一页 ▶"
                               Command="{Binding NextPageCommand}"
                               Style="{StaticResource PageButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Row 4: 提示信息 -->
            <Border Grid.Row="4"
                   Background="#E3F2FD"
                   BorderBrush="#2196F3"
                   BorderThickness="1"
                   CornerRadius="3"
                   Padding="15">
                <StackPanel>
                    <TextBlock Text="💡 提示：" FontWeight="Bold" FontSize="13" Foreground="#1976D2"/>
                    <TextBlock Text="• 双击患者行或点击【选择】按钮进入看诊流程"
                              FontSize="12"
                              Foreground="#666"
                              Margin="0,5,0,0"/>
                    <TextBlock Text="• 选择后将自动创建医案（MedicalCase），进入诊断录入（Step 2）"
                              FontSize="12"
                              Foreground="#666"
                              Margin="0,3,0,0"/>
                    <TextBlock Text="• 支持按拼音码搜索（例如输入'LS'查找'李四'）"
                              FontSize="12"
                              Foreground="#666"
                              Margin="0,3,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
