<UserControl x:Class="LYBT.Desktop.Patients.Views.PatientSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:converters="clr-namespace:LYBT.Desktop.Infrastructure.Converters;assembly=LYBT.Desktop.Infrastructure"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <!-- 转换器已统一到Infrastructure/Converters.xaml (OpenSpec: consolidate-wpf-converters) -->
    <!-- OpenSpec: refactor-patient-selection - 使用UnifiedDesignSystem全局样式 -->

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>   <!-- 顶部导航栏 -->
            <RowDefinition Height="*"/>    <!-- 主内容区（三区域布局） -->
            <RowDefinition Height="80"/>   <!-- 底部操作栏 -->
        </Grid.RowDefinitions>

        <!-- 顶部导航栏 -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="20,0">
                <TextBlock Text="患者选择" FontSize="20" FontWeight="Bold"
                          VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
                <Button Content="返回主页"
                       Command="{Binding BackToHomeCommand}"
                       HorizontalAlignment="Right"
                       Style="{StaticResource SecondaryButton}"/>
            </Grid>
        </Border>

        <!-- 主内容区：三区域布局（左300px双行 + 右自适应） -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>  <!-- 左侧：患者信息 + 待看诊队列 -->
                <ColumnDefinition Width="10"/>   <!-- 分隔 -->
                <ColumnDefinition Width="*"/>    <!-- 右侧：全部患者列表 -->
            </Grid.ColumnDefinitions>

            <!-- 左侧：患者信息详情 + 待看诊队列 -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="180"/>  <!-- 患者信息详情 -->
                    <RowDefinition Height="10"/>   <!-- 分隔 -->
                    <RowDefinition Height="*"/>    <!-- 待看诊队列（自适应） -->
                </Grid.RowDefinitions>

                <!-- 患者信息详情模块 -->
                <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="15" CornerRadius="5"
                       BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <Grid>
                        <!-- 有患者时显示患者信息 -->
                        <Grid>
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentPatient}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- 标题 -->
                            <TextBlock Grid.Row="0" Text="患者信息" FontWeight="Bold" FontSize="14"
                                      Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,10"/>

                            <!-- 患者详细信息 -->
                            <StackPanel Grid.Row="1">
                                <!-- 姓名 -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="姓　　名：" Width="80" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                                    <TextBlock Text="{Binding CurrentPatient.Name}" FontWeight="Bold" FontSize="12"/>
                                </StackPanel>

                                <!-- 手机号（脱敏） -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="手　机　号：" Width="80" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                                    <TextBlock Text="{Binding CurrentPatient.PhoneNumber}" FontSize="12"/>
                                </StackPanel>

                                <!-- 年龄 -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="年　　龄：" Width="80" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                                    <TextBlock FontSize="12">
                                        <Run Text="{Binding CurrentPatient.Age, Mode=OneWay}"/>
                                        <Run Text="岁"/>
                                    </TextBlock>
                                </StackPanel>

                                <!-- 地址 -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="地　　址：" Width="80" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" VerticalAlignment="Top"/>
                                    <TextBlock Text="{Binding CurrentPatient.Address}" FontSize="12" TextWrapping="Wrap" MaxWidth="180"/>
                                </StackPanel>

                                <!-- 过敏史 -->
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="过　敏　史：" Width="80" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" VerticalAlignment="Top"/>
                                    <TextBlock Text="{Binding CurrentPatient.AllergyHistory, TargetNullValue='无'}"
                                              FontSize="12" TextWrapping="Wrap" MaxWidth="180" Foreground="{StaticResource ErrorBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>

                        <!-- 未选择患者时的提示 -->
                        <TextBlock Text="请从列表中选择患者" FontSize="13" Foreground="{StaticResource TextHintBrush}"
                                  HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentPatient}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>

                <!-- 待看诊队列 -->
                <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" Padding="15" CornerRadius="5"
                       BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- 标题和刷新按钮 -->
                        <Grid Grid.Row="0" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="待看诊队列" FontWeight="Bold" FontSize="14"
                                      Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>

                            <!-- Epic #2210 Task 3.2.2: FR-006 手动刷新队列 -->
                            <Button Grid.Column="1" Content="🔄" ToolTip="刷新待诊队列"
                                   Command="{Binding RefreshPendingQueueCommand}"
                                   IsEnabled="{Binding IsRefreshing, Converter={StaticResource InverseBooleanConverter}}"
                                   Width="30" Height="30" FontSize="16"
                                   Background="Transparent" BorderThickness="0"
                                   Cursor="Hand"/>
                        </Grid>

                        <!-- 待看诊列表 - UI美化: 对齐、字体、间距优化 -->
                        <DataGrid Grid.Row="1"
                                 Visibility="{Binding HasNoPendingPatients, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                                 ItemsSource="{Binding PendingQueue}"
                                 SelectedItem="{Binding SelectedPendingPatient}"
                                 AutoGenerateColumns="False"
                                 IsReadOnly="True"
                                 SelectionMode="Single"
                                 CanUserAddRows="False"
                                 CanUserDeleteRows="False"
                                 CanUserResizeRows="False"
                                 HeadersVisibility="Column"
                                 GridLinesVisibility="None"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 RowHeight="42"
                                 FontFamily="Microsoft YaHei"
                                 FontSize="13">

                            <!-- OpenSpec: refactor-patient-selection Task 2.1 - 键盘导航支持 -->
                            <!-- 双击/回车：进入看诊 -->
                            <DataGrid.InputBindings>
                                <MouseBinding Gesture="LeftDoubleClick"
                                            Command="{Binding StartConsultationCommand}"/>
                                <KeyBinding Key="Return"
                                           Command="{Binding StartConsultationCommand}"/>
                            </DataGrid.InputBindings>

                            <!-- 列头样式 -->
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                    <Setter Property="FontSize" Value="12"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Setter Property="Height" Value="32"/>
                                    <Setter Property="Padding" Value="8,0"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>

                            <!-- 单元格样式 -->
                            <DataGrid.CellStyle>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Padding" Value="8,0"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                                    <ContentPresenter VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>

                            <DataGrid.Columns>
                                <!-- 患者姓名 -->
                                <DataGridTextColumn Header="姓名"
                                                  Binding="{Binding PatientName}"
                                                  Width="70">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- 手机号（脱敏） -->
                                <DataGridTextColumn Header="电话"
                                                  Binding="{Binding PhoneMasked}"
                                                  Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- 状态 -->
                                <DataGridTextColumn Header="状态"
                                                  Binding="{Binding Type}"
                                                  Width="*">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                            <Setter Property="FontWeight" Value="Medium"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>

                            <!-- 行样式：选中高亮 + 斑马纹 -->
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="#F0F0F0"/>
                                    <Setter Property="Margin" Value="0"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="{StaticResource DataGridSelectedBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{StaticResource DataGridHoverBrush}"/>
                                        </Trigger>
                                        <Trigger Property="AlternationIndex" Value="1">
                                            <Setter Property="Background" Value="{StaticResource DataGridAlternateRowBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                        </DataGrid>

                        <!-- Epic #2210 Task 3.2.3: FR-007 空状态UI -->
                        <StackPanel Grid.Row="1"
                                   Visibility="{Binding HasNoPendingPatients, Converter={StaticResource BooleanToVisibilityConverter}}"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center">
                            <!-- 图标 -->
                            <TextBlock Text="📋" FontSize="48" 
                                      HorizontalAlignment="Center" Margin="0,0,0,20"/>
                            
                            <!-- 主标题 -->
                            <TextBlock Text="暂无待诊患者" FontSize="16" FontWeight="Bold"
                                      Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                            <!-- 副标题 -->
                            <TextBlock Text="从左侧选择患者或等待新的挂号" FontSize="12"
                                      Foreground="{StaticResource TextHintBrush}" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <!-- 右侧：全部患者列表 -->
            <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}" Padding="15" CornerRadius="5"
                   BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>  <!-- 标题 -->
                        <RowDefinition Height="Auto"/>  <!-- 搜索框和按钮区 -->
                        <RowDefinition Height="*"/>     <!-- 患者列表 -->
                        <RowDefinition Height="Auto"/>  <!-- 分页控件 -->
                    </Grid.RowDefinitions>

                    <!-- 标题 -->
                    <TextBlock Grid.Row="0" Text="全部患者" FontWeight="Bold" FontSize="16"
                              Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,15"/>

                    <!-- 搜索框和按钮区 -->
                    <Grid Grid.Row="1" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>    <!-- 搜索框 -->
                            <ColumnDefinition Width="Auto"/> <!-- 搜索状态指示 -->
                            <ColumnDefinition Width="Auto"/> <!-- 新建患者按钮 -->
                        </Grid.ColumnDefinitions>

                        <!-- 搜索框（实时搜索，500ms防抖） -->
                        <TextBox Grid.Column="0"
                                Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                                Height="35"
                                Padding="10,0"
                                VerticalContentAlignment="Center"
                                BorderBrush="{StaticResource DisabledBrush}"
                                BorderThickness="1">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
                                    <Style.Triggers>
                                        <Trigger Property="Text" Value="">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <VisualBrush Stretch="None" AlignmentX="Left">
                                                        <VisualBrush.Visual>
                                                            <TextBlock Text=" 输入姓名/手机号/拼音码搜索（实时）"
                                                                      Foreground="{StaticResource DisabledBrush}"
                                                                      FontSize="12"
                                                                      Margin="10,0,0,0"/>
                                                        </VisualBrush.Visual>
                                                    </VisualBrush>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>

                        <!-- OpenSpec: refactor-patient-selection Task 2.2 - 搜索状态指示 -->
                        <TextBlock Grid.Column="1"
                                  Text="搜索中..."
                                  FontSize="12"
                                  Foreground="{StaticResource TextHintBrush}"
                                  VerticalAlignment="Center"
                                  Margin="10,0,0,0"
                                  Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                        <!-- 新建患者按钮 -->
                        <Button Grid.Column="2"
                               Content="+ 新建患者"
                               Command="{Binding NewPatientCommand}"
                               Width="100" Height="35" Margin="10,0,0,0"
                               Style="{StaticResource SecondaryButton}"/>
                    </Grid>

                    <!-- 患者列表 - UI美化: 对齐、字体、间距优化 -->
                    <DataGrid Grid.Row="2"
                             ItemsSource="{Binding Patients}"
                             SelectedItem="{Binding SelectedPatient}"
                             AutoGenerateColumns="False"
                             IsReadOnly="True"
                             SelectionMode="Single"
                             CanUserAddRows="False"
                             CanUserDeleteRows="False"
                             CanUserResizeRows="False"
                             HeadersVisibility="Column"
                             GridLinesVisibility="None"
                             BorderBrush="{StaticResource BorderBrush}"
                             BorderThickness="1"
                             Background="Transparent"
                             RowHeight="42"
                             AlternationCount="2"
                             FontFamily="Microsoft YaHei"
                             FontSize="13">

                        <!-- OpenSpec: refactor-patient-selection Task 2.1 - 键盘导航支持 -->
                        <!-- 双击/回车：进入看诊 -->
                        <DataGrid.InputBindings>
                            <MouseBinding Gesture="LeftDoubleClick"
                                        Command="{Binding StartConsultationCommand}"/>
                            <KeyBinding Key="Return"
                                       Command="{Binding StartConsultationCommand}"/>
                        </DataGrid.InputBindings>

                        <!-- 列头样式 -->
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="FontWeight" Value="Medium"/>
                                <Setter Property="Height" Value="36"/>
                                <Setter Property="Padding" Value="12,0"/>
                                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <!-- 单元格样式 -->
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="12,0"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Padding="{TemplateBinding Padding}"
                                                   Background="{TemplateBinding Background}">
                                                <ContentPresenter VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>

                        <DataGrid.Columns>
                            <!-- 姓名 -->
                            <DataGridTextColumn Header="姓名"
                                              Binding="{Binding Name}"
                                              Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- 性别 - OpenSpec: unify-frontend-backend-types Phase 1 -->
                            <DataGridTextColumn Header="性别"
                                              Binding="{Binding GenderDisplay}"
                                              Width="60">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- 年龄 -->
                            <DataGridTextColumn Header="年龄"
                                              Width="60">
                                <DataGridTextColumn.Binding>
                                    <Binding Path="Age" StringFormat="{}{0}岁"/>
                                </DataGridTextColumn.Binding>
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- 电话 -->
                            <DataGridTextColumn Header="电话"
                                              Binding="{Binding PhoneNumber}"
                                              Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontFamily" Value="Consolas"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- 拼音码 -->
                            <DataGridTextColumn Header="拼音码"
                                              Binding="{Binding PinYinCode}"
                                              Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource TextHintBrush}"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontFamily" Value="Consolas"/>
                                        <Setter Property="FontSize" Value="12"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- 创建时间 -->
                            <DataGridTextColumn Header="创建时间"
                                              Binding="{Binding CreatedAt, StringFormat='{}{0:yyyy-MM-dd}'}"
                                              Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource TextHintBrush}"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="FontSize" Value="12"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>

                        <!-- 行样式：选中高亮 + 斑马纹 + 悬停效果 -->
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#F0F0F0"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{StaticResource DataGridSelectedBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource DataGridHoverBrush}"/>
                                    </Trigger>
                                    <Trigger Property="AlternationIndex" Value="1">
                                        <Setter Property="Background" Value="{StaticResource DataGridAlternateRowBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>

                    <!-- 分页控件 -->
                    <Grid Grid.Row="3" Margin="0,15,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- 分页信息 -->
                        <TextBlock Grid.Column="0"
                                  VerticalAlignment="Center"
                                  Foreground="{StaticResource TextSecondaryBrush}" FontSize="12">
                            <Run Text="共 "/>
                            <Run Text="{Binding TotalCount}" FontWeight="Bold"/>
                            <Run Text=" 条记录，每页 "/>
                            <Run Text="20" FontWeight="Bold"/>
                            <Run Text=" 条"/>
                        </TextBlock>

                        <!-- 分页按钮 -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Content="上一页"
                                   Command="{Binding PreviousPageCommand}"
                                   Width="80" Height="30" Margin="0,0,10,0"
                                   Style="{StaticResource SecondaryButton}"/>

                            <TextBlock Text="{Binding CurrentPage}"
                                      VerticalAlignment="Center"
                                      FontWeight="Bold" FontSize="14"
                                      Margin="10,0"/>

                            <TextBlock Text="/"
                                      VerticalAlignment="Center"
                                      Foreground="{StaticResource DisabledBrush}"
                                      Margin="5,0"/>

                            <TextBlock Text="{Binding TotalPages}"
                                      VerticalAlignment="Center"
                                      Foreground="{StaticResource TextSecondaryBrush}"
                                      Margin="5,0,10,0"/>

                            <Button Content="下一页"
                                   Command="{Binding NextPageCommand}"
                                   Width="80" Height="30"
                                   Style="{StaticResource SecondaryButton}"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- 底部操作栏 -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" Padding="20"
               BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>    <!-- StatusBar -->
                    <ColumnDefinition Width="Auto"/> <!-- 开始诊断按钮 -->
                </Grid.ColumnDefinitions>

                <!-- Epic #2210 Issue #2217: StatusBar消息显示 -->
                <TextBlock Grid.Column="0"
                          Text="{Binding StatusBarMessage}"
                          VerticalAlignment="Center"
                          FontSize="13"
                          Margin="0,0,20,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <!-- 默认黑色 -->
                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            <!-- 错误消息时红色 -->
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding StatusBarIsError}" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource ErrorBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Epic #1583: IsEnabled由ViewModel的CanExecuteStartConsultation自动管理 -->
                <Button Grid.Column="1"
                       Content="开始诊断"
                       Command="{Binding StartConsultationCommand}"
                       Width="120" Height="40"
                       Style="{StaticResource PrimaryButton}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
