<UserControl x:Class="LYBT.Desktop.Patients.Controls.PatientSelectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:LYBT.Desktop.Infrastructure.Controls;assembly=LYBT.Desktop.Infrastructure"
             xmlns:local="clr-namespace:LYBT.Desktop.Patients.Controls"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <!-- 患者选择控件
         OpenSpec: fix-elementname-binding-architecture

         架构优化：移除ElementName=Root绑定，改用DataContext透传模式
         预期 DataContext: PatientSelectionViewModel

         左侧：患者列表（工具栏+搜索+列表）
         右侧：患者详情（使用PatientViewControl）-->

    <controls:MasterDetailLayout
        HasSelection="{Binding HasSelection}"
        MasterWidth="2*"
        DetailWidth="3*">

        <!-- Master: 患者列表区域 -->
        <controls:MasterDetailLayout.MasterContent>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>  <!-- 工具栏 -->
                    <RowDefinition Height="Auto"/>  <!-- 搜索框 -->
                    <RowDefinition Height="*"/>     <!-- 列表 -->
                </Grid.RowDefinitions>

                <!-- 工具栏 -->
                <controls:DataGridToolbar Grid.Row="0"
                    CreateCommand="{Binding NewPatientCommand}"
                    RefreshCommand="{Binding RefreshCommand}"/>

                <!-- 搜索框 -->
                <controls:SearchBox Grid.Row="1"
                    SearchText="{Binding SearchKeyword, Mode=TwoWay}"
                    SearchCommand="{Binding SearchCommand}"
                    Placeholder="搜索患者姓名或拼音码..."/>

                <!-- 患者列表 -->
                <DataGrid Grid.Row="2"
                    ItemsSource="{Binding Patients}"
                    SelectedItem="{Binding SelectedPatient, Mode=TwoWay}"
                    AutoGenerateColumns="False"
                    IsReadOnly="True"
                    SelectionMode="Single"
                    SelectionUnit="FullRow"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    CanUserReorderColumns="True"
                    CanUserResizeColumns="True"
                    CanUserSortColumns="True"
                    GridLinesVisibility="Horizontal"
                    HeadersVisibility="Column"
                    MouseDoubleClick="PatientDataGrid_MouseDoubleClick">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="姓名" Binding="{Binding Name}" Width="80"/>
                        <DataGridTextColumn Header="性别" Binding="{Binding Gender}" Width="50"/>
                        <DataGridTextColumn Header="年龄" Binding="{Binding Age}" Width="50"/>
                        <DataGridTextColumn Header="电话" Binding="{Binding PhoneNumber}" Width="120"/>
                        <DataGridTextColumn Header="最近就诊" Binding="{Binding LastVisitTime, StringFormat=yyyy-MM-dd}" Width="100"/>
                        <DataGridTextColumn Header="就诊次数" Binding="{Binding VisitCount}" Width="70"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </controls:MasterDetailLayout.MasterContent>

        <!-- Detail: 患者详情区域 -->
        <controls:MasterDetailLayout.DetailContent>
            <local:PatientViewControl
                PatientName="{Binding PatientDetail.Name}"
                PinYinCode="{Binding PatientDetail.PinYinCode}"
                Gender="{Binding PatientDetail.Gender}"
                BirthDate="{Binding PatientDetail.BirthDate}"
                Age="{Binding PatientDetail.Age}"
                IdNumber="{Binding PatientDetail.IdNumber}"
                IdType="{Binding PatientDetail.IdType}"
                MaritalStatus="{Binding PatientDetail.MaritalStatus}"
                BloodType="{Binding PatientDetail.BloodType}"
                PhoneNumber="{Binding PatientDetail.PhoneNumber}"
                Address="{Binding PatientDetail.Address}"
                EmergencyContactName="{Binding PatientDetail.EmergencyContactName}"
                EmergencyContactPhone="{Binding PatientDetail.EmergencyContactPhone}"
                EmergencyContactRelation="{Binding PatientDetail.EmergencyContactRelation}"
                AllergyHistory="{Binding PatientDetail.AllergyHistory}"
                MedicalHistory="{Binding PatientDetail.MedicalHistory}"
                LastVisitTime="{Binding PatientDetail.LastVisitTime}"
                VisitCount="{Binding PatientDetail.VisitCount}"
                Status="{Binding PatientDetail.Status}"
                ShowStatus="False"
                DisableReason="{Binding PatientDetail.DisableReason}"
                CreatedAt="{Binding PatientDetail.CreatedAt}"
                UpdatedAt="{Binding PatientDetail.UpdatedAt}"/>
        </controls:MasterDetailLayout.DetailContent>

        <!-- Empty: 空状态 -->
        <controls:MasterDetailLayout.EmptyContent>
            <controls:EmptyState
                Title="请选择患者"
                Subtitle="从左侧列表选择患者，或点击新建创建新患者"
                ActionText="新建患者"
                ActionCommand="{Binding NewPatientCommand}"/>
        </controls:MasterDetailLayout.EmptyContent>

    </controls:MasterDetailLayout>
</UserControl>
