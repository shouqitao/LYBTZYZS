<UserControl x:Class="LYBT.Desktop.MedicalCase.Views.CompletionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="White">
    <Grid>
        <!-- 垂直居中布局 -->
        <StackPanel VerticalAlignment="Center" 
                    HorizontalAlignment="Center"
                    Margin="40">
            
            <!-- 成功图标 -->
            <Viewbox Width="80" Height="80" Margin="0,0,0,30">
                <Canvas Width="24" Height="24">
                    <!-- ✅ 绿色对勾图标 -->
                    <Ellipse Width="24" Height="24" Fill="#52C41A" />
                    <Path Data="M7,12 L10,15 L17,8"
                          Stroke="White"
                          StrokeThickness="2"
                          StrokeStartLineCap="Round"
                          StrokeEndLineCap="Round"
                          StrokeLineJoin="Round"/>
                </Canvas>
            </Viewbox>

            <!-- 完成提示 -->
            <TextBlock Text="看诊完成"
                       FontSize="32"
                       FontWeight="Bold"
                       Foreground="#262626"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <!-- 医案编号 -->
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Center"
                        Margin="0,0,0,40">
                <TextBlock Text="医案编号："
                           FontSize="16"
                           Foreground="#595959"/>
                <TextBlock Text="{Binding MedicalCaseNumber}"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="#0078D4"/>
            </StackPanel>

            <!-- 主操作按钮区 -->
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Center"
                        Margin="0,0,0,30">
                
                <!-- 继续看诊按钮（绿色） -->
                <Button Content="继续看诊"
                        Command="{Binding ContinueConsultationCommand}"
                        Width="200"
                        Height="60"
                        FontSize="18"
                        FontWeight="SemiBold"
                        Margin="0,0,15,0"
                        Background="#52C41A"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="4"
                                                BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#73D13D"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#389E0D"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- 返回主页按钮（灰色） -->
                <Button Content="返回主页"
                        Command="{Binding ReturnHomeCommand}"
                        Width="200"
                        Height="60"
                        FontSize="18"
                        FontWeight="SemiBold"
                        Background="#D9D9D9"
                        Foreground="#262626"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="4"
                                                BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#BFBFBF"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#8C8C8C"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

            <!-- 辅助功能按钮区 -->
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Center">
                
                <!-- 打印处方按钮 -->
                <Button Content="打印处方"
                        Command="{Binding PrintPrescriptionCommand}"
                        Width="160"
                        Height="45"
                        FontSize="14"
                        Margin="0,0,10,0"
                        Background="White"
                        Foreground="#0078D4"
                        BorderBrush="#0078D4"
                        BorderThickness="1"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#E6F7FF"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#BAE7FF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- 查看病案详情按钮 -->
                <Button Content="查看病案详情"
                        Command="{Binding ViewDetailCommand}"
                        Width="160"
                        Height="45"
                        FontSize="14"
                        Background="White"
                        Foreground="#0078D4"
                        BorderBrush="#0078D4"
                        BorderThickness="1"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="4">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#E6F7FF"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#BAE7FF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
