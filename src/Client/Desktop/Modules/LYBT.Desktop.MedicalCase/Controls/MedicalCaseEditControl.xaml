<!--
    MedicalCaseEditControl - 医案编辑控件（已统一为Compact模式）
    OpenSpec: refactor-medicalcase-workspace V2
    OpenSpec: refactor-medicalcase-management
    OpenSpec: unify-herb-controls-to-herbs-module

    Compact模式: Workspace紧凑型显示，诊断+四诊+处方编辑
    V2布局:
    - 诊断区3行: 现病史 / 舌诊+脉诊 / 中医诊断*
    - 处方区: 工具条 + HerbListControl + 底部信息栏
-->
<UserControl x:Class="LYBT.Desktop.MedicalCase.Controls.MedicalCaseEditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:LYBT.Desktop.Infrastructure.Controls;assembly=LYBT.Desktop.Infrastructure"
             xmlns:converters="clr-namespace:LYBT.Desktop.Infrastructure.Converters;assembly=LYBT.Desktop.Infrastructure"
             xmlns:herbList="clr-namespace:LYBT.Desktop.Infrastructure.Controls.HerbList;assembly=LYBT.Desktop.Infrastructure"
             xmlns:diagnosis="clr-namespace:LYBT.Desktop.Infrastructure.Controls.Diagnosis;assembly=LYBT.Desktop.Infrastructure"
             xmlns:local="clr-namespace:LYBT.Desktop.MedicalCase.Controls"
             x:Name="Root">

    <!--
        资源加载说明 (OpenSpec: cleanup-control-resource-merging):
        - 不要在控件级别合并 UnifiedComponents.xaml，避免资源重复加载
        - 样式/转换器通过 DynamicResource 引用 App 级别已加载的资源
        - 本地资源 CompactSectionBorderStyle 保留，仅供此控件使用
    -->
    <UserControl.Resources>
        <ResourceDictionary>
            <!-- Compact模式区域边框样式 -->
            <Style x:Key="CompactSectionBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource SecondaryRegionBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource SecondaryBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="16,12"/>
            </Style>

            <!-- 舌诊选项列表 -->
            <x:Array x:Key="TongueDiagnosisOptions" Type="sys:String"
                     xmlns:sys="clr-namespace:System;assembly=mscorlib">
                <sys:String>淡红舌</sys:String>
                <sys:String>红舌</sys:String>
                <sys:String>暗红舌</sys:String>
                <sys:String>紫暗舌</sys:String>
                <sys:String>胖大舌</sys:String>
                <sys:String>瘦薄舌</sys:String>
                <sys:String>裂纹舌</sys:String>
                <sys:String>齿痕舌</sys:String>
                <sys:String>薄白苔</sys:String>
                <sys:String>白厚苔</sys:String>
                <sys:String>黄苔</sys:String>
                <sys:String>黄腻苔</sys:String>
            </x:Array>

            <!-- 脉诊选项列表 -->
            <x:Array x:Key="PulseDiagnosisOptions" Type="sys:String"
                     xmlns:sys="clr-namespace:System;assembly=mscorlib">
                <sys:String>浮脉</sys:String>
                <sys:String>沉脉</sys:String>
                <sys:String>迟脉</sys:String>
                <sys:String>数脉</sys:String>
                <sys:String>滑脉</sys:String>
                <sys:String>涩脉</sys:String>
                <sys:String>弦脉</sys:String>
                <sys:String>紧脉</sys:String>
                <sys:String>虚脉</sys:String>
                <sys:String>实脉</sys:String>
                <sys:String>弱脉</sys:String>
                <sys:String>细脉</sys:String>
            </x:Array>

            <!-- 中医证型选项列表 -->
            <x:Array x:Key="SyndromeOptions" Type="sys:String"
                     xmlns:sys="clr-namespace:System;assembly=mscorlib">
                <sys:String>风寒束表证</sys:String>
                <sys:String>风热犯肺证</sys:String>
                <sys:String>暑湿感冒证</sys:String>
                <sys:String>脾胃虚弱证</sys:String>
                <sys:String>脾胃湿热证</sys:String>
                <sys:String>胃阴不足证</sys:String>
                <sys:String>肝郁气滞证</sys:String>
                <sys:String>肝胆湿热证</sys:String>
                <sys:String>肝阳上亢证</sys:String>
                <sys:String>心脾两虚证</sys:String>
                <sys:String>心肺气虚证</sys:String>
                <sys:String>痰热壅肺证</sys:String>
                <sys:String>肾阴亏虚证</sys:String>
                <sys:String>肾阳不足证</sys:String>
                <sys:String>肾精不足证</sys:String>
            </x:Array>

            <!-- 现病史常用术语选项列表 (W3-6) -->
            <x:Array x:Key="PresentIllnessOptions" Type="sys:String"
                     xmlns:sys="clr-namespace:System;assembly=mscorlib">
                <sys:String>头痛3天</sys:String>
                <sys:String>发热2天</sys:String>
                <sys:String>咳嗽1周</sys:String>
                <sys:String>失眠半月</sys:String>
                <sys:String>胃痛3天</sys:String>
                <sys:String>腹胀1周</sys:String>
                <sys:String>腰痛1月</sys:String>
                <sys:String>乏力1周</sys:String>
                <sys:String>心悸3天</sys:String>
                <sys:String>眩晕1周</sys:String>
                <sys:String>口干1月</sys:String>
                <sys:String>便秘1周</sys:String>
                <sys:String>腹泻2天</sys:String>
                <sys:String>关节疼痛1月</sys:String>
                <sys:String>咽痛3天</sys:String>
                <sys:String>鼻塞流涕2天</sys:String>
            </x:Array>

            <!-- 处方用法选项列表 -->
            <x:Array x:Key="PrescriptionUsageOptions" Type="sys:String"
                     xmlns:sys="clr-namespace:System;assembly=mscorlib">
                <sys:String>水煎服</sys:String>
                <sys:String>开水冲服</sys:String>
                <sys:String>研末服</sys:String>
                <sys:String>泡服</sys:String>
            </x:Array>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid DataContext="{Binding ElementName=Root}">
        <!--
            Compact模式（统一模式，已移除Full模式）
            Workspace布局 (V2布局): 工具条 + 诊断区3行 + 处方区
        -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- 流程步骤指示器 -->
                <RowDefinition Height="Auto"/>  <!-- 患者信息 -->
                <RowDefinition Height="Auto"/>  <!-- 工具条 -->
                <RowDefinition Height="Auto"/>  <!-- 诊断区 -->
                <RowDefinition Height="Auto"/>  <!-- 处方决策 -->
                <RowDefinition Height="*"/>     <!-- 处方区 -->
                <RowDefinition Height="Auto"/>  <!-- 备注 -->
                <RowDefinition Height="Auto"/>  <!-- 完整性检查 -->
            </Grid.RowDefinitions>

            <!-- 流程步骤指示器 -->
            <local:WorkflowStepIndicator Grid.Row="0"
                                        CurrentStep="{Binding CurrentStep, Mode=OneWay}"
                                        Margin="0,0,0,16"
                                        HorizontalAlignment="Left"/>

            <!-- 患者信息行 (Compact模式) -->
            <Border Grid.Row="1" Background="{DynamicResource SecondaryRegionBrush}"
                    CornerRadius="8" Padding="16,12" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="患者:" Foreground="{DynamicResource SecondaryTextBrush}"
                               Margin="0,0,4,0"/>
                    <TextBlock Grid.Column="1" Text="{Binding PatientName, TargetNullValue='-'}"
                               FontWeight="SemiBold" Margin="0,0,16,0"/>
                    <TextBlock Grid.Column="2" Text="{Binding ConsultationDate, StringFormat=yyyy-MM-dd, TargetNullValue='-'}"
                               Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,16,0"/>
                    <TextBlock Grid.Column="4" Text="{Binding DoctorName, TargetNullValue='-'}"
                               Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,16,0"/>
                </Grid>
            </Border>

            <!-- P2-2: 右上角工具条 - 增强处方决策引导 -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,16">
                <!-- 套验方 - 快速应用经典方剂 -->
                <Button TabIndex="5"
                        Command="{Binding ImportFormulaCommand}"
                        Style="{DynamicResource SecondaryButton}"
                        Margin="0,0,12,0"
                        ToolTipService.InitialShowDelay="500"
                        ToolTipService.ShowDuration="5000">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📚" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="套验方" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button.ToolTip>
                        <ToolTip Style="{DynamicResource EnhancedToolTipStyle}">
                            <StackPanel MaxWidth="300">
                                <TextBlock Text="套验方" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                                <TextBlock Text="快速应用经典方剂，适合常见证型" Margin="0,0,0,4"/>
                                <TextBlock Text="• 从验方库中选择方剂" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,2"/>
                                <TextBlock Text="• 自动填充药材和剂量" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,2"/>
                                <TextBlock Text="• 可在处方中继续调整" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12"/>
                            </StackPanel>
                        </ToolTip>
                    </Button.ToolTip>
                </Button>

                <!-- 历史处方 - 复用患者过往处方 -->
                <Button TabIndex="6"
                        Command="{Binding ImportHistoryCommand}"
                        Style="{DynamicResource SecondaryButton}"
                        Margin="0,0,12,0"
                        ToolTipService.InitialShowDelay="500"
                        ToolTipService.ShowDuration="5000">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📋" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="历史处方" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button.ToolTip>
                        <ToolTip Style="{DynamicResource EnhancedToolTipStyle}">
                            <StackPanel MaxWidth="300">
                                <TextBlock Text="历史处方" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                                <TextBlock Text="复用该患者的历史处方，适合慢性病调理" Margin="0,0,0,4"/>
                                <TextBlock Text="• 显示患者最近的处方记录" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,2"/>
                                <TextBlock Text="• 一键复制到当前处方" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,2"/>
                                <TextBlock Text="• 保留原处方供参考" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12"/>
                            </StackPanel>
                        </ToolTip>
                    </Button.ToolTip>
                </Button>

                <!-- 清空 - 清空所有药材 -->
                <Button TabIndex="7"
                        Command="{Binding ClearAllCommand}"
                        Style="{DynamicResource LinkButtonStyle}"
                        Foreground="{StaticResource ValidationErrorBrush}"
                        ToolTipService.InitialShowDelay="500"
                        ToolTipService.ShowDuration="5000">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🗑️" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="清空" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button.ToolTip>
                        <ToolTip Style="{DynamicResource EnhancedToolTipStyle}">
                            <StackPanel MaxWidth="300">
                                <TextBlock Text="清空处方" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                                <TextBlock Text="清空当前处方中的所有药材" Margin="0,0,0,4"/>
                                <TextBlock Text="• 清空所有已添加的药材" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,2"/>
                                <TextBlock Text="• 剂数和用法保留不变" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" Margin="0,0,0,2"/>
                                <TextBlock Text="• 可重新开始组方" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12"/>
                            </StackPanel>
                        </ToolTip>
                    </Button.ToolTip>
                </Button>
            </StackPanel>

            <!-- 诊断区 - 四诊采集 (望闻问切) -->
            <StackPanel Grid.Row="3">
                <!-- P2-1: 四诊采集按望闻问切分组 -->
                <!-- 望诊 - 舌诊 -->
                <Border Style="{StaticResource CompactSectionBorderStyle}" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="{DynamicResource PrimaryBrush}"
                                CornerRadius="6,0,0,6" Padding="12,8" Margin="0,0,12,0">
                            <TextBlock Text="望" FontSize="16" FontWeight="Bold"
                                       Foreground="White" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="舌诊" FontWeight="SemiBold" FontSize="13"
                                       Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,4"/>
                            <ComboBox TabIndex="9"
                                      SelectedItem="{Binding Consultation.TongueDiagnosis, Mode=TwoWay}"
                                      ItemsSource="{StaticResource TongueDiagnosisOptions}"
                                      Style="{DynamicResource FilterComboBox}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 闻诊 - 暂未实现 -->
                <Border Style="{StaticResource CompactSectionBorderStyle}" Margin="0,0,0,12"
                        Background="{DynamicResource SecondaryRegionBrush}" Opacity="0.6">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="{DynamicResource SecondaryTextBrush}"
                                CornerRadius="6,0,0,6" Padding="12,8" Margin="0,0,12,0">
                            <TextBlock Text="闻" FontSize="16" FontWeight="Bold"
                                       Foreground="White" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="闻诊（听声音、嗅气味）" FontStyle="Italic"
                                       Foreground="{DynamicResource SecondaryTextBrush}"
                                       VerticalAlignment="Center" Margin="0,4"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 问诊 - 现病史 -->
                <Border Style="{StaticResource CompactSectionBorderStyle}" Margin="0,0,0,12">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Background="{DynamicResource PrimaryBrush}"
                                    CornerRadius="6,0,0,6" Padding="12,8" Margin="0,0,12,0">
                                <TextBlock Text="问" FontSize="16" FontWeight="Bold"
                                           Foreground="White" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Grid.Column="1" Text="现病史" FontWeight="SemiBold" FontSize="13"
                                       Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                        </Grid>
                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox Grid.Column="0"
                                      TabIndex="8"
                                      IsEditable="True"
                                      Text="{Binding Consultation.PresentIllness, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"
                                      ItemsSource="{StaticResource PresentIllnessOptions}"
                                      Style="{DynamicResource FilterComboBox}"
                                      MinHeight="32"
                                      VerticalContentAlignment="Top"/>
                            <TextBlock Grid.Column="1"
                                       Style="{DynamicResource FieldSuccessIndicatorStyle}"
                                       Visibility="{Binding Consultation.IsPresentIllnessValid, Converter={x:Static converters:Cvt.BoolToVis}}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 切诊 - 脉诊 -->
                <Border Style="{StaticResource CompactSectionBorderStyle}" Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="{DynamicResource PrimaryBrush}"
                                CornerRadius="6,0,0,6" Padding="12,8" Margin="0,0,12,0">
                            <TextBlock Text="切" FontSize="16" FontWeight="Bold"
                                       Foreground="White" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="脉诊" FontWeight="SemiBold" FontSize="13"
                                       Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,4"/>
                            <ComboBox TabIndex="10"
                                      SelectedItem="{Binding Consultation.PulseDiagnosis, Mode=TwoWay}"
                                      ItemsSource="{StaticResource PulseDiagnosisOptions}"
                                      Style="{DynamicResource FilterComboBox}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 中医辨证区 (必填) -->
                <Border Style="{StaticResource CompactSectionBorderStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"
                                   Foreground="{DynamicResource PrimaryTextBrush}">
                            <Run Text="中医辨证"/>
                            <Run Text="*" Foreground="{StaticResource ValidationErrorBrush}"/>
                        </TextBlock>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                     TabIndex="11"
                                     Text="{Binding Consultation.TcmDiagnosis, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{DynamicResource ValidatingTextBoxStyle}"
                                     MinHeight="36"/>
                            <TextBlock Grid.Column="1"
                                       Style="{DynamicResource FieldSuccessIndicatorStyle}"
                                       Visibility="{Binding Consultation.IsDiagnosisComplete, Converter={x:Static converters:Cvt.BoolToVis}}"/>
                        </Grid>
                        <TextBlock Text="{Binding Consultation.ValidationMessage}"
                                   Style="{DynamicResource ValidationErrorMessageVisibleStyle}"
                                   Margin="0,4,0,0"/>
                        <ComboBox Margin="0,4,0,0"
                                  SelectedItem="{Binding Consultation.TcmDiagnosis, Mode=TwoWay}"
                                  ItemsSource="{StaticResource SyndromeOptions}"
                                  Style="{DynamicResource FilterComboBox}"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- P2-2: 处方决策引导 - 增强视觉提示 -->
            <Border Grid.Row="4" Style="{StaticResource CompactSectionBorderStyle}" Margin="0,0,0,16"
                    Background="{DynamicResource SecondaryRegionBrush}">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="💊" FontSize="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="处方决策" FontWeight="SemiBold" FontSize="14"
                                   Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                    </Grid>
                    <TextBlock Text="请选择本次看诊是否需要开具处方" Margin="0,4,0,12"
                               Foreground="{DynamicResource SecondaryTextBrush}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- 需要处方 -->
                        <Border Grid.Column="0" Style="{DynamicResource DecisionCardStyle}"
                                Background="{DynamicResource SuccessBrush}" Opacity="0.1"
                                CornerRadius="8" Padding="16,12" Margin="0,0,8,0">
                            <StackPanel>
                                <RadioButton Content="需要处方" GroupName="PrescriptionNeed"
                                             IsChecked="{Binding NeedsPrescription}"
                                             FontWeight="SemiBold"
                                             ToolTip="选择此项后，可添加药材、设置剂数和用法"/>
                                <TextBlock Text="添加药材、设置剂数用法" FontSize="11"
                                           Foreground="{DynamicResource SecondaryTextBrush}"
                                           Margin="24,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- 不需要处方 -->
                        <Border Grid.Column="1" Style="{DynamicResource DecisionCardStyle}"
                                Background="{DynamicResource SecondaryTextBrush}" Opacity="0.1"
                                CornerRadius="8" Padding="16,12" Margin="0,0,8,0">
                            <StackPanel>
                                <RadioButton Content="不需要处方" GroupName="PrescriptionNeed"
                                             IsChecked="{Binding NeedsPrescription, Converter={x:Static converters:InverseBooleanConverter.Instance}}"
                                             FontWeight="SemiBold"
                                             ToolTip="仅做辨证，不开具处方（如咨询、调理建议等）"/>
                                <TextBlock Text="仅做辨证记录" FontSize="11"
                                           Foreground="{DynamicResource SecondaryTextBrush}"
                                           Margin="24,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- 稍后决定 -->
                        <Border Grid.Column="2" Style="{DynamicResource DecisionCardStyle}"
                                Background="{DynamicResource WarningBrush}" Opacity="0.1"
                                CornerRadius="8" Padding="16,12">
                            <StackPanel>
                                <RadioButton Content="稍后决定" GroupName="PrescriptionNeed"
                                             IsEnabled="False" Opacity="0.5"
                                             FontWeight="SemiBold"
                                             ToolTip="暂未实现，请选择需要或不需要处方"/>
                                <TextBlock Text="暂未实现" FontSize="11"
                                           Foreground="{DynamicResource SecondaryTextBrush}"
                                           Margin="24,4,0,0" Opacity="0.5"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 处方区 -->
            <Border Grid.Row="5" Style="{StaticResource CompactSectionBorderStyle}"
                    IsEnabled="{Binding IsPrescriptionEnabled}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>     <!-- 药材列表 -->
                        <RowDefinition Height="Auto"/>  <!-- 底部信息栏 -->
                    </Grid.RowDefinitions>

                    <!-- 药材列表 - OpenSpec: unify-control-data-binding -->
                    <herbList:HerbListControl Grid.Row="0"
                        AllHerbs="{Binding AllHerbs}"
                        HerbItems="{Binding Prescription.Items, Mode=TwoWay}"
                        IsEditMode="True"
                        Columns="4"/>

                    <!-- 底部信息栏 -->
                    <Border Grid.Row="1"
                            Background="{DynamicResource RegionBrush}"
                            CornerRadius="6"
                            Padding="12,8"
                            Margin="0,12,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>  <!-- 药材数 -->
                                <ColumnDefinition Width="Auto"/>  <!-- 剂数 -->
                                <ColumnDefinition Width="Auto"/>  <!-- 用法 -->
                                <ColumnDefinition Width="*"/>     <!-- 总价 -->
                            </Grid.ColumnDefinitions>

                            <!-- 药材数 - OpenSpec: unify-control-data-binding -->
                            <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,24,0">
                                <Run Text="共"/>
                                <Run Text="{Binding Prescription.ItemCount, Mode=OneWay}" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}"/>
                                <Run Text="味药材"/>
                            </TextBlock>
                            <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="68,0,0,0" Visibility="{Binding Prescription.HasItems, Converter={x:Static converters:Cvt.BoolToVis}}">
                                <Run Text="✓" Foreground="{DynamicResource SuccessBrush}" FontSize="14" FontWeight="Bold"/>
                            </TextBlock>

                            <!-- 剂数 - OpenSpec: unify-control-data-binding -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,24,0">
                                <TextBlock Text="剂数:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBox Width="60" TabIndex="12"
                                         Text="{Binding Prescription.DosageCount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{DynamicResource EditableTextBoxStyle}"
                                         MinHeight="28"
                                         Padding="8,4"/>
                                <TextBlock Text="剂" VerticalAlignment="Center" Margin="4,0,0,0"/>
                            </StackPanel>

                            <!-- 用法 - OpenSpec: unify-control-data-binding -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,24,0">
                                <TextBlock Text="用法:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <ComboBox Width="120" TabIndex="13"
                                          SelectedItem="{Binding Prescription.Usage, Mode=TwoWay}"
                                          ItemsSource="{StaticResource PrescriptionUsageOptions}"
                                          Style="{DynamicResource FilterComboBox}"/>
                            </StackPanel>

                            <!-- 总价 - OpenSpec: unify-control-data-binding -->
                            <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <TextBlock Text="单剂价:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{DynamicResource SuccessBrush}" Margin="0,0,16,0">
                                    <Run Text="¥"/>
                                    <Run Text="{Binding Prescription.SingleDosePrice, StringFormat='{}{0:F2}', Mode=OneWay}"/>
                                </TextBlock>
                                
                                <TextBlock Text="总价:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock VerticalAlignment="Center" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource SuccessBrush}">
                                    <Run Text="¥"/>
                                    <Run Text="{Binding Prescription.TotalPrice, StringFormat='{}{0:F2}', Mode=OneWay}"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- 备注 - W2-3: 统一 Remark 数据源 -->
            <Border Grid.Row="6" Style="{StaticResource CompactSectionBorderStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="备注" FontWeight="SemiBold" FontSize="13"
                               Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,4"/>
                    <TextBox TabIndex="15"
                             x:Name="RemarkTextBox"
                             Text="{Binding Remark, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Style="{DynamicResource ValidatingTextBoxStyle}"
                             TextWrapping="Wrap" MinHeight="40" AcceptsReturn="True"
                             MaxHeight="120"/>
                    <TextBlock FontSize="11" HorizontalAlignment="Right" Margin="0,2,0,0"
                               Foreground="{DynamicResource SecondaryTextBrush}">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0}/1000">
                                <Binding Path="Text.Length" ElementName="RemarkTextBox" FallbackValue="0"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- Completeness Check Indicator - Phase 1.4: Dynamic completeness check -->
            <Border Grid.Row="7" Style="{StaticResource CompactSectionBorderStyle}" Margin="0,12,0,0"
                    Background="{DynamicResource SecondaryRegionBrush}">
                <StackPanel>
                    <TextBlock Text="完整性检查" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"
                               Foreground="{DynamicResource PrimaryTextBrush}"/>

                    <!-- 中医诊断 -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Margin="0,0,4,0">
                            <Run Text="●" FontWeight="Bold"
                                 Foreground="{Binding Completeness.DiagnosisComplete, Converter={x:Static converters:Cvt.BoolToColor}, ConverterParameter=Green|Amber}"/>
                            <Run Text="中医诊断"/>
                        </TextBlock>
                        <TextBlock Margin="0,0,4,0" Foreground="{DynamicResource SecondaryTextBrush}"
                                     Visibility="{Binding Completeness.DiagnosisComplete, Converter={x:Static converters:Cvt.BoolToVis}}">
                            <Run Text="已填写" FontSize="12"/>
                        </TextBlock>
                        <TextBlock Margin="0,0,4,0" Foreground="{DynamicResource SecondaryTextBrush}"
                                     Visibility="{Binding Completeness.DiagnosisComplete, Converter={x:Static converters:Cvt.InverseBoolToVis}}">
                            <Run Text="待填写" FontSize="12"/>
                        </TextBlock>
                    </StackPanel>

                    <!-- 处方决策 -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Margin="0,0,4,0">
                            <Run Text="●" FontWeight="Bold"
                                 Foreground="{Binding Completeness.PrescriptionDecisionComplete, Converter={x:Static converters:Cvt.BoolToColor}, ConverterParameter=Green|Amber}"/>
                            <Run Text="处方决策"/>
                        </TextBlock>
                        <TextBlock Margin="0,0,4,0" Foreground="{DynamicResource SecondaryTextBrush}"
                                     Visibility="{Binding Completeness.PrescriptionDecisionComplete, Converter={x:Static converters:Cvt.BoolToVis}}">
                            <Run Text="已决定" FontSize="12"/>
                        </TextBlock>
                    </StackPanel>

                    <!-- 处方药材 -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Margin="0,0,4,0">
                            <Run Text="●" FontWeight="Bold"
                                 Foreground="{Binding Completeness.PrescriptionContentComplete, Converter={x:Static converters:Cvt.BoolToColor}, ConverterParameter=Green|Amber}"/>
                            <Run Text="处方药材"/>
                        </TextBlock>
                        <TextBlock Margin="0,0,4,0" Foreground="{DynamicResource SecondaryTextBrush}"
                                     Visibility="{Binding IsPrescriptionEnabled, Converter={x:Static converters:Cvt.BoolToVis}}">
                            <Run Text="{Binding Completeness.PrescriptionItemCount, StringFormat='{}{0} 味'}" FontSize="12"/>
                        </TextBlock>
                        <TextBlock Margin="0,0,4,0" Foreground="{DynamicResource SecondaryTextBrush}"
                                     Visibility="{Binding IsPrescriptionEnabled, Converter={x:Static converters:Cvt.InverseBoolToVis}}">
                            <Run Text="无需处方" FontSize="12"/>
                        </TextBlock>
                    </StackPanel>

                    <!-- 剂数 -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Margin="0,0,4,0">
                            <Run Text="●" FontWeight="Bold"
                                 Foreground="{Binding Completeness.DosageCountComplete, Converter={x:Static converters:Cvt.BoolToColor}, ConverterParameter=Green|Amber}"/>
                            <Run Text="剂数"/>
                        </TextBlock>
                        <TextBlock Margin="0,0,4,0" Foreground="{DynamicResource SecondaryTextBrush}"
                                     Visibility="{Binding IsPrescriptionEnabled, Converter={x:Static converters:Cvt.BoolToVis}}">
                            <Run Text="{Binding Completeness.DosageCount, StringFormat='{}{0} 剂'}" FontSize="12"/>
                        </TextBlock>
                        <TextBlock Margin="0,0,4,0" Foreground="{DynamicResource SecondaryTextBrush}"
                                     Visibility="{Binding IsPrescriptionEnabled, Converter={x:Static converters:Cvt.InverseBoolToVis}}">
                            <Run Text="无需处方" FontSize="12"/>
                        </TextBlock>
                    </StackPanel>

                    <!-- 完成提示 -->
                    <TextBlock Margin="0,8,0,0" FontWeight="Medium"
                               Foreground="{DynamicResource SuccessBrush}"
                               Visibility="{Binding Completeness.CanCompleteCase, Converter={x:Static converters:Cvt.BoolToVis}}">
                        可以完成看诊
                    </TextBlock>
                    <TextBlock Margin="0,8,0,0" FontWeight="Medium"
                               Foreground="{DynamicResource WarningBrush}"
                               Visibility="{Binding Completeness.CanCompleteCase, Converter={x:Static converters:Cvt.InverseBoolToVis}}">
                        尚未完成所有必填项
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
