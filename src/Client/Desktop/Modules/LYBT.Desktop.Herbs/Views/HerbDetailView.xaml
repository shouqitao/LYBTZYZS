<!--
    HerbDetailView - 药材详情视图
    OpenSpec: extract-detail-controls Task 2.3

    重构说明：
    - 使用 BaseDetailContainer 容器实现查看/编辑模式分离
    - ViewContent: 使用 HerbViewControl 预览控件
    - EditContent: 使用 HerbEditControl 编辑控件
-->
<UserControl x:Class="LYBT.Desktop.Herbs.Views.HerbDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:views="clr-namespace:LYBT.Desktop.Infrastructure.Views;assembly=LYBT.Desktop.Infrastructure"
             xmlns:herbControls="clr-namespace:LYBT.Desktop.Herbs.Controls"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/LYBT.Desktop.Infrastructure;component/Themes/UnifiedComponents.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <views:BaseDetailContainer
        Title="{Binding PageTitle}"
        IsEditMode="{Binding IsEditMode, Mode=OneWay}"
        GoBackCommand="{Binding GoBackCommand}"
        SwitchToEditCommand="{Binding SwitchToEditModeCommand}"
        SaveCommand="{Binding SubmitCommand}"
        CancelCommand="{Binding CancelCommand}"
        SaveButtonText="{Binding SaveButtonText}"
        IsLoading="{Binding IsLoading}"
        LoadingMessage="{Binding StatusMessage}">

        <!-- ========================================
             查看模式内容 (ViewContent)
             使用 HerbViewControl 预览控件
             ======================================== -->
        <views:BaseDetailContainer.ViewContent>
            <herbControls:HerbViewControl
                HerbName="{Binding Name}"
                PinYinCode="{Binding PinYinCode}"
                Origin="{Binding Origin}"
                Spec="{Binding Spec}"
                Unit="{Binding Unit}"
                Price="{Binding Price}"
                CostPrice="{Binding CostPrice}"
                Status="{Binding Status}"
                HerbEffect="{Binding Effect}"
                Usage="{Binding Usage}"
                Remark="{Binding Remark}"
                ShowStatus="{Binding IsEditOrViewMode}"/>
        </views:BaseDetailContainer.ViewContent>

        <!-- ========================================
             编辑模式内容 (EditContent)
             使用 HerbEditControl 编辑控件
             ======================================== -->
        <views:BaseDetailContainer.EditContent>
            <herbControls:HerbEditControl
                HerbName="{Binding Name, Mode=TwoWay}"
                PinYinCode="{Binding PinYinCode}"
                Origin="{Binding Origin, Mode=TwoWay}"
                Spec="{Binding Spec, Mode=TwoWay}"
                Unit="{Binding Unit, Mode=TwoWay}"
                Price="{Binding Price, Mode=TwoWay}"
                CostPrice="{Binding CostPrice, Mode=TwoWay}"
                Status="{Binding Status, Mode=TwoWay}"
                StatusOptions="{Binding StatusOptions}"
                Effect="{Binding Effect, Mode=TwoWay}"
                Usage="{Binding Usage, Mode=TwoWay}"
                Remark="{Binding Remark, Mode=TwoWay}"
                IsNameEditable="{Binding IsNameEditable}"
                ShowStatus="{Binding IsEditOrViewMode}"/>
        </views:BaseDetailContainer.EditContent>
    </views:BaseDetailContainer>
</UserControl>
