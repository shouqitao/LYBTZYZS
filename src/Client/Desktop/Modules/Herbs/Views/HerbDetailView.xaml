<UserControl x:Class="LYBT.Desktop.Herbs.Views.HerbDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" 
                        Command="{Binding BackCommand}"
                        Background="Transparent" BorderThickness="0" Padding="8"
                        Foreground="White"
                        Margin="0,0,16,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="←" VerticalAlignment="Center" FontSize="16"/>
                        <TextBlock Text="返回" Margin="4,0,0,0"/>
                    </StackPanel>
                </Button>
                
                <TextBlock Grid.Column="1" 
                           Text="中药材详情" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Foreground="White"
                           VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Command="{Binding EditCommand}"
                            Background="Transparent" BorderThickness="0" Padding="8"
                            Foreground="White"
                            Margin="4"
                            Visibility="{Binding IsReadOnly, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✏" VerticalAlignment="Center" FontSize="16"/>
                            <TextBlock Text="编辑" Margin="4,0,0,0"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Command="{Binding SaveCommand}"
                            Background="Transparent" BorderThickness="0" Padding="8"
                            Foreground="White"
                            Margin="4"
                            Visibility="{Binding IsReadOnly, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💾" VerticalAlignment="Center" FontSize="16"/>
                            <TextBlock Text="保存" Margin="4,0,0,0"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Command="{Binding CancelEditCommand}"
                            Background="Transparent" BorderThickness="0" Padding="8"
                            Foreground="White"
                            Margin="4"
                            Visibility="{Binding IsReadOnly, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                        <TextBlock Text="✖" FontSize="16" VerticalAlignment="Center"/>
                    </Button>
                    
                    <Button Command="{Binding ViewUsageHistoryCommand}"
                            Background="Transparent" BorderThickness="0" Padding="8"
                            Foreground="White"
                            Margin="4">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📈" VerticalAlignment="Center" FontSize="16"/>
                            <TextBlock Text="用药记录" Margin="4,0,0,0"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Command="{Binding PrintCommand}"
                            Background="Transparent" BorderThickness="0" Padding="8"
                            Foreground="White"
                            Margin="4">
                        <TextBlock Text="🖨" FontSize="16" VerticalAlignment="Center"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 内容区域 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 药材基本信息卡片 -->
                <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <Grid Margin="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <Border Grid.Column="0" 
                                Width="80" Height="80"
                                CornerRadius="8"
                                Background="{DynamicResource SecondaryHueLightBrush}"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                Margin="0,0,20,0">
                            <TextBlock Text="🌿" 
                                       FontSize="36"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                        
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding HerbName}" 
                                       FontSize="24" 
                                       FontWeight="Bold"/>
                            
                            <TextBlock FontSize="16" Foreground="Gray" Margin="0,4,0,0">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} · {1}/单位">
                                        <Binding Path="Origin"/>
                                        <Binding Path="Price"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                            
                            <TextBlock FontSize="14" Foreground="Gray" Margin="0,4,0,0">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="规格: {0} · 单位: {1}">
                                        <Binding Path="Spec"/>
                                        <Binding Path="Unit"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <Border Background="{DynamicResource SuccessBrush}" 
                                    CornerRadius="12" 
                                    Padding="12,4">
                                <TextBlock Text="{Binding StatusText}" 
                                           Foreground="White"
                                           FontWeight="Bold"
                                           FontSize="12"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 详细信息 -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <Expander Header="基本信息" IsExpanded="True">
                        <Grid Margin="16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- 第一行 -->
                            <TextBlock Grid.Row="0" Grid.Column="0" 
                                       Text="药材名称:" 
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                            <TextBox Grid.Row="0" Grid.Column="1" 
                                     Text="{Binding Herb.Name, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     Margin="0,8,16,8"/>
                            
                            <TextBlock Grid.Row="0" Grid.Column="2" 
                                       Text="拼音码:" 
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                            <TextBox Grid.Row="0" Grid.Column="3" 
                                     Text="{Binding Herb.PinYinCode, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     Margin="0,8"/>
                            
                            <!-- 第二行 -->
                            <TextBlock Grid.Row="1" Grid.Column="0" 
                                       Text="产地:" 
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                            <TextBox Grid.Row="1" Grid.Column="1" 
                                     Text="{Binding Herb.Origin, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     Margin="0,8,16,8"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="2" 
                                       Text="规格:" 
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                            <TextBox Grid.Row="1" Grid.Column="3" 
                                     Text="{Binding Herb.Spec, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     Margin="0,8"/>
                            
                            <!-- 第三行 -->
                            <TextBlock Grid.Row="2" Grid.Column="0" 
                                       Text="单位:" 
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                            <TextBox Grid.Row="2" Grid.Column="1" 
                                     Text="{Binding Herb.Unit, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     Margin="0,8,16,8"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="2" 
                                       Text="单价(元):" 
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                            <TextBox Grid.Row="2" Grid.Column="3" 
                                     Text="{Binding Herb.Price, Mode=TwoWay, StringFormat=F2}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     Margin="0,8"/>
                            
                            <!-- 第四行 -->
                            <TextBlock Grid.Row="3" Grid.Column="0" 
                                       Text="创建时间:" 
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" 
                                       Text="{Binding CreateTime, StringFormat='yyyy-MM-dd HH:mm'}"
                                       VerticalAlignment="Center"
                                       Margin="0,8,16,8"/>
                            
                            <TextBlock Grid.Row="3" Grid.Column="2" 
                                       Text="更新时间:" 
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                            <TextBlock Grid.Row="3" Grid.Column="3" 
                                       Text="{Binding UpdateTime, StringFormat='yyyy-MM-dd HH:mm'}"
                                       VerticalAlignment="Center"
                                       Margin="0,8"/>
                        </Grid>
                    </Expander>
                </Border>

                <!-- 功效用法信息 -->
                <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                    <Expander Header="功效用法" IsExpanded="True">
                        <Grid Margin="16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- 功效说明 -->
                            <TextBlock Grid.Row="0" 
                                       Text="功效说明:" 
                                       FontWeight="Bold"
                                       VerticalAlignment="Top"
                                       Margin="0,8"/>
                            <TextBox Grid.Row="0" 
                                     Text="{Binding Herb.Effect, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     TextWrapping="Wrap"
                                     MinHeight="60"
                                     Margin="0,32,0,8"/>
                            
                            <!-- 用法用量 -->
                            <TextBlock Grid.Row="1" 
                                       Text="用法用量:" 
                                       FontWeight="Bold"
                                       VerticalAlignment="Top"
                                       Margin="0,8"/>
                            <TextBox Grid.Row="1" 
                                     Text="{Binding Herb.Usage, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     TextWrapping="Wrap"
                                     MinHeight="60"
                                     Margin="0,32,0,8"/>
                            
                            <!-- 备注 -->
                            <TextBlock Grid.Row="2" 
                                       Text="备注:" 
                                       FontWeight="Bold"
                                       VerticalAlignment="Top"
                                       Margin="0,8"/>
                            <TextBox Grid.Row="2" 
                                     Text="{Binding Herb.Remark, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     TextWrapping="Wrap"
                                     MinHeight="40"
                                     Margin="0,32,0,8"/>
                        </Grid>
                    </Expander>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- 加载遮罩 -->
        <Grid Grid.Row="0" Grid.RowSpan="2" 
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
              Background="#80000000">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Width="50" Height="50"
                             IsIndeterminate="True"
                             Margin="0,0,0,16"/>
                <TextBlock Text="正在加载药材信息..."
                           Foreground="White"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>