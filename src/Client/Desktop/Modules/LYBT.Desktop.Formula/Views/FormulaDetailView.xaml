<UserControl x:Class="LYBT.Desktop.Formula.Views.FormulaDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:controls="clr-namespace:LYBT.Desktop.Formula.Controls"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200">

    <UserControl.Resources>
        <ResourceDictionary>
            <!-- 卡片阴影 -->
            <DropShadowEffect x:Key="CardShadow"
                              BlurRadius="16"
                              ShadowDepth="2"
                              Opacity="0.08"
                              Color="#000000"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部操作栏 -->
        <Grid Grid.Row="0" Margin="40,28,40,0">
            <Border Background="White"
                    CornerRadius="16"
                    Padding="32,24"
                    Effect="{StaticResource CardShadow}">
                <Grid MinHeight="64">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- 返回按钮 -->
                    <Button Grid.Column="0"
                            Command="{Binding BackCommand}"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="#64748B"
                            FontSize="15"
                            FontWeight="Medium"
                            Cursor="Hand"
                            Padding="14,10"
                            VerticalAlignment="Center">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="8"
                                                    Padding="{TemplateBinding Padding}">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="◀"
                                                               FontSize="14"
                                                               Foreground="{TemplateBinding Foreground}"
                                                               VerticalAlignment="Center"
                                                               Margin="0,0,8,0"/>
                                                    <TextBlock Text="返回"
                                                               FontSize="{TemplateBinding FontSize}"
                                                               FontWeight="{TemplateBinding FontWeight}"
                                                               Foreground="{TemplateBinding Foreground}"
                                                               VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#F1F5F9" />
                                                    <Setter Property="Foreground" Value="#475569" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- 标题 -->
                    <TextBlock Grid.Column="1"
                               Text="验方详情"
                               FontSize="26"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               Foreground="#1E293B"
                               Margin="20,0,0,0" />

                    <!-- 操作按钮组 - FormulaDetailView仅用于查看，不提供编辑功能 -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- 编辑功能已移除：配方编辑请使用EditFormulaDialog -->
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- 内容区域 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="40,28,40,28" Background="#F8FAFC">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- 验方基本信息卡片 -->
                <Border Grid.Row="0"
                        Background="White"
                        CornerRadius="16"
                        Padding="32,24"
                        Effect="{StaticResource CardShadow}"
                        Margin="0,0,0,28">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                Width="80" Height="80"
                                CornerRadius="8"
                                Background="#3B82F6"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                Margin="0,0,20,0">
                            <TextBlock Text="📜"
                                       FontSize="36"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="White" />
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding FormulaName}"
                                       FontSize="24"
                                       FontWeight="Bold"
                                       Foreground="#1E293B" />

                            <TextBlock FontSize="16" Foreground="#64748B" Margin="0,4,0,0">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} · {1}味药材">
                                        <Binding Path="Category" />
                                        <Binding Path="HerbCount" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>

                            <TextBlock FontSize="14" Foreground="#64748B" Margin="0,4,0,0">
                                <TextBlock.Text>
                                    <Binding Path="TotalPrice" StringFormat="总价: ¥{0:F2}" />
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <Border Background="#10B981"
                                    CornerRadius="12"
                                    Padding="12,4"
                                    Margin="0,0,0,8">
                                <TextBlock Text="{Binding StatusMessage}"
                                           Foreground="White"
                                           FontWeight="Bold"
                                           FontSize="12" />
                            </Border>

                            <Border Background="#F59E0B"
                                    CornerRadius="8"
                                    Padding="8,3"
                                    Visibility="{Binding IsShared, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="共享"
                                           Foreground="White"
                                           FontWeight="Bold"
                                           FontSize="10" />
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 基本信息 -->
                <Border Grid.Row="1"
                        Background="White"
                        CornerRadius="16"
                        Padding="32,24"
                        Effect="{StaticResource CardShadow}"
                        Margin="0,0,0,28">
                    <Expander Header="基本信息" IsExpanded="True">
                        <Grid Margin="16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- 第一行 -->
                            <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="验方名称:"
                                       FontSize="15"
                                       FontWeight="Bold"
                                       Foreground="#374151"
                                       VerticalAlignment="Center"
                                       Margin="0,12" />
                            <TextBox Grid.Row="0" Grid.Column="1"
                                     Text="{Binding Formula.Name, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     FontSize="15"
                                     Margin="0,12,16,12" />

                            <!-- Issue #2149: Formula模块不涉及配制难度，已删除 -->

                            <!-- 第二行 -->
                            <TextBlock Grid.Row="1" Grid.Column="0"
                                       Text="性味归经:"
                                       FontSize="15"
                                       FontWeight="Bold"
                                       Foreground="#374151"
                                       VerticalAlignment="Top"
                                       Margin="0,12" />
                            <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3"
                                     Text="{Binding Formula.Property, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     FontSize="15"
                                     TextWrapping="Wrap"
                                     MinHeight="60"
                                     Margin="0,12" />

                            <!-- 第三行 -->
                            <TextBlock Grid.Row="2" Grid.Column="0"
                                       Text="功效:"
                                       FontSize="15"
                                       FontWeight="Bold"
                                       Foreground="#374151"
                                       VerticalAlignment="Top"
                                       Margin="0,12" />
                            <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3"
                                     Text="{Binding Formula.Effect, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     FontSize="15"
                                     TextWrapping="Wrap"
                                     MinHeight="60"
                                     Margin="0,12" />

                            <!-- 第四行 -->
                            <TextBlock Grid.Row="3" Grid.Column="0"
                                       Text="用法:"
                                       FontSize="15"
                                       FontWeight="Bold"
                                       Foreground="#374151"
                                       VerticalAlignment="Top"
                                       Margin="0,12" />
                            <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3"
                                     Text="{Binding Formula.Usage, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     FontSize="15"
                                     TextWrapping="Wrap"
                                     MinHeight="60"
                                     Margin="0,12" />

                            <!-- 第五行 -->
                            <TextBlock Grid.Row="4" Grid.Column="0"
                                       Text="创建时间:"
                                       FontSize="15"
                                       FontWeight="Bold"
                                       Foreground="#374151"
                                       VerticalAlignment="Center"
                                       Margin="0,12" />
                            <TextBlock Grid.Row="4" Grid.Column="1"
                                       Text="{Binding Formula.CreatedAt, StringFormat='yyyy-MM-dd HH:mm'}"
                                       FontSize="15"
                                       Foreground="#6B7280"
                                       VerticalAlignment="Center"
                                       Margin="0,12,16,12" />

                            <TextBlock Grid.Row="4" Grid.Column="2"
                                       Text="更新时间:"
                                       FontSize="15"
                                       FontWeight="Bold"
                                       Foreground="#374151"
                                       VerticalAlignment="Center"
                                       Margin="0,12" />
                            <TextBlock Grid.Row="4" Grid.Column="3"
                                       Text="{Binding Formula.UpdatedAt, StringFormat='yyyy-MM-dd HH:mm'}"
                                       FontSize="15"
                                       Foreground="#6B7280"
                                       VerticalAlignment="Center"
                                       Margin="0,12" />
                        </Grid>
                    </Expander>
                </Border>

                <!-- 药材组成 -->
                <Border Grid.Row="2"
                        Background="White"
                        CornerRadius="16"
                        Padding="32,24"
                        Effect="{StaticResource CardShadow}"
                        Margin="0,0,0,28">
                    <Expander Header="药材组成" IsExpanded="True">
                        <Grid Margin="16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!-- 药材统计信息 -->
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                                <TextBlock Text="{Binding HerbCount, StringFormat='共{0}味药材'}"
                                           FontSize="15"
                                           FontWeight="Bold"
                                           Foreground="#374151"
                                           Margin="0,0,20,0" />
                                <TextBlock Text="{Binding TotalPrice, StringFormat='总价: ¥{0:F2}'}"
                                           FontSize="15"
                                           FontWeight="Bold"
                                           Foreground="#3B82F6" />
                            </StackPanel>

                            <!-- 药材列表 - Issue #2149: 改用UniformGrid 4列卡片布局 -->
                            <ScrollViewer Grid.Row="1"
                                         VerticalScrollBarVisibility="Auto"
                                         HorizontalScrollBarVisibility="Disabled"
                                         MaxHeight="400">
                                <ItemsControl ItemsSource="{Binding HerbItems}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="4" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <controls:HerbCardControl
                                                IsEditMode="{Binding DataContext.IsEditMode, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                DeleteCommand="{Binding DataContext.DeleteHerbCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                DosageCompletedCommand="{Binding DataContext.DosageCompletedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                HerbSelectedCommand="{Binding DataContext.HerbSelectedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Expander>
                </Border>

                <!-- 详细描述和备注 -->
                <Border Grid.Row="3"
                        Background="White"
                        CornerRadius="16"
                        Padding="32,24"
                        Effect="{StaticResource CardShadow}">
                    <Expander Header="详细描述" IsExpanded="False">
                        <Grid Margin="16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!-- 描述 -->
                            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                                <TextBlock Text="验方描述:"
                                           FontSize="15"
                                           FontWeight="Bold"
                                           Foreground="#374151"
                                           Margin="0,0,0,8" />
                                <TextBox Text="{Binding Formula.Description, Mode=TwoWay}"
                                         IsReadOnly="{Binding IsReadOnly}"
                                         FontSize="15"
                                         TextWrapping="Wrap"
                                         MinHeight="60"
                                         Padding="12,10" />
                            </StackPanel>

                            <!-- 备注 -->
                            <StackPanel Grid.Row="1">
                                <TextBlock Text="备注:"
                                           FontSize="15"
                                           FontWeight="Bold"
                                           Foreground="#374151"
                                           Margin="0,0,0,8" />
                                <TextBox Text="{Binding Formula.Remark, Mode=TwoWay}"
                                         IsReadOnly="{Binding IsReadOnly}"
                                         FontSize="15"
                                         TextWrapping="Wrap"
                                         MinHeight="40"
                                         Padding="12,10" />
                            </StackPanel>
                        </Grid>
                    </Expander>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- 加载遮罩 -->
        <Grid Grid.Row="0" Grid.RowSpan="2"
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
              Background="#80000000">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Width="50" Height="50"
                             IsIndeterminate="True"
                             Margin="0,0,0,16" />
                <TextBlock Text="正在加载验方信息..."
                           Foreground="White"
                           FontSize="16"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
