<UserControl x:Class="LYBT.Desktop.Formula.Views.FormulaDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:converters="clr-namespace:LYBT.Desktop.Formula.Converters"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200">

    <UserControl.Resources>
        <ResourceDictionary>
            <!-- 布尔值到可见性转换器 -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

            <!-- 布尔值反转转换器（Issue #2075: 用于按钮IsEnabled绑定）-->
            <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>

            <!-- 卡片阴影 -->
            <DropShadowEffect x:Key="CardShadow"
                              BlurRadius="16"
                              ShadowDepth="2"
                              Opacity="0.08"
                              Color="#000000"/>

            <!-- 单行文本框样式 -->
            <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="Padding" Value="14,12"/>
                <Setter Property="BorderBrush" Value="#D1D5DB"/>
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <ScrollViewer x:Name="PART_ContentHost" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="#3B82F6" />
                                </Trigger>
                                <Trigger Property="IsReadOnly" Value="True">
                                    <Setter Property="Background" Value="#F9FAFB" />
                                    <Setter Property="Foreground" Value="#6B7280" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 多行文本框样式 -->
            <Style x:Key="ModernMultilineTextBoxStyle" TargetType="TextBox">
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="Padding" Value="14,12"/>
                <Setter Property="BorderBrush" Value="#D1D5DB"/>
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
                <Setter Property="AcceptsReturn" Value="True"/>
                <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <ScrollViewer x:Name="PART_ContentHost"
                                              VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="#3B82F6" />
                                </Trigger>
                                <Trigger Property="IsReadOnly" Value="True">
                                    <Setter Property="Background" Value="#F9FAFB" />
                                    <Setter Property="Foreground" Value="#6B7280" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="#F8FAFC">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部操作栏 -->
        <Border Grid.Row="0"
                Background="White"
                CornerRadius="16"
                Padding="32,24"
                Margin="40,28,40,0"
                Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- 返回按钮 -->
                <Button Grid.Column="0"
                        Command="{Binding BackCommand}"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="#64748B"
                        FontSize="15"
                        FontWeight="Medium"
                        Cursor="Hand"
                        Padding="14,10"
                        VerticalAlignment="Center">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8"
                                                Padding="{TemplateBinding Padding}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="◀"
                                                           FontSize="14"
                                                           Foreground="{TemplateBinding Foreground}"
                                                           VerticalAlignment="Center"
                                                           Margin="0,0,8,0"/>
                                                <TextBlock Text="返回"
                                                           FontSize="{TemplateBinding FontSize}"
                                                           FontWeight="{TemplateBinding FontWeight}"
                                                           Foreground="{TemplateBinding Foreground}"
                                                           VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#F1F5F9" />
                                                <Setter Property="Foreground" Value="#475569" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- 标题 -->
                <TextBlock Grid.Column="1"
                           Text="验方详情"
                           FontSize="26"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Foreground="#1E293B"
                           Margin="20,0,0,0" />

            </Grid>
        </Border>

        <!-- 内容区域 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="40,28,40,28" Background="#F8FAFC">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- 基本信息 -->
                <Border Grid.Row="0"
                        Background="White"
                        CornerRadius="16"
                        Padding="32,24"
                        Effect="{StaticResource CardShadow}"
                        Margin="0,0,0,28">
                    <Expander Header="基本信息" IsExpanded="True">
                        <Grid Margin="16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- 第一行 -->
                            <TextBlock Grid.Row="0" Grid.Column="0"
                                       Text="验方名称:"
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       Margin="0,8" />
                            <TextBox Grid.Row="0" Grid.Column="1"
                                     Text="{Binding FormulaName, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     Style="{StaticResource ModernTextBoxStyle}"
                                     Margin="0,8,16,8" />

                            <TextBlock Grid.Row="0" Grid.Column="2"
                                       Text="拼音码:"
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       Margin="0,8" />
                            <TextBox Grid.Row="0" Grid.Column="3"
                                     Text="{Binding PinYinCode, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     Style="{StaticResource ModernTextBoxStyle}"
                                     Margin="0,8" />

                            <!-- 第二行 -->
                            <TextBlock Grid.Row="1" Grid.Column="0"
                                       Text="性味归经:"
                                       FontWeight="Bold"
                                       VerticalAlignment="Top"
                                       Margin="0,8" />
                            <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3"
                                     Text="{Binding Property, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     Style="{StaticResource ModernMultilineTextBoxStyle}"
                                     MinHeight="40"
                                     Margin="0,8" />

                            <!-- 第三行 -->
                            <TextBlock Grid.Row="2" Grid.Column="0"
                           Text="功效*:"
                           FontWeight="Bold"
                           Foreground="Red"
                           VerticalAlignment="Top"
                           Margin="0,8" />
                            <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3"
                                     Text="{Binding Effect, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     Style="{StaticResource ModernMultilineTextBoxStyle}"
                                     MinHeight="60"
                                     Margin="0,8" />

                            <!-- 第四行 -->
                            <TextBlock Grid.Row="3" Grid.Column="0"
                                       Text="用法:"
                                       FontWeight="Bold"
                                       VerticalAlignment="Top"
                                       Margin="0,8" />
                            <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3"
                                     Text="{Binding Usage, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     Style="{StaticResource ModernMultilineTextBoxStyle}"
                                     MinHeight="60"
                                     Margin="0,8" />

                            <!-- 第五行 -->
                            <TextBlock Grid.Row="4" Grid.Column="0"
                                       Text="创建时间:"
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       Margin="0,8" />
                            <TextBlock Grid.Row="4" Grid.Column="1"
                                       Text="{Binding CreatedAtDisplay}"
                                       VerticalAlignment="Center"
                                       Margin="0,8,16,8" />

                            <TextBlock Grid.Row="4" Grid.Column="2"
                                       Text="更新时间:"
                                       FontWeight="Bold"
                                       VerticalAlignment="Center"
                                       Margin="0,8" />
                            <TextBlock Grid.Row="4" Grid.Column="3"
                                       Text="{Binding UpdatedAtDisplay}"
                                       VerticalAlignment="Center"
                                       Margin="0,8" />
                        </Grid>
                    </Expander>
                </Border>

                <!-- 药材组成（Issue #2075: 8列DataGrid快速录入布局）-->
                <Border Grid.Row="1"
                        Background="White"
                        CornerRadius="16"
                        Padding="32,24"
                        Effect="{StaticResource CardShadow}"
                        Margin="0,0,0,28">
                    <Expander Header="药材组成（快速录入）" IsExpanded="True">
                        <Grid Margin="16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!-- 操作按钮区（Issue #2075: 添加行、删除行、清空）-->
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                                <Button Content="添加行"
                                        Command="{Binding CommandHandler.AddHerbCommand}"
                                        IsEnabled="{Binding CommandHandler.IsReadOnly, Converter={StaticResource InverseBooleanConverter}}"
                                        Background="#10B981"
                                        Foreground="White"
                                        BorderThickness="0"
                                        FontSize="14"
                                        FontWeight="Medium"
                                        Height="36"
                                        MinWidth="100"
                                        Margin="0,0,12,0"
                                        Cursor="Hand">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="8"
                                                                Padding="16,8">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#059669" />
                                                            </Trigger>
                                                            <Trigger Property="IsEnabled" Value="False">
                                                                <Setter Property="Background" Value="#D1D5DB" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <Button Content="删除行"
                                        Command="{Binding CommandHandler.RemoveHerbCommand}"
                                        IsEnabled="{Binding CommandHandler.IsReadOnly, Converter={StaticResource InverseBooleanConverter}}"
                                        Background="#EF4444"
                                        Foreground="White"
                                        BorderThickness="0"
                                        FontSize="14"
                                        FontWeight="Medium"
                                        Height="36"
                                        MinWidth="100"
                                        Margin="0,0,12,0"
                                        Cursor="Hand">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="8"
                                                                Padding="16,8">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#DC2626" />
                                                            </Trigger>
                                                            <Trigger Property="IsEnabled" Value="False">
                                                                <Setter Property="Background" Value="#D1D5DB" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <Button Content="清空"
                                        Command="{Binding CommandHandler.ClearAllCommand}"
                                        IsEnabled="{Binding CommandHandler.IsReadOnly, Converter={StaticResource InverseBooleanConverter}}"
                                        Background="#F59E0B"
                                        Foreground="White"
                                        BorderThickness="0"
                                        FontSize="14"
                                        FontWeight="Medium"
                                        Height="36"
                                        MinWidth="100"
                                        Cursor="Hand">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="8"
                                                                Padding="16,8">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#D97706" />
                                                            </Trigger>
                                                            <Trigger Property="IsEnabled" Value="False">
                                                                <Setter Property="Background" Value="#D1D5DB" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>

                            <!-- 8列DataGrid（Issue #2075: 药材1+用量1、药材2+用量2、药材3+用量3、药材4+用量4）-->
                            <DataGrid Grid.Row="1"
                                      ItemsSource="{Binding HerbRows}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="True"
                                      IsReadOnly="{Binding IsReadOnly}"
                                      GridLinesVisibility="Horizontal"
                                      HeadersVisibility="Column"
                                      SelectionMode="Single"
                                      AlternationCount="2"
                                      MinHeight="200"
                                      MaxHeight="400">
                                <DataGrid.Columns>
                                    <!-- 药材1 -->
                                    <!-- 药材1 - 智能过滤ComboBox (Issue #2076) -->
                                    <DataGridTemplateColumn Header="药材1" Width="150">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Herb1.Name}" VerticalAlignment="Center" Padding="5,0"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                        <DataGridTemplateColumn.CellEditingTemplate>
                                            <DataTemplate>
                                                <ComboBox SelectedItem="{Binding Herb1, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                          ItemsSource="{Binding DataContext.FilterManager.FilteredHerbs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          IsEditable="True"
                                                          TextSearch.TextPath="Name"
                                                          IsTextSearchEnabled="True"
                                                          StaysOpenOnEdit="True"
                                                          MaxDropDownHeight="200">
                                                    <i:Interaction.Triggers>
                                                        <i:EventTrigger EventName="TextChanged">
                                                            <prism:InvokeCommandAction Command="{Binding DataContext.FilterHerbsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                       CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ComboBox}, Path=Text}"/>
                                                        </i:EventTrigger>
                                                        <!-- Issue #2077: 键盘导航 -->
                                                        <i:EventTrigger EventName="PreviewKeyDown">
                                                            <prism:InvokeCommandAction Command="{Binding DataContext.HandleKeyNavigationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                       />
                                                        </i:EventTrigger>
                                                    </i:Interaction.Triggers>
                                                    <ComboBox.ItemTemplate>
                                                        <DataTemplate>
                                                            <StackPanel>
                                                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                                                <TextBlock Text="{Binding PinYinCode}" FontSize="10" Foreground="Gray"/>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </ComboBox.ItemTemplate>
                                                </ComboBox>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellEditingTemplate>
                                    </DataGridTemplateColumn>

                                    <!-- 用量1 -->
                                    <DataGridTextColumn Header="用量1(g)"
                                                        Binding="{Binding Quantity1, Mode=TwoWay, StringFormat=F1}"
                                                        Width="80" />

                                    <!-- 药材2 -->
                                    <!-- 药材2 - 智能过滤ComboBox (Issue #2076) -->
                                    <DataGridTemplateColumn Header="药材2" Width="150">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Herb2.Name}" VerticalAlignment="Center" Padding="5,0"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                        <DataGridTemplateColumn.CellEditingTemplate>
                                            <DataTemplate>
                                                <ComboBox SelectedItem="{Binding Herb2, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                          ItemsSource="{Binding DataContext.FilterManager.FilteredHerbs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          IsEditable="True"
                                                          TextSearch.TextPath="Name"
                                                          IsTextSearchEnabled="True"
                                                          StaysOpenOnEdit="True"
                                                          MaxDropDownHeight="200">
                                                    <i:Interaction.Triggers>
                                                        <i:EventTrigger EventName="TextChanged">
                                                            <prism:InvokeCommandAction Command="{Binding DataContext.FilterHerbsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                       CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ComboBox}, Path=Text}"/>
                                                        </i:EventTrigger>
                                                        <!-- Issue #2077: 键盘导航 -->
                                                        <i:EventTrigger EventName="PreviewKeyDown">
                                                            <prism:InvokeCommandAction Command="{Binding DataContext.HandleKeyNavigationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                       />
                                                        </i:EventTrigger>
                                                    </i:Interaction.Triggers>
                                                    <ComboBox.ItemTemplate>
                                                        <DataTemplate>
                                                            <StackPanel>
                                                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                                                <TextBlock Text="{Binding PinYinCode}" FontSize="10" Foreground="Gray"/>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </ComboBox.ItemTemplate>
                                                </ComboBox>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellEditingTemplate>
                                    </DataGridTemplateColumn>

                                    <!-- 用量2 -->
                                    <DataGridTextColumn Header="用量2(g)"
                                                        Binding="{Binding Quantity2, Mode=TwoWay, StringFormat=F1}"
                                                        Width="80" />

                                    <!-- 药材3 -->
                                    <!-- 药材3 - 智能过滤ComboBox (Issue #2076) -->
                                    <DataGridTemplateColumn Header="药材3" Width="150">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Herb3.Name}" VerticalAlignment="Center" Padding="5,0"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                        <DataGridTemplateColumn.CellEditingTemplate>
                                            <DataTemplate>
                                                <ComboBox SelectedItem="{Binding Herb3, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                          ItemsSource="{Binding DataContext.FilterManager.FilteredHerbs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          IsEditable="True"
                                                          TextSearch.TextPath="Name"
                                                          IsTextSearchEnabled="True"
                                                          StaysOpenOnEdit="True"
                                                          MaxDropDownHeight="200">
                                                    <i:Interaction.Triggers>
                                                        <i:EventTrigger EventName="TextChanged">
                                                            <prism:InvokeCommandAction Command="{Binding DataContext.FilterHerbsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                       CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ComboBox}, Path=Text}"/>
                                                        </i:EventTrigger>
                                                        <!-- Issue #2077: 键盘导航 -->
                                                        <i:EventTrigger EventName="PreviewKeyDown">
                                                            <prism:InvokeCommandAction Command="{Binding DataContext.HandleKeyNavigationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                       />
                                                        </i:EventTrigger>
                                                    </i:Interaction.Triggers>
                                                    <ComboBox.ItemTemplate>
                                                        <DataTemplate>
                                                            <StackPanel>
                                                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                                                <TextBlock Text="{Binding PinYinCode}" FontSize="10" Foreground="Gray"/>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </ComboBox.ItemTemplate>
                                                </ComboBox>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellEditingTemplate>
                                    </DataGridTemplateColumn>

                                    <!-- 用量3 -->
                                    <DataGridTextColumn Header="用量3(g)"
                                                        Binding="{Binding Quantity3, Mode=TwoWay, StringFormat=F1}"
                                                        Width="80" />

                                    <!-- 药材4 -->
                                    <!-- 药材4 - 智能过滤ComboBox (Issue #2076) -->
                                    <DataGridTemplateColumn Header="药材4" Width="150">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Herb4.Name}" VerticalAlignment="Center" Padding="5,0"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                        <DataGridTemplateColumn.CellEditingTemplate>
                                            <DataTemplate>
                                                <ComboBox SelectedItem="{Binding Herb4, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                          ItemsSource="{Binding DataContext.FilterManager.FilteredHerbs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                          IsEditable="True"
                                                          TextSearch.TextPath="Name"
                                                          IsTextSearchEnabled="True"
                                                          StaysOpenOnEdit="True"
                                                          MaxDropDownHeight="200">
                                                    <i:Interaction.Triggers>
                                                        <i:EventTrigger EventName="TextChanged">
                                                            <prism:InvokeCommandAction Command="{Binding DataContext.FilterHerbsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                       CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ComboBox}, Path=Text}"/>
                                                        </i:EventTrigger>
                                                        <!-- Issue #2077: 键盘导航 -->
                                                        <i:EventTrigger EventName="PreviewKeyDown">
                                                            <prism:InvokeCommandAction Command="{Binding DataContext.HandleKeyNavigationCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                                       />
                                                        </i:EventTrigger>
                                                    </i:Interaction.Triggers>
                                                    <ComboBox.ItemTemplate>
                                                        <DataTemplate>
                                                            <StackPanel>
                                                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                                                <TextBlock Text="{Binding PinYinCode}" FontSize="10" Foreground="Gray"/>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </ComboBox.ItemTemplate>
                                                </ComboBox>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellEditingTemplate>
                                    </DataGridTemplateColumn>

                                    <!-- 用量4 -->
                                    <DataGridTextColumn Header="用量4(g)"
                                                        Binding="{Binding Quantity4, Mode=TwoWay, StringFormat=F1}"
                                                        Width="80" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Expander>
                </Border>

                <!-- 底部按钮栏（仅编辑模式显示）-->
                <Border Grid.Row="2"
                        Background="White"
                        CornerRadius="16"
                        Padding="32,24"
                        Effect="{StaticResource CardShadow}"
                        Margin="0,0,0,28"
                        Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <!-- 取消按钮 -->
                        <Button Command="{Binding CancelEditCommand}"
                                Background="White"
                                BorderBrush="#D1D5DB"
                                BorderThickness="1"
                                Foreground="#374151"
                                FontSize="15"
                                FontWeight="Medium"
                                Height="48"
                                MinWidth="120"
                                Cursor="Hand"
                                Margin="0,0,16,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="10"
                                                        Padding="24,12">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#F9FAFB" />
                                                        <Setter Property="BorderBrush" Value="#9CA3AF" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                            <TextBlock Text="取消" />
                        </Button>

                        <!-- 保存修改按钮 -->
                        <Button Command="{Binding SaveCommand}"
                                Background="#3B82F6"
                                Foreground="White"
                                BorderThickness="0"
                                FontSize="15"
                                FontWeight="Medium"
                                Height="48"
                                MinWidth="120"
                                Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        CornerRadius="10"
                                                        Padding="24,12">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#2563EB" />
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter Property="Background" Value="#1D4ED8" />
                                                    </Trigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter Property="Background" Value="#D1D5DB" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                            <!-- Issue #2083: 绑定SaveButtonText以支持动态文案 -->
                            <TextBlock Text="{Binding SaveButtonText}" />
                        </Button>
                    </StackPanel>
                </Border>

                <!-- 详细描述和备注 -->
                <Border Grid.Row="3"
                        Background="White"
                        CornerRadius="16"
                        Padding="32,24"
                        Effect="{StaticResource CardShadow}">
                    <Expander Header="详细描述" IsExpanded="False">
                        <Grid Margin="16">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!-- 描述 -->
                            <TextBlock Grid.Row="0"
                                       Text="验方描述:"
                                       FontWeight="Bold"
                                       VerticalAlignment="Top"
                                       Margin="0,8" />
                            <TextBox Grid.Row="0"
                                     Text="{Binding Formula.Description, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     Style="{StaticResource ModernMultilineTextBoxStyle}"
                                     MinHeight="60"
                                     Margin="0,32,0,8" />

                            <!-- 备注 -->
                            <TextBlock Grid.Row="1"
                                       Text="备注:"
                                       FontWeight="Bold"
                                       VerticalAlignment="Top"
                                       Margin="0,8" />
                            <TextBox Grid.Row="1"
                                     Text="{Binding Remark, Mode=TwoWay}"
                                     IsReadOnly="{Binding IsReadOnly}"
                                     Style="{StaticResource ModernMultilineTextBoxStyle}"
                                     MinHeight="40"
                                     Margin="0,32,0,8" />
                        </Grid>
                    </Expander>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- 加载遮罩 -->
        <Grid Grid.Row="0" Grid.RowSpan="2"
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
              Background="#80000000">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar Width="50" Height="50"
                             IsIndeterminate="True"
                             Margin="0,0,0,16" />
                <TextBlock Text="正在加载验方信息..."
                           Foreground="White"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
