<UserControl x:Class="LYBT.Desktop.Consultation.Views.PrescriptionPlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <Grid Background="White">
        <Border Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="1" 
                CornerRadius="8" Margin="20" Padding="30">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                
                <!-- 图标和标题 -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <TextBlock Text="💊" FontSize="32" Margin="0,0,10,0"/>
                    <TextBlock Text="处方开具" FontSize="24" FontWeight="Bold" 
                              Foreground="#2C3E50" VerticalAlignment="Center"/>
                </StackPanel>
                
                <!-- 说明文字 -->
                <TextBlock Text="处方功能已独立为专门模块，提供更专业的处方管理功能" 
                          FontSize="14" Foreground="#7F8C8D" 
                          HorizontalAlignment="Center" Margin="0,0,0,30"/>
                
                <!-- 导航按钮 -->
                <Button Content="进入处方模块" 
                        Command="{Binding NavigateToPrescriptionsCommand}"
                        Background="#3498DB" Foreground="White" 
                        Padding="20,10" FontSize="14" FontWeight="Medium"
                        BorderThickness="0" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#2980B9"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
                
                <!-- 辅助说明 -->
                <TextBlock Text="当前医疗案例ID将自动传递到处方模块" 
                          FontSize="12" Foreground="#95A5A6" 
                          HorizontalAlignment="Center" Margin="0,15,0,0"/>
                          
            </StackPanel>
        </Border>
    </Grid>
</UserControl>