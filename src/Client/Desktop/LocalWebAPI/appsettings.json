{
  "_comment": "本地JWT密钥为占位符：LocalWebAPI 内嵌于桌面进程，密钥无法对最终用户保密；生产/部署场景请勿将真实密钥写入本文件，改用环境变量 Jwt__SecretKey 注入（>=32字符）。A-28 P1-7：配置节统一为 Jwt（与 Server/Shell 一致）。",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost;Database=LYBTDB_Local;Trusted_Connection=True;TrustServerCertificate=true;MultipleActiveResultSets=true"
  },
  "Jwt": {
    "SecretKey": "__REPLACE_WITH_BASE64_64_BYTES__"
  }
}
