<prism:PrismApplication
                x:Class="LYBT.UI.WPF.App"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:prism="http://prismlibrary.com/"
                xmlns:local="clr-namespace:LYBT.UI.WPF.Converters"
                xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <!-- 注意：移除了 StartupUri，以避免启动两个窗口 -->
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- MaterialDesign resource dictionaries -->
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="DeepPurple" SecondaryColor="Lime" />
                <!-- Updated for MaterialDesignThemes v5 -->
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- 添加 BoolToVisibilityConverter 资源 -->
            <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

            <!-- 反向布尔转换器 -->
            <local:InverseBooleanConverter x:Key="InverseBooleanConverter" />

            <!-- 字符串到可见性转换器 -->
            <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />

            <!-- 导航按钮选中背景色转换器 -->
            <local:SelectedNavButtonBackgroundConverter x:Key="SelectedNavButtonBackgroundConverter" />

            <!-- 导航按钮选中前景色转换器 -->
            <local:SelectedNavButtonForegroundConverter x:Key="SelectedNavButtonForegroundConverter" />

            <!-- 枚举转描述特性 -->
            <local:EnumDescriptionConverter x:Key="EnumDescriptionConverter"/>

            <!-- 空转布尔转换器 -->
            <local:NullToBoolConverter x:Key="NullToBoolConverter"/>
            <local:StringEqualsConverter x:Key="StringEqualsConverter"/>
            <local:StringEqualsMultiConverter x:Key="StringEqualsMultiConverter"/>
            <local:ZeroToVisibilityConverter x:Key="ZeroToVisibilityConverter"/>
            <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        </ResourceDictionary>
    </Application.Resources>
</prism:PrismApplication>           