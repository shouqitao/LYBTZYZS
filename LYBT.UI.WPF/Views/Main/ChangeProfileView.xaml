<UserControl x:Class="LYBT.UI.WPF.Views.Main.ChangeProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Image Source="/Assets/bg.jpg" Stretch="UniformToFill" Opacity="0.6" Panel.ZIndex="0"/>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Panel.ZIndex="1">
            <Border Padding="32" MinWidth="570" MinHeight="645" Background="#F9FAFB" CornerRadius="14" BorderBrush="#F8F8EC" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.16" BlurRadius="18" ShadowDepth="5"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="修改个人信息" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center" Foreground="#333"/>

                    <StackPanel>
                        <TextBlock Text="姓名" />
                        <TextBox Text="{Binding RealName, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="6" FontSize="13" Background="#F4F5F7" BorderBrush="#F8F8EC" BorderThickness="1" Margin="0,0,0,8"/>
                        <TextBlock Text="邮箱" />
                        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="6" FontSize="13" Background="#F4F5F7" BorderBrush="#F8F8EC" BorderThickness="1" Margin="0,0,0,8"/>
                        <TextBlock Text="联系电话" />
                        <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="6" FontSize="13" Background="#F4F5F7" BorderBrush="#F8F8EC" BorderThickness="1" Margin="0,0,0,20"/>
                    </StackPanel>

                    <Button Content="保 存"
                            Command="{Binding SaveCommand}"
                            Height="32" Margin="0,0,0,10"
                            FontSize="15" FontWeight="Bold" Background="#F8F8EC"
                            BorderThickness="0" Cursor="Hand"/>
                    <Button Content="取 消"
                            Command="{Binding CancelCommand}"
                            Height="32" FontSize="15"
                            Foreground="Black" Background="#F4F5F7"
                            BorderBrush="#F8F8EC" BorderThickness="1" Cursor="Hand"/>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
