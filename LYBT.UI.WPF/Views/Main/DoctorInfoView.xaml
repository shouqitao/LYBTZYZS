<UserControl x:Class="LYBT.UI.WPF.Views.Main.DoctorInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:enums="clr-namespace:LYBT.Common.Enums;assembly=LYBT.Common"
             xmlns:converters="clr-namespace:LYBT.UI.WPF.Converters"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="GenderValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:Gender"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="TitleValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:DoctorTitle"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="StatusValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:DoctorStatus"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <converters:EnumDescriptionConverter x:Key="EnumDescriptionConverter" />
    </UserControl.Resources>
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="姓名：" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

        <TextBlock Text="电话：" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

        <TextBlock Text="性别：" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding Source={StaticResource GenderValues}}" SelectedItem="{Binding Gender}" Margin="0,0,0,8">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Text="出生日期：" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <DatePicker Grid.Row="3" Grid.Column="1" SelectedDate="{Binding Birthday}" Margin="0,0,0,8"/>

        <TextBlock Text="拼音码：" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding PinyinCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

        <TextBlock Text="执业证号：" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding LicenseNumber, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

        <TextBlock Text="职称：" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <ComboBox Grid.Row="6" Grid.Column="1" ItemsSource="{Binding Source={StaticResource TitleValues}}" SelectedItem="{Binding Title}" Margin="0,0,0,8">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Text="状态：" Grid.Row="7" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <ComboBox Grid.Row="7" Grid.Column="1" ItemsSource="{Binding Source={StaticResource StatusValues}}" SelectedItem="{Binding DoctorStatus}" Margin="0,0,0,8">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Text="备注：" Grid.Row="8" Grid.Column="0" VerticalAlignment="Top" Margin="0,0,10,8"/>
        <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding Remark, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="80" TextWrapping="Wrap" Margin="0,0,0,8"/>

        <Button Content="提交" Grid.Row="9" Grid.ColumnSpan="2" Command="{Binding SubmitCommand}" HorizontalAlignment="Center" Width="100" Margin="0,10,0,0"/>
    </Grid>
</UserControl>
