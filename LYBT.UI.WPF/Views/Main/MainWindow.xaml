<Window x:Class="LYBT.UI.WPF.Views.Main.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="凌隐宝堂中医诊所管理系统"
        Icon="/Assets/icon.ico"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesign.Brush.Background}">

    <Window.Resources>
        <!-- 标题栏按钮样式已移除，因为不再需要自定义标题栏 -->
    </Window.Resources>

    <!-- 移除WindowChrome设置，因为不需要自定义标题栏 -->

    <Grid>
        <!-- 背景图片 -->
        <Image Source="/Assets/bg.jpg" 
               Stretch="UniformToFill" 
               Opacity="0.3" 
               Panel.ZIndex="0"/>

        <!-- 功能区：登录、修改密码等界面 -->
        <ContentControl prism:RegionManager.RegionName="FunctionRegion"
                        Visibility="{Binding IsFunctionVisible, Converter={StaticResource BoolToVisibilityConverter}}"
                        Panel.ZIndex="1"/>

        <!-- 主界面区域（登录后可见，初始隐藏） -->
        <!-- 移除DockPanel，直接使用Grid布局，不包含标题栏 -->
        <Grid Visibility="{Binding IsMainVisible, Converter={StaticResource BoolToVisibilityConverter}}"
              Panel.ZIndex="1">

            <!-- 主要内容区域 - 加载整合后的主布局，保持原有结构兼容性 -->
            <ContentControl prism:RegionManager.RegionName="MainContentRegion"/>
        </Grid>
    </Grid>
</Window>