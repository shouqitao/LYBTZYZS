<Window x:Class="LYBT.UI.WPF.Views.Main.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:converters="clr-namespace:LYBT.UI.WPF.Converters"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="凌隐宝堂中医诊所"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <!-- 功能区：登录、修改密码等界面 -->
        <ContentControl prism:RegionManager.RegionName="FunctionRegion"
                        Visibility="{Binding IsFunctionVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
        <!-- 主界面区域（登录后可见，初始隐藏） -->
        <DockPanel Visibility="{Binding IsMainVisible, Converter={StaticResource BoolToVisibilityConverter}}">
            <!-- 顶部Header -->
            <Border DockPanel.Dock="Top" Height="100" Background="#374151">
                <Grid>
                    <TextBlock Text="凌隐宝堂中医诊所"
                   Foreground="White" FontSize="20"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Left"
                   Margin="20,0,0,0"/>
                    <!-- 右上角功能按钮 -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,22,0">
                        <Button Content="修改密码"
                                Width="100" Height="34"
                                Margin="0,0,10,0"
                                Command="{Binding ShowChangePasswordCommand}"
                                Background="#FF57606A" Foreground="White"
                                FontWeight="Bold" BorderThickness="0"
                                Cursor="Hand"/>
                        <Button Content="修改信息"
                                Width="100" Height="34"
                                Margin="0,0,10,0"
                                Command="{Binding ShowChangeProfileCommand}"
                                Background="#FF57606A" Foreground="White"
                                FontWeight="Bold" BorderThickness="0"
                                Cursor="Hand"/>
                        <Button Content="退出登录"
                                Width="100" Height="34"
                                Command="{Binding LogoutCommand}"
                                Background="#FF57606A" Foreground="White"
                                FontWeight="Bold" BorderThickness="0"
                                Cursor="Hand"/>
                    </StackPanel>
                </Grid>
            </Border>
            <!-- 主内容区域，加载 HomeView -->
            <ContentControl DockPanel.Dock="Bottom" prism:RegionManager.RegionName="MainContentRegion"/>
        </DockPanel>
    </Grid>
</Window>
