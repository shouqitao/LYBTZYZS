<UserControl x:Class="LYBT.UI.WPF.Views.Main.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             KeyDown="UserControl_KeyDown">
    <Border Padding="30" Margin="60" Background="#FAFAFA" CornerRadius="8" BorderBrush="#DDDDDD" BorderThickness="1" VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="350">
        <StackPanel>
            <!-- Logo -->
            <Image Source="/Assets/logo.jpg" Height="60" Width="60" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="用户登录" FontWeight="Bold" FontSize="22" HorizontalAlignment="Center" Margin="0,0,0,18"/>

            <!-- 用户名 -->
            <TextBlock Text="用户名" Margin="0,0,0,4" Foreground="#333333" FontSize="14"/>
            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Height="32" FontSize="15" Margin="0,0,0,14"
                     Name="userNameBox" />

            <!-- 密码 -->
            <TextBlock Text="密码" Margin="0,0,0,4" Foreground="#333333" FontSize="14"/>
            <PasswordBox x:Name="passwordBox" PasswordChanged="PasswordBox_PasswordChanged" Height="32" FontSize="15" Margin="0,0,0,14"/>

            <!-- 记住密码+登录 -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
                <CheckBox Content="记住密码" IsChecked="{Binding IsRemember}" VerticalAlignment="Center"/>
            </StackPanel>
            <Button Content="登录" Command="{Binding LoginCommand}" Height="36" FontSize="16" Margin="0,4,0,0" Name="loginButton"/>

            <!-- 错误消息 -->
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,14,0,0" FontSize="14" TextWrapping="Wrap"/>

            <!-- 底部版权 -->
            <TextBlock Text="© 2024 凌隐宝堂中医诊所 All Rights Reserved."
                       Foreground="#888" FontSize="12" HorizontalAlignment="Center" Margin="0,30,0,0"/>
        </StackPanel>
    </Border>
</UserControl>
