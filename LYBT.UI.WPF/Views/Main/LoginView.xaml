<UserControl x:Class="LYBT.UI.WPF.Views.Main.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
            prism:ViewModelLocator.AutoWireViewModel="True" >
    <StackPanel VerticalAlignment="Center" Margin="30">
        <TextBlock Text="用户登录" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,20"/>
        <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />
        <PasswordBox x:Name="passwordBox" PasswordChanged="PasswordBox_PasswordChanged" Margin="0,0,0,10"/>
        <CheckBox Content="记住密码" IsChecked="{Binding IsRemember}" Margin="0,0,0,10"/>
        <Button Content="登录" Command="{Binding LoginCommand}" Margin="0,10,0,0" Height="35"/>
        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,10,0,0"/>
    </StackPanel>
</UserControl>
