<UserControl x:Class="LYBT.UI.WPF.Views.Admin.DoctorInfoApprovalView"  
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"  
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
             xmlns:prism="http://prismlibrary.com/"  
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Margin="10">
        <!-- Changed 'Padding' to 'Margin' as Grid does not support 'Padding' -->
        <StackPanel>
            <ListView ItemsSource="{Binding Requests}" SelectedItem="{Binding SelectedRequest}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="姓名" DisplayMemberBinding="{Binding Name}" Width="120"/>
                        <GridViewColumn Header="手机号" DisplayMemberBinding="{Binding Phone}" Width="120"/>
                        <GridViewColumn Header="提交时间" DisplayMemberBinding="{Binding CreatedTime}" Width="160"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="批准" Command="{Binding ApproveCommand}" Margin="5" Padding="10,5"/>
                <Button Content="驳回" Command="{Binding RejectCommand}" Margin="5" Padding="10,5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
