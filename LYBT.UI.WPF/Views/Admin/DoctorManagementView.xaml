<UserControl x:Class="LYBT.UI.WPF.Views.Admin.DoctorManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:enums="clr-namespace:LYBT.Common.Enums;assembly=LYBT.Common"
             xmlns:helpers="clr-namespace:LYBT.UI.WPF.Helpers"
             xmlns:converters="clr-namespace:LYBT.UI.WPF.Converters"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <converters:EnumDescriptionConverter x:Key="EnumDescriptionConverter" />
        <converters:NullToBoolConverter x:Key="NullToBoolConverter" />
        <ObjectDataProvider x:Key="GenderValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:Gender"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="TitleValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:DoctorTitle"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="StatusValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:DoctorStatus"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.2*"/>
            <ColumnDefinition Width="1.4*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBox Width="180" Margin="0,0,8,0" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="查找" Command="{Binding SearchCommand}"/>
            </StackPanel>
            <DataGrid Grid.Row="1" ItemsSource="{Binding Doctors}" SelectedItem="{Binding SelectedDoctor, Mode=TwoWay}"
          AutoGenerateColumns="False" Margin="0,0,8,0" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="账号" Binding="{Binding UserName}" Width="*"/>
                    <DataGridTextColumn Header="姓名" Binding="{Binding RealName}" Width="*"/>
                    <DataGridTextColumn Header="联系号码" Binding="{Binding ContactNumber}" Width="*"/>
                    <DataGridTemplateColumn Header="性别" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Gender, Converter={StaticResource EnumDescriptionConverter}}" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="出生日期" Binding="{Binding Birthday, StringFormat=yyyy-MM-dd}" Width="110"/>
                    <DataGridTextColumn Header="拼音码" Binding="{Binding PinyinCode}" Width="*"/>
                    <DataGridTextColumn Header="执业证号" Binding="{Binding LicenseNumber}" Width="*"/>
                    <DataGridTemplateColumn Header="职称" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Title, Converter={StaticResource EnumDescriptionConverter}}" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="状态" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Status, Converter={StaticResource EnumDescriptionConverter}}" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="备注" Binding="{Binding Remark}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="新增医生" Command="{Binding AddDoctorCommand}" Margin="0,0,8,0"/>
                <Button Content="编辑医生" Command="{Binding EditDoctorCommand}" Margin="0,0,8,0"
                        IsEnabled="{Binding SelectedDoctor, Converter={StaticResource NullToBoolConverter}}"/>
            </StackPanel>
            <Border Grid.Row="1" Margin="0" Padding="18" Background="#FAFAFA" CornerRadius="8" BorderBrush="#CCC" BorderThickness="1" IsEnabled="{Binding IsEditable}">
                <StackPanel>
                    <TextBlock Text="{Binding EditModeTitle}" FontWeight="Bold" FontSize="17" Margin="0,0,0,16"/>
                    <TextBlock Text="姓名" />
                    <TextBox Text="{Binding EditingDoctor.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                    <TextBlock Text="手机号" />
                    <TextBox Text="{Binding EditingDoctor.Phone}" Margin="0,0,0,8"/>
                    <TextBlock Text="性别" />
                    <ComboBox ItemsSource="{Binding Source={StaticResource GenderValues}}" SelectedItem="{Binding EditingDoctor.Gender}"
                              Margin="0,0,0,8">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <TextBlock Text="出生日期" />
                    <DatePicker SelectedDate="{Binding EditingDoctor.Birthday}" Margin="0,0,0,8"/>
                    <TextBlock Text="拼音码" />
                    <TextBox Text="{Binding EditingDoctor.PinyinCode}" Margin="0,0,0,8"/>
                    <TextBlock Text="执业证号" />
                    <TextBox Text="{Binding EditingDoctor.LicenseNumber}" Margin="0,0,0,8"/>
                    <TextBlock Text="职称" />
                    <ComboBox ItemsSource="{Binding Source={StaticResource TitleValues}}" SelectedItem="{Binding EditingDoctor.Title}"
                              Margin="0,0,0,8">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <TextBlock Text="状态" />
                    <ComboBox ItemsSource="{Binding Source={StaticResource StatusValues}}" SelectedItem="{Binding EditingDoctor.Status}"
                              Margin="0,0,0,8">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <TextBlock Text="备注" />
                    <TextBox Text="{Binding EditingDoctor.Remark}" Margin="0,0,0,8"/>
                    <Button Content="保存" Command="{Binding SaveDoctorCommand}" Margin="0,10,0,0"/>
                    <Button Content="取消" Command="{Binding CancelCommand}" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
