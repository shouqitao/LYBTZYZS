<UserControl x:Class="LYBT.UI.WPF.Views.Admin.FormulaTemplatesManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:controls="clr-namespace:LYBT.WPFControls;assembly=LYBT.WPFControls"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Margin="0,0,10,0" Padding="18" Background="#FAFAFA" CornerRadius="8" BorderBrush="#CCC" BorderThickness="1">
            <Grid>
                <controls:CommonListView ShowPaging="False"
                                     ItemsSource="{Binding Templates}"
                                     SelectedItem="{Binding SelectedTemplate, Mode=TwoWay}"
                                     SearchCommand="{Binding RefreshCommand}">
                <controls:CommonListView.ActionContent>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="刷新" Command="{Binding RefreshCommand}" Margin="0,0,8,0"/>
                    <Button Content="新增" Command="{Binding AddCommand}" Margin="0,0,8,0"/>
                    <Button Content="编辑" Command="{Binding EditCommand}" Margin="0,0,8,0" IsEnabled="{Binding SelectedTemplate, Converter={StaticResource NullToBoolConverter}}"/>
                    <Button Content="删除" Command="{Binding DeleteCommand}" Margin="0,0,8,0" IsEnabled="{Binding SelectedTemplate, Converter={StaticResource NullToBoolConverter}}"/>
                    <Button Content="批量导入经典方" Command="{Binding ImportCommand}" Margin="0,0,8,0"/>
                    <Button Content="批量导出经典方" Command="{Binding ExportCommand}"/>
                </StackPanel>
            </controls:CommonListView.ActionContent>
            <controls:CommonListView.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="160"/>
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="*"/>
            </controls:CommonListView.Columns>
                </controls:CommonListView>
            </Grid>
        </Border>
        <Border Grid.Column="1" Margin="0" Padding="18" Background="#FAFAFA" CornerRadius="8" BorderBrush="#CCC" BorderThickness="1">
            <controls:CommonProfileView DataContext="{Binding ProfileViewModel}"
                                       Title="{Binding EditModeTitle}"
                                       SaveCommand="{Binding SaveCommand}"
                                       CancelCommand="{Binding CancelCommand}"
                                       IsEditable="{Binding IsEditable}">
                <controls:CommonProfileView.ProfileContent>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="名称" Width="60"/>
                            <TextBox Text="{Binding Template.Name}" Width="300"/>
                        </StackPanel>
                        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False"
                                  SelectedItem="{Binding SelectedItem}" Height="200" Margin="0,0,0,10"
                                  Visibility="{Binding IsEditable, Converter={StaticResource BoolToVisibilityConverter}}"
                                  CellEditEnding="EditItemsGrid_CellEditEnding">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="药材" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding HerbName}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <ComboBox IsEditable="True" StaysOpenOnEdit="True"
                                                      ItemsSource="{Binding DataContext.AllHerbs, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                      DisplayMemberPath="Name" SelectedValuePath="Id"
                                                      SelectedValue="{Binding HerbId, Mode=TwoWay}"
                                                      Text="{Binding HerbName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                      SelectionChanged="HerbComboBox_SelectionChanged"
                                                      KeyUp="HerbComboBox_KeyUp"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="剂量" Binding="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                                <DataGridTextColumn Header="单位" Binding="{Binding Unit}" IsReadOnly="True" Width="80"/>
                                <DataGridTemplateColumn Width="40">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="×" Command="{Binding DataContext.RemoveHerbCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                    CommandParameter="{Binding}" Width="24" Height="24"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" SelectedItem="{Binding SelectedItem}" Height="200" Margin="0,0,0,10" Visibility="{Binding IsEditable, Converter={StaticResource InverseBooleanConverter}}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="药材" Binding="{Binding HerbName}" Width="*"/>
                                <DataGridTextColumn Header="剂量" Binding="{Binding Quantity}" Width="80"/>
                                <DataGridTextColumn Header="单位" Binding="{Binding Unit}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <TextBlock Text="备注"/>
                        <TextBox Text="{Binding Template.Remark}" Margin="0,0,0,10"/>
                    </StackPanel>
                </controls:CommonProfileView.ProfileContent>
            </controls:CommonProfileView>
        </Border>
    </Grid>
</UserControl>
