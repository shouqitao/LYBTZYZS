<UserControl x:Class="LYBT.UI.WPF.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <TabControl>
            <TabItem Header="用户管理">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="新增用户" Margin="0,0,10,0" Click="AddUser_Click"/>
                    </StackPanel>

                    <DataGrid x:Name="UserDataGrid"
                              Grid.Row="1"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                            <DataGridTextColumn Header="用户名" Binding="{Binding Username}" Width="*"/>
                            <DataGridTextColumn Header="邮箱" Binding="{Binding Email}" Width="*"/>
                            <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="Auto"/>
                            <DataGridTemplateColumn Header="操作" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="编辑" Margin="0,0,5,0"
                                                    CommandParameter="{Binding}"
                                                    Click="EditUser_Click"/>
                                            <Button Content="{Binding Status, Converter={StaticResource StatusToButtonTextConverter}}"
                                                    CommandParameter="{Binding}"
                                                    Click="ToggleUserStatus_Click"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="药材管理">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="新增药材" Margin="0,0,10,0" Click="AddHerb_Click"/>
                    </StackPanel>

                    <DataGrid x:Name="HerbDataGrid"
                              Grid.Row="1"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                            <DataGridTextColumn Header="药材名称" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="功效" Binding="{Binding Effect}" Width="*"/>
                            <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="Auto"/>
                            <DataGridTemplateColumn Header="操作" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="编辑" Margin="0,0,5,0"
                                                    CommandParameter="{Binding}"
                                                    Click="EditHerb_Click"/>
                                            <Button Content="{Binding Status, Converter={StaticResource StatusToButtonTextConverter}}"
                                                    CommandParameter="{Binding}"
                                                    Click="ToggleHerbStatus_Click"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="药方管理">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="新增药方" Margin="0,0,10,0" Click="AddPrescription_Click"/>
                    </StackPanel>

                    <DataGrid x:Name="PrescriptionDataGrid"
                              Grid.Row="1"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                            <DataGridTextColumn Header="药方名称" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="*"/>
                            <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="Auto"/>
                            <DataGridTemplateColumn Header="操作" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="编辑" Margin="0,0,5,0"
                                                    CommandParameter="{Binding}"
                                                    Click="EditPrescription_Click"/>
                                            <Button Content="{Binding Status, Converter={StaticResource StatusToButtonTextConverter}}"
                                                    CommandParameter="{Binding}"
                                                    Click="TogglePrescriptionStatus_Click"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
