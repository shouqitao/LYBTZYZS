<UserControl x:Class="LYBT.UI.WPF.Views.Controls.PendingDispenseListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataGrid AutoGenerateColumns="False"
              ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
              SelectedItem="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}">
        <DataGrid.Columns>
            <DataGridTextColumn Header="病人" Binding="{Binding PatientName}" Width="*"/>
            <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80"/>
            <DataGridTextColumn Header="时间" Binding="{Binding DispenseTime}" Width="130"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
