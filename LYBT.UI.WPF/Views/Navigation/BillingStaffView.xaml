<UserControl x:Class="LYBT.UI.WPF.Views.Navigation.BillingStaffView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:converters="clr-namespace:LYBT.UI.WPF.Converters;assembly=LYBT.UI.WPF"
             xmlns:controls="clr-namespace:LYBT.UI.WPF.Views.Controls"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="刷新" Command="{Binding RefreshCommand}" Margin="0,0,8,0" />
            <Button Content="标记已支付" Command="{Binding MarkPaidCommand}" IsEnabled="{Binding SelectedBill, Converter={StaticResource NullToBoolConverter}}" />
        </StackPanel>
        <controls:PendingPaymentListControl Grid.Row="1" ItemsSource="{Binding Bills}" SelectedItem="{Binding SelectedBill, Mode=TwoWay}" />
    </Grid>
</UserControl>
