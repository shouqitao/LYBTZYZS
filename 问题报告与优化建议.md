------

# **问题报告与优化建议**

## 一、后端问题与建议

### 1. 架构模式

**问题：**

- 当前采用单体架构，虽然模块化清晰，但在业务耦合度较高的模块（如 Consultation）上，依赖过重，风险集中。
- 缺少领域层，业务规则主要散落在 Service 中，逻辑难以集中复用。

**建议：**

- 保持单体架构（适合 20 人以下诊所），但**引入领域服务或领域事件**，分离核心业务逻辑，降低模块间直接依赖。
- 为核心模块（如 Consultation）设置**中介服务或事件总线**，避免过度依赖。

------

### 2. 数据库设计

**问题：**

- 患者表、处方表等主要表未对常用查询字段（如电话、姓名）建立索引，可能影响查询性能。
- 外键删除策略默认级联，可能导致误删历史记录。
- 部分表（如 Formula）缺乏对子表（药材组成）的直观存储，导致一致性问题。

**建议：**

- **增加索引**：在患者电话、姓名、病例号等高频查询字段加索引。
- **外键约束优化**：对患者、医生等历史性实体，改为 `Restrict` 删除，避免误删。
- **范式化补充**：为 Formula 等模块建立清晰的关联子表，保证数据一致性。

------

### 3. 事务与数据一致性

**问题：**

- Service 层 SaveChanges 分散调用，跨模块操作时缺乏统一事务，可能导致部分提交。

**建议：**

- **引入 Unit of Work** 或使用 TransactionScope 包裹关键业务流程（如“挂号→看诊→处方”），确保原子性。

------

### 4. 接口设计

**问题：**

- 存在不一致：部分接口直接返回 `Ok()`，未使用统一响应封装。
- 接口粒度不统一，例如 PatientsController 有 “QuickCreate” 与标准创建接口，增加维护负担。
- 各 Controller 局部 try-catch 过多，异常处理冗余。

**建议：**

- **统一响应格式**：所有接口返回 `ApiResponse`。
- **规范接口粒度**：合并相似接口，采用参数区分模式。
- **统一异常处理**：使用全局异常中间件，减少 Controller 层样板代码。

------

### 5. 安全与权限

**问题：**

- 所有用户默认角色为 Admin，缺乏医生/前台/管理员的区分。
- 无密码复杂度策略或登录失败锁定机制。

**建议：**

- **完善角色体系**：增加 Doctor、Receptionist、Admin 等角色，按需授权。
- **增加安全策略**：设置密码强度校验、多次失败锁定，关键操作写入审计日志。

------

### 6. 日志与监控

**问题：**

- 日志和异常分散在各模块，横切关注点重复。
- 性能监控较浅，没有外部指标采集。

**建议：**

- **集中日志处理**：通过中间件或AOP统一记录。
- **引入APM/健康检查**：监控数据库连接、请求延迟，必要时加入 Prometheus/Grafana。

------

## 二、前端问题与建议

### 1. 架构模式

**问题：**

- MVVM + Prism 架构清晰，但部分模块 ViewModel 曾过于臃肿。
- API 调用模式存在三种（Refit直返/ServiceResult封装/ApiErrorHandler包装），理解成本高。

**建议：**

- **保持MVVM/Prism架构**，但进一步**拆分复杂ViewModel**为组件。
- **统一API调用模式**：后端返回标准 ApiResponse，前端仅保留一种处理模式。

------

### 2. 模块设计

**问题：**

- 模块划分合理，但部分 UI 组件可能在不同页面重复（如患者信息、处方明细）。
- 状态管理主要依赖 SessionManager，虽集中，但容易过载。

**建议：**

- **提升组件复用**：提炼公共组件库（如病人卡片、处方控件）。
- **增强状态管理**：在 SessionManager 基础上引入轻量状态库（或事件聚合器），减少单点负担。

------

### 3. 用户体验与性能

**问题：**

- 列表操作可能随数据量增加而卡顿。
- 异步提示覆盖面不均，部分操作缺少“加载中”反馈。

**建议：**

- **启用列表虚拟化**：在患者、药材等大列表中启用虚拟化。
- **完善异步反馈**：统一通过 NotificationService 显示操作进度和错误。

------

### 4. 可扩展性与维护性

**问题：**

- 当前测试覆盖率低（约2.7%），回归风险高。
- 部分Service仍存在重复代码。

**建议：**

- **增加前后端集成测试**，覆盖“挂号→看诊→开方→收费”的全流程。
- **提炼Service基类**，统一分页查询、CRUD等常见逻辑。

------

# **总结**

- **整体优势：** 架构采用单体+模块化，后端分层明确，前端MVVM+Prism，契约清晰，能满足20人以下诊所的稳定运营。
- **主要问题：** 事务管理不足、接口风格不完全统一、角色权限粗放、前端API调用模式多样化、日志和异常分散。
- **核心建议：**
  1. 引入领域层/事务机制，强化数据一致性。
  2. 统一接口响应与异常处理，规范API粒度。
  3. 完善角色权限和密码安全策略。
  4. 集中日志监控，引入健康检查。
  5. 前端统一接口模式，提炼通用组件和Service基类。
  6. 提升测试覆盖率，建立完整集成测试链路。

这些改进措施能让系统在保持现有功能的前提下，进一步提高 **健壮性、可维护性和可扩展性**。

------

要不要我帮你把这份报告整理成 **正式的 Markdown 文档格式**（带目录、分级标题、编号），方便直接作为内部评审或交付文档使用？